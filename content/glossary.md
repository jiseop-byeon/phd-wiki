---
title: R. Glossary
tags: [reference]
study-depth: Literacy
depth-goal: "Distinguish neighboring terms and read them consistently across papers."
mastery-when: "Raise a term to Working through its linked concept or method page when it enters daily use."
---

A lookup glossary for every page in this wiki: basic terms, terms beginners get stuck on, hard terms, and frequent terms. Each entry gives the English and Korean name, a one-line definition in both languages, and the page that teaches it. The same entries power the dotted-underline tooltips on every page: hover (or tap) a marked word to see its definition. Add a term whenever a page uses one it does not define, then run `python3 scripts/build_glossary_index.py`.
이 위키의 모든 페이지를 위한 용어 사전이다. 기본 용어, 초심자가 막히는 용어, 어려운 용어, 자주 나오는 용어를 담는다. 각 항목에는 영어·한국어 이름, 두 언어의 한 줄 정의, 그 개념을 가르치는 페이지가 있다. 같은 항목이 모든 페이지의 점선 밑줄 툴팁에도 쓰인다. 표시된 단어에 마우스를 올리거나 탭하면 정의가 뜬다. 페이지가 정의하지 않은 용어를 쓰면 여기에 추가하고 `python3 scripts/build_glossary_index.py`를 실행한다.

## Confusable pairs · 혼동하기 쉬운 쌍

What actually stalls a reader is rarely an unfamiliar word — it is two words that look interchangeable and are not.
논문 독해에서 실제로 막히는 지점은 낯선 용어보다 비슷한 용어의 구분이다.

- **Probability vs Likelihood** — Probability fixes the parameters and treats the data as variable; likelihood fixes the data and is a function of the parameters. The "L" in MLE is the latter. · 확률은 파라미터를 고정하고 데이터를 변수로 본다; 우도는 데이터를 고정하고 파라미터의 함수로 본다. → [[02-foundations/probability|확률 §4]]
- **Entropy vs Cross-entropy vs KL** — $H(p)$ is a distribution's own uncertainty; $H(p,q)$ is the cost of coding data from $p$ with a code built for $q$; KL is the difference, $H(p,q) - H(p)$. · $H(p)$는 자기 자신의 불확실성; $H(p,q)$는 $p$를 $q$의 부호로 인코딩하는 비용; KL = 그 차이. → [[02-foundations/information-theory|정보이론]]
- **Logits vs Probabilities** — Real-valued scores before softmax vs the distribution after it. Losses are usually computed directly from logits for numerical stability. · softmax 이전의 실수 점수 vs 이후의 확률. 손실은 보통 logits에서 직접 계산한다(수치 안정성).
- **Policy vs Controller** — Both map available information to an action, but they come from different formulations and carry different assumptions: a controller may include internal state, an estimator, reference tracking, and a dynamics model (model-based design, stability guarantees); a policy may be stochastic or history-conditioned (learning context). · 둘 다 가용 정보를 행동으로 사상하지만, 서로 다른 정식화에서 나와 다른 가정을 담는다: 제어기는 내부 상태·추정기·기준 추종·동역학 모델을 품을 수 있고(모델 기반 설계, 안정성 보장), 정책은 확률적이거나 이력에 조건부일 수 있다(학습 맥락). → [[04-robotics/modern-robotics/ch11-robot-control|MR 11장]]
- **World model vs Model-based RL** — A world model is the learned dynamics model itself; model-based RL is the methodology that uses one for planning or training. · 월드모델은 학습된 동역학 모델 그 자체; model-based RL은 그것을 계획/학습에 쓰는 방법론. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **VLM vs VLA** — A VLM models image–language relationships (embeddings, similarity, classification, text generation — CLIP is a VLM that generates no text); what distinguishes a VLA is that its output is a robot action (usually a VLM backbone plus an action head). · VLM은 이미지-언어 관계를 모델링한다(임베딩, 유사도, 분류, 텍스트 생성 — CLIP은 텍스트를 생성하지 않는 VLM이다). VLA의 구별점은 출력이 로봇 행동이라는 것이다(보통 VLM 백본 + 행동 헤드). → [[01-canonical-papers/notes/4-vla/rt-2|RT-2]]
- **Kinesthetic vs Tactile (cutaneous)** — Kinesthetic sensing reads muscle, tendon, and joint receptors: limb position, movement, force. Tactile/cutaneous sensing reads skin mechanoreceptors: pressure, vibration, slip, stretch (four afferents: Merkel, Meissner, Pacinian, Ruffini — Johansson & Flanagan 2009). A "haptic device" usually displays kinesthetic force through a tool; a "tactile display" stimulates the skin directly. · 근감각(kinesthetic)은 근육·힘줄·관절 수용기를 읽는다: 사지 위치·운동·힘. 촉각(tactile/cutaneous)은 피부 기계수용기를 읽는다: 압력·진동·미끄러짐·늘림(네 구심신경: Merkel, Meissner, Pacinian, Ruffini). "햅틱 장치"는 보통 도구를 통해 근감각 힘을 전시하고, "촉각 디스플레이"는 피부를 직접 자극한다. → [[06-research-practice/psychophysics-human-measurement|심리물리 §5]]
- **Detection threshold vs JND** — The absolute threshold is the smallest stimulus detectable at all (50% point of the psychometric function); the JND (difference threshold) is the smallest detectable *change* relative to a reference, and by Weber's law scales with it. Device specs care about both: the floor an artifact must stay under is the detection threshold in free space but the JND during contact. · 절대 임계값은 검출 가능한 가장 작은 자극(심리측정 함수의 50% 지점); JND(차이 임계값)는 기준 대비 검출 가능한 가장 작은 *변화*이고 Weber 법칙에 따라 기준에 비례한다. 장치 사양엔 둘 다 필요하다: 인공물이 숨어야 하는 바닥은 자유 공간에서는 절대 임계값, 접촉 중에는 JND다. → [[06-research-practice/psychophysics-human-measurement|심리물리 §1]]
- **Behavior cloning vs Imitation learning vs Offline RL** — BC is a subset of IL (supervised learning on demonstrations); IL covers demonstration use broadly (including DAgger and IRL); offline RL learns from fixed logged data with a reward signal and may improve beyond demonstrated actions when the data and objective support it. · BC는 IL의 부분집합이고, IL에는 DAgger와 IRL도 포함된다. Offline RL은 보상이 붙은 고정 로그 데이터에서 학습하며 데이터와 목적함수가 뒷받침할 때 시연 행동을 넘어설 수 있다.
- **Diffusion vs Flow matching** — Two training formulations for transporting noise to data. FM regresses the velocity field of a chosen probability path and contains diffusion paths as a special case; straight OT-style paths are one such choice, enabling fewer integration steps (not all FM is straight). · 노이즈를 데이터로 수송하는 두 학습 정식화. FM은 선택한 확률 경로의 속도장을 회귀하며 디퓨전 경로를 특수 사례로 포함한다; OT식 직선 경로는 그 선택지 중 하나이고 적분 스텝을 줄여 준다(모든 FM이 직선인 것은 아니다). → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Pose vs Configuration** — A pose is one rigid body's SE(3) position and orientation; a configuration is the whole robot's degrees of freedom, all joint angles included. · pose는 강체 하나의 SE(3) 위치·자세; configuration은 로봇 전체의 자유도다. → [[02-foundations/se3-geometry|SE(3)]], [[04-robotics/modern-robotics/ch02-configuration-space|MR 2장]]
- **Space vs Body (Jacobian/twist)** — The same quantity expressed in a different frame, related by $[\text{Ad}_T]$. The single most common source of sign errors in MR. · 같은 양을 어느 프레임에서 표현했는가의 차이; MR에서 가장 흔한 실수 지점. → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR 3장]]
- **LQR vs MPC** — Infinite-horizon LQR solves a Riccati equation offline for a fixed linear gain $u = -Kx$; MPC re-solves a finite-horizon *constrained* optimization at every step. · 무한 지평 LQR은 오프라인 고정 이득, MPC는 매 스텝 제약 최적화 재풀이. → [[04-robotics/mpc|MPC]]
- **Pole placement vs LQR** — Same feedback form $u = -Kx$; pole placement picks the closed-loop eigenvalues by hand, LQR picks them implicitly by minimizing a quadratic cost. · 같은 $u=-Kx$지만 극점을 손으로 고르느냐, 비용 최소화가 고르느냐의 차이. → [[04-robotics/control-theory-ce397|제어 이론 §7]]
- **Controllability vs Stabilizability** — Controllability means *every* state is reachable; stabilizability only requires every *unstable* mode to be reachable — the weaker and exactly correct condition for a stabilizing controller to exist. · 가제어성은 모든 상태 도달, 안정화 가능성은 불안정 모드만 도달하면 되는 더 약하고 정확한 조건. → [[04-robotics/control-theory-ce397|제어 이론 §6]]
- **Open-loop vs Closed-loop** — Whether new observations are incorporated while the plan executes; a receding horizon is the device that makes a planner closed-loop. · 계획을 실행하는 동안 새 관측을 반영하는가; receding horizon은 closed-loop을 만드는 장치다.
- **Objective vs Metric** — What training minimizes vs what evaluation reports. They frequently differ (train with cross-entropy, evaluate with mAP), and the gap is sometimes the paper's whole point. · 학습이 최소화하는 것 vs 평가가 보고하는 것. 둘은 자주 다르고(교차 엔트로피로 학습, mAP로 평가), 그 간극이 논문의 요점인 경우도 있다. → [[02-foundations/ml-practice|ML 실무 §3]]
- **Feature vs Representation vs Embedding vs Latent** — Overlapping but not interchangeable: a feature is a variable extracted from observations or intermediate computation; a representation is how information is held overall (used when discussing quality); an embedding maps objects into a continuous vector space; a latent is an unobserved generative or probabilistic variable. · 겹쳐 쓰이지만 호환되지는 않는다: feature는 관측이나 중간 계산에서 뽑아낸 변수, representation은 정보가 전체적으로 담기는 방식(품질을 논할 때 쓴다), embedding은 대상을 연속 벡터 공간으로 보내는 사상, latent는 관측되지 않는 생성·확률 변수다.
- **Inference vs Prediction vs Generation** — All three mean "run the trained model", but inference is execution in general, prediction is output with a ground truth, and generation is sampling from a distribution. Statistical inference (parameter estimation) and deep-learning inference (a forward pass) are different words. · 셋 다 "학습된 모델을 돌린다"는 뜻이지만, inference는 실행 일반, prediction은 정답이 있는 출력, generation은 분포에서의 샘플링이다. 통계학의 inference(모수 추정)와 딥러닝의 inference(순전파)는 다른 말이니 주의.
- **Conditioning vs Prompting** — Conditioning is the structural idea of supplying information as input; prompting is the interface of supplying that condition as text. All prompting is conditioning, not the reverse (image, pose, and state conditioning exist). · 조건화는 정보를 입력으로 넣는다는 구조적 발상이고, 프롬프팅은 그 조건을 텍스트로 주는 인터페이스다. 모든 프롬프팅은 조건화지만 역은 아니다 — 이미지·자세·상태 조건화도 있다.
- **Pretraining vs Fine-tuning vs Post-training** — Large general data → adaptation on task data → (recent usage) the whole alignment stage including SFT and RLHF. π0's "post-training" specifically means its high-quality task-data stage. · 대규모 일반 데이터로 사전학습 → 과제 데이터로 미세조정 → 최근에는 SFT와 RLHF를 포함한 정렬 단계 전체를 post-training이라 부르기도 한다. π0에서 post-training은 구체적으로 고품질 과제 데이터 단계다. → [[01-canonical-papers/notes/4-vla/pi0\|π0]]
- **Generalization vs Robustness vs Transfer** — New samples from the same distribution / a perturbed or shifted distribution / a different task or domain. Check which one a paper actually measures. · 같은 분포의 새 샘플 / 교란·이동된 분포 / 다른 과제·도메인으로의 이전.
- **Zero-shot vs Few-shot vs OOD** — Zero task examples / a handful / evaluation outside the training distribution. Even for zero-shot, the real question is whether similar data sat in pretraining. · 과제 예시 0개 / 소수 / 학습 분포 밖 평가. zero-shot이라도 진짜 물어야 할 것은 비슷한 데이터가 사전학습에 있었는가다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]], [[01-canonical-papers/notes/1-foundations/gpt-3|GPT-3]] (zero/few-shot 자체), [[02-foundations/ml-practice|ML 실무 §1]] (분포 이동)
- **Intrinsics vs Extrinsics** — Properties of the camera itself (focal length, principal point, distortion; fixed until the lens is touched) vs the camera's SE(3) pose relative to another frame (recalibrate when the mount changes). · 카메라 자체의 성질(초점거리, 주점, 왜곡 — 렌즈를 건드리기 전까지 고정) / 다른 프레임에 대한 카메라의 SE(3) pose(마운트가 바뀌면 다시 보정). → [[04-robotics/geometric-perception-calibration|Geometric Perception]]
- **Geometric vs Deep perception** — Geometric perception gives pose, shape, and frame relations (*where*, at *what scale*); deep perception gives semantics (*what*). Modern pipelines mix them, and reading requires knowing which stage is which. · 기하 인식은 자세·형상·프레임 관계를(*어디에*, *어떤 스케일로*), 딥 인식은 의미를(*무엇인지*) 준다. 현대 파이프라인은 둘을 섞으므로, 읽을 때 어느 단계가 어느 쪽인지 알아야 한다.
- **State machine vs Behavior tree** — Both are execution layers: an FSM is a graph of states and transitions (simple, but tangles as it grows); a behavior tree composes sequence/fallback nodes modularly (common in field systems). · 둘 다 실행 계층이다: FSM은 상태와 전이의 그래프(단순하지만 커질수록 엉킨다), behavior tree는 sequence/fallback 노드를 모듈식으로 조합한다(필드 시스템에서 흔하다). → [[04-robotics/robot-systems-deployment|Robot Systems §6]]
- **State vs Observation vs Estimate vs Belief** — The hidden variables the model needs / the sensor values / the inferred point summary / the distribution over possible states. · state는 모델에 필요한 숨은 변수, observation은 센서값, estimate는 점 요약, belief는 분포. → [[04-robotics/state-estimation-slam|State Estimation]]
- **Covariance vs Confidence** — Covariance is the variance and correlation structure of the estimation error *under the assumed model*. If the model is wrong it can be small and overconfident; it is not a synonym for confidence in general. · covariance는 가정한 모델 아래 추정 오차의 구조이지 일반적 confidence와 동의어가 아니다.
- **Odometry vs Localization vs Mapping vs SLAM** — Relative motion / pose within a known map / the map given poses / poses and map jointly. · 상대 이동 / 알려진 지도에서 pose / pose가 주어진 지도 / 공동 추정. → [[04-robotics/state-estimation-slam|State Estimation & SLAM]]
- **Path vs Trajectory vs Plan vs Policy** — Geometry without timing / time-indexed states and inputs / a proposed sequence of future decisions / a rule from current information to an action. · 시간 없는 기하 곡선 / 시간별 상태·입력 / 미래 결정의 제안 / 정보에서 행동으로의 규칙. → [[04-robotics/planning-decision-making|Planning]]
- **Planner vs Controller** — A planner produces future references or actions; a controller keeps execution on them with feedback. In MPC and learned policies the boundary merges, so check the actual inputs, outputs, and rates. · planner는 미래 reference를 만들고 controller는 feedback으로 실행을 유지한다.
- **Form closure vs Force closure** — Blocking motion by contact geometry alone vs resisting an arbitrary external wrench with admissible contact forces. Both depend on the contact model and friction assumptions. · 접촉 기하로 막는 조건 / 허용 접촉력으로 임의 외력 wrench에 저항하는 조건. → [[04-robotics/contact-force-tactile|Contact]]
- **Manipulability ellipsoid vs apparent-mass ellipsoid** — Two ellipses drawn at the same end-effector, read in opposite directions: the manipulability ellipsoid's long axis is the direction that is *easy to move*, the apparent-mass ellipsoid's long axis is the direction that is *hard to move*. They are reciprocal views of the same geometry. · 같은 말단에 그리는 두 타원이고 정반대로 읽는다: 가조작성 타원의 긴 축은 *움직이기 쉬운* 방향, 겉보기 질량 타원의 긴 축은 *움직이기 어려운* 방향이다. 같은 기하의 서로 역수인 두 관점. → [[02-foundations/manipulator-kinematics-dynamics|10. §6]]
- **Impedance vs Admittance** — Shaping the relation from motion error to interaction force vs from measured force to motion response. · 운동 오차에서 힘으로의 관계 형성 / 측정 힘에서 운동 응답으로의 관계 형성. → [[04-robotics/contact-force-tactile|Contact]]
- **Frequency vs Latency vs Jitter** — Updates per second / time from input to effect / the variation in that time. A high Hz does not guarantee low latency. · 초당 update 수 / 입력에서 효과까지의 시간 / 그 시간의 변동. → [[04-robotics/robot-systems-deployment|Robot Systems]]
- **Hazard vs Risk** — A potential source of harm vs the combination of likelihood/exposure and consequence under a defined method. · 잠재적 harm의 원천 / 가능성·노출과 consequence의 결합. → [[04-robotics/hri-safety|HRI & Safety]]
- **Fail-safe vs Fail-operational** — Failure leads toward a lower-risk state vs a selected function continues through specified failures. · 실패 시 더 낮은 위험 상태로 전이 / 정해진 실패 중에도 선택 기능 유지. → [[04-robotics/hri-safety|HRI & Safety]]
- **Repeatability vs Reproducibility vs Replicability** — Rerunning the same setup / an independent team rerunning your artifacts and procedure / an independent implementation reaching the same claim. Usage varies by community, so a paper must define it. · 같은 설정을 다시 돌리기 / 독립된 팀이 당신의 산출물과 절차를 다시 돌리기 / 독립 구현이 같은 주장에 도달하기. 커뮤니티별 용법이 달라 논문 안에서 정의해야 한다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design]]

## 0–9 · A–C

- **0/1 knapsack** · 0/1 배낭 — Choose items to take whole or leave, maximizing total value within a weight capacity $W$. It is NP-hard; the standard exact method is an $O(nW)$ table, and greedy by value density can fail. · 물건을 통째로 넣거나 빼서 무게 한도 $W$ 안에서 가치 합을 최대화하는 문제. NP-난해이며, 표준 정확 해법은 $O(nW)$ 표이고 가치 밀도 그리디는 틀릴 수 있다. → [[02-foundations/algorithms/dynamic-programming|11.5 §4]], [[02-foundations/algorithms/greedy-mst|11.4 §4]]
- **π0 (pi-zero)** · π0 — A VLA family (π0, π0.5, π0-FAST) that pairs a VLM backbone with a flow-matching action expert producing continuous action chunks. "π0-style" now names the VLM-plus-continuous-action-expert design point. · VLM 백본에 연속 행동 청크를 만드는 flow matching 행동 전문가를 붙인 VLA 계열(π0, π0.5, π0-FAST). "π0식"은 이제 VLM + 연속 행동 전문가라는 설계 지점을 가리킨다. → [[01-canonical-papers/notes/4-vla/pi0|π0]]
- **3D CNN** · 3D CNN — A video backbone whose convolution kernels span time as well as space; I3D inflates 2D kernels to 3D and pretrains on a large clip dataset. Memory grows with frames; the window stays short. · 커널이 공간과 시간에 함께 걸친 합성곱 비디오 백본. I3D는 2D 커널을 3D로 팽창하고 대규모 클립 데이터로 사전학습한다. 메모리가 프레임 수에 비례하고 시간 창이 짧다. → [[04-robotics/video-action-understanding|Video §3]]
- **6D rotation representation** · 6D 표현 — Encoding a rotation by the first two columns of its matrix, re-orthonormalized with Gram–Schmidt. Robot-learning papers use it because low-dimensional rotation coordinates must jump somewhere, which hurts regression. · 회전 행렬의 앞 두 열을 출력하고 Gram-Schmidt로 정규직교화해 회전을 나타내는 방식. 저차원 회전 좌표는 어딘가에서 반드시 튀어 회귀를 망치므로 로봇 학습 논문이 이것을 쓴다. → [[02-foundations/se3-geometry|SE(3) §2]]
- **ABI (application binary interface)** · ABI — The compiled layout of functions and data structures that other binaries were linked against. Overriding an underlay package with an ABI-changing version makes dependents crash with no sensible message. · 다른 바이너리가 링크될 때 기준이 된 함수와 자료구조의 컴파일된 배치. ABI를 바꾸는 버전으로 언더레이 패키지를 덮으면 그것에 의존하는 패키지가 알 수 없는 메시지와 함께 죽는다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §6]]
- **Ablation** · 절제 실험(ablation) — An experiment that removes or changes one component, objective, data choice or hyperparameter and reports the effect. It is how a paper argues a piece mattered; it does not test interactions between pieces. · 구성요소·목적함수·데이터·하이퍼파라미터 하나를 빼거나 바꿔 그 효과를 보고하는 실험. 어떤 부품이 중요했다고 논증하는 방식이며, 요인 간 상호작용까지 검증하지는 않는다. → [[02-foundations/ml-practice|ML Practice §4]], [[01-canonical-papers/how-to-read|How to Read §2]]
- **Absolute trajectory error (ATE)** · Absolute Trajectory Error(ATE) — The global error between an estimated trajectory and ground truth; Relative Pose Error (RPE) instead measures local drift over intervals. A low average can hide rare catastrophic tracking losses. · 추정 궤적과 정답 궤적 사이의 전역 오차이고, Relative Pose Error(RPE)는 구간별 국소 drift를 잰다. 낮은 평균이 드문 파국적 추적 손실을 가릴 수 있다. → [[04-robotics/state-estimation-slam|State estimation §9]]
- **Acceptance rate** · 채택률 — The share of submissions a venue accepts. Quote only official figures: IROS and NeurIPS publish a rate, ICRA publishes counts but no percentage, and RSS and CoRL publish none. · 제출 논문 중 venue가 채택한 비율. 공식 수치만 인용한다. IROS·NeurIPS는 비율을 발표하고, ICRA는 건수만 내며, RSS·CoRL은 아무것도 발표하지 않는다. → [[06-research-practice/venue-strategy|Venue Strategy §3]]
- **Accuracy (top-k accuracy)** · 정확도 — The fraction of predictions that are correct; top-5 counts a hit when the truth is among five guesses. On imbalanced data it misleads: with 6% positives, a detector that always says "fine" scores 94%. · 맞춘 예측의 비율이며, top-5는 정답이 다섯 후보 안에 있으면 맞은 것으로 센다. 불균형 데이터에서는 오도한다: 양성이 6%면 늘 "정상"이라 답하는 검출기도 94%가 나온다. → [[02-foundations/ml-practice|ML Practice §3]]
- **ACT (Action Chunking with Transformers)** · ACT — An imitation policy that predicts the next 100 joint actions at once with a CVAE whose decoder is a Transformer, and smooths execution with temporal ensembling. It learned fine bimanual tasks from about 50 demonstrations. · 디코더가 Transformer인 CVAE로 다음 100개 관절 행동을 한 번에 예측하고 temporal ensembling으로 실행을 매끄럽게 하는 모방 정책. 시연 약 50개로 정밀 양팔 과제를 학습했다. → [[01-canonical-papers/notes/4-vla/act|ACT]]
- **Action (.action)** · 액션(action) — A long-running, cancellable goal with streaming feedback and a final result, defined by a .action file of goal, result and feedback. Built from topics and services; navigation and manipulation use it. · 오래 걸리고 취소할 수 있으며 피드백을 흘려보내고 최종 결과를 내는 목표. goal·result·feedback 세 부분의 .action 파일로 정의한다. 토픽과 서비스로 만들어졌고 내비게이션과 매니퓰레이션이 쓴다. → [[04-robotics/ros2/services-actions-parameters|25.3 Services & Actions §4]]
- **Action anticipation** · 행동 예측(anticipation) — Predicting the label of an action that starts $\tau$ seconds ahead, $p(y_{t+\tau}\mid x_{1:t})$, from video up to time $t$ only. Accuracy falls as $\tau$ grows, so results belong on a curve over $\tau$. · $t$까지의 영상만으로 $\tau$초 뒤 시작될 행동의 레이블 $p(y_{t+\tau}\mid x_{1:t})$를 예측한다. $\tau$가 커지면 정확도가 떨어지므로 결과는 $\tau$에 대한 곡선으로 보고한다. → [[04-robotics/video-action-understanding|Video §4]]
- **Action binning (action discretization)** · 행동 이산화(구간 나누기) — Turning each continuous action dimension into one of a fixed number of bins (256 in RT-1, RT-2 and OpenVLA), so control becomes next-token classification. It fits multimodal, high-frequency motion poorly. · 연속 행동의 각 차원을 정해진 개수의 구간 중 하나로 바꾸는 것(RT-1·RT-2·OpenVLA는 256개)으로, 제어가 다음 토큰 분류가 된다. 다봉적이고 고주파인 움직임에는 잘 맞지 않는다. → [[01-canonical-papers/notes/4-vla/rt-1|RT-1]], [[01-canonical-papers/notes/4-vla/index|VLA Map §1]]
- **Action chunking** · 행동 청킹 — Predicting actions in blocks of tens of steps instead of one at a time, which shrinks compounding error. · 행동을 하나씩이 아니라 수십 스텝 덩어리로 예측해 복합 오차를 줄이는 기법. → [[01-canonical-papers/notes/4-vla/act|ACT]], [[01-canonical-papers/notes/4-vla/diffusion-policy|Diffusion Policy]]
- **Action-conditioned predictive model** · 행동 조건부 예측 모델 — A learned model that predicts what will happen, such as collision, bumpiness, or position, if the robot executes a candidate action sequence; a planner then picks the sequence with the best predicted events. · 후보 행동열을 실행하면 충돌·흔들림·위치 같은 어떤 사건이 일어날지 예측하는 학습 모델. 계획기는 예측 사건이 가장 좋은 행동열을 고른다. → [[01-canonical-papers/notes/9-navigation/badgr|BADGR]]
- **Action-conditioned world model** · 행동 조건 월드모델 — A world model whose predicted future depends on an action you feed it, so an agent can try actions inside it. Sora-style video generators lack this; Genie, Cosmos post-training and V-JEPA 2-AC add it. · 넣어 준 행동에 따라 예측하는 미래가 달라지는 월드모델로, 에이전트가 그 안에서 행동을 시험해 볼 수 있다. Sora식 비디오 생성기에는 없고, Genie·Cosmos 사후학습·V-JEPA 2-AC가 이를 더한다. → [[01-canonical-papers/notes/5-world-models/genie|Genie]], [[01-canonical-papers/notes/5-world-models/cosmos|Cosmos]]
- **Action head (action expert)** · 행동 헤드(action expert) — The output part of a policy that turns features into actions. Expressive heads (diffusion, flow matching) can represent several action modes; in VLAs an action expert turns VLM backbone output into continuous control. · 특징을 행동으로 바꾸는 정책의 출력 부분. 디퓨전·flow matching 같은 표현력 있는 헤드는 여러 행동 모드를 표현할 수 있고, VLA에서는 action expert가 VLM 백본의 출력을 연속 제어로 바꾼다. → [[02-foundations/rl-basics|RL Basics §6]], [[03-deep-learning/lineage|Lineage]]
- **Action horizon (chunk size)** · 청크 크기(행동 지평) — How many future actions one policy query predicts, such as 100 in ACT or 16 in Diffusion Policy. Longer chunks cut compounding error but react more slowly; often only the first few are executed. · 정책을 한 번 질의할 때 예측하는 미래 행동의 개수(ACT는 100, Diffusion Policy는 16 등). 길수록 복합 오차는 줄지만 반응이 느려지며, 흔히 앞부분 몇 개만 실행한다. → [[01-canonical-papers/notes/4-vla/diffusion-policy|Diffusion Policy]], [[01-canonical-papers/notes/4-vla/act|ACT]]
- **Action interface (action space)** · 행동 인터페이스 — What a policy's "action" physically commands: joint position, velocity, torque, motor current, end-effector pose, impedance target, or a skill. The same learned model behaves differently when the interface or control rate changes. · 정책의 "action"이 물리적으로 명령하는 것. 관절 위치·속도·토크, 모터 전류, 말단 pose, 임피던스 타깃, 상위 스킬 중 무엇인지. 인터페이스나 제어 주기가 바뀌면 같은 학습 모델도 다르게 행동한다. → [[04-robotics/robot-systems-deployment|Robot Systems §2]]
- **Action recognition** · 행동 인식 — Assigning one action label to a trimmed clip, one already cut to contain just the action, usually scored by top-1/top-5 accuracy. A recognition number does not show the model can anticipate. · 행동 하나만 담도록 이미 잘린 클립에 레이블 하나를 붙이는 과제로, 보통 top-1/top-5 정확도로 채점한다. 인식 수치는 예측 능력을 입증하지 않는다. → [[04-robotics/video-action-understanding|Video §1]]
- **Action token** · 액션 토큰 — The unit a VLA emits on the way to motion: a skill name, code, affordance, trajectory, goal state, latent code, raw command, or reasoning. Its formulation is the main design choice separating VLAs. · VLA가 움직임에 이르기까지 내놓는 단위: skill 이름, 코드, 어포던스, 궤적, 목표 상태, 잠재 코드, 원시 명령, 추론. 이것을 어떤 형식으로 잡느냐가 VLA를 가르는 핵심 설계 선택이다. → [[01-canonical-papers/notes/4-vla/index|VLA Map §1]]
- **Activation function** · 활성함수 — The simple nonlinearity applied between layers (ReLU $\max(0,z)$ is the common one). Without it, stacked layers collapse into a single matrix. · 층 사이에 적용하는 단순한 비선형 함수. 없으면 층을 쌓아도 행렬 하나로 접힌다. → [[02-foundations/neural-network-basics|0.7]]
- **Active object** · active object(능동 물체) — In egocentric video, the one object actually being acted on among the many visible. Identifying it is distinct from, and usually harder than, detection, and it is what matters for intent. · 자기중심 영상에 보이는 많은 물체 가운데 실제로 작용받는 하나. 식별은 검출과 별개이고 대개 더 어려우며, 의도에 중요한 것은 이쪽이다. → [[04-robotics/egocentric-perception|Egocentric §2]]
- **Actor-critic** · 액터-크리틱 — A policy-gradient method with two learned parts: the actor is the policy; the critic is a value function learned by TD, whose error $\delta$ serves as a one-sample advantage estimate. PPO and SAC are examples. · 두 부분을 학습하는 정책 그래디언트 방법: 액터는 정책이고, 크리틱은 TD로 배우는 가치 함수이며 그 오차 $\delta$가 1-샘플 어드밴티지 추정으로 쓰인다. PPO와 SAC가 액터-크리틱이다. → [[02-foundations/rl-basics|RL Basics §4]]
- **Actuator** · 액추에이터 — The motor, hydraulic cylinder, or other device that produces the force or torque a controller commands. Its limits (saturation, rate limits, dead zones) bound what any control law can achieve. · 제어기가 명령한 힘이나 토크를 실제로 만드는 모터·유압 실린더 같은 장치. 포화·속도 한계·데드존 같은 그 한계가 어떤 제어 법칙으로 이룰 수 있는 것을 제한한다. → [[04-robotics/control-theory-ce397|Control Theory §9]]
- **Actuator network (learned actuator model)** · 액추에이터 넷(학습된 액추에이터 모델) — A neural network trained on logged machine data that maps actuator commands, such as hydraulic valve commands, to joint motion. Embedded in the simulator, it makes simulated dynamics match the real machine. · 기계 로그 데이터로 학습해 유압 밸브 명령 같은 구동 명령을 관절 운동으로 사상하는 신경망. 시뮬레이터에 내장해 시뮬레이션 동역학을 실제 기계에 맞춘다. → [[01-canonical-papers/notes/8-construction/egli-rl|Egli RL]]
- **adaLN-Zero** · adaLN-Zero — DiT's conditioning layer: normalization whose scale and shift are predicted from the timestep and class, initialized to zero so each block starts as a no-op. It beat cross-attention and in-context tokens in DiT's ablations. · DiT의 조건 주입 층: 스케일과 이동을 타임스텝·클래스에서 예측하는 정규화이며, 0으로 초기화해 각 블록이 no-op에서 출발한다. DiT 절제 실험에서 cross-attention과 in-context 토큰을 이겼다. → [[01-canonical-papers/notes/6-diffusion/dit|DiT]]
- **Adam** · Adam — An optimizer that gives each parameter its own step size, scaled down by a running average of its squared gradients, and adds momentum plus a bias correction for the zero-initialized averages. · 각 파라미터의 스텝 크기를 제곱 그래디언트의 이동 평균으로 따로 줄이고, 모멘텀과 0으로 초기화된 평균에 대한 편향 보정을 더한 최적화기. → [[02-foundations/optimization|Optimization §3]]
- **AdamW** · AdamW — Adam with weight decay applied as a shrink outside the adaptive step, so every weight decays at the same rate; an L2 term in Adam's loss instead decays weights with large gradient histories less. · weight decay를 적응 스텝 바깥에서 직접 줄이는 방식으로 적용한 Adam. 모든 가중치가 같은 비율로 줄어든다. Adam 손실에 L2 항을 더하면 그래디언트 이력이 큰 가중치가 덜 줄어든다. → [[02-foundations/optimization|Optimization §3]]
- **Adaptation module (base policy plus adapter)** · 적응 모듈 — A small network that estimates a latent description of the current dynamics (terrain, payload, wear) from recent proprioceptive history and feeds it to a base policy, so behaviour adapts online within fractions of a second. · 최근 고유수용 이력에서 현재 동역학(지형·하중·마모)을 기술하는 잠재 변수를 추정해 기본 정책에 넣는 작은 네트워크. 행동이 1초의 몇 분의 일 만에 온라인으로 적응한다. → [[01-canonical-papers/notes/9-navigation/rma|RMA]]
- **Adapter** · 어댑터 — Parameter-efficient adaptation while most pretrained weights remain frozen. Classical adapters insert trainable bottleneck modules; [[01-canonical-papers/notes/1-foundations/lora|LoRA]] instead parameterizes a low-rank weight update that can be merged into the original weights, usually adding no inference latency after merging. · 사전학습 가중치 대부분을 고정한 채 적은 파라미터로 적응하는 방법. 고전적 adapter는 학습 가능한 병목 모듈을 끼우고, [[01-canonical-papers/notes/1-foundations/lora|LoRA]]는 원 가중치에 합칠 수 있는 저랭크 갱신을 매개변수화해 병합 후에는 보통 추론 지연을 더하지 않는다. → [[02-foundations/neural-network-basics|Neural Network Basics §6]]
- **Adaptive autonomy** · 적응적 자율성 — Changing how much of the work the robot does on its own, for example handing more to the robot or back to the worker as conditions or worker state change. · 조건이나 작업자 상태에 따라 로봇이 스스로 하는 몫을 늘리거나 작업자에게 되돌리는 등, 로봇 자율의 정도를 바꾸는 것. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Additivity** · 가법성 — One of the two conditions for linearity: adding inputs first gives the same output as adding outputs, $f(x+y)=f(x)+f(y)$ for all $x, y$. With homogeneity it forms the superposition principle. · 선형성의 두 조건 중 하나. 입력을 먼저 더한 결과가 출력을 더한 결과와 같다: 모든 $x, y$에 대해 $f(x+y)=f(x)+f(y)$. 동차성과 합쳐 중첩 원리가 된다. → [[02-foundations/engineering-math|0.5 §4.5]]
- **ADE and FDE (average and final displacement error)** · ADE·FDE — ADE is the Euclidean distance between predicted and true positions averaged over the forecast horizon; FDE is that distance at the last step. $\text{minADE}_k$ keeps the best of $k$ samples, rewarding coverage, not calibration. · ADE는 예측 위치와 실제 위치 사이 유클리드 거리를 예측 구간 전체에 걸쳐 평균한 값, FDE는 마지막 시점의 그 거리다. $\text{minADE}_k$는 $k$개 샘플 중 가장 가까운 것만 남기므로 보정이 아니라 커버리지를 보상한다. → [[04-robotics/human-intent-prediction|Human Intent §1]]
- **Adjacency list** · 인접 리스트 — A graph representation storing, for each vertex, the list of its neighbours. It uses $O(V + E)$ memory and is the right default for sparse graphs, which are nearly all real ones. · 정점마다 이웃 목록을 저장하는 그래프 표현. 메모리는 $O(V + E)$이고, 거의 모든 실제 그래프인 희소 그래프의 기본 선택이다. → [[02-foundations/algorithms/graph-algorithms|11.6 §1]]
- **Adjacency matrix** · 인접 행렬 — A $V \times V$ table whose entry $(u, v)$ records whether, or with what weight, an edge exists. Edge tests are $O(1)$ but memory is $O(V^2)$, so it suits dense graphs of a few thousand vertices. · $(u, v)$ 칸에 간선 유무나 가중치를 적는 $V \times V$ 표. 간선 검사는 $O(1)$이지만 메모리가 $O(V^2)$이라, 정점 수천 개 규모의 조밀 그래프에 맞는다. → [[02-foundations/algorithms/graph-algorithms|11.6 §1]]
- **Adjoint map** · adjoint — The 6×6 matrix $[\text{Ad}_T]$ built from a pose $T=(R,p)$ that converts a twist from one frame to another, $\mathcal{V}_s = [\text{Ad}_T]\mathcal{V}_b$. It also relates the space and body Jacobians. · 자세 $T=(R,p)$로 만든 6×6 행렬 $[\text{Ad}_T]$로, twist를 한 프레임에서 다른 프레임으로 옮긴다: $\mathcal{V}_s = [\text{Ad}_T]\mathcal{V}_b$. space·body 야코비안도 이것으로 연결된다. → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR ch.3 §4]]
- **Admissible heuristic** · 허용적 휴리스틱 — A cost-to-go estimate that never overestimates the true remaining cost, $h(n) \le h^*(n)$. It keeps A\* optimal, but if it is not also consistent, A\* must reopen closed vertices. · 실제 남은 비용을 절대 과대추정하지 않는 추정값, $h(n) \le h^*(n)$. A\*를 최적으로 유지하지만, 일관적이지 않으면 A\*가 닫힌 정점을 다시 열어야 한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §6]]
- **Admission test (scope rule)** · 입장 시험 — The program's filter for any new topic: does this directly improve the construction manipulation research question? Yes allows Working or Mastery; no keeps it at Literacy, which is not a demotion. · 새 주제를 거르는 프로그램의 기준: 이것이 건설 매니퓰레이션 연구 질문을 직접 개선하는가? 예면 Working이나 Mastery로 올릴 수 있고, 아니오면 Literacy에 머문다. 강등이 아니다. → [[07-research-program/index|Research Program §7]]
- **Admittance control** · 어드미턴스 제어 — Measure or estimate the external force, run it through a virtual mass-damper-spring model to generate a motion reference, and let an inner position or velocity loop track it. Suits robots that accept only motion commands. · 측정·추정한 외력을 가상 질량-감쇠-스프링 모델에 넣어 운동 기준을 만들고, 내부 위치·속도 루프가 그것을 추종한다. 운동 명령만 받는 로봇에 맞는다. → [[04-robotics/force-compliance-control|Force & Compliance Control §2]]
- **Admittance display** · admittance display — A haptic device that measures applied force and commands motion. It relies on a high-bandwidth motion servo and is often built on a non-backdrivable industrial mechanism. · 가해진 힘을 측정해 운동을 명령하는 햅틱 장치. 고대역폭 motion servo에 의존하며, 역구동이 되지 않는 산업용 메커니즘 위에 만드는 경우가 많다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §1]]
- **Advantage** · 어드밴티지 — $A = Q - V$: how much better an action is than that state's average choice. · 어떤 행동이 그 상태의 평균적 선택보다 얼마나 나은가. → [[02-foundations/rl-basics|RL 기초]]
- **Aerial additive manufacturing (aerial AM)** · 공중 적층 제조 — Cooperating drones that deposit and inspect building material in flight. The Nature 2022 demonstration is an existence proof with payload, material, and scale limits. · 협력하는 드론이 비행 중 건축 재료를 적층하고 검사하는 방식. Nature 2022 시연은 탑재 중량·재료·규모에 한계가 있는 존재 증명이다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §4]]
- **Affine map** · 아핀 사상 — A linear map plus a constant offset, $f(x)=Ax+b$. It is linear only when $b=0$; the quick test is $f(0)=b\neq 0$. A neural network's linear layer $Wx+b$ is affine. · 선형 사상에 상수 절편을 더한 것, $f(x)=Ax+b$. $b=0$일 때만 선형이고, 빠른 판별법은 $f(0)=b\neq 0$이다. 신경망의 선형층 $Wx+b$는 아핀이다. → [[02-foundations/engineering-math|0.5 §4.5]]
- **Affordance** · 어포던스 — What an object or state makes possible for a particular robot (graspable, pushable). In SayCan-style planners an affordance model scores whether a skill can succeed *here and now*, which is what grounds a language plan. · 특정 로봇에게 그 대상·상태가 무엇을 가능하게 하는가. 언어 계획을 접지하는 것이 이 점수다. → [[01-canonical-papers/notes/4-vla/saycan|SayCan]]
- **Alarm fatigue** · 경보 피로 — Operators ignoring or disabling warnings because too many are false. A warning system with 20% precision raises four false alarms per true one, so adding alarms can reduce safety. · 오경보가 너무 많아 작업자가 경고를 무시하거나 꺼 버리는 현상. 정밀도 20%인 경고 시스템은 진짜 하나당 오경보 넷을 내므로, 경보를 늘리면 오히려 안전이 떨어질 수 있다. → [[04-robotics/hri-safety|HRI & Safety §5]], [[04-robotics/human-intent-prediction|Human Intent §5]]
- **Aliasing** · 에일리어싱 — Frequencies above half the sampling rate reappear as false lower ones: a 60 Hz vibration sampled at 50 Hz looks like 10 Hz. Only an anti-alias filter before sampling or downsampling prevents it. · 샘플링 주파수의 절반보다 높은 성분이 가짜 낮은 주파수로 다시 나타나는 현상(50 Hz로 샘플링한 60 Hz 진동이 10 Hz로 보인다). 샘플링이나 다운샘플링 전의 안티에일리어스 필터만이 막을 수 있다. → [[02-foundations/signal-processing|Signal Processing §2]]
- **Alignment (LLM alignment)** · 정렬(alignment) — Making a model's outputs match what users actually want (helpful, honest, harmless) rather than merely likely text. InstructGPT's objective gap: next-token likelihood is not user intent. · 모델 출력을 그럴듯한 텍스트가 아니라 사용자가 실제로 원하는 것(도움, 정직, 무해)에 맞추는 일. 다음 토큰 우도는 사용자 의도와 같지 않다는 것이 InstructGPT의 출발점이다. → [[01-canonical-papers/notes/1-foundations/instructgpt|InstructGPT]]
- **ALOHA** · ALOHA — A low-cost open-source bimanual teleoperation rig: two leader arms puppeteer two follower arms, with four cameras. It became the field's standard data-collection setup (Mobile ALOHA, ALOHA 2). · 저비용 오픈소스 양팔 원격조작 장비: 리더 팔 두 개로 팔로워 팔 두 개를 조종하고 카메라 네 대를 단다. 이후 분야의 표준 데이터 수집 장비가 됐다(Mobile ALOHA, ALOHA 2). → [[01-canonical-papers/notes/4-vla/act|ACT]]
- **AMCL (Adaptive Monte Carlo Localization)** · AMCL — Nav2's particle-filter localizer over a static occupancy grid; it outputs the map → odom transform. It needs an initial pose and, by default, does not recover a kidnapped robot. · 정적 점유 격자 위에서 도는 Nav2의 파티클 필터 위치 추정기로, map → odom 변환을 낸다. 초기 자세가 필요하고, 기본 설정에서는 옮겨진 로봇(kidnapped robot)을 되찾지 못한다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §7]]
- **ament (ament_cmake, ament_python)** · ament — The build system of CMake macros and Python conventions that decides where a package's outputs go; colcon invokes it. The two build types are ament_cmake (C++, interfaces) and ament_python. · 패키지 산출물이 어디로 갈지 정하는 CMake 매크로와 Python 관례로 이루어진 빌드 시스템이고, colcon이 이것을 호출한다. build type은 ament_cmake(C++, 인터페이스)와 ament_python 둘이다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §4]]
- **ament index** · ament 인덱스 — A directory of marker files under share/ament_index that ROS 2 tools scan to list installed packages; a package without its marker is invisible to them. · share/ament_index 아래의 마커 파일 모음으로, ROS 2 도구가 설치된 패키지를 나열할 때 훑는다. 마커가 없는 패키지는 도구에게 보이지 않는다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §4]]
- **Amortization (amortized inference)** · 상각(amortize) — Paying once in training so each later query is one forward pass: a VAE encoder outputs the posterior directly instead of optimizing per datapoint, and Dreamer's policy replaces online planning. · 학습에서 한 번 비용을 치러 이후 질의를 forward pass 한 번으로 만드는 것: VAE 인코더는 데이터포인트마다 최적화하지 않고 사후분포를 바로 출력하며, Dreamer의 정책은 온라인 플래닝을 대신한다. → [[01-canonical-papers/notes/6-diffusion/vae|VAE]], [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **Amortized cost** · 분할상환 비용 — The worst-case total cost of a sequence of operations divided by their number. No probability is involved: one resize costs $O(n)$, yet dynamic-array append is $O(1)$ amortized. · 연산 열 전체의 최악 비용을 연산 수로 나눈 값. 확률과 무관하다: 리사이즈 한 번은 $O(n)$이지만 동적 배열의 append는 분할상환 $O(1)$이다. → [[02-foundations/algorithms/complexity-recursion|11.1 §4]]
- **Analytic inverse kinematics (closed-form IK)** · 해석적 IK — Solving IK with explicit formulas instead of iteration, possible only for special geometries such as a 6R arm with a spherical wrist. It enumerates every solution branch exactly. · 반복 대신 명시적 공식으로 IK를 푸는 것으로, 구면 손목을 가진 6R 팔 같은 특수한 기하에서만 가능하다. 모든 해의 가지를 정확히 열거한다. → [[04-robotics/modern-robotics/ch06-inverse-kinematics|MR ch.6 §1]]
- **Anchor bolt (anchor-bolt setting)** · 앵커 볼트 — A steel bolt set into a hole drilled in concrete so that equipment, hangers, or panels can be fastened to it. Robotic setting combines overhead drilling with thrust and position control. · 콘크리트에 뚫은 구멍에 박아 장비·행거·패널을 고정하는 강철 볼트. 로봇 설치는 천장 드릴링의 추력 제어와 위치 제어를 함께 요구한다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Anchor box** · 앵커 박스 — Preset reference boxes of several scales and aspect ratios placed at every feature-map location; the detector predicts objectness and box offsets relative to each. DETR removed them. · 특징 맵의 모든 위치에 놓은 여러 스케일·종횡비의 기준 박스. 검출기는 각 앵커에 대해 물체성 점수와 박스 이동량을 예측한다. DETR이 이것을 없앴다. → [[01-canonical-papers/notes/2-computer-vision/faster-r-cnn|Faster R-CNN]]
- **Angular resolution (LiDAR)** · 각분해능 — The angle between adjacent laser rays. Point spacing on a surface grows with distance, roughly range × tan(angle), so small defects can fall between samples far away. · 인접한 레이저 광선 사이의 각도. 표면의 점 간격은 대략 거리 × tan(각도)로 거리에 따라 커져, 먼 곳의 작은 결함은 샘플 사이로 빠질 수 있다. → [[05-construction-robotics/site-perception|Site Perception §1]]
- **Anomaly detection** · 이상 탐지 — Flagging inputs unlike the training data instead of trusting a prediction on them. In WVN, unfamiliar image embeddings reconstruct badly and are marked unconfident rather than scored. · 학습 데이터와 다른 입력의 예측을 믿지 않고 따로 표시하는 것. WVN에서는 낯선 이미지 임베딩의 복원 손실이 커서 점수 대신 신뢰도 낮음으로 처리된다. → [[01-canonical-papers/notes/9-navigation/wild-visual-navigation|WVN]]
- **Antipodal grasp** · antipodal 파지 — A two-contact grasp whose contact points are positioned so that the line joining them lies inside *both* friction cones. It is the practical criterion behind essentially every two-finger grasp planner, learned or analytic. · 두 접촉점을 잇는 선이 *두* 마찰 원뿔 안에 모두 들어가도록 배치된 두 점 파지. 학습이든 해석이든 사실상 모든 두 손가락 파지 계획기의 실용적 판정 기준이다. → [[04-robotics/grasping|15. §2]]
- **Anti-windup** · anti-windup — The mechanism that stops a PID integrator accumulating while the actuator is saturated; every real implementation has one, and a PID baseline without it is unfairly weak. · 액추에이터 포화 중 PID 적분항이 계속 쌓이는 것을 막는 장치. 실제 구현에는 전부 들어 있으므로, 이것이 없는 PID 베이스라인은 부당하게 약한 상대다. → [[04-robotics/control-theory-ce397|제어 이론 §7]]
- **APC (article processing charge)** · APC(게재료) — A fee the authors pay for each accepted paper. ISARC, for example, gates publication by paid registration rather than an APC. · 채택된 논문마다 저자가 내는 게재료. 예컨대 ISARC는 APC가 아니라 유료 등록비로 게재를 제한한다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Approximation ratio (approximation factor)** · 근사 비율 — A proven bound on how far an algorithm's answer can be from the optimum, such as greedy set cover's factor of about $\ln n$. It is the guarantee sought when exact optimization is NP-hard. · 알고리즘의 답이 최적값에서 얼마나 멀어질 수 있는지에 대한 증명된 한계(예: 그리디 집합 덮개의 약 $\ln n$배). 정확한 최적화가 NP-난해일 때 찾는 보장이다. → [[02-foundations/algorithms/greedy-mst|11.4 §7]]
- **Arbitration** · 중재(arbitration) — How shared control combines human and autonomous commands: blending, constraints, or role allocation. A plain blend can cancel two individually safe commands into an unsafe one, so conflict handling and override must be specified. · 공유 제어가 사람 명령과 자율 명령을 결합하는 방식으로, 혼합·제약·역할 배분이 있다. 단순 혼합은 각각 안전한 두 명령을 상쇄해 위험한 명령으로 만들 수 있으므로 충돌 처리와 override를 명시해야 한다. → [[04-robotics/hri-safety|HRI & Safety §3]]
- **Architectural robotic fabrication (digital fabrication)** · 건축 로봇 패브리케이션 — An architecture-led tradition, founded at ETH by Gramazio Kohler, that treats robot motion as part of design and builds full-scale structures such as Mesh Mould walls and timber assemblies. · ETH의 Gramazio Kohler가 창시한 건축 주도 전통으로, 로봇 동작을 설계의 일부로 다루며 Mesh Mould 벽이나 목재 조립 같은 실규모 구조물을 만든다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §2]]
- **Archival publication** · archival 출판 — A paper that counts as the permanent publication of record, which blocks submitting the same work again. ACM and IEEE reserve the word for journals; ICRA treats a workshop paper with a DOI as archival. · 영구적인 공식 출판 기록으로 인정되어 같은 연구를 다시 제출하지 못하게 만드는 논문. ACM과 IEEE는 이 단어를 저널에만 쓰고, ICRA는 DOI가 있는 워크숍 논문을 archival로 본다. → [[06-research-practice/venue-strategy|Venue Strategy §1]], [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **arXiv** · arXiv — The open preprint server where most ML and robotics papers first appear, without peer review. Its Comments field and journal-ref note acceptance, but both are author-supplied and often left empty. · 대부분의 ML·로보틱스 논문이 심사 없이 처음 올라오는 공개 프리프린트 서버. Comments 필드와 journal-ref가 게재 여부를 알려 주지만, 둘 다 저자가 직접 채우며 비어 있는 경우가 흔하다. → [[01-canonical-papers/how-to-read|How to Read §4.6]]
- **As-built vs as-designed (nominal geometry)** · as-built와 설계 형상 — As-designed is the geometry in the BIM or drawings; as-built is what was actually constructed and measured. They differ, so a nominal design coordinate is not automatically a valid robot target. · 설계 형상은 BIM·도면 속 기하이고 as-built는 실제로 지어져 측정된 상태다. 둘은 다르므로 명목 설계 좌표가 곧바로 유효한 로봇 목표가 되지는 않는다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §1]]
- **A\* search** · A\* 탐색 — Graph search that expands vertices in order of $f = g + h$, cost so far plus a heuristic estimate of cost to go. With a consistent heuristic it returns an optimal path, expanding each vertex once. · 지금까지의 비용과 남은 비용 추정의 합 $f = g + h$ 순서로 정점을 확장하는 그래프 탐색. 일관적 휴리스틱이면 각 정점을 한 번만 확장하고 최적 경로를 돌려준다. → [[02-foundations/algorithms/graph-algorithms|11.6 §6]]
- **Asset (building asset)** · 자산 — A tracked building component, such as a pipe, anchor, or panel, carrying its own ID in the BIM or facility records, so that inspection findings can be attached to it. · 배관·앵커·패널처럼 BIM이나 시설 기록에서 고유 ID로 추적되는 건물 부재. 점검 결과를 여기에 연결한다. → [[05-construction-robotics/site-perception|Site Perception §2]]
- **Asymptotic optimality** · 점근적 최적성(asymptotic optimality) — The property that solution cost converges to the optimum as samples grow. Plain RRT lacks it; RRT\* and PRM\* restore it by connecting samples within a radius shrinking like $(\log n/n)^{1/d}$. Not a real-time guarantee. · 표본이 늘면 해의 비용이 최적값으로 수렴한다는 성질. 일반 RRT에는 없고, RRT\*와 PRM\*는 $(\log n/n)^{1/d}$처럼 줄어드는 반경 안의 표본을 연결해 되찾는다. 실시간 예산에서의 품질 보장은 아니다. → [[04-robotics/planning-decision-making|Planning §5.5]]
- **Asymptotic stability** · 점근 안정성 — The state returns to the equilibrium, not just stays near it. For a linear system: every eigenvalue has negative real part (continuous time) or magnitude below 1 (discrete). It says nothing about speed or overshoot. · 상태가 평형점 근처에 머물 뿐 아니라 그리로 돌아간다는 성질. 선형계에서는 모든 고유값의 실수부가 음수(연속 시간)이거나 크기가 1 미만(이산)이다. 속도나 오버슈트에 대해서는 아무것도 말하지 않는다. → [[04-robotics/control-theory-ce397|Control Theory §4]]
- **atan2** · atan2 — The two-argument arctangent atan2(y, x), which returns a point's angle over the full circle. Unlike arctan(y/x) it keeps both signs, so it never confuses opposite quadrants or breaks at x = 0. · 인자가 둘인 아크탄젠트 atan2(y, x)로, 점의 각도를 한 바퀴 전체 범위에서 돌려준다. arctan(y/x)와 달리 두 부호를 모두 보존해 반대 사분면을 혼동하지 않고 x = 0에서도 깨지지 않는다. → [[02-foundations/engineering-math|Engineering Math §7]]
- **Attention** · 어텐션 — Weighting values (V) by the similarity between queries (Q) and keys (K), connecting any two positions in a sequence in one step. · 쿼리(Q)와 키(K)의 유사도로 값(V)을 가중합하는 연산. 시퀀스의 임의의 두 위치를 한 스텝에 잇는다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Attention-based recurrent encoder (belief encoder)** · 어텐션 기반 순환 인코더 — An encoder that fuses proprioception with exteroception: the recurrence carries a belief over time and attention sets how much weight the terrain map gets, so the controller learns when to trust it. · 고유수용감각과 외수용 감각을 융합하는 인코더. 순환이 시간에 걸쳐 믿음을 나르고 어텐션이 지형 지도의 가중치를 정해, 제어기가 지도를 언제 믿을지 학습한다. → [[01-canonical-papers/notes/9-navigation/miki-perceptive-locomotion|Miki 2022]]
- **AUC** · AUC — Area under the ROC curve, equal to the probability that a randomly chosen positive scores above a randomly chosen negative. Threshold-free and **base-rate-blind**: it is unchanged when positives become ten times rarer, while precision collapses — so an AUC from a balanced benchmark says little about a deployment where the event is rare. · ROC 곡선 아래 넓이이며, 무작위로 고른 양성이 무작위로 고른 음성보다 높은 점수를 받을 확률과 같다. 문턱값에 무관하고 **기저율을 보지 못한다**: 양성이 열 배 드물어져도 그대로인 반면 정밀도는 무너진다. 균형 잡힌 벤치마크의 AUC가 사건이 드문 현장에 대해 말해 주는 바가 적은 이유다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Autocorrelation** · 자기상관 — $E[x(t)x(t+\tau)]$: how strongly a signal at one instant predicts itself $\tau$ later. A noisy signal's frequency content, seen in the time domain. · 어느 순간의 신호가 $\tau$ 뒤의 자기 자신을 얼마나 예측하는가. 잡음 신호의 주파수 내용을 시간 영역에서 본 것. → [[02-foundations/probability|Probability §5]]
- **Autoencoder** · 오토인코더 — A network trained to reproduce its own input through a narrow bottleneck, so the bottleneck becomes a compressed representation. *Masked* variants hide part of the input and reconstruct it; *variational* ones make the bottleneck a distribution. · 좁은 병목을 통과시켜 자기 입력을 재현하도록 학습해서 그 병목이 압축된 표현이 되게 하는 신경망. *마스킹* 변형은 입력 일부를 가리고 복원하고, *변분* 변형은 병목을 분포로 만든다. → [[02-foundations/neural-network-basics|Neural Network Basics §6]]
- **Automatic differentiation (autodiff)** · 자동 미분 — Computing a program's derivatives by chaining each primitive operation's local derivative. Forward mode needs one pass per input; reverse mode needs one pass per output, so one pass yields every gradient of a scalar loss. · 각 기본 연산의 국소 도함수를 연쇄해 프로그램의 도함수를 계산하는 것. 순방향 모드는 입력마다, 역방향 모드는 출력마다 한 번의 패스가 필요해 스칼라 손실의 모든 그래디언트를 한 번에 얻는다. → [[02-foundations/calculus-backprop|Calculus & Backprop §2]]
- **Automation in Construction** · Automation in Construction — Elsevier's flagship construction-technology journal, single-anonymized. It judges a robotics result on whether the construction problem is real, not on method novelty against robotics baselines. · Elsevier가 내는 건설 기술 분야 대표 저널로, 단측 익명 심사를 한다. 로보틱스 결과를 로보틱스 베이스라인 대비 방법의 새로움이 아니라 건설 문제가 진짜인가로 판정한다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Autonomy spectrum (levels of autonomy)** · 자율성 스펙트럼 — The range from direct teleoperation through assisted teleoperation, shared autonomy, supervisory control, and conditional autonomy to full autonomy. "Autonomous" is incomplete without the task, operating domain, intervention policy, reset, and fallback. · 직접 원격조작, 보조 원격조작, 공유 자율성, 감독 제어, 조건부 자율성, 완전 자율성으로 이어지는 범위. "autonomous"라는 말은 과제·운용 도메인·개입 정책·리셋·폴백 없이는 불완전하다. → [[04-robotics/hri-safety|HRI & Safety §1]]
- **Autoregressive** · 자기회귀 — Generating one token at a time, each conditioned on previous tokens. GPT-family models and many VLAs use this factorization; sequential decoding makes latency grow with output length as well as model cost. · 이전 토큰들에 조건부로 다음 토큰을 하나씩 생성하는 방식. GPT 계열과 많은 VLA가 이 분해를 쓰며, 순차 디코딩 때문에 지연은 모델 비용뿐 아니라 출력 길이에 따라서도 늘어난다. → [[02-foundations/neural-network-basics|Neural Network Basics §6]], [[01-canonical-papers/notes/1-foundations/gpt-3|GPT-3]]
- **Availability** · 가용성(availability) — The share of time a system is up and able to work, counting time lost to repair and recovery. Field reports give it alongside failures per hour, cycle or distance. · 시스템이 가동되어 일할 수 있는 시간의 비율로, 수리와 회복에 잃은 시간까지 반영한다. 현장 보고에서는 시간·사이클·거리당 실패와 함께 제시한다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §6]]
- **Axis-angle (rotation vector)** · 축-각 — A rotation written as a unit axis and an angle about it. Stored as one 3-vector it is minimal and geometric, but composing two rotations in this form is awkward. · 회전을 단위 축과 그 축에 대한 각도로 쓰는 방식. 회전벡터 하나로 저장하면 최소이고 기하적이지만, 이 형태로 두 회전을 합성하기는 번거롭다. → [[02-foundations/se3-geometry|SE(3) §2]]
- **Backbone** · 백본(backbone) — The bulk of a network's layers that turn raw input into features, before the small head that produces the answer. Backbones are often pretrained on large data and then reused or frozen. · 원시 입력을 특징으로 바꾸는 신경망 층들의 대부분으로, 답을 내는 작은 헤드 앞에 놓인다. 흔히 큰 데이터로 사전학습한 뒤 재사용하거나 얼린다. → [[02-foundations/neural-network-basics|0.7 §1]]
- **Backdrivability** · 역구동 가능성 — A backdrivable joint can be turned by an external push without the motor and gearbox resisting it. It helps impedance control and safe contact, but it is not part of the definition of impedance control. · 역구동 가능한 관절은 모터와 감속기의 저항 없이 외부에서 밀어 돌릴 수 있다. 임피던스 제어와 안전한 접촉에 도움이 되지만 임피던스 제어의 정의에 속하지는 않는다. → [[04-robotics/force-compliance-control|Force & Compliance Control §2]]
- **Backlash** · 백래시 — Free play in gears or joints, so the output does not move until the input has taken up the slack. It is a dynamics-gap source and a precision error on construction drilling robots. · 기어나 관절의 헐거운 틈 때문에 입력이 틈을 다 메울 때까지 출력이 움직이지 않는 현상. 동역학 격차의 원천이자 건설 드릴링 로봇의 정밀도 오차다. → [[05-construction-robotics/sim-to-real|Sim-to-Real §1]], [[05-construction-robotics/construction-manipulation|Construction Manipulation §4]]
- **Backpropagation (backprop)** · 역전파 — Reverse-mode automatic differentiation applied to a network: loss sensitivities flow from output to input through transposed Jacobians, giving every parameter's gradient in one backward pass costing about 2–3 forward passes. · 신경망에 적용한 역방향 자동 미분. 손실의 민감도가 전치 야코비안을 거쳐 출력에서 입력으로 흘러, 순전파 약 2~3회 비용의 역전파 한 번으로 모든 파라미터의 그래디언트를 얻는다. → [[02-foundations/calculus-backprop|Calculus & Backprop §2]]
- **Backpropagation through dynamics** · 동역학을 통한 역전파 — Training a policy by pushing gradients of predicted value back through a differentiable learned world model's imagined steps, instead of searching through the model. It is Dreamer's replacement for PlaNet's planning. · 모델 속을 탐색하는 대신, 미분 가능한 학습된 월드모델의 상상된 스텝을 거꾸로 따라 예측 가치의 그래디언트를 흘려 정책을 학습하는 것. Dreamer가 PlaNet의 플래닝을 대체한 방식이다. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **Backtracking** · 백트래킹 — Building a solution one decision at a time (choose, explore, un-choose) and abandoning a partial solution as soon as it cannot be completed. The standard exact method for small combinatorial searches. · 결정을 하나씩 내리며(선택, 탐색, 선택 취소) 해를 만들고, 완성될 수 없는 부분 해는 즉시 버리는 탐색. 작은 조합 탐색의 표준 정확 해법이다. → [[02-foundations/algorithms/complexity-recursion|11.1 §6]]
- **Bag playback with --clock** · --clock 재생 — Playing a bag with --clock makes the player publish /clock, so nodes with use_sim_time true live at the recording's time. Either half without the other makes TF fail or timers freeze. · --clock으로 bag을 재생하면 플레이어가 /clock을 발행해, use_sim_time이 켜진 노드가 기록 당시 시간을 산다. 한쪽만 있으면 TF가 실패하거나 타이머가 멈춘다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §8]]
- **Balanced binary search tree (self-balancing BST)** · 균형 BST — A BST that restores balance with local rotations after each insert or delete, keeping height $O(\log n)$ in the worst case. Red-black and AVL trees are the standard kinds; C++ std::map is one. · 삽입·삭제마다 국소 회전으로 균형을 되찾아 높이를 최악에도 $O(\log n)$으로 유지하는 BST. 레드-블랙 트리와 AVL 트리가 표준이고, C++ std::map이 그 예다. → [[02-foundations/algorithms/data-structures|11.2 §5]]
- **Bandwidth (control bandwidth)** · 대역폭 — Roughly the highest frequency up to which a feedback loop still tracks references and rejects disturbances, often read as the gain-crossover frequency. Delay, right-half-plane zeros, and actuator limits constrain how high it can go. · 피드백 루프가 기준을 추종하고 외란을 억제할 수 있는 대략의 최고 주파수로, 흔히 이득 교차 주파수로 읽는다. 지연·우반평면 영점·액추에이터 한계가 그 상한을 제약한다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Bang-bang control** · 뱅뱅 구조 — Control that always sits at one of its limits, switching between maximum and minimum with nothing in between. Time-optimal time scaling along a fixed path has this structure: full acceleration, then full deceleration. · 입력이 항상 최댓값 아니면 최솟값에 붙어 있고 그 사이 값은 쓰지 않는 제어. 고정 경로 위의 시간 최적 시간 스케일링이 이 구조를 갖는다: 최대 가속 후 최대 감속. → [[04-robotics/modern-robotics/ch09-trajectory-generation|MR ch.9]]
- **Barometric tactile sensing** · 기압식 촉각 센싱 — Sealing a cavity of elastomer over an off-the-shelf MEMS pressure die, so a commercial part that is already calibrated does the transduction. Gives a clean DC-stable reading cheaply; the cavity's size is what limits how densely you can pack taxels. · 기성 MEMS 압력 다이 위에 엘라스토머 공동을 밀폐해서, 이미 보정된 상용 부품이 변환을 맡게 하는 방식. 값싸게 DC에서 안정된 값을 주지만, 공동의 크기가 taxel을 얼마나 촘촘히 넣을 수 있는지를 제한한다. → [[04-robotics/tactile-visuotactile|14. Tactile & Visuotactile §2.5]]
- **Base case** · 기저 사례 — The input on which a recursive function returns without recursing. Every valid input must eventually reach it, and in an induction proof it plays the role of the base step. · 재귀 함수가 재귀하지 않고 바로 반환하는 입력. 모든 유효한 입력이 결국 여기에 도달해야 하며, 귀납 증명에서 기저 단계 역할을 한다. → [[02-foundations/algorithms/complexity-recursion|11.1 §5]]
- **Baseline (experimental)** · 베이스라인 — The existing method a new one must beat in a results table. It is only meaningful if tuned and matched on data, compute and evaluation protocol; weak baselines are the field's chronic sin. · 결과 표에서 새 방법이 이겨야 하는 기존 방법. 튜닝되고 데이터·연산량·평가 프로토콜이 맞춰졌을 때만 의미가 있으며, 약한 베이스라인은 이 분야의 고질병이다. → [[02-foundations/ml-practice|ML Practice §4]]
- **Baseline (policy gradient)** · 베이스라인(정책 그래디언트) — A state-dependent value $b(s)$ subtracted from the return in a policy gradient. It leaves the gradient unbiased but cuts variance; choosing $b \approx V(s)$ turns the weight into the advantage. · 정책 그래디언트에서 리턴에서 빼는 상태 의존 값 $b(s)$. 그래디언트를 무편향으로 둔 채 분산을 줄이며, $b \approx V(s)$로 두면 가중치가 어드밴티지가 된다. → [[02-foundations/rl-basics|RL Basics §4]]
- **Base pose error** · 베이스 자세 오차 — Error in where a mobile robot's base actually is. Because the tool pose composes base pose with arm kinematics, this error adds to tool error, often dominantly. · 이동 로봇 베이스가 실제로 어디 있는지에 대한 오차. 공구 자세는 베이스 자세와 팔 기구학의 합성이므로 이 오차가 공구 오차에 더해지며 종종 지배적이다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §3]]
- **Base rate** · 기저율 — How common an event is before any evidence. Rare events keep most alarms false: a detector with 95% sensitivity and 5% false alarms, at a 1% crack rate, is right 16% of the time. · 증거를 보기 전 사건이 얼마나 흔한가. 드문 사건이면 좋은 감지기라도 경보 대부분이 거짓이다: 균열 비율 1%에서 민감도 95%, 오경보율 5%인 경보는 16%만 맞다. → [[02-foundations/probability|Probability §1]]
- **Batch / minibatch** · 배치/미니배치 — The group of samples whose losses are averaged for one parameter update. Batch size is a hyperparameter; dataset size ÷ batch size = iterations per epoch. · 한 번의 파라미터 갱신을 위해 손실을 평균하는 샘플 묶음. 배치 크기는 하이퍼파라미터이고, 데이터셋 크기 ÷ 배치 크기 = 에폭당 반복 수다. → [[02-foundations/neural-network-basics|0.7 §4]]
- **BatchNorm (batch normalization, BN)** · BatchNorm(배치 정규화) — Standardizing each feature over the mini-batch, then applying a learnable scale $\gamma$ and shift $\beta$. Inference uses running averages instead; it breaks with small batches, which is why Transformers use LayerNorm. · 각 특징을 미니배치 평균·분산으로 표준화한 뒤 학습 가능한 스케일 $\gamma$와 시프트 $\beta$를 적용한다. 추론에서는 이동 평균을 쓴다. 작은 배치에서 깨지므로 Transformer는 LayerNorm을 쓴다. → [[01-canonical-papers/notes/1-foundations/batch-norm|BatchNorm]]
- **Bayes filter** · 베이즈 필터 — The recursive estimator behind state estimation: predict by pushing the previous belief through the process model, then correct by weighting it with the measurement likelihood. Kalman and particle filters implement it. · 상태 추정의 뼈대인 재귀 추정기: 이전 belief를 과정 모델에 통과시켜 예측하고, 측정 우도로 가중해 보정한다. 칼만 필터와 파티클 필터가 이것을 구현한다. → [[04-robotics/state-estimation-slam|State estimation §4]]
- **Bayes' rule (Bayes' theorem)** · 베이즈 정리 — $P(\theta \mid x) = P(x \mid \theta)P(\theta)/P(x)$: reweight a prior belief by how well each hypothesis explains the data. It follows from factoring one joint probability in two orders. · 사전 믿음을 각 가설이 데이터를 얼마나 잘 설명하는지로 다시 가중하는 공식. 결합 확률 하나를 두 순서로 분해해 같다고 놓으면 나온다. → [[02-foundations/probability|Probability §1]]
- **BCI (brain–computer interface)** · BCI(뇌–컴퓨터 인터페이스) — A system that decodes brain signals, usually EEG, into commands. In construction HRC it has driven a robot hands-free in a testbed. · 뇌 신호(주로 EEG)를 명령으로 해독하는 시스템. 건설 HRC에서는 테스트베드에서 로봇을 핸즈프리로 구동하는 데 쓰였다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Behavior cloning (BC)** · 행동 복제(BC) — Imitation by plain supervised learning: maximize the log-probability of the expert's action given the observation, over demonstration pairs. Stable and reward-free, but vulnerable to covariate shift and compounding error. · 순수 지도학습으로 하는 모방: 시연 쌍에 대해 관측이 주어졌을 때 전문가 행동의 로그 확률을 최대화한다. 안정적이고 보상이 필요 없지만 covariate shift와 복합 오차에 취약하다. → [[02-foundations/rl-basics|RL Basics §6]], [[01-canonical-papers/how-to-read|How to Read §3]]
- **Behavior tree** · 행동 트리(behavior tree) — An execution layer composing robot behaviors modularly from sequence, fallback, and decorator nodes, with failure handling built into the structure. · sequence·fallback·decorator 노드로 로봇 행동을 모듈적으로 합성하는 실행 계층. → [[04-robotics/robot-systems-deployment|Robot Systems §6]]
- **Bellman equation** · 벨만 방정식 — The fixed-point statement that today's value = reward + discounted tomorrow's value. The root of value-based and model-based RL (policy gradients do not depend on it directly). · 오늘의 가치 = 보상 + 할인된 내일의 가치라는 고정점 방정식. 가치 기반·모델 기반 RL의 뿌리다(정책 경사는 여기에 직접 기대지 않는다). → [[02-foundations/rl-basics|RL 기초]]
- **Bellman–Ford algorithm** · Bellman–Ford — Single-source shortest paths by relaxing every edge $V-1$ times, which is dynamic programming over the number of edges used. It allows negative weights, detects negative cycles with one extra round, and costs $O(VE)$. · 모든 간선을 $V-1$번 완화하는, 사용한 간선 수에 대한 동적 계획법 형태의 단일 출발 최단 경로 알고리즘. 음수 가중치를 허용하고 한 번 더 돌려 음수 사이클을 검출하며 $O(VE)$다. → [[02-foundations/algorithms/graph-algorithms|11.6 §5]]
- **Benchmark** · 벤치마크 — A fixed set of data or tasks plus an evaluation protocol that lets methods be compared. Near-ceiling (saturated) benchmarks reward overfitting to their quirks, so gains there generalize least. · 방법들을 비교할 수 있게 고정해 둔 데이터·과제 묶음과 평가 규약. 천장 근처의(포화된) 벤치마크는 그 버릇에 과적합하는 것을 보상하므로, 거기서의 이득이 가장 일반화되지 않는다. → [[02-foundations/ml-practice|ML Practice §5]]
- **Bias (neural network)** · 편향 — The learned vector $b$ added after a layer's matrix multiply, $Wx + b$. It is a parameter like the weights, and unrelated to statistical bias of an estimator. · 층의 행렬곱 뒤에 더하는 학습 벡터 $b$($Wx + b$). 가중치와 같은 파라미터이며, 추정량의 통계적 편향과는 무관하다. → [[02-foundations/neural-network-basics|0.7 §1]]
- **Bias–variance tradeoff** · 편향–분산 트레이드오프 — An estimator's bias is how far it misses on average over many datasets; its variance is how much it jumps between datasets. RL papers' "unbiased but high-variance" gradient estimators sit on this tradeoff. · 추정기의 편향은 여러 데이터셋에 걸쳐 평균적으로 빗나가는 정도, 분산은 데이터셋마다 요동치는 정도다. RL 논문의 "불편이지만 고분산인" 그래디언트 추정기가 이 트레이드오프 위에 있다. → [[02-foundations/probability|Probability §4]]
- **Big-Omega (Ω)** · Ω 표기 — $f = \Omega(g)$ means $f$ is eventually at least a constant multiple of $g$: an asymptotic lower bound. For example, every comparison sort needs $\Omega(n \log n)$ comparisons. · $f = \Omega(g)$는 $f$가 결국 $g$의 상수배 이상이라는 뜻, 즉 점근적 하한이다. 예컨대 모든 비교 정렬에는 $\Omega(n \log n)$번의 비교가 필요하다. → [[02-foundations/algorithms/complexity-recursion|11.1 §2]]
- **Big-O notation (O)** · Big-O(빅오) — $f = O(g)$ means $f$ is eventually at most a constant multiple of $g$: an asymptotic upper bound. In conversation people often say big-O when they mean the tight bound Θ. · $f = O(g)$는 $f$가 결국 $g$의 상수배 이하라는 뜻, 즉 점근적 상한이다. 대화에서는 딱 맞는 상한·하한인 Θ를 뜻하면서 빅오라고 말하는 경우가 많다. → [[02-foundations/algorithms/complexity-recursion|11.1 §2]]
- **Big-Theta (Θ)** · Θ 표기 — $f = \Theta(g)$ means $f$ is both $O(g)$ and $\Omega(g)$: bounded above and below by constant multiples of $g$ for large $n$. It is the tight bound. · $f = \Theta(g)$는 $f$가 $O(g)$이면서 $\Omega(g)$라는 뜻, 즉 큰 $n$에서 $g$의 상수배로 위아래가 막힌다. 딱 맞는(tight) 상한·하한이다. → [[02-foundations/algorithms/complexity-recursion|11.1 §2]]
- **Bilateral teleoperation** · 양방향 원격조작 — A teleoperation architecture in which force flows back from the follower to the leader, so the operator feels the environment. Unilateral systems send motion only and are blind to contact; bilateral systems can feel an insertion, and can also go unstable. · 힘이 팔로워에서 리더로 되돌아와 조작자가 환경을 느끼는 원격조작 구조. 단방향은 운동만 보내 접촉에 눈이 멀었고, 양방향은 삽입을 느낄 수 있지만 불안정해질 수도 있다. → [[04-robotics/teleoperation-demonstration|12. §2]]
- **Bimanual manipulation (dual-arm coordination)** · 양팔 조작(양팔 협응) — Manipulation that uses two arms together, such as holding with one while acting with the other. Demonstrations must record both arms at once, and with a mobile base, the base as well. · 한 팔로 잡고 다른 팔로 작업하는 식으로 두 팔을 함께 쓰는 조작. 시연은 두 팔을 동시에 기록해야 하고, 모바일 베이스가 있으면 베이스까지 함께 기록해야 한다. → [[01-canonical-papers/notes/7-robotics/mobile-aloha|Mobile ALOHA]]
- **BIM (Building Information Modeling)** · BIM — The structured digital design model of a building, in which each component is an object with an ID, geometry, and properties. It is a design model, not a live digital twin. · 건물의 구조화된 디지털 설계 모델로, 각 부재가 ID·형상·속성을 가진 객체다. 실시간 디지털 트윈이 아니라 설계 모델이다. → [[05-construction-robotics/site-perception|Site Perception §1]], [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §2]]
- **Binary heap (heap)** · 이진 힙 — A complete binary tree stored in an array with every parent no larger than its children, so the minimum sits at the root. Push and pop cost $O(\log n)$; building one from an array costs $O(n)$. · 모든 부모가 자식 이하인 완전 이진 트리를 배열에 저장한 것으로, 최솟값이 루트에 있다. push와 pop은 $O(\log n)$이고, 배열에서 힙 만들기(heapify)는 $O(n)$이다. → [[02-foundations/algorithms/data-structures|11.2 §4]]
- **Binary search** · 이진 탐색 — Finding the first position where a monotone yes/no condition becomes true by repeatedly testing the middle of the undecided range and discarding the half that cannot contain it, in $O(\log n)$ steps. · 단조적인 예/아니오 조건이 처음 참이 되는 위치를, 미정 구간의 가운데를 반복해 묻고 답이 있을 수 없는 절반을 버려 $O(\log n)$ 단계에 찾는 방법. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §6]]
- **Binary search on the answer** · 답에 대한 이진 탐색 — Finding the smallest feasible value by binary search over candidate values with a feasibility check. It works when feasibility is monotone and checking a value is easier than optimizing directly. · 후보 값에 대해 실행 가능성 검사를 술어로 이진 탐색해 실행 가능한 최솟값을 찾는 기법. 실행 가능성이 단조이고 값 하나를 검사하는 것이 직접 최적화보다 쉬울 때 쓴다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §6]]
- **Binary search tree (BST)** · 이진 탐색 트리 — A binary tree in which every key in a node's left subtree is smaller and every key in its right subtree is larger. Search, insert and delete cost $O(h)$ for tree height $h$. · 모든 노드에서 왼쪽 서브트리의 모든 키는 작고 오른쪽 서브트리의 모든 키는 큰 이진 트리. 탐색·삽입·삭제는 트리 높이 $h$에 대해 $O(h)$다. → [[02-foundations/algorithms/data-structures|11.2 §5]]
- **Binding (of a panel)** · 걸림·끼임 — A rigid sheet touching an out-of-square or uneven frame before reaching its planned pose, so it jams instead of seating even though the nominal placement was correct. · 단단한 시트가 계획 자세에 이르기 전에 직각이 틀리거나 울퉁불퉁한 틀에 먼저 닿아, 명목 배치가 맞았는데도 제자리에 앉지 못하고 끼는 현상. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §1]]
- **Bin picking (bin clearing)** · 통 비우기 — Repeatedly grasping objects out of a cluttered bin until it is empty. It is a well-posed setting with a known workspace, usually reported as success rate and mean picks per hour. · 어수선한 통에서 물체를 비워질 때까지 반복해 집어내는 과제. 작업 구역이 알려진 잘 정의된 설정이며, 보통 성공률과 시간당 평균 집기 수로 보고한다. → [[01-canonical-papers/notes/7-robotics/anygrasp|AnyGrasp]]
- **BLEU** · BLEU — A machine-translation quality metric measuring n-gram overlap with reference translations. · 생성문과 참조 번역의 n-gram 겹침을 측정하는 기계번역 지표.
- **Blind locomotion (proprioceptive locomotion)** · 맹목 로코모션 — Legged control from proprioception alone, with no cameras or LiDAR. It stays robust when perception fails but must feel terrain before adapting, which costs speed and energy efficiency. · 카메라나 LiDAR 없이 고유수용감각만으로 하는 보행 제어. 인식이 실패해도 강건하지만 지형을 밟아 봐야 적응하므로 속도와 에너지 효율을 잃는다. → [[01-canonical-papers/notes/9-navigation/lee-quadruped-terrain|Lee 2020]]
- **Bloom filter** · 블룸 필터 — A bit array plus $k$ hash functions that answers "have I seen x?" in fixed memory. It can give false positives but never false negatives, stores no keys, and cannot delete. · 비트 배열과 해시 함수 $k$개로 "x를 본 적이 있는가?"에 고정 메모리로 답하는 구조. 거짓 양성은 있을 수 있지만 거짓 음성은 없고, 키를 저장하지 않으며 삭제할 수 없다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Bond** · bond — A periodic heartbeat between the lifecycle manager and each server; if a server stops responding for bond_timeout, the manager brings the whole stack down rather than leave it half-running. · 라이프사이클 매니저와 각 서버 사이의 주기적 heartbeat. 서버가 bond_timeout 동안 응답하지 않으면 매니저는 반쯤 돌게 두지 않고 스택 전체를 내린다. → [[04-robotics/ros2/services-actions-parameters|25.3 Services & Actions §8]]
- **Bootstrap confidence interval (bootstrap CI)** · 부트스트랩 CI — An interval made by resampling the observed values with replacement thousands of times, recomputing the statistic each time, and reporting percentiles, the 2.5th and 97.5th for 95%. Unreliable with very few samples. · 관측값을 복원추출로 수천 번 다시 뽑아 매번 통계량을 계산하고 그 백분위수(95%라면 2.5와 97.5)를 보고하는 구간. 표본이 매우 적으면 믿기 어렵다. → [[02-foundations/probability|Probability §6]]
- **Bootstrapping (RL)** · 부트스트래핑 — Updating a value estimate toward a target that contains your own current estimate, as in the TD target $r + \gamma V(s')$, rather than toward a completed return. Buys data and compute efficiency — each transition is consumed where it is generated — and is one leg of the deadly triad. · 완결된 리턴이 아니라 자신의 현재 추정값이 들어 있는 목표를 향해 가치 추정을 갱신하는 것. TD 목표 $r + \gamma V(s')$가 그렇다. 각 전이를 생성된 자리에서 소비하므로 데이터와 계산 효율을 사 주며, deadly triad의 한 다리다. → [[02-foundations/rl-basics|RL Basics §3.5]]
- **Bottleneck block** · 병목(bottleneck) 블록 — A ResNet block of 1×1, 3×3, then 1×1 convolutions: the first 1×1 convolution shrinks the channel count so the costly 3×3 runs on a narrow tensor, and the last restores width. · 1×1, 3×3, 1×1 합성곱을 차례로 쌓은 ResNet 블록. 앞의 1×1 합성곱이 채널 수를 줄여 비싼 3×3이 좁은 텐서에서 돌게 하고, 뒤의 1×1이 폭을 되돌린다. → [[01-canonical-papers/notes/1-foundations/resnet|ResNet]]
- **Bounding box** · 박스(bounding box) — The rectangle a detector outputs around an object, given by position and size, usually with a class label and confidence score. · 검출기가 물체 둘레에 출력하는 직사각형으로, 위치와 크기로 표현하며 보통 클래스 라벨과 신뢰도 점수가 함께 붙는다. → [[01-canonical-papers/notes/2-computer-vision/yolo|YOLO]]
- **Bradley–Terry model** · Bradley–Terry 모델 — The standard model of pairwise preferences: the probability that A is preferred to B is the sigmoid of the reward difference. Fitting it is logistic regression on reward differences, and it underlies RLHF reward models. · 쌍별 선호의 표준 모델: A가 B보다 선호될 확률은 보상 차이의 시그모이드다. 적합은 보상 차이에 대한 로지스틱 회귀이고, RLHF 보상 모델의 바탕이다. → [[02-foundations/rl-basics|RL Basics §11]]
- **Breadth-first search (BFS)** · 너비 우선 탐색 — Graph search that visits every vertex one edge away, then two, and so on, using a FIFO queue. It finds fewest-edge paths in $O(V + E)$ but is wrong on weighted edges. · FIFO 큐로 간선 하나 거리의 정점 전부, 그다음 둘 거리 순으로 방문하는 그래프 탐색. $O(V + E)$에 간선 수가 가장 적은 경로를 찾지만 가중 간선에는 틀린다. → [[02-foundations/algorithms/graph-algorithms|11.6 §2]]
- **Brockett's condition** · Brockett 조건 — The result that nonholonomic systems such as a unicycle or car cannot be stabilized to a point by any continuous time-invariant feedback, which is why practical controllers track trajectories instead. · 외바퀴·자동차 같은 비홀로노믹 시스템은 어떤 연속 시불변 피드백으로도 한 점에 안정화할 수 없다는 결과. 실전 제어기가 점 대신 궤적을 추종하는 이유다. → [[04-robotics/modern-robotics/ch13-wheeled-mobile-robots|MR ch.13]]
- **bt_navigator (behavior tree navigator)** · bt_navigator — The Nav2 node that receives a navigation goal and ticks an XML behavior tree whose leaves call the planner, controller and behavior servers; it neither plans nor controls itself. · 내비게이션 목표를 받아, 잎이 플래너·제어기·행동 서버를 호출하는 XML 행동 트리를 tick하는 Nav2 노드. 스스로 계획하거나 제어하지 않는다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §2]]
- **Bucket fill factor** · 버킷 충전율 — The fraction of a bucket's rated volume actually filled per dig. Ideal productivity is bucket volume × fill factor × cycles per hour. · 한 번 굴착에서 버킷 정격 용량 중 실제로 채운 비율. 이상적 생산성은 버킷 용량 × 충전율 × 시간당 사이클 수다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §6]]
- **Bucket filling** · 버킷 채우기 — The digging phase in which an excavator or wheel loader pushes its bucket through soil or a pile to fill it. Contact with the material is uncertain and changes from soil to soil. · 굴착기나 휠 로더가 버킷을 흙이나 재료 더미에 밀어 넣어 채우는 굴착 단계. 재료와의 접촉이 불확실하고 토질마다 달라진다. → [[01-canonical-papers/notes/8-construction/egli-rl|Egli RL]]
- **Budget matching** · 예산 정렬 — Giving a method and its ablation or baseline the same data, compute, tuning effort and pretrained assets. Otherwise removing a component also removes resources, and the comparison no longer isolates that component. · 방법과 그 절제 모델·베이스라인에 같은 데이터, 연산, 튜닝 노력, 사전학습 자산을 주는 것. 그러지 않으면 구성요소를 빼면서 자원도 빠져, 비교가 그 구성요소를 분리하지 못한다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §5]]
- **Bundle adjustment (BA)** · 번들 조정 — Jointly refining camera poses and 3D points by nonlinear least squares that minimizes total reprojection error across all views; the batch back end of structure from motion and visual SLAM. · 모든 시점의 reprojection error 합을 최소화하도록 카메라 pose와 3D 점을 비선형 최소자승으로 함께 다듬는 일. SfM과 시각 SLAM의 일괄 back end다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Cable-driven parallel robot (CDPR)** · 케이블 구동 병렬 로봇 — A robot that moves its platform by winding several cables anchored to a surrounding frame, giving a very large workspace. It has been used to install curtain-wall modules. · 주변 틀에 고정한 여러 케이블을 감고 풀어 플랫폼을 움직이는 로봇으로, 작업 공간이 매우 넓다. 커튼월 모듈 설치에 쓰였다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §4]]
- **Calibrated trust** · 보정된 신뢰 — Reliance that matches the system's actual capability and uncertainty. High trust is not automatically good; self-reported trust should be paired with behavior during recoverable system errors. · 의존이 시스템의 실제 능력과 불확실성에 맞는 상태. 높은 신뢰가 자동으로 좋은 것은 아니며, 자기보고 신뢰는 회복 가능한 오류 중의 행동과 함께 봐야 한다. → [[04-robotics/hri-safety|HRI & Safety §4]]
- **Calibration (probability calibration)** · 보정(calibration) — Agreement between predicted probabilities and observed frequencies: among cases scored 0.7, the event should occur 70% of the time. Deep classifiers are often overconfident; only calibrated probabilities support a decision rule. · 예측 확률과 관측 빈도의 일치. 0.7로 점수 매긴 사례들 중 70%에서 사건이 일어나야 한다. 딥 분류기는 흔히 과신하며, 보정된 0.7은 결정 규칙을 지지하지만 보정 안 된 0.9는 그러지 못한다. → [[04-robotics/human-intent-prediction|Human Intent §4]]
- **Callback** · 콜백 — A function you register with ROS 2 that it calls when something happens: a message arrives, a timer expires, a request comes in. You do not write the loop; the executor calls the callbacks. · 메시지 도착, 타이머 만료, 요청 수신 같은 일이 생기면 ROS 2가 불러 주도록 등록하는 함수. 루프는 직접 쓰지 않고 executor가 콜백을 호출한다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §5]]
- **Callback deadlock (synchronous call in a callback)** · 콜백 교착 — A node freezing silently because a callback blocks on a service result that only another callback in the same thread or mutually exclusive group could deliver. Fix: call_async, or separate groups plus multi-threaded executor. · 콜백이 같은 스레드나 같은 mutually exclusive 그룹의 다른 콜백만 전달할 수 있는 서비스 결과를 기다리며 멈춰, 오류 없이 노드가 얼어붙는 것. 해결: call_async, 또는 다른 그룹과 멀티 스레드 executor. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §9]]
- **Callback group** · 콜백 그룹 — A grouping of callbacks that controls which may run in parallel. Anything created without a group joins the node's default group, which is mutually exclusive. · 어떤 콜백끼리 병렬로 돌 수 있는지 정하는 콜백 묶음. 그룹 없이 만든 것은 노드의 기본 그룹에 들어가고, 그 기본 그룹은 mutually exclusive다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §9]]
- **Call for papers (CFP)** · CFP(요강) — A venue's official announcement of scope, deadlines, page limits and submission rules. Venue policies change yearly, so re-read the current CFP before acting on any advice. · venue가 범위, 마감, 쪽수 제한, 제출 규칙을 공식적으로 알리는 문서. 정책이 해마다 바뀌므로 어떤 조언이든 따르기 전에 현재 CFP를 다시 읽는다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Call stack (stack frame)** · 호출 스택(스택 프레임) — The memory holding one frame per active function call. A recursion of depth $D$ therefore uses $\Theta(D)$ extra space even if it allocates nothing, and that belongs in the space analysis. · 활성 함수 호출마다 프레임 하나를 담는 메모리. 그래서 깊이 $D$의 재귀는 아무것도 할당하지 않아도 $\Theta(D)$ 추가 공간을 쓰며, 이를 공간 분석에 넣어야 한다. → [[02-foundations/algorithms/complexity-recursion|11.1 §5]]
- **Capacitive tactile sensing** · 정전용량식 촉각 센싱 — Reads pressure as the capacitance change of a conductor–insulator–conductor sandwich as its gap closes. Sensitive, holds a static reading, can be made of fabric; disturbed by stray capacitance, including nearby human bodies. · 도체–절연체–도체 샌드위치의 간격이 좁아질 때의 정전용량 변화로 압력을 읽는다. 감도가 좋고 정적 값을 유지하며 천으로도 만든다. 근처 사람 몸을 포함한 부유 용량에 교란된다. → [[04-robotics/tactile-visuotactile|Tactile §2.5]]
- **Capture point** · capture point — Where a legged robot must step to come to a complete stop. In the linear inverted pendulum model it lies $\dot x/\omega_0$ ahead of the center of mass; DCM generalizes it to 3D. · 보행 로봇이 완전히 멈추려면 다음 발을 디뎌야 하는 곳. 선형 도립진자 모형에서 무게중심보다 $\dot x/\omega_0$만큼 앞에 있고, DCM이 이를 3차원으로 일반화한다. → [[04-robotics/legged-locomotion|Legged Locomotion §1.5]]
- **Cartesian path (fraction)** · Cartesian path — A straight-line end-effector motion computed by interpolating poses and solving IK at each; it returns the fraction of the path achieved (0 to 1), which must be checked before executing. · end effector 자세를 보간하고 각 점에서 IK를 풀어 만든 직선 운동. 달성한 경로 비율(0~1)을 돌려주며, 실행 전에 반드시 확인해야 한다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §7]]
- **Catastrophic forgetting** · 파국적 망각 — Losing previously learned knowledge when a pretrained network is fine-tuned on new data. RT-2 counters it by co-fine-tuning; ControlNet avoids it by freezing the base model. · 사전학습 네트워크를 새 데이터로 파인튜닝할 때 이전에 배운 지식을 잃어버리는 현상. RT-2는 공동 파인튜닝으로, ControlNet은 베이스 모델을 얼려서 이를 피한다. → [[01-canonical-papers/notes/4-vla/rt-2|RT-2]], [[01-canonical-papers/notes/6-diffusion/controlnet|ControlNet]]
- **Causal mask (masked self-attention)** · 인과 마스크 — A mask that blocks each token from attending to later tokens, so a decoder can be trained on whole sequences in parallel yet still generate left to right. · 각 토큰이 뒤쪽 토큰을 참조하지 못하게 막는 마스크. 디코더가 시퀀스 전체를 병렬로 학습하면서도 왼쪽에서 오른쪽으로 생성할 수 있게 한다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **CEM (cross-entropy method)** · CEM(cross-entropy method) — A sampling optimizer: draw many action sequences, keep the best-scoring few, refit the sampling distribution to them, repeat. PlaNet uses it to plan in latent space, executing the first action and replanning. · 샘플링 최적화 기법: 행동 시퀀스를 여럿 뽑고, 점수가 가장 좋은 몇 개만 남겨 분포를 다시 맞추고, 반복한다. PlaNet은 이것으로 잠재 공간에서 플래닝하고 첫 행동만 실행한 뒤 재계획한다. → [[01-canonical-papers/notes/5-world-models/planet|PlaNet]]
- **Center of mass (CoM)** · 무게중심 — The mass-weighted average position of a body. Legged balance criteria (support polygon, ZMP, capture point) are statements about where it, or its projection on the ground, sits. · 물체 질량의 가중 평균 위치. 보행 균형 기준(지지 다각형, ZMP, capture point)은 이 점이나 그 지면 투영이 어디 있는가에 관한 진술이다. → [[04-robotics/legged-locomotion|Legged Locomotion §1.5]]
- **Central limit theorem (CLT)** · CLT(중심극한정리) — The centred, $\sqrt N$-scaled sum of many i.i.d. effects with finite variance tends to a Gaussian. It is why noise models default to Gaussians, and it fails without finite variance. · 분산이 유한한 i.i.d. 효과 여럿의 합을 중심화하고 $\sqrt N$으로 스케일하면 가우시안에 가까워진다는 정리. 노이즈 모델이 가우시안을 기본값으로 쓰는 이유이며, 분산이 유한하지 않으면 성립하지 않는다. → [[02-foundations/probability|Probability §3]]
- **CFG (Classifier-Free Guidance)** · CFG — A sampling technique that extrapolates along the difference between conditional and unconditional predictions to raise condition fidelity. · 조건부/무조건부 예측의 차이 방향으로 외삽해 조건 충실도를 높이는 샘플링 기법. → [[01-canonical-papers/notes/6-diffusion/classifier-free-guidance|CFG]]
- **Charge amplifier** · 전하 증폭기 — The readout circuit for piezoelectric sensors: its output voltage tracks the tiny generated charge, which would otherwise be lost to cable and input capacitance. The charge still leaks away, so static load is unreadable. · 압전 센서의 판독 회로로, 출력 전압이 발생한 미세 전하를 따라가게 해 케이블·입력단 정전용량에 묻히지 않게 한다. 그래도 전하가 빠져나가므로 정적 하중은 읽지 못한다. → [[04-robotics/tactile-visuotactile|Tactile §2.5]]
- **Checkpoint** · 체크포인트 — The parameters saved at some point during training. Choosing which checkpoint to report must use the validation set; choosing by test score inflates the reported number. · 학습 도중 어느 시점에 저장한 파라미터. 보고할 체크포인트는 validation으로 골라야 하며, test 점수로 고르면 보고 수치가 부풀려진다. → [[02-foundations/neural-network-basics|0.7 §6]], [[02-foundations/ml-practice|ML Practice §1]]
- **Cherry-picking** · 체리피킹 — Showing only the best runs, usually in qualitative figures and videos. The corrective question is what the median rollout looks like. · 가장 잘된 실행만, 흔히 정성적 그림이나 영상으로 보여 주는 것. 바로잡는 질문은 중앙값 롤아웃이 어떻게 생겼는가다. → [[02-foundations/ml-practice|ML Practice §5]]
- **Citation drift** · 2차 인용 드리프트 — A claim or number changing as it passes from paper to paper through secondary citations. Defeat it by resolving the DOI or arXiv ID, finding the sentence in the source, and matching the version. · 주장이나 숫자가 2차 인용을 거치며 논문에서 논문으로 옮겨 가는 동안 변하는 것. DOI나 arXiv ID로 원문을 찾고, 그 문장을 원문에서 확인하고, 판본을 맞춰 막는다. → [[01-canonical-papers/how-to-read|How to Read §4.6]]
- **Claim–evidence table** · 주장–증거 표 — A table filled before experiments listing each intended claim with its necessary comparison, metric and boundary. A row you cannot fill means adding the comparison, narrowing the claim, or leaving it unanswered. · 실험 전에 채우는 표로, 의도한 주장마다 필요한 비교, 지표, 경계를 적는다. 채울 수 없는 행이 있으면 비교를 추가하거나, 주장을 좁히거나, 답하지 않은 채로 둔다. → [[06-research-practice/research-questions-claims|Research Questions §7]]
- **Claim type** · 주장의 유형 — The kind of statement a result makes: descriptive, comparative, causal, generalization, mechanistic, or safety/reliability. Each needs different evidence; a drop when a component is removed supports a scoped dependency, not a full causal story. · 결과가 하는 문장의 종류: 기술적, 비교적, 인과적, 일반화, 기전적, 안전/신뢰성. 각각 필요한 증거가 다르다. 구성요소를 뺐을 때의 성능 하락은 범위가 한정된 의존성을 지지할 뿐 완전한 인과 이야기를 증명하지 않는다. → [[06-research-practice/research-questions-claims|Research Questions §4]]
- **Class-agnostic** · 클래스 불가지(class-agnostic) — Producing object regions without naming the category. SAM knows where things are but not what they are, so it is paired with a detector or VLM for semantics. · 범주 이름 없이 물체 영역만 내는 성질. SAM은 어디에 있는지는 알지만 무엇인지는 모르므로, 의미를 위해 검출기나 VLM과 짝지어 쓴다. → [[01-canonical-papers/notes/2-computer-vision/sam|SAM]]
- **Class-agnostic segmentation** · 클래스 불가지론적 분할 — Splitting an image or map into separate object instances without assigning categories, so unfamiliar objects still get their own segments; open-vocabulary features can name them afterwards. · 범주를 붙이지 않고 영상이나 지도를 개별 물체 인스턴스로 나누는 것. 낯선 물체도 자기 영역을 받고, 이름은 나중에 개방 어휘 특징으로 붙인다. → [[01-canonical-papers/notes/9-navigation/clio|Clio]]
- **Classifier guidance** · 분류기 가이던스 — Steering a diffusion sampler by adding a separately trained, noise-robust classifier's gradient to the score. It needs that extra classifier and has no natural form for free text, which classifier-free guidance removed. · 따로 학습한 노이즈에 강건한 분류기의 그래디언트를 score에 더해 디퓨전 샘플링을 조향하는 것. 추가 분류기가 필요하고 자유 텍스트 조건에는 자연스러운 형태가 없어, CFG가 이를 대체했다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]], [[01-canonical-papers/notes/6-diffusion/classifier-free-guidance|CFG]]
- **Class token ([class], [CLS])** · [class] 토큰 — An extra learned token prepended to the patch or word tokens; its final embedding serves as the whole-input summary for classification. · 패치·단어 토큰 앞에 붙이는 학습된 토큰 하나. 마지막 층에서의 임베딩을 입력 전체의 요약으로 삼아 분류에 쓴다. → [[01-canonical-papers/notes/1-foundations/vit|ViT]]
- **Client library (rclcpp, rclpy)** · 클라이언트 라이브러리 — The language-specific API you write nodes with: rclcpp for C++, rclpy for Python. Both wrap rcl, the shared C core, which sits above rmw. · 노드를 작성할 때 쓰는 언어별 API. C++은 rclcpp, Python은 rclpy이고, 둘 다 공용 C 코어인 rcl을 감싸며 rcl은 rmw 위에 있다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §4]]
- **CLIP** · CLIP — An image encoder and a text encoder trained contrastively on 400M web image–text pairs into one shared embedding space. It scores image–text agreement but generates no text; its encoders feed many VLMs. · 웹 이미지-텍스트 4억 쌍으로 이미지 인코더와 텍스트 인코더를 대조학습해 하나의 공유 임베딩 공간에 정렬한 모델. 일치도를 점수화할 뿐 텍스트는 생성하지 않으며, 그 인코더가 많은 VLM의 시각 입력이 된다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **Clipped double-Q** · 클리핑된 이중 Q — Training two value networks and using the smaller of their estimates, as in TD3 and SAC. It counters overestimation bias, which bootstrapping amplifies because an over-large value feeds its own next target. · 가치 신경망 두 개를 학습해 두 추정값 중 작은 쪽을 타깃으로 쓰는 기법(TD3, SAC). 과대추정 편향을 겨냥하며, 과하게 큰 가치가 다음 타깃으로 다시 들어가므로 부트스트랩이 그 편향을 증폭한다. → [[02-foundations/rl-basics|RL Basics §3.5]]
- **CLIP score** · CLIP 점수 — Cosine similarity between a generated image's CLIP embedding and its text prompt's, used as an automatic "does this match the prompt?" metric. Cheap and popular, but it measures agreement with *one model's* notion of alignment, not human judgement. · 생성 이미지와 프롬프트의 CLIP 임베딩 코사인 유사도. 자동 정합 지표이지만 사람의 판단이 아니라 *한 모델의* 정렬 개념과의 일치를 잰다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **/clock topic** · /clock — The topic (rosgraph_msgs/msg/Clock) on which a simulator or bag playback publishes simulated time. With use_sim_time set and nothing publishing it, the node's time stays at zero and timers never fire. · 시뮬레이터나 bag 재생이 시뮬레이션 시간을 발행하는 토픽(rosgraph_msgs/msg/Clock). use_sim_time이 켜졌는데 발행자가 없으면 노드 시간은 0에 머물고 타이머가 돌지 않는다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §10]]
- **Closed-loop adaptive HRC (vs worker sensing)** · 폐루프 적응형 HRC — Worker sensing only classifies a worker's state; adaptive HRC exists when that estimate changes robot behavior and the coupled loop is evaluated on human outcomes. · 작업자 센싱은 작업자 상태를 분류할 뿐이고, 그 추정값이 로봇 행동을 바꾸고 결합된 루프를 인간 결과로 평가할 때 적응형 HRC가 된다. → [[05-construction-robotics/hrc-worker-centered|HRC §1]]
- **Closed-loop evaluation** · 폐루프 평가 — Scoring a policy by actually executing it under feedback, so its own errors compound. Open-loop evaluation scores predictions against logged data offline and is far easier; numbers are comparable only within one regime. · 정책을 피드백 아래에서 실제로 실행해 자기 오차가 누적되게 두고 채점하는 것. 개루프 평가는 기록된 데이터에 대한 예측을 오프라인으로 채점해 훨씬 쉽고, 수치는 같은 체제 안에서만 비교 가능하다. → [[02-foundations/ml-practice|ML Practice §5]]
- **Closed-loop twin** · 폐루프 트윈 — A digital twin with a bidirectional connection: site observations update the model, and model state generates or gates robot actions. It may still cover only one task or site. · 양방향 연결을 가진 디지털 트윈으로, 현장 관측이 모델을 갱신하고 모델 상태가 로봇 행동을 생성하거나 통제한다. 여전히 한 과제·현장에 국한될 수 있다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §2]]
- **Closure (of a distribution family)** · 닫힘 — An operation applied to a member of the family returns another member — e.g. an affine map of a Gaussian is Gaussian. It is why Kalman filtering stays exact instead of approximating. · 가족의 원소에 어떤 연산을 해도 결과가 여전히 그 가족 안에 있는 성질. 칼만 필터가 근사 없이 정확할 수 있는 이유. → [[02-foundations/probability|Probability §3]]
- **Cloud robotics** · 클라우드 로보틱스 — Moving a robot system's storage or computation onto remote servers. In Yu et al., VR demonstrations are pooled in the cloud as reusable digital assets for similar tasks. · 로봇 시스템의 저장이나 계산을 원격 서버로 옮기는 방식. Yu 등에서는 VR 시연을 클라우드에 모아 비슷한 과제에 재사용하는 디지털 자산으로 만든다. → [[01-canonical-papers/notes/8-construction/yu-imitation|Yu 2024]]
- **CNN (convolutional neural network)** · CNN(합성곱 신경망) — A network built from stacked convolution layers that slide small shared filters over the image, usually with pooling and a classifier at the end. AlexNet's win made it vision's default until ViT. · 작은 공유 필터를 이미지 위로 미는 합성곱 층을 쌓고 보통 풀링과 마지막 분류기를 붙인 신경망. AlexNet의 우승 이후 ViT가 나오기 전까지 비전의 기본이었다. → [[01-canonical-papers/notes/1-foundations/alexnet|AlexNet]]
- **Coarse-to-fine registration (initial alignment)** · 거친 정합과 정밀 정합(초기 정렬) — Aligning scans in two steps: a rough pose, for example from SLAM, brings scans close, then fine registration locks them into one frame. Final map quality still rests on the fine step. · 두 단계로 스캔을 맞추는 방식. SLAM 등에서 얻은 거친 자세로 스캔을 가까이 놓고, 정밀 정합이 공통 좌표계에 고정한다. 최종 지도 품질은 여전히 정밀 단계에 달려 있다. → [[01-canonical-papers/notes/8-construction/cho-slam|Cho SLAM 2018]]
- **COCO** · COCO — The standard benchmark for object detection and segmentation; detector papers report their accuracy on it as AP. · 물체 검출과 분할의 표준 벤치마크. 검출기 논문은 이 데이터셋에서의 정확도를 AP로 보고한다. → [[01-canonical-papers/notes/2-computer-vision/detr|DETR]]
- **Co-fine-tuning (co-training)** · 공동 파인튜닝 — Fine-tuning a pretrained model on a mix of new task data and its original pretraining-style data, so it gains the task without forgetting. RT-2 mixes robot episodes with web VQA data. · 사전학습 모델을 새 과제 데이터와 원래의 사전학습류 데이터를 섞어 파인튜닝해, 잊지 않으면서 과제를 얻게 하는 것. RT-2는 로봇 에피소드를 웹 VQA 데이터와 섞는다. → [[01-canonical-papers/notes/4-vla/rt-2|RT-2]]
- **Cogging** · cogging — Torque ripple from the attraction between rotor magnets and stator slots, felt as small detents when you turn an unpowered motor by hand. It corrupts the light free-space feel an impedance display needs. · 회전자 자석과 고정자 슬롯 사이 인력에서 생기는 토크 요동으로, 전원 없는 모터를 손으로 돌릴 때 작은 걸림으로 느껴진다. 임피던스 장치에 필요한 가벼운 자유공간 감각을 해친다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §1]]
- **colcon** · colcon — The build tool: it finds packages in src, orders them by declared dependencies, builds each by its build type, and installs results into install/ with setup files. By default install means copy. · 빌드 도구. src에서 패키지를 찾고, 선언된 의존성으로 순서를 정하고, build type에 따라 각각 빌드한 뒤 결과를 setup 파일과 함께 install/에 설치한다. 기본적으로 설치는 복사다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §3]]
- **Collapse (representation collapse)** · 붕괴(collapse) — The failure where a self-supervised network maps every input to the same output, trivially satisfying a matching objective. DINO prevents it with centering and sharpening. · 자기지도 신경망이 모든 입력을 같은 출력으로 보내 매칭 목적함수를 공짜로 만족시키는 실패. DINO는 centering과 sharpening으로 이를 막는다. → [[01-canonical-papers/notes/2-computer-vision/dino|DINO]]
- **Collision geometry (collision element)** · collision 요소 — The shape in a URDF link that collision checkers and physics engines query, separate from the visual mesh. Use primitives or a convex decomposition; a detailed mesh here makes planners and simulation crawl. · 충돌 검사기와 물리 엔진이 조회하는 URDF 링크의 형상으로, visual 메시와 따로 둔다. 원시 도형이나 볼록 분해를 써야 하며, 정밀 메시를 넣으면 플래너와 시뮬레이션이 극도로 느려진다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §4]]
- **Column space · span** · 열공간·span — The span of a set of vectors is everything reachable by scaling them and adding them up; a matrix's column space is the span of its columns, i.e. every output $Ax$ it can produce. · span은 벡터들을 스칼라배해 더해 도달 가능한 것 전체; 행렬의 열공간은 열들의 span, 즉 $Ax$가 낼 수 있는 출력 전체다. → [[02-foundations/linear-algebra|Linear Algebra §2]]
- **Command and state interfaces** · 명령 인터페이스와 상태 인터페이스 — Named per-joint strings like shoulder/position: state interfaces are what hardware can be read for, command interfaces what it can be told. Only one active controller may claim a command interface. · shoulder/position 같은 관절별 이름. 상태 인터페이스는 하드웨어에서 읽을 수 있는 값, 명령 인터페이스는 하드웨어에 지시할 수 있는 값이다. 명령 인터페이스는 활성 제어기 하나만 점유(claim)할 수 있다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §6]]
- **Common spatial patterns (CSP)** · CSP(공통 공간 패턴) — An EEG feature method that learns weightings of electrode channels so that the filtered signal's variance best separates two mental states, such as imagining the left versus the right hand. · 전극 채널의 가중 조합을 학습해, 필터된 신호의 분산이 왼손 상상과 오른손 상상 같은 두 정신 상태를 가장 잘 가르게 하는 EEG 특징 기법. → [[01-canonical-papers/notes/8-construction/liu-jebelli-bci|BCI teleoperation 2021]]
- **Comparator** · 비교 대상(comparator) — The condition a new method is compared against, matched on everything except the factor under test, such as the same planner with fixed friction. A question needs intervention, comparator, outcome, conditions and scope. · 새 방법과 비교하는 조건으로, 시험하는 요인 외에는 모두 맞춘다(예: 고정 마찰을 쓰는 같은 계획기). 질문에는 개입, 비교 대상, 결과, 조건, 범위가 필요하다. → [[06-research-practice/research-questions-claims|Research Questions §1]]
- **Comparison sort** · 비교 정렬 — A sort that learns about the input only by comparing pairs, such as merge sort or quicksort. Every comparison sort needs $\Omega(n \log n)$ comparisons; counting and radix sort escape this by reading key values. · 원소 쌍을 비교해서만 입력을 아는 정렬(병합 정렬, 퀵정렬 등). 모든 비교 정렬에는 $\Omega(n \log n)$번 비교가 필요하며, 계수 정렬과 기수 정렬은 키 값을 직접 읽어 이 하한을 피한다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §4]]
- **Complementarity (contact complementarity)** · complementarity — Ideal rigid contact written as gap $\phi(q)\ge 0$, normal force $f_n\ge 0$, and $\phi(q)f_n=0$: force only while touching, no gap while force is positive. An idealization, not a description of material deformation. · 간극 $\phi(q)\ge 0$, 법선력 $f_n\ge 0$, $\phi(q)f_n=0$으로 쓴 이상적 강체 접촉. 닿아 있을 때만 힘이 있고 힘이 양수면 간극이 닫혀 있다. 재료 변형의 기술이 아닌 이상화다. → [[04-robotics/contact-force-tactile|Contact §1]]
- **Complementary filter** · 상보 필터 — A fusion filter that combines two sensors trusted in different frequency bands, such as a low-passed accelerometer with a high-passed gyro in an IMU. · 서로 다른 주파수 대역에서 믿을 만한 두 센서를 합치는 융합 필터. IMU에서 저역통과한 가속도계와 고역통과한 자이로를 더하는 것이 예다. → [[02-foundations/signal-processing|Signal Processing §4]]
- **Complementary sensitivity ($T$)** · 상보 감도 — The transfer function $T = PC/(1+PC)$ from reference and measurement noise to output. Bound to the sensitivity by $S + T = 1$, so making one small at a frequency makes the other close to 1 there — the reason disturbance rejection and noise immunity cannot both be bought at the same frequency. · 기준 신호와 측정 잡음에서 출력으로 가는 전달함수 $T = PC/(1+PC)$. $S + T = 1$로 감도와 묶여 있어, 어느 주파수에서 하나를 작게 만들면 다른 하나가 그곳에서 1에 가까워진다. 외란 제거와 잡음 내성을 같은 주파수에서 함께 살 수 없는 이유다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Complementary slackness** · 상보 여유성 — The KKT condition $\lambda_i g_i = 0$: each inequality constraint either binds with a positive multiplier or is free with a zero multiplier. It is the case split in solving a constrained problem. · KKT 조건 $\lambda_i g_i = 0$. 각 부등식 제약은 양의 승수와 함께 걸려 있거나 승수 0으로 풀려 있다. 제약 문제를 풀 때의 경우 나누기가 곧 이것이다. → [[02-foundations/optimization|Optimization §4]]
- **Complete-workflow comparison (incumbent workflow)** · 전체 공정 비교 — Comparing a robot against the whole existing way of doing the job, including setup, calibration, material feeding, rework, and recovery, rather than robot task time alone. · 로봇 작업 시간만이 아니라 준비·보정·재료 공급·재작업·복구까지 포함한 기존 작업 방식 전체와 로봇을 비교하는 것. → [[05-construction-robotics/industry-deployment|Industry & Deployment §3]]
- **Compliance** · 컴플라이언스 — How much something yields under force; the inverse of stiffness. It can be passive (a spring, compliant wrist, or RCC) or rendered by a controller, and only passive compliance acts within a millisecond impact. · 힘을 받았을 때 얼마나 물러나는가, 곧 강성의 역수. 스프링·유연 손목·RCC 같은 수동 컴플라이언스일 수도, 제어기가 구현할 수도 있으며, 밀리초 충돌 안에서 작동하는 것은 수동 컴플라이언스뿐이다. → [[04-robotics/force-compliance-control|Force & Compliance Control §5]]
- **Compounding error** · 복합 오차 — Small per-step mistakes pushing a learned policy into states its training data never covered, where mistakes grow. Plain behavior cloning accumulates cost as $O(\epsilon T^2)$ over horizon $T$, versus $O(\epsilon T)$ for DAgger. · 스텝마다의 작은 실수가 학습된 정책을 학습 데이터가 덮지 않은 상태로 밀어내고 거기서 실수가 커지는 현상. 순수 행동 복제는 지평 $T$에 대해 비용이 $O(\epsilon T^2)$로, DAgger는 $O(\epsilon T)$로 누적된다. → [[02-foundations/rl-basics|RL Basics §6]]
- **Computation graph (ROS graph)** · 계산 그래프(computation graph) — The live set of nodes and their topic, service and action connections. It is not written down anywhere; it is discovered at runtime, so you debug it by querying the running system. · 노드들과 그 사이의 토픽·서비스·액션 연결로 이루어진 살아 있는 집합. 어디에도 적혀 있지 않고 실행 중에 발견되므로, 돌아가는 시스템에 질의해서 디버깅한다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §3]]
- **Computed torque control** · 계산 토크 제어 — Model-based control that uses inverse dynamics to cancel the arm's own mass, Coriolis, and gravity terms, leaving a simple linear controller to handle the residual. Its performance is exactly as good as the dynamic parameters it is given, which is why it is rarely used raw on hardware. · 역동역학으로 팔 자신의 질량·코리올리·중력 항을 상쇄하고, 남은 오차만 단순한 선형 제어기에 맡기는 모델 기반 제어. 성능은 주어진 동역학 파라미터의 정확도만큼이다. → [[02-foundations/manipulator-kinematics-dynamics|10. §5]]
- **Compute-optimal training (Chinchilla scaling)** · 연산 최적 학습 — Choosing model size and token count to minimize loss for a fixed compute budget. Chinchilla found both should grow in equal proportion, about 20 tokens per parameter. · 고정된 연산 예산에서 손실이 가장 낮도록 모델 크기와 토큰 수를 고르는 것. Chinchilla는 둘을 같은 비율로 키워야 하며 파라미터당 약 20토큰이라고 보였다. → [[01-canonical-papers/notes/1-foundations/scaling-laws|Scaling Laws]]
- **Condensed MPC formulation** · condensed(축약) 형태 — Writing an MPC problem with the states eliminated through the dynamics so only inputs remain: fewer variables, but a dense Hessian whose conditioning can worsen with horizon. Suits short horizons with input constraints only. · 동역학으로 상태를 소거해 입력만 남긴 MPC 정식화. 변수는 적지만 헤시안이 조밀하고 지평이 길어지면 조건수가 나빠질 수 있다. 짧은 지평과 입력 제약만 있는 경우에 맞는다. → [[04-robotics/mpc|MPC §2]]
- **Condensing** · 응축(condensing) — Substituting the dynamics equations into an MPC problem to eliminate the states, leaving only the control inputs as variables. The result is a dense convex QP. · MPC 문제에 동역학 등식을 대입해 상태를 소거하고 제어 입력만 변수로 남기는 것. 결과는 조밀한 볼록 QP다. → [[02-foundations/optimization|Optimization §5]]
- **Conditional flow matching (conditional path)** · 조건부 flow matching(조건부 경로) — Training on per-sample targets: conditioned on one data point, the path and its velocity are simple, and averaging these regressions provably gives the gradients of the intractable marginal objective. · 샘플별 타깃으로 학습하는 방식: 데이터 한 점을 조건으로 하면 경로와 속도가 단순해지고, 이 회귀들을 평균하면 계산 불가능한 주변 목적함수의 그래디언트와 같음이 증명된다. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Conditional value-at-risk (CVaR)** · CVaR(꼬리 위험) — A tail-risk measure: the expected cost within the worst fraction of outcomes, such as the worst decile. Optimizing it instead of the mean yields plans that avoid rare failures like rollover. · 꼬리 위험 척도로, 결과 중 가장 나쁜 일부(예: 최악 10분위) 안에서의 기대 비용이다. 평균 대신 이것을 최적화하면 전복 같은 드문 실패를 피하는 계획이 나온다. → [[04-robotics/traversability-off-road|Traversability §3]]
- **Condition dropout (null conditioning)** · 조건 드롭(널 토큰) — Randomly replacing the condition with a null token during training (about 10% of the time), so one network learns both conditional and unconditional predictions. It is what makes classifier-free guidance possible. · 학습 중 조건을 무작위로(약 10%) 널 토큰으로 바꿔, 한 네트워크가 조건부·무조건부 예측을 모두 배우게 하는 것. CFG를 가능하게 하는 장치다. → [[01-canonical-papers/notes/6-diffusion/classifier-free-guidance|CFG]]
- **Condition number** · 조건수 — For a general matrix in the 2-norm, $\kappa_2(A) = \sigma_{max}/\sigma_{min}$; for a symmetric positive-definite matrix such as a Hessian it equals the eigenvalue ratio. It measures the sensitivity of a linear system and the difficulty of an optimization. · 2-노름 기준 일반 행렬에서는 $\kappa_2(A) = \sigma_{max}/\sigma_{min}$이고, 헤시안 같은 대칭 양정부호 행렬에서는 고윳값 비와 같다. 선형계의 민감도와 최적화 난이도를 재는 값이다. → [[02-foundations/linear-algebra|선형대수]]
- **Conduit** · 전선관 — A tube, usually metal or plastic, that protects and routes electrical wiring. Fitting pipe or conduit is an insertion task prone to wedging and jamming. · 전기 배선을 보호하고 경로를 잡는 금속·플라스틱 관. 배관·전선관 끼움은 wedging과 jamming이 잘 일어나는 삽입 과제다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Conference paper** · 학회 논문 — A peer-reviewed paper with a fixed deadline and page limit, the standard publication unit in ML and robotics. Workshop papers are lightly reviewed early signals; journal papers go through revision rounds and are usually fuller. · 고정 마감과 쪽수 제한이 있는 동료 심사 논문으로, ML·로보틱스의 표준 출판 단위다. 워크숍 논문은 가볍게 심사된 이른 신호이고, 저널 논문은 개정 라운드를 거쳐 대개 더 완전하다. → [[01-canonical-papers/how-to-read|How to Read §4.6]]
- **Confidence interval (CI)** · 신뢰구간 — A range computed so that, over repeated experiments, 95% of such ranges would contain the true value. For a success rate from n trials the 95% half-width is at most about $\pm 1/\sqrt{n}$. · 실험을 반복하면 그렇게 만든 구간의 95%가 참값을 포함하도록 계산한 범위. n회 시행의 성공률이라면 95% 반폭이 최대 약 $\pm 1/\sqrt{n}$이다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §4]]
- **Configuration space (C-space)** · C-space — The set of all configurations of a robot. Its dimension is the number of degrees of freedom, and its shape matters: a 2R arm's C-space is a torus, because joint angles wrap around. · 로봇의 모든 컨피규레이션의 집합. 차원은 자유도 수이고 모양이 중요하다: 관절 각이 감기므로 2R 팔의 C-space는 원환면이다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **Conformal prediction** · conformal prediction — Turning held-out residuals into a prediction set or interval with a distribution-free coverage guarantee under exchangeability. The guarantee is marginal over exchangeable cases, not a probability for one particular scene or subgroup. · held-out 잔차로 교환가능성 하에 분포 무관 커버리지 보장이 붙은 예측 집합이나 구간을 만드는 방법. 보장은 교환 가능한 사례 전체에 대한 주변 커버리지이지, 특정 장면이나 부분집단에 대한 확률이 아니다. → [[04-robotics/human-intent-prediction|Human Intent §4]]
- **Confound** · 혼입(confound) — An uncontrolled difference other than the proposed factor that offers an alternative explanation for a result, such as a new architecture that also received extra data and a pretrained encoder. · 제안한 요인 외에 결과를 달리 설명할 수 있는 통제되지 않은 차이. 예를 들어 새 구조가 추가 데이터와 사전학습 인코더까지 받았다면 그 차이가 혼입이다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §2]]
- **Confusion matrix (TP, FP, FN, TN)** · 혼동 행렬 — The 2×2 table of true positives, false positives, false negatives and true negatives for a binary detector. Accuracy, precision, recall, TPR and FPR are all ratios of its four counts. · 이진 검출기의 참양성·거짓양성·거짓음성·참음성 네 개수를 늘어놓은 2×2 표. 정확도·정밀도·재현율·TPR·FPR은 모두 이 네 숫자의 비율이다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Conjugate gradient** · conjugate gradient(켤레 기울기) — An iterative solver for $Ax=b$ with $A$ symmetric positive definite that needs only matrix–vector products, never $A$ itself. TRPO used it to apply a natural-gradient step without forming the Fisher matrix; PPO exists partly because doing so was awkward enough to be worth avoiding. · $A$가 대칭 양정부호일 때 $Ax=b$를 푸는 반복 솔버로, $A$ 자체가 아니라 행렬–벡터 곱만 필요하다. TRPO는 피셔 행렬을 만들지 않고 자연 그래디언트 스텝을 밟으려고 이것을 썼고, 그 번거로움을 피할 값어치가 있었다는 것이 PPO가 존재하는 이유의 일부다. → [[02-foundations/optimization|Optimization §3]]
- **Connected component** · 연결 성분 — A maximal set of vertices in an undirected graph that can all reach each other. BFS or DFS finds them in $O(V + E)$; union-find maintains them as edges arrive over time. · 무방향 그래프에서 서로 모두 도달 가능한 정점들의 극대 집합. BFS나 DFS로 $O(V + E)$에 찾고, 간선이 시간에 따라 도착하면 union-find로 유지한다. → [[02-foundations/algorithms/data-structures|11.2 §7]]
- **Consistency model (diffusion distillation)** · consistency 모델(증류) — A fast generator, often distilled from a diffusion model, that jumps from a noisy point straight to a clean sample. It cuts sampling from tens of network calls to one to four. · 흔히 디퓨전 모델에서 증류하는 빠른 생성기로, 노이즈 섞인 점에서 깨끗한 샘플로 곧장 건너뛴다. 샘플링을 네트워크 호출 수십 번에서 1~4번으로 줄인다. → [[01-canonical-papers/notes/6-diffusion/ddim|DDIM]], [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]]
- **Consistent heuristic (monotone heuristic)** · 일관적 휴리스틱 — A heuristic with $h(n) \le c(n, n') + h(n')$ on every edge and zero at the goal. It implies admissibility and guarantees that A\* never needs to re-expand a vertex. · 모든 간선에서 $h(n) \le c(n, n') + h(n')$이고 목표에서 0인 휴리스틱. 허용성을 함의하며, A\*가 정점을 다시 확장할 필요가 없음을 보장한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §6]]
- **Constrained decoding** · 제약된 디코딩 — Restricting a language model's output at each step to a set of valid tokens. RT-2 uses it so the VLM can only emit legal action tokens when controlling the robot. · 언어모델이 매 스텝 유효한 토큰 집합 안에서만 출력하도록 제한하는 것. RT-2는 로봇을 제어할 때 VLM이 올바른 행동 토큰만 내도록 이것을 쓴다. → [[01-canonical-papers/notes/4-vla/rt-2|RT-2]]
- **Constrained MDP (CMDP)** · 제약 MDP — An MDP with a second, cost signal whose expected total must stay under a limit; reward is optimized subject to that bound, often with Lagrangian methods. Stronger than penalizing violations inside the reward. · 기대 총합이 한도 아래에 머물러야 하는 별도의 비용 신호가 있는 MDP로, 그 상한 아래에서 보상을 최적화하며 흔히 라그랑주 방법을 쓴다. 보상 안에 위반 페널티를 넣는 것보다 강하다. → [[02-foundations/rl-basics|RL Basics §9]]
- **Constraint qualification (Slater's condition, LICQ)** · 제약 자격 조건 — A regularity assumption under which KKT conditions must hold at an optimum. Slater: some feasible point satisfies every inequality strictly. LICQ: gradients of equality and active inequality constraints are linearly independent. · 최적점에서 KKT 조건이 반드시 성립하게 해 주는 정칙성 가정. Slater 조건은 모든 부등식을 엄격히 만족하는 점이 있다는 것, LICQ는 등식 제약과 활성 부등식 제약의 그래디언트가 선형 독립이라는 것. → [[02-foundations/optimization|Optimization §4]]
- **Constraint softening** · 제약 연화(constraint softening) — Replacing hard MPC state constraints with penalized slack variables, so the solver can return a costly violation instead of nothing. It does not guarantee feasibility if the remaining hard constraints conflict. · MPC의 경성 상태 제약을 벌점 붙은 슬랙 변수로 바꿔, 솔버가 아무것도 못 돌려주는 대신 비용을 치른 위반을 돌려주게 하는 것. 남은 경성 제약이 충돌하면 실행 가능성을 보장하지 않는다. → [[04-robotics/mpc|MPC §3]]
- **Construction 3D printing** · 건설 3D 프린팅 — Additive manufacturing of building elements by depositing concrete or similar material layer by layer; Contour Crafting seeded it, and shotcrete printing sprays material instead. · 콘크리트 같은 재료를 층층이 쌓아 건물 요소를 만드는 적층 제조. Contour Crafting이 씨앗을 뿌렸고, 숏크리트 프린팅은 재료를 뿜어 붙인다. → [[05-construction-robotics/lineage|Lineage §1]], [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §2]]
- **Construction deviation** · 시공 편차 — The true difference between built and designed geometry. Before claiming it, separate sensor noise, calibration, registration error, and model tolerance, since registration error can masquerade as deviation. · 실제로 지어진 형상과 설계 형상의 참된 차이. 주장하기 전에 센서 노이즈·보정·정합 오차·모델 공차를 분리해야 하며, 정합 오차는 편차로 위장할 수 있다. → [[05-construction-robotics/site-perception|Site Perception §2]]
- **Construct validity** · 구성 타당도(construct validity) — Whether a measurement actually measures the thing the claim names. A study that measures workload with NASA-TLX and concludes better perception lacks it. · 측정이 주장이 이름 붙인 대상을 실제로 재는가. NASA-TLX로 작업부하를 재고 지각이 좋아졌다고 결론 내린 연구에는 이것이 없다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §6]]
- **Contact-implicit MPC** · 접촉 내재 MPC — MPC in which contact mode switches are part of the optimization, making the problem non-smooth. Convex legged MPC sidesteps this by fixing the contact schedule in advance. · 접촉 모드 전환이 최적화 안에 들어 있는 MPC로, 문제가 비매끄러워진다. 보행 convex MPC는 접촉 스케줄을 미리 고정해 이를 피한다. → [[04-robotics/mpc|MPC §4]]
- **Contact-rich manipulation** · 접촉이 많은(contact-rich) 매니퓰레이션 — Manipulation whose success depends on sustained or changing contact forces, such as insertion, wiping, assembly, or drilling. The label requires an explicit task, contact model, and perturbation definition to mean anything. · 삽입·닦기·조립·드릴링처럼 지속되거나 변하는 접촉력에 성공이 달린 매니퓰레이션. 이 표현은 과제·접촉 모델·교란 정의를 명시해야 의미를 가진다. → [[04-robotics/contact-force-tactile|Contact §9]]
- **Contact schedule** · 접촉 스케줄 — The pre-specified timing of which feet touch the ground when. Model-based legged controllers such as convex MPC receive it and solve only for forces, which keeps the problem convex. · 어느 발이 언제 땅에 닿는지를 미리 정한 시간표. convex MPC 같은 모델 기반 보행 제어기는 이것을 받아 힘만 풀어서 문제를 볼록하게 유지한다. → [[04-robotics/legged-locomotion|Legged Locomotion §2]]
- **Contact-state estimation** · 접촉 상태 추정 — Classifying which discrete contact mode the system is in — no contact, one point, two points, line, seated — rather than estimating a continuous force. It is the state a task-level planner actually needs, and its evidence is largely tactile. · 연속적인 힘이 아니라 시스템이 어떤 이산 접촉 모드에 있는지를 분류하는 것. 과제 수준 계획기가 실제로 필요로 하는 상태이며, 그 증거는 대체로 촉각이다. → [[04-robotics/tactile-visuotactile|14. §3]]
- **Contact transition** · 접촉 천이 — The moment a robot goes from free motion into contact. A stiff impact can be over within about a millisecond, so approach speed, apparent mass, and stiffness set the peak force before feedback can react. · 로봇이 자유 운동에서 접촉으로 넘어가는 순간. 단단한 충돌은 1밀리초 안팎에 끝날 수 있어, 피드백이 반응하기 전에 접근 속도·겉보기 질량·강성이 최대 힘을 정한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §5]]
- **Context vector** · 문맥 벡터 — The attention-weighted sum of encoder states, $c_i = \sum_j \alpha_{ij} h_j$, recomputed for every output step so the decoder gets a fresh summary of the source each time. · 인코더 상태의 어텐션 가중합 $c_i = \sum_j \alpha_{ij} h_j$. 출력 스텝마다 새로 계산되어 디코더가 매번 원문의 다른 요약을 받는다. → [[01-canonical-papers/notes/1-foundations/bahdanau-attention|Bahdanau Attention]]
- **Context window** · 문맥 창 — The span of past input a model can attend to at once. A map-free video policy remembers nothing older than its window, which limits long searches in large buildings. · 모델이 한 번에 주의를 줄 수 있는 과거 입력의 범위. 지도 없는 비디오 정책은 창보다 오래된 것을 기억하지 못해 큰 건물에서의 긴 탐색에 한계가 있다. → [[01-canonical-papers/notes/9-navigation/navid|NaVid]]
- **Continuous normalizing flow (CNF)** · 연속 정규화 흐름(CNF) — A generative model defined by an ODE that moves noise to data along a learned velocity field. Classic CNF training had to simulate the ODE, which flow matching avoids. · 학습된 속도장을 따라 노이즈를 데이터로 옮기는 ODE로 정의된 생성 모델. 고전적 CNF 학습은 ODE를 시뮬레이션해야 했고, flow matching이 이를 피한다. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Continuum limit** · 연속 극한 — A sum whose steps have been shrunk all the way to infinitesimal — which is exactly what an integral is. Papers use the phrase when a discrete chain (diffusion noising steps, say) is replaced by a differential equation. · 더하는 단위를 무한소까지 줄인 합 — 적분이 바로 그것이다. 이산 사슬(예: 디퓨전 노이즈 단계)을 미분방정식으로 바꿀 때 쓰는 표현. → [[02-foundations/engineering-math|0.5 §3]]
- **Contractor** · 시공사 — The company that builds a project under contract, managing trades, equipment, and schedule on site. Contractor-side robotics programs, like Kajima's, run machines on their own projects. · 계약에 따라 공사를 수행하며 현장의 공종·장비·공정을 관리하는 회사. Kajima 같은 시공사 쪽 로봇 프로그램은 자기 공사 현장에서 기계를 운용한다. → [[05-construction-robotics/lineage|Lineage §3]]
- **Contrastive learning** · 대조 학습 — Learning embeddings that pull matched pairs together and push mismatched pairs apart. · 짝이 맞는 쌍은 가깝게, 아닌 쌍은 멀게 임베딩을 학습. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]], [[02-foundations/information-theory|InfoNCE]]
- **Contribution (scientific, system, artifact)** · 기여(과학적·시스템·산출물) — What a paper adds: new knowledge supported by evidence, an integrated capability, or a reusable code, dataset, benchmark or platform. A system contribution must state what it establishes beyond assembly effort. · 논문이 더하는 것: 증거가 지지하는 새 지식, 통합된 능력, 재사용 가능한 코드·데이터셋·벤치마크·플랫폼. 시스템 기여는 조립 노력 너머에 무엇을 확립하는지 밝혀야 한다. → [[06-research-practice/research-questions-claims|Research Questions §3]]
- **Controller manager (read–update–write loop)** · 컨트롤러 매니저 — The ros2_control process that loads hardware and controllers, matches interfaces, and each cycle calls read on hardware, update on active controllers, write to hardware, at update_rate (default 100 Hz). · 하드웨어와 제어기를 로드하고 인터페이스를 맞추며, 매 주기 update_rate(기본 100 Hz)로 하드웨어 read, 활성 제어기 update, 하드웨어 write를 호출하는 ros2_control 프로세스. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §7]], [[04-robotics/ros2/from-simulation-to-hardware|25.11 Sim to Hardware §3]]
- **ControlNet** · ControlNet — A trainable copy of a frozen diffusion U-Net's encoder that reads a spatial condition (edges, pose, depth) and feeds the base through zero convolutions. It adds precise spatial control without altering the base model. · 얼린 디퓨전 U-Net 인코더의 학습 가능한 복제본으로, 공간 조건(에지·포즈·깊이)을 읽어 zero convolution을 거쳐 베이스에 넣는다. 베이스 모델을 바꾸지 않고 정밀한 공간 제어를 더한다. → [[01-canonical-papers/notes/6-diffusion/controlnet|ControlNet]]
- **Control variable** · 통제 변수 — A condition held fixed or explicitly modeled across conditions, such as the robot, controller or demonstrations, so that it cannot explain the difference in outcome. · 조건들 사이에서 고정하거나 명시적으로 모델링하는 조건(로봇, 제어기, 시연 등). 결과 차이를 설명하지 못하게 하려는 것이다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §1]]
- **Convexity (convex set, convex function)** · 볼록성 — A set is convex if it contains every segment between its points; a function is convex if it lies on or below its chords. For convex problems every local minimum is global. · 집합은 두 점 사이 선분을 모두 포함하면 볼록, 함수는 현 위나 아래에 놓이면 볼록이다. 볼록 집합 위에서 볼록 함수를 최소화하면 모든 지역 최솟값이 전역이다. → [[02-foundations/optimization|Optimization §2]]
- **Convolution** · 합성곱 — $y[n] = \sum_k x[k]h[n-k]$: flip the impulse response, slide, multiply, add. Every linear time-invariant system does exactly this; a CNN layer learns a bank of such kernels, computed without the flip. · 임펄스 응답을 뒤집고, 밀고, 곱하고, 더하는 연산. 모든 선형 시불변 시스템은 정확히 이것을 하며, CNN 층은 이런 커널 묶음을 학습하되 뒤집기 없이 계산한다. → [[02-foundations/signal-processing|Signal Processing §1]]
- **Convolution theorem** · 합성곱 정리 — Convolution in time equals multiplication in frequency, so filtering a signal multiplies its spectrum by the filter's frequency response. This is why filters are analyzed in the frequency domain. · 시간 영역의 합성곱은 주파수 영역의 곱셈과 같아, 신호를 필터링하면 스펙트럼에 필터의 주파수 응답이 곱해진다. 필터를 주파수 영역에서 분석하는 이유다. → [[02-foundations/signal-processing|Signal Processing §3]]
- **Coriolis and centrifugal terms** · 코리올리·원심력 항 — The velocity-quadratic part of the manipulator equation, $C(\theta,\dot\theta)\dot\theta$: the coupling by which one joint's motion exerts torque on another. Not friction, and zero at rest — which is why slow experiments hide model error. · 매니퓰레이터 방정식의 속도 이차 항. 한 관절의 운동이 다른 관절에 토크를 가하는 결합이며, 마찰이 아니고 정지 시 0이다 — 느린 실험이 모델 오차를 숨기는 이유. → [[02-foundations/manipulator-kinematics-dynamics|10. §4]]
- **CoRL (Conference on Robot Learning)** · CoRL — The main robot-learning conference, reviewed double-blind on OpenReview with a one-page, non-interactive rebuttal. It publishes no official acceptance rate. · 로봇 학습 분야의 주요 학회. OpenReview에서 이중 블라인드로 심사하고 반박문은 1쪽이며 비대화형이다. 공식 채택률을 발표하지 않는다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Cosine similarity** · 코사인 유사도 — The dot product of two vectors divided by their lengths, the cosine of the angle between them. It ignores vector norms and is the retrieval score of embedding models such as CLIP. · 두 벡터의 내적을 길이의 곱으로 나눈 값, 즉 둘 사이 각의 코사인. 벡터 노름을 무시하며, CLIP 같은 임베딩 모델의 검색 점수다. → [[02-foundations/linear-algebra|Linear Algebra §1]]
- **Costmap** · costmap — A grid storing the cost of driving through each cell, which a planner minimizes. For learned traversability it is robot-specific and should be conditioned on commanded speed, unlike an obstacle-only occupancy grid. · 각 셀을 지나는 비용을 담아 계획기가 최소화하는 격자. 학습된 traversability에서는 로봇마다 다르고 명령 속도에 조건화되어야 하며, 장애물만 표시하는 점유 격자와 다르다. → [[04-robotics/traversability-off-road|Traversability §1]]
- **Costmap filter (keepout zone)** · costmap 필터 — A Nav2 plugin driven by a filter mask image that encodes policy rather than geometry: KeepoutFilter blocks areas, SpeedFilter caps velocity, BinaryFilter toggles a topic. Filters are not inflated. · 기하가 아니라 정책을 담은 필터 마스크 이미지로 동작하는 Nav2 플러그인. KeepoutFilter는 구역을 막고, SpeedFilter는 속도를 제한하고, BinaryFilter는 토픽을 켜고 끈다. 필터에는 inflation이 적용되지 않는다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §9]]
- **Costmap layers** · costmap 계층 — The plugins stacked in order to build a Nav2 costmap: static (stored map), obstacle (marking and raytrace clearing from scans), voxel (3D version), and inflation last. · Nav2 costmap을 순서대로 쌓아 만드는 플러그인. static(저장된 지도), obstacle(스캔으로 표시하고 raytrace로 지움), voxel(3D 버전), 그리고 마지막에 inflation. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §4]]
- **Cost-to-go** · cost-to-go — The minimum total future cost from a given state onward. In LQR it is the quadratic $x^\top P x$, and substituting it into the HJB equation yields the Riccati equation. · 주어진 상태에서 앞으로 치를 최소 총비용. LQR에서는 이차형식 $x^\top P x$이고, 이것을 HJB 방정식에 넣으면 리카티 방정식이 나온다. → [[04-robotics/lqr-lqg|LQR/LQG §1]]
- **Coulomb friction** · Coulomb 마찰 — The approximation $\lVert f_t\rVert\le\mu f_n$: tangential force is at most the friction coefficient times normal force. Forces inside it can stick, beyond it slip; real $\mu$ varies with material, speed, dust, and wear. · 접선력이 마찰 계수 곱하기 법선력 이하라는 근사 $\lVert f_t\rVert\le\mu f_n$. 이 범위 안의 힘은 고착할 수 있고 경계나 그 너머에서는 미끄러진다. 실제 $\mu$는 재료·속도·먼지·마모에 따라 변한다. → [[04-robotics/contact-force-tactile|Contact §2]]
- **Counterbalancing** · counterbalance — Arranging the order of conditions so each appears equally often in each position, so learning, battery drain, wear or operator effects do not favor one method. Randomizing the order is the alternative. · 조건의 순서를 각 조건이 각 위치에 똑같이 자주 오도록 배치해, 학습·배터리·마모·운용자 효과가 한 방법에 유리하게 작용하지 않게 하는 것. 순서 무작위화가 대안이다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §2]]
- **Counterfactual** · counterfactual(대안) — What would happen without the intervention, such as what a worker would otherwise do. An impact claim names it, plus the beneficiary and an observable change, to be checkable. · 개입이 없을 때 일어날 일, 예컨대 로봇이 없으면 작업자가 무엇을 하는가. 임팩트 주장은 이것과 수혜자, 관찰 가능한 변화를 밝혀야 확인할 수 있다. → [[06-research-practice/real-world-impact|Real-World Impact §1]]
- **Coupon (test specimen)** · 시편 — A small sample piece, such as one weld or printed segment, tested in place of the full structure. It cannot validate tolerance accumulation at building scale. · 구조물 전체 대신 시험하는 용접 하나나 프린팅 조각 같은 작은 시료. 건물 규모의 공차 누적은 검증하지 못한다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §3]]
- **Covariance** · 공분산 — The matrix describing the magnitude and co-movement of several estimation errors; its uncertainty reading is trustworthy only when the estimator's model assumptions hold. · 여러 추정 오차의 크기와 함께 움직이는 방향을 나타내는 행렬. 그것이 말하는 불확실성은 추정기의 모델 가정이 성립할 때만 믿을 수 있다. → [[04-robotics/state-estimation-slam|State Estimation]]
- **Covariate shift** · covariate shift(공변량 이동) — In imitation, the mismatch between training on the expert's states and running on the policy's own: small errors drift the state off-distribution, where errors compound. It is the reason DAgger exists. · 모방학습에서 전문가의 상태로 학습하고 자기 상태에서 실행하는 데서 오는 불일치. 작은 오차가 상태를 분포 밖으로 밀고 거기서 오차가 누적된다. DAgger가 존재하는 이유다. → [[02-foundations/rl-basics|RL Basics §6]]
- **Critic (DWB, MPPI)** · critic — A scoring plugin that rates a candidate trajectory on one criterion, such as closeness to the path or distance to obstacles; DWB and MPPI controllers weight many critics to choose a command. · 경로 근접도나 장애물 거리 같은 기준 하나로 후보 궤적을 평가하는 플러그인. DWB와 MPPI 제어기는 여러 critic에 가중치를 주어 명령을 고른다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §6]]
- **Cross-attention** · cross-attention — Attention where the queries come from one stream and the keys/values from another — text attending to image patches, or an action expert attending to a VLM's tokens. Same arithmetic as self-attention; only the source of $Q$ versus $K,V$ differs, and that difference is what makes a model *multimodal*. · 쿼리는 한 스트림에서, 키·값은 다른 스트림에서 오는 어텐션. 연산은 self-attention과 같고 $Q$와 $K,V$의 출처만 다르며, 그 차이가 모델을 멀티모달로 만든다. → [[02-foundations/linear-algebra|Linear Algebra §1]]
- **Cross-embodiment** · 교차-embodiment — Pooling demonstrations recorded on many different robot bodies into one training set, as Open X-Embodiment does, so one policy can learn from all of them. · 서로 다른 여러 로봇 몸에서 기록한 시연을 하나의 학습 데이터로 모아(Open X-Embodiment처럼) 한 정책이 전부에서 배우게 하는 것. → [[02-foundations/rl-basics|RL Basics §6]]
- **Cross-entropy** · 교차 엔트로피 — $-E_p[\log q]$: the standard classification and language-model loss, and MLE in disguise. · 분류·언어모델의 표준 손실이며 그 정체는 MLE. → [[02-foundations/information-theory|정보이론]]
- **Crossover frequency** · 교차 주파수 — Gain crossover is the frequency where the loop gain's magnitude equals 1; phase crossover is where the loop phase reaches −180°. The phase margin is read at the first, the gain margin at the second. · 이득 교차는 루프 이득의 크기가 1이 되는 주파수, 위상 교차는 루프 위상이 −180°에 이르는 주파수다. 위상 여유는 앞의 점에서, 이득 여유는 뒤의 점에서 읽는다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Cross-subject transfer (cross-user transfer)** · 사용자 간 전이 — Whether a model trained on one person's biosignals works for a new person without recalibration. EEG decoders are often reported per subject, which leaves this question open. · 한 사람의 생체신호로 학습한 모델이 재보정 없이 새 사람에게도 작동하는가. EEG 디코더는 흔히 피험자별로 보고되어 이 질문이 열린 채로 남는다. → [[01-canonical-papers/notes/8-construction/liu-jebelli-bci|BCI teleoperation 2021]]
- **Cross-training** · 교차 훈련 — A human–robot teaming method in which human and robot practise each other's roles, so each can anticipate the other. Construction HRC imports it from manufacturing. · 사람과 로봇이 서로의 역할을 연습해 상대를 예측하게 만드는 인간–로봇 팀 방법. 건설 HRC가 제조 분야에서 들여온다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Curriculum learning** · 커리큘럼 학습 — Changing the *task* rather than the algorithm: start easy and raise difficulty as success rate crosses a threshold. Cheap and often does most of the work, so a baseline that did not get the same curriculum is not a fair comparison. · 알고리즘이 아니라 *과제*를 바꾸는 것; 베이스라인이 같은 커리큘럼을 못 받았다면 공정한 비교가 아니다. → [[02-foundations/rl-basics|RL Basics §8]]
- **Curse of dimensionality** · 차원의 저주 — The explosive growth of cost with dimension: a DP table over a 6-DoF arm at 100 bins per joint has $10^{12}$ entries, and KD-tree pruning stops working in high dimensions. · 차원이 늘면 비용이 폭발적으로 커지는 현상. 관절당 100칸인 6자유도 팔의 DP 표는 $10^{12}$칸이 되고, 고차원에서는 KD-tree의 가지치기가 작동하지 않는다. → [[02-foundations/algorithms/data-structures|11.2 §8]], [[02-foundations/algorithms/dynamic-programming|11.5 §7]]
- **Curtain wall** · 커튼월 — A building's non-structural outer skin of glass and metal panels hung from the structure. Installing it means moving large, heavy, tolerance-sensitive modules into place. · 구조체에 매다는, 하중을 받지 않는 유리·금속 패널 외피. 설치는 크고 무거우며 공차에 민감한 모듈을 제자리로 옮겨 맞추는 일이다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Cut property** · 컷 속성 — If an edge is the lightest one crossing some split of the vertices into two groups, some minimum spanning tree contains it. Prim's and Kruskal's algorithms both apply it repeatedly. · 정점을 두 그룹으로 나눈 어떤 컷을 가로지르는 간선 중 가장 가벼운 간선은 어떤 최소 신장 트리에 포함된다. Prim과 Kruskal 알고리즘 모두 이를 반복 적용한다. → [[02-foundations/algorithms/greedy-mst|11.4 §6]]
- **CVAE** · CVAE — A conditional VAE: it holds the distribution of plausible outputs given a condition inside a latent variable. · 조건부 VAE. → [[01-canonical-papers/notes/4-vla/act|ACT]]
- **CVPR (Computer Vision and Pattern Recognition)** · CVPR — The flagship computer-vision conference, which publishes official submission counts. It counts any peer-reviewed workshop paper longer than four pages, excluding references, as a prior publication. · 컴퓨터 비전 분야 대표 학회로, 공식 제출 건수를 발표한다. 참고문헌 제외 4쪽을 넘는 심사된 워크숍 논문은 무엇이든 선행 출판으로 센다. → [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **Cycle time (earthmoving)** · 사이클 시간 — The time for one fill–move–dump–return cycle. Productivity scales with cycles per hour, but field figures must add downtime, setup, and recovery to the time denominator. · 채우기–이동–붓기–복귀 한 사이클에 걸리는 시간. 생산성은 시간당 사이클 수에 비례하지만 현장 수치는 중단·준비·복구 시간을 분모에 넣어야 한다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §6]]

## D–F

- **DAgger** · DAgger — An interactive imitation method: run the learner, have the expert label the states it actually visited, add them to the data, retrain. It fixes covariate shift and reduces compounding cost to $O(\epsilon T)$. · 상호작용적 모방 방법: 학습자를 실행하고, 실제로 방문한 상태를 전문가가 라벨하고, 데이터에 더해 재학습한다. covariate shift를 고치고 누적 비용을 $O(\epsilon T)$로 줄인다. → [[02-foundations/rl-basics|RL Basics §6]]
- **Damping ratio ($\zeta$)** · 감쇠비 — The number deciding whether a second-order response rings: $\zeta<1$ oscillates, $\zeta=1$ is critically damped, $\zeta>1$ is sluggish. With $\omega_n$ it fixes settling time and overshoot. · 2차 응답이 울리는지를 정하는 수: $\zeta<1$이면 진동하고, $\zeta=1$이 임계 감쇠, $\zeta>1$이면 굼뜨다. $\omega_n$과 함께 정착 시간과 오버슈트를 결정한다. → [[04-robotics/control-theory-ce397|제어 이론 §5]]
- **Data association** · data association(연관) — Deciding which measurement came from which landmark or object before any filter or optimizer update. In SLAM a wrong answer corrupts the map; in tracking it swaps identities. · 필터나 최적화기가 갱신하기 전에 어느 측정이 어느 landmark나 물체에서 왔는지 정하는 일. SLAM에서 틀리면 지도가 망가지고, 추적에서 틀리면 정체가 뒤바뀐다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Data augmentation** · 데이터 증강 — Training on transformed copies of examples, such as crops or lighting changes, to discourage shortcuts. It helps only when the transformation preserves the label and resembles variation that matters at deployment. · 자르기·조명 변화 같은 변형 사본으로 학습해 지름길 학습을 억제하는 것. 변환이 정답을 보존하고 배포에서 의미 있는 변동을 닮을 때만 도움이 된다. → [[02-foundations/ml-practice|ML Practice §2]]
- **Data engine** · 데이터 엔진 — A model-in-the-loop labeling flywheel: the model helps annotate, the annotations retrain the model, until labeling is fully automatic. SAM built its 1.1B-mask dataset this way. · 모델이 참여하는 라벨링 플라이휠. 모델이 주석을 돕고, 그 주석으로 모델을 다시 학습하기를 반복해 결국 완전 자동 라벨링에 이른다. SAM은 이렇게 11억 개 마스크를 만들었다. → [[01-canonical-papers/notes/2-computer-vision/sam|SAM]]
- **Data leakage** · 데이터 누수 — Test information sneaking into training, through duplicates, temporal overlap, or pretraining contamination. It is the first thing to suspect when numbers look too good. · 중복, 시간적 겹침, 사전학습 오염 등을 통해 test 정보가 학습에 스며드는 것. 숫자가 너무 좋아 보일 때 가장 먼저 의심할 것이다. → [[02-foundations/ml-practice|ML Practice §1]]
- **Data mixture (dataset weighting)** · 데이터 혼합물(데이터셋 가중치) — The recipe of which datasets a generalist model trains on and in what proportions. OXE-based models such as OpenVLA weight datasets carefully, and results depend on this composition. · 범용 모델이 어떤 데이터셋을 어떤 비율로 섞어 학습하는가의 레시피. OpenVLA 같은 OXE 기반 모델은 데이터셋 가중치를 신중히 주며, 결과가 이 구성에 좌우된다. → [[01-canonical-papers/notes/4-vla/open-x-embodiment|Open X-Embodiment]], [[01-canonical-papers/notes/4-vla/openvla|OpenVLA]]
- **Data pyramid** · 데이터 피라미드 — GR00T N1's data strategy for scarce robot data: web-scale human video at the base, simulation and neural trajectories in the middle, expensive real robot demonstrations only at the top. · 로봇 데이터가 귀할 때의 GR00T N1 데이터 전략: 바닥은 웹 규모 인간 비디오, 중간은 시뮬레이션과 신경 궤적, 꼭대기만 비싼 실제 로봇 시연. → [[01-canonical-papers/notes/4-vla/gr00t-n1|GR00T N1]]
- **DCT action tokenization (FAST)** · 이산 코사인 변환 기반 행동 토큰화(FAST) — Compressing an action chunk with the discrete cosine transform before turning it into tokens, instead of binning each dimension at each timestep. FAST reports that per-step binning fails on dexterous, high-frequency data. · 차원별·시점별로 구간을 나누는 대신, 행동 청크를 이산 코사인 변환으로 압축한 뒤 토큰으로 만드는 방식. FAST는 시점별 구간 나누기가 정교한 고주파 데이터에서 잘 작동하지 않는다고 보고한다. → [[01-canonical-papers/notes/4-vla/index|VLA Map §1]]
- **DDIM (DDIM sampling)** · DDIM — A deterministic sampler for an already trained DDPM network: guess the clean sample, re-noise it to a lower level, repeat. It takes 10–50 steps instead of 1000 with no retraining. · 이미 학습된 DDPM 네트워크용 결정론적 샘플러: 깨끗한 샘플을 추정하고, 더 낮은 노이즈 수준으로 다시 노이즈화하고, 반복한다. 재학습 없이 1000 스텝 대신 10~50 스텝이면 된다. → [[01-canonical-papers/notes/6-diffusion/ddim|DDIM]]
- **DDIM inversion** · DDIM 역변환 — Running the deterministic DDIM map backwards to find the noise that produces a given image or trajectory. Because the map is nearly invertible, the noise acts as a latent code for editing and interpolation. · 결정론적 DDIM 사상을 거꾸로 돌려 주어진 이미지나 궤적을 만드는 노이즈를 찾는 것. 사상이 거의 가역이므로 그 노이즈가 편집·보간용 잠재 코드 역할을 한다. → [[01-canonical-papers/notes/6-diffusion/ddim|DDIM]]
- **DDPG (deep deterministic policy gradient, Ape-X DDPG)** · DDPG — An off-policy actor-critic RL algorithm for continuous actions: a critic learns Q-values and a deterministic actor follows their gradient. Ape-X DDPG spreads data collection over many parallel workers. · 연속 행동을 위한 오프폴리시 actor-critic RL 알고리즘. critic이 Q값을 배우고 결정적 actor가 그 경사를 따른다. Ape-X DDPG는 데이터 수집을 여러 병렬 작업자에 분산한다. → [[01-canonical-papers/notes/8-construction/apolinarska-timber|Apolinarska timber 2021]]
- **DDS (Data Distribution Service)** · DDS — The OMG industry standard that ROS 2 borrows for its wire protocol (DDSI-RTPS). It provides masterless discovery, per-connection QoS and security plugins, at the cost of QoS-mismatch failures. · ROS 2가 통신 프로토콜(DDSI-RTPS)로 빌려 쓰는 OMG 산업 표준. 마스터 없는 discovery, 연결별 QoS, 보안 플러그인을 주지만, 대가로 QoS 불일치라는 실패 방식이 생긴다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §5]]
- **Deadline (QoS)** · deadline — The expected maximum time between consecutive messages on a topic. When exceeded, the subscription gets a requested-deadline-missed event, which lets a node tell a dead stream from a slow one. · 토픽에서 연속한 메시지 사이의 최대 간격 기대치. 넘기면 서브스크립션이 requested deadline missed 이벤트를 받아, 멈춘 스트림과 느린 스트림을 구분할 수 있다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §2]]
- **Deadly triad** · deadly triad(치명적 삼요소) — Function approximation, bootstrapping and off-policy training. With any two present but not all three, instability can be avoided; all three together can make value estimates diverge without bound — in prediction as well as control, and in dynamic programming with a known model, so it is not a noise or exploration problem. Target networks, replay tuning, clipped double-$Q$ and offline-RL pessimism are all mitigations of one leg. · 함수 근사, 부트스트랩, 오프폴리시 학습. 셋 중 둘만 있으면 불안정을 피할 수 있지만 셋이 모이면 가치 추정이 한없이 발산할 수 있다 — 제어뿐 아니라 예측에서도, 모델을 아는 동적 계획법에서도 일어나므로 잡음이나 탐색의 문제가 아니다. 타깃 네트워크, 리플레이 조율, 클리핑된 이중 $Q$, 오프라인 RL의 비관주의가 전부 어느 한 다리를 완화하는 장치다. → [[02-foundations/rl-basics|RL Basics §3.5]]
- **Dead zone (deadband)** · 데드존 — A range of small commands that produce no motion, common in hydraulic valves. It is one of the nonlinearities between a policy's output and actual joint motion. · 작은 명령이 아무 운동도 만들지 못하는 구간으로, 유압 밸브에 흔하다. 정책 출력과 실제 관절 운동 사이에 끼는 비선형성 중 하나다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §1]]
- **Deflection (arm deflection)** · 변형(처짐) — Elastic bending of an arm or structure under load, such as drilling thrust, which shifts the tool away from its commanded pose. · 드릴링 추력 같은 하중 아래 팔이나 구조가 탄성적으로 휘어 공구가 명령 자세에서 벗어나는 현상. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Deformable object** · 변형체 — An object such as rope, cloth, soil, a bag, or a flexible panel whose shape changes under load. Its state is high-dimensional and history-dependent, and grasp closure is not defined on a shape that changes. · 로프·천·흙·자루·휘는 패널처럼 하중에 따라 형상이 바뀌는 물체. 상태가 고차원이고 이력에 의존하며, 변하는 형상 위에는 파지 closure가 정의되지 않는다. → [[04-robotics/contact-force-tactile|Contact §7]], [[04-robotics/grasping|Grasping §6]]
- **Degradation problem** · 열화 문제 — Deeper plain networks reaching higher training error than shallower ones. Since training error worsens too, the cause is optimization, not overfitting; residual connections fixed it. · 평범하게 깊게 쌓은 신경망이 얕은 것보다 학습 오차가 더 높아지는 현상. 학습 오차부터 나빠지므로 원인은 과적합이 아니라 최적화이며, residual 연결이 이를 해결했다. → [[01-canonical-papers/notes/1-foundations/resnet|ResNet]]
- **Degrees of freedom (DoF)** · 자유도 — The minimum number of coordinates needed to specify a robot's configuration completely. A spatial 6R arm has 6; closed loops remove freedoms, so a planar four-bar linkage has only 1. · 로봇의 컨피규레이션을 완전히 지정하는 데 필요한 최소 좌표 수. 공간 6R 팔은 6이고, 닫힌 루프는 자유도를 줄여 평면 4절 링크는 1이다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **Delay margin** · 지연 여유 — The largest pure time delay a loop tolerates before instability, $\tau_{\max} = \varphi_m/\omega_{gc}$ (phase margin in radians over gain-crossover frequency). It is closely related to the phase margin for loops whose gain decays quickly, and it is the first-order way to read a perception-to-actuation latency budget as a bandwidth limit — not an exact bound, since the crossover itself moves. · 루프가 불안정해지기 전까지 견디는 최대 순수 시간 지연, $\tau_{\max} = \varphi_m/\omega_{gc}$(라디안 위상 여유를 이득 교차 주파수로 나눈 값). 이득이 빠르게 감쇠하는 루프에서는 위상 여유와 밀접하고, 인식-구동 지연 예산을 대역폭 한계로 읽는 1차 근사다. 교차 주파수 자체가 움직이므로 엄밀한 상한은 아니다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Delta manipulator (delta robot)** · 델타 매니퓰레이터 — A parallel mechanism whose three arms drive one light end platform, giving fast, precise translation. On a printing drone it self-aligns to cancel airframe motion at the nozzle. · 세 팔이 가벼운 말단 플랫폼 하나를 움직여 빠르고 정밀한 병진을 내는 병렬 기구. 프린팅 드론에서는 스스로 정렬해 노즐의 기체 흔들림을 상쇄한다. → [[01-canonical-papers/notes/8-construction/aerial-am-2022|Aerial AM 2022]]
- **DEM (discrete element method)** · 이산요소법(DEM) — A simulation that models a granular material such as soil grain by grain. It is accurate but slow, so it serves as the reference that real-time soil models are validated against. · 흙 같은 입상 재료를 알갱이 하나하나로 모델링하는 시뮬레이션. 정확하지만 느려서, 실시간 토질 모델을 검증하는 기준으로 쓰인다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §4]]
- **Demonstration (demo)** · 시연 — A recorded expert execution of a task as observation–action pairs, collected by teleoperation, VR, kinesthetic teaching or scripted policies. Diversity of scenes and objects usually matters more than the raw count. · 과제를 전문가가 수행한 기록으로, 관측–행동 쌍으로 이루어지며 원격조작·VR·직접 교시·스크립트 정책으로 모은다. 개수보다 장면·물체의 다양성이 대개 더 중요하다. → [[02-foundations/rl-basics|RL Basics §6]]
- **Denavit–Hartenberg parameters (D-H)** · D-H(Denavit-Hartenberg) — The classical convention that attaches a frame to every link and describes each joint by four numbers relative to the previous link's frame: link length, twist, offset, joint angle. Modern Robotics uses PoE instead. · 링크마다 프레임을 붙이고 각 관절을 이전 링크 프레임에 대한 네 숫자(링크 길이, 비틀림, 오프셋, 관절각)로 기술하는 고전 규약. Modern Robotics는 대신 PoE를 쓴다. → [[04-robotics/modern-robotics/ch04-forward-kinematics|MR ch.4]]
- **Denoising** · 노이즈 제거 — Predicting and removing the noise added to a corrupted sample. In diffusion the network is trained to output $\epsilon$ (the noise) rather than the clean data, and generation is repeated denoising from pure noise. · 오염된 샘플에 더해진 노이즈를 예측해 제거하는 것. 디퓨전에서 신경망은 깨끗한 데이터가 아니라 노이즈 $\epsilon$을 출력하도록 학습되고, 생성은 순수 노이즈에서 출발한 반복적 디노이징이다. → [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]]
- **Denoising score matching (DSM)** · denoising score matching — Learning the score of noisy data by training a network to undo added Gaussian noise. DDPM's noise-prediction loss is the same objective up to scaling, which links diffusion and score-based models. · 가우시안 노이즈를 더한 데이터에서 그 노이즈를 되돌리도록 학습해 score를 배우는 방법. DDPM의 노이즈 예측 손실은 스케일 차이를 빼면 같은 목적함수이며, 디퓨전과 score 기반 모델을 잇는다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]], [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]]
- **Dense prediction** · 밀집 예측 — Producing an output for every pixel rather than one label per image: segmentation, depth, or a diffusion model's noise estimate. It needs high-resolution and multi-scale features. · 이미지당 라벨 하나가 아니라 픽셀마다 출력을 내는 과제. 분할, 깊이, 디퓨전의 노이즈 예측이 여기에 속하며 고해상도·다중 스케일 특징이 필요하다. → [[01-canonical-papers/notes/2-computer-vision/u-net|U-Net]]
- **Dependency tags (depend, exec_depend)** · 의존 태그 — package.xml tags that say when a dependency is needed: depend for build and run, build_depend for build only, exec_depend for run only, test_depend for tests. Pure Python packages use exec_depend. · 의존성이 언제 필요한지 말하는 package.xml 태그. depend는 빌드와 실행, build_depend는 빌드만, exec_depend는 실행만, test_depend는 테스트용. 순수 Python 패키지는 exec_depend를 쓴다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §5]]
- **Dependent variable** · 종속 변수 — The measured outcome of an experiment, such as insertion success, peak force or recovery time. · 실험에서 측정하는 결과(삽입 성공, 최대 힘, 회복 시간 등). → [[06-research-practice/experimental-design-reproducibility|Experimental Design §1]]
- **Deployment ladder** · 배치 사다리 — Rungs of sim-to-real evidence: simulator-only with held-out parameters, hardware-in-the-loop, supervised real trials, adaptation with fixed evaluation cases, and repeated operation across materials, machines, sites, and days. · sim-to-real 증거의 단계: 미관측 파라미터의 시뮬레이터 평가, hardware-in-the-loop, 감독 실제 시험, 평가 사례를 고정한 적응, 재료·기계·현장·날짜를 바꾼 반복 운용. → [[05-construction-robotics/sim-to-real|Sim-to-Real §3]]
- **Depth camera (RGB-D, ToF, structured light)** · RGB-D / ToF / 구조광 — A sensor that measures depth $Z$ at every pixel, by time-of-flight or a projected light pattern. It has range limits and degrades in sunlight and on reflective or dark materials. · 픽셀마다 깊이 $Z$를 측정하는 센서로, 빛의 비행시간이나 투사한 빛 패턴을 쓴다. 거리 한계가 있고 햇빛과 반사·어두운 재질에서 성능이 떨어진다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2]]
- **Depth-first search (DFS)** · 깊이 우선 탐색 — Graph search that follows one branch as far as possible before backing up. Its discovery and finish times detect cycles and yield topological order and strongly connected components, in $O(V + E)$. · 한 가지를 끝까지 따라간 뒤 되돌아오는 그래프 탐색. 발견·종료 시각으로 사이클을 검출하고, 위상 순서와 강연결 요소를 $O(V + E)$에 얻는다. → [[02-foundations/algorithms/graph-algorithms|11.6 §2]]
- **Deque (double-ended queue)** · 덱 — A sequence with $O(1)$ insertion and removal at both ends, Python's collections.deque. Use it for BFS queues and sliding windows instead of list.pop(0), which is $O(n)$. · 양 끝에서 $O(1)$ 삽입·삭제가 되는 시퀀스(Python의 collections.deque). BFS 큐와 슬라이딩 윈도에는 $O(n)$인 list.pop(0) 대신 이것을 쓴다. → [[02-foundations/algorithms/data-structures|11.2 §2]]
- **Descriptor** · 기술자 — A compact fingerprint of a keypoint's neighbourhood, such as SIFT's 128 floats or ORB's 256 bits, that lets the same point be matched across images by vector distance. · 키포인트 주변을 요약한 짧은 지문(SIFT는 실수 128개, ORB는 비트 256개)으로, 벡터 거리로 이미지 사이에서 같은 점을 짝짓게 한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Deskewing** · deskewing — Correcting a lidar scan for the fact that the robot moved during the sweep. A paper that skips it on a fast platform is reporting a map built from distorted scans. · 라이다가 훑는 동안 로봇이 움직였다는 사실을 스캔에서 보정하는 일. 빠른 플랫폼에서 이를 빠뜨린 논문은 왜곡된 스캔으로 만든 지도를 보고하는 것이다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Desk reject** · 데스크 리젝 — Rejection by the editor before any reviewer sees the paper, for scope or rule violations. Submitting to a double-anonymized journal such as CACAIE without anonymizing risks one. · 심사자에게 보내기 전에 편집자가 범위나 규칙 위반을 이유로 거절하는 것. CACAIE처럼 양측 익명인 저널에 익명화하지 않고 내면 이를 각오해야 한다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Detectability** · 검출 가능성(detectability) — Every mode that does not decay on its own shows up in the measurement, or for LQR in the cost. Otherwise an optimal controller can ignore a quietly diverging mode and fail to stabilize. · 스스로 감쇠하지 않는 모든 모드가 측정량에, LQR에서는 비용에 나타난다는 조건. 이것이 없으면 최적 제어기가 조용히 발산하는 모드를 무시해 안정화에 실패할 수 있다. → [[04-robotics/lqr-lqg|LQR/LQG §2]]
- **Determinant $\det A$** · 행렬식 — The factor by which a linear map scales volume. $\det A = 0$ means it flattens space, so information is lost and no inverse exists; $\det R = +1$ is what makes a rotation a rotation rather than a reflection. · 선형 사상이 부피를 몇 배로 만드는가. $\det A = 0$이면 공간을 납작하게 뭉개 정보가 사라지므로 역행렬이 없고, $\det R = +1$이라는 조건이 회전을 반사가 아닌 회전으로 만든다. → [[02-foundations/engineering-math|0.5 §10]]
- **Dexterous hand (multi-fingered hand)** · 다지 손 — A hand with several fingers and many actuated DoF: 16–20 for anthropomorphic hands, far fewer for common underactuated three-fingered hands. Grasp synthesis becomes search in a high-dimensional space, reduced by grasp taxonomies or eigengrasps. · 손가락이 여럿이고 구동 자유도가 많은 손. 인간형 손은 16~20개, 흔한 부족구동 세 손가락 손은 훨씬 적다. 파지 합성이 고차원 탐색이 되어 파지 분류 체계나 eigengrasp로 축소한다. → [[04-robotics/grasping|Grasping §5]]
- **Differentiable rendering** · 미분 가능한 렌더링 — Rendering whose output pixels have gradients with respect to scene parameters, so a photo-matching loss can optimize the 3D scene directly. NeRF and 3DGS both rely on it. · 출력 픽셀이 장면 파라미터에 대해 그래디언트를 갖는 렌더링. 사진과의 오차 손실로 3D 장면을 직접 최적화할 수 있으며, NeRF와 3DGS 모두 이것에 기댄다. → [[01-canonical-papers/notes/2-computer-vision/nerf|NeRF]]
- **Differential drive** · 차동 구동 — A mobile base steered by spinning two wheels at different speeds: equal speeds drive straight, opposite speeds turn in place. Its kinematics reduce to the unicycle model. · 두 바퀴를 서로 다른 속도로 돌려 조향하는 모바일 베이스. 같은 속도면 직진, 반대 속도면 제자리 회전이다. 기구학은 외바퀴 모델로 귀결된다. → [[04-robotics/modern-robotics/ch13-wheeled-mobile-robots|MR ch.13]]
- **Differential dynamic programming (DDP)** · DDP — A second-order trajectory optimizer that alternates an LQR-like backward pass around the current trajectory with a forward rollout. A standard nonlinear MPC solver, with local optima and sensitivity to initialization. · 현재 궤적 주위의 LQR 같은 역방향 패스와 순방향 롤아웃을 번갈아 도는 2차 궤적 최적화기. 비선형 MPC의 표준 솔버이며 국소 최적과 초기화 민감성이 따른다. → [[04-robotics/mpc|MPC §4]]
- **Differential entropy** · 미분 엔트로피 — The continuous-variable analogue of entropy, computed from a density rather than probabilities, so unlike discrete entropy it can be negative. For a fixed mean and variance the Gaussian maximizes it. · 엔트로피를 연속 변수로 옮긴 것으로, 확률 대신 밀도로 계산해 이산 엔트로피와 달리 음수가 될 수 있다. 평균과 분산이 고정되면 가우시안이 이를 최대로 만든다. → [[02-foundations/probability|Probability §3]]
- **Differential flatness (flat output)** · 미분 평탄성(differential flatness) — A system is flat when some outputs $z$, as many as the inputs, determine every state and input through finitely many derivatives. Any smooth $z(t)$ then yields a feasible trajectory, so planning becomes curve fitting. · 입력 개수만큼의 출력 $z$와 그 유한 차수 도함수로 모든 상태와 입력이 정해지면 시스템은 평탄하다. 그러면 매끄러운 $z(t)$ 하나가 동역학적으로 실행 가능한 궤적을 주어, 계획이 곡선 맞추기가 된다. → [[04-robotics/planning-decision-making|Planning §5.5]]
- **Diffusion model** · 디퓨전 모델 — A generative model that learns to reverse a fixed noising process. · 데이터에 노이즈를 점진적으로 섞는 고정 과정을 학습으로 되돌려 생성하는 모델. → [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]]
- **Diffusion Policy** · Diffusion Policy(디퓨전 정책) — A visuomotor policy that generates a chunk of future actions by denoising noise conditioned on recent observations, executes the first few, and replans. It commits to one demonstrated mode instead of averaging modes. · 최근 관측을 조건으로 노이즈를 제거해 미래 행동 청크를 생성하고, 앞부분만 실행한 뒤 재계획하는 시각-운동 정책. 여러 시연 모드를 평균하지 않고 하나를 골라 따른다. → [[01-canonical-papers/notes/4-vla/diffusion-policy|Diffusion Policy]]
- **Digital model** · 디지털 모델 — The lowest level called a digital twin: static BIM or CAD geometry with no live state or synchronization with the physical site. · 디지털 트윈이라 불리는 가장 낮은 수준으로, 실시간 상태나 현장 동기화가 없는 정적 BIM·CAD 형상. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §2]]
- **Digital shadow** · 디지털 섀도 — A model that physical data keeps updating, but that commands nothing back. A live sensor dashboard is typically a shadow, not a closed-loop twin. · 물리 데이터가 계속 갱신하지만 물리계에 아무것도 지시하지 않는 모델. 실시간 센서 대시보드는 대개 폐루프 트윈이 아니라 섀도다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §2]]
- **Digital twin** · 디지털 트윈 — A *maintained* operational state linked to a physical system, as opposed to a static BIM/CAD model. Only a bidirectional (closed) loop — observation updates the model, the model changes the next action — earns the name for robotics. · 정적 BIM이 아니라 물리 시스템과 연결되어 유지되는 운용 상태; 양방향 폐루프여야 로보틱스에서 이 이름값을 한다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins]]
- **Dijkstra's algorithm** · Dijkstra 알고리즘 — Single-source shortest paths for non-negative weights: repeatedly finalize the unfinished vertex with the smallest tentative distance. With a binary heap it costs $O((V + E)\log V)$; one negative edge can break it. · 음수가 아닌 가중치에서의 단일 출발 최단 경로: 잠정 거리가 가장 작은 미확정 정점을 반복해서 확정한다. 이진 힙으로 $O((V + E)\log V)$이며, 음수 간선 하나로 깨질 수 있다. → [[02-foundations/algorithms/graph-algorithms|11.6 §4]]
- **Directed acyclic graph (DAG)** · DAG(방향 비순환 그래프) — A directed graph with no cycle. Exactly these graphs have a topological order, and shortest or longest paths in them take $O(V + E)$ even with negative weights. · 사이클이 없는 방향 그래프. 위상 순서가 존재하는 것은 정확히 이런 그래프이고, 음수 가중치가 있어도 최단·최장 경로를 $O(V + E)$에 구한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §3]]
- **Direct shooting** · direct shooting — Trajectory optimization over controls only, with states simulated forward from the initial state. Variables are few and dynamics always hold, but long horizons are badly conditioned because early inputs move everything after them. · 제어만 최적화하고 상태는 초기 상태에서 앞으로 시뮬레이션해 얻는 궤적 최적화. 변수가 적고 동역학이 늘 성립하지만, 앞쪽 입력 하나가 뒤 궤적 전체를 움직여 지평이 길면 조건수가 나빠진다. → [[04-robotics/planning-decision-making|Planning §6]]
- **Direct transcription** · direct transcription — Trajectory optimization with both states and controls as decision variables and the dynamics written as equality constraints. More variables, but each constraint touches only neighbouring steps and the solver can start from an infeasible guess. · 상태와 제어를 모두 결정 변수로 두고 동역학을 등식 제약으로 쓰는 궤적 최적화. 변수는 많지만 각 제약이 이웃한 스텝만 건드리고, 실행 불가능한 초기 추정에서도 출발할 수 있다. → [[04-robotics/planning-decision-making|Planning §6]]
- **Discount factor ($\gamma$)** · 할인율 — The number $\gamma \in [0,1]$ that weights a reward $k$ steps ahead by $\gamma^k$. It keeps infinite sums finite and encodes impatience; $\gamma = 1$ is admissible when episodes terminate. · $k$스텝 뒤의 보상에 $\gamma^k$ 가중치를 주는 수 $\gamma \in [0,1]$. 무한 합을 유한하게 하고 조급함을 인코딩하며, 에피소드가 종료하면 $\gamma = 1$도 허용된다. → [[02-foundations/rl-basics|RL Basics §1]]
- **Discovery** · discovery(탐색) — How nodes find each other with no central master: they advertise themselves on the network, answer each other's advertisements, and announce when they leave. Default DDS discovery uses multicast. · 중앙 마스터 없이 노드들이 서로를 찾는 방식. 네트워크에 자신을 알리고, 서로의 알림에 응답하고, 떠날 때 알린다. 기본 DDS discovery는 멀티캐스트를 쓴다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §4]]
- **Discrete Fourier transform (DFT, FFT)** · DFT/FFT — $X[k] = \sum_n x[n]e^{-j2\pi kn/N}$ measures how much of each frequency a sampled signal contains, one dot product per frequency. The FFT algorithm computes all $N$ values in $O(N\log N)$. · 샘플된 신호에 각 주파수가 얼마나 들어 있는지를 주파수마다 내적 하나로 재는 변환. FFT 알고리즘은 $N$개 값 전부를 $O(N\log N)$에 계산한다. → [[02-foundations/signal-processing|Signal Processing §3]]
- **Discussion section** · Discussion — The part of a paper that interprets results: causes, mechanisms, limitations and transfer. Results report measured outcomes; a mechanistic explanation belongs in discussion with its diagnostic evidence and alternatives. · 결과를 해석하는 논문의 부분: 원인, 기전, 한계, 이전 가능성. Results는 측정된 결과를 보고하고, 기전적 설명은 진단 증거와 대안 설명과 함께 Discussion에 둔다. → [[06-research-practice/scientific-writing-peer-review|Scientific Writing §5]]
- **Dissertation (thesis)** · 학위논문 — The single extended argument a PhD is judged on. Three independent specializations make three dissertations; this program's is one manipulation problem that needs navigation and HRI as conditions. · 박사 학위를 판정받는 하나의 긴 논증. 독립된 전문 분야 셋은 학위논문 셋이 되므로, 이 프로그램의 학위논문은 내비게이션과 HRI를 조건으로 필요로 하는 하나의 매니퓰레이션 문제다. → [[07-research-program/index|Research Program §2]]
- **Distribution (distro, LTS)** · 배포판 — A versioned, named release of ROS 2 such as Jazzy Jalisco, tied to an Ubuntu version and a support window; LTS releases are supported for five years. · Jazzy Jalisco처럼 이름 붙은 ROS 2 버전 릴리스로, 특정 Ubuntu 버전과 지원 기간에 묶인다. LTS 릴리스는 5년간 지원된다. → [[04-robotics/ros2/index|25. ROS 2 §3]]
- **Distribution shift** · 분포 이동 — Test data drawn from a different distribution than training data, such as a new site, robot or lighting. It is the normal condition in robotics, which is why papers report seen/unseen splits. · 학습과 다른 분포에서 온 test 데이터(새 현장, 새 로봇, 새 조명). 로보틱스의 일상적 조건이며, 논문들이 seen/unseen 분할을 보고하는 이유다. → [[02-foundations/ml-practice|ML Practice §1]]
- **Disturbance** · 외란 — An unknown input that pushes a system away from what the command intended, such as a load or an open window. Reducing its effect without having to measure it is the main thing feedback buys. · 부하나 열린 창문처럼 명령이 의도한 바에서 시스템을 밀어내는 알 수 없는 입력. 그것을 측정하지 않고도 영향을 줄이는 것이 피드백이 사는 핵심이다. → [[04-robotics/control-theory-ce397|Control Theory §1]]
- **DiT (Diffusion Transformer)** · DiT — Diffusion with the U-Net replaced by a Transformer over latent patches; the architecture inside π0 and GR00T action heads. · 디퓨전의 U-Net을 잠재 패치 위의 Transformer로 교체한 백본. → [[01-canonical-papers/notes/6-diffusion/dit|DiT]]
- **Divide and conquer** · 분할 정복 — Split the input into smaller instances of the same problem, solve them recursively, and combine their answers. The cost is a recurrence; if the pieces overlap, dynamic programming is the better tool. · 입력을 같은 문제의 더 작은 사례로 나눠 재귀로 푼 뒤 답을 합치는 설계법. 비용은 점화식으로 나오며, 조각이 겹치면 동적 계획법이 더 맞는 도구다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §1]]
- **DOI (digital object identifier)** · DOI — A permanent identifier assigned to a published document so it can always be resolved. Having one can make a workshop paper count as an archival publication, as ICRA's rule states. · 출판된 문서에 붙는 영구 식별자로, 언제든 원문을 찾아가게 해 준다. ICRA 규칙처럼 DOI가 있으면 워크숍 논문도 archival 출판으로 취급될 수 있다. → [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **Domain randomization** · 도메인 무작위화 — Sampling simulator parameters such as mass, friction, motor gains, latency, and terrain over ranges during training, so the policy must be robust across them. The ranges are part of the result. · 학습 중 질량·마찰·모터 이득·지연·지형 같은 시뮬레이터 파라미터를 범위 안에서 표본으로 뽑아, 정책이 그 전체에 걸쳐 강건하도록 강제하는 것. 무작위화 범위가 결과의 일부다. → [[04-robotics/legged-locomotion|Legged Locomotion §2]]
- **Double-anonymous review (double-blind review)** · 양측 익명 심사(이중 블라인드) — Review in which neither reviewers nor authors know each other's identity. IEEE RAS journals switched in January 2025 and ICRA and IROS in 2026, so authors cite their own earlier work in the third person. · 심사자와 저자가 서로의 신원을 모르는 심사. IEEE RAS 저널은 2025년 1월, ICRA·IROS는 2026년판부터 바꿨으므로 자기 이전 연구도 3인칭으로 인용한다. → [[06-research-practice/venue-strategy|Venue Strategy §2]], [[06-research-practice/venue-strategy|Venue Strategy §5]]
- **Double integrator** · 이중 적분기 — The simplest mechanical plant: a force accelerates a mass, so position is the input integrated twice ($\ddot p = u$). The standard hand-solvable LQR example. · 가장 단순한 기계 플랜트: 힘이 질량을 가속하므로 위치는 입력을 두 번 적분한 것이다($\ddot p = u$). 손으로 풀리는 표준 LQR 예제다. → [[04-robotics/lqr-lqg|LQR/LQG §3]]
- **DPO (Direct Preference Optimization)** · DPO — Preference learning without an explicit reward model or RL loop: the Bradley–Terry loss is rewritten in log-probability ratios between the trained policy and a frozen reference policy. · 명시적 보상 모델이나 RL 루프 없이 하는 선호 학습: Bradley–Terry 손실을 학습 중인 정책과 고정된 기준 정책의 로그 확률 비로 다시 써서 직접 학습한다. → [[02-foundations/rl-basics|RL Basics §11]]
- **DQN (Deep Q-Network)** · DQN — Q-learning with a neural-network Q-function, stabilized by a replay buffer and a target network. Value-based, so it suits discrete actions better than continuous ones. · 신경망 Q 함수를 쓰는 Q-learning으로, 리플레이 버퍼와 타깃 네트워크로 안정화한다. 가치 기반이라 연속 행동보다 이산 행동에 맞는다. → [[02-foundations/rl-basics|RL Basics §3]]
- **Drake** · Drake — An open-source (BSD-3) robotics toolbox led by Toyota Research Institute, notable for hydroelastic contact. It has no peer-reviewed paper, so cite the software citation it provides. · Toyota Research Institute가 주도하는 오픈소스(BSD-3) 로보틱스 도구로, 하이드로일래스틱 접촉이 특징이다. 심사받은 논문이 없으므로 스스로 제시하는 소프트웨어 인용을 쓴다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §2]], [[06-research-practice/simulators-benchmarks-datasets|Simulators §3]]
- **Drift** · drift(드리프트) — Slowly accumulating error in an estimate integrated from relative motion (odometry, IMU bias), or the wandering baseline of a sensor such as a taxel. Only absolute references or loop closure remove estimate drift. · 상대 운동을 적분한 추정값에 천천히 쌓이는 오차(오도메트리, IMU bias), 또는 taxel 같은 센서의 기준값이 떠도는 현상. 추정 drift는 절대 기준이나 loop closure만 없앤다. → [[04-robotics/state-estimation-slam|State estimation §7]], [[04-robotics/tactile-visuotactile|Tactile §2]]
- **Driving-point impedance** · 구동점 임피던스 — The force-versus-velocity relation the environment feels when it pushes on the robot at the contact point. Colgate and Hogan showed a linear manipulator is stable with every passive environment exactly when this is passive. · 환경이 접촉점에서 로봇을 밀 때 느끼는 힘 대 속도 관계. Colgate와 Hogan은 선형 매니퓰레이터가 모든 수동 환경과 결합해 안정한 것이 정확히 이것이 수동적일 때임을 보였다. → [[04-robotics/force-compliance-control|Force & Compliance Control §5]]
- **Dropout** · dropout(드롭아웃) — A regularizer that randomly zeroes a fraction of activations at each training step, so the network cannot rely on any single unit; it is switched off at inference. Popularized by AlexNet. · 학습 스텝마다 활성값 일부를 무작위로 0으로 만들어 신경망이 특정 유닛에 기대지 못하게 하는 정규화 기법이며, 추론 때는 끈다. AlexNet이 널리 알렸다. → [[02-foundations/ml-practice|ML Practice §2]]
- **Dry-stone wall** · 돌담 — A wall built from irregular stones stacked without mortar, so each stone's placement must be planned for stability. ETH's HEAP excavator built a 6 m one autonomously. · 모르타르 없이 불규칙한 돌을 쌓아 만든 벽으로, 돌마다 안정성을 고려해 배치를 계획해야 한다. ETH의 HEAP 굴착기가 6 m 높이 벽을 자율로 쌓았다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §4]]
- **Drywall (hanging, finishing)** · 드라이월(석고보드) — Large gypsum-board sheets fastened to a wall or ceiling frame. Hanging places and fastens sheets; finishing sands and scrapes joints, where removal depth is sub-millimetre. · 벽·천장 틀에 고정하는 큰 석고보드 시트. 시공은 시트를 대고 체결하는 일이고, 마감은 이음부를 샌딩·긁기하는 일로 제거 깊이가 밀리미터 이하다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Duality** · 쌍대성 — Minimizing the Lagrangian over the variables gives a dual function that lower-bounds the optimum (weak duality); for convex problems with a constraint qualification the bound is tight (strong duality). · 라그랑지안을 변수에 대해 최소화한 쌍대 함수는 최적값의 하한이고(약쌍대성), 제약 자격 조건을 갖춘 볼록 문제에서는 그 하한이 정확하다(강쌍대성). → [[02-foundations/optimization|Optimization §4]]
- **Dual-system architecture (System 1 / System 2)** · 이중 시스템 설계 — A robot model split by speed: a slow VLM (System 2) decides what to do; a fast action module (System 1) produces continuous motion. GR00T N1 runs them at about 10 Hz and 120 Hz. · 속도로 나눈 로봇 모델: 느린 VLM(System 2)이 무엇을 할지 정하고, 빠른 행동 모듈(System 1)이 그것을 연속 동작으로 바꾼다. GR00T N1은 각각 약 10 Hz, 최대 120 Hz로 돌린다. → [[01-canonical-papers/notes/4-vla/gr00t-n1|GR00T N1]]
- **Dubins and Reeds–Shepp paths** · Dubins·Reeds–Shepp 경로 — Closed-form shortest paths between two poses for a car with a minimum turning radius, built from full-lock arcs and straights; Dubins drives forward only, Reeds–Shepp may reverse. They ignore obstacles and serve as steering functions. · 최소 회전 반경이 있는 차가 두 pose 사이를 가는 닫힌 형태 최단 경로로, 최대 조향 호와 직진 구간으로 이뤄진다. Dubins는 전진만, Reeds–Shepp은 후진도 허용한다. 장애물을 무시하며 조향 함수로 쓰인다. → [[04-robotics/planning-decision-making|Planning §5.5]]
- **Durability (volatile, transient local)** · durability(내구성) — The QoS policy deciding whether a publisher keeps samples for subscribers that join late (transient local) or not (volatile). Late joiners get old messages only if both sides use transient local. · 늦게 들어온 구독자를 위해 퍼블리셔가 샘플을 보관할지(transient local), 말지(volatile) 정하는 QoS 정책. 양쪽이 모두 transient local이어야 늦게 온 쪽이 지난 메시지를 받는다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §3]]
- **Dynamic array** · 동적 배열 — A resizable array (Python list, C++ std::vector) that keeps spare capacity and, when full, copies into a block larger by a constant factor. That geometric growth makes append $O(1)$ amortized. · 여유 용량을 두고, 가득 차면 상수배 더 큰 블록으로 복사하는 크기 가변 배열(Python list, C++ std::vector). 이 기하급수적 증가 덕에 append가 분할상환 $O(1)$이다. → [[02-foundations/algorithms/complexity-recursion|11.1 §4]]
- **Dynamic consistency** · 동역학적 일관성(dynamic consistency) — A null-space projector is dynamically consistent when secondary torques cause no task acceleration even while the arm moves, not only at rest. Among projectors of that form, only the inertia-weighted Jacobian inverse achieves it. · 부차 토크가 정지 상태뿐 아니라 팔이 움직이는 동안에도 작업 가속을 만들지 않으면 영공간 투영자가 동역학적으로 일관되다고 한다. 그 형태의 투영자 중 관성 가중 야코비안 역만 이를 만족한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §4]]
- **Dynamic programming (DP)** · 동적 계획법(DP) — Solving a problem by defining subproblems, relating each to smaller ones by a recurrence, and computing each once in a valid order. It needs optimal substructure and pays off when subproblems overlap. · 부분 문제를 정의하고 점화식으로 더 작은 것과 관계 지은 뒤, 올바른 순서로 각각 한 번씩 계산해 푸는 방법. 최적 부분 구조가 필요하고 부분 문제가 겹칠 때 이득이 크다. → [[02-foundations/algorithms/dynamic-programming|11.5 §1]]
- **Dynamic resolution (native resolution)** · 동적 해상도 — Encoding an image at its own size into however many visual tokens it needs, instead of resizing every image to a fixed square. It preserves detail for documents and OCR. · 모든 이미지를 고정 정사각형으로 리사이즈하지 않고, 원래 크기대로 필요한 만큼의 시각 토큰으로 인코딩하는 방식. 문서·OCR에서 세부를 보존한다. → [[01-canonical-papers/notes/3-vlm/qwen-vl|Qwen-VL]]
- **Dynamic time warping (DTW)** · 동적 시간 와핑(DTW) — A DP that aligns two time series by the monotone matching with least total distance, so demonstrations that pause or rush are compared by shape, not timing. It is not a metric. · 두 시계열을 국소 거리 합이 최소인 단조 대응으로 정렬하는 DP로, 멈추거나 서두른 시연을 타이밍이 아니라 모양으로 비교하게 한다. 거리 함수(metric)는 아니다. → [[02-foundations/algorithms/dynamic-programming|11.5 §7]]
- **Early stopping** · early stopping(조기 종료) — Stopping training, or keeping the checkpoint, where validation loss is lowest, before it turns back up as the model starts memorizing. · 모델이 암기하기 시작해 validation 손실이 다시 오르기 전, 그 손실이 가장 낮은 지점에서 학습을 멈추거나 그 체크포인트를 쓰는 것. → [[02-foundations/ml-practice|ML Practice §2]]
- **Earthmoving** · 토공 — Excavating, loading, hauling, and placing soil or rock with heavy machines such as excavators and wheel loaders; the richest stream of construction physical AI. · 굴착기·휠로더 같은 중장비로 흙이나 암석을 파고, 싣고, 나르고, 까는 일. 건설 physical AI에서 가장 풍부한 연구 스트림이다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §1]]
- **EDA (electrodermal activity)** · EDA(피부 전기 활동) — Changes in skin conductance driven by sweat-gland activity, measured with wearables as a correlate of stress or arousal in worker sensing. · 땀샘 활동에 따른 피부 전도도 변화로, 작업자 센싱에서 스트레스·각성의 상관 지표로 웨어러블로 측정한다. → [[05-construction-robotics/hrc-worker-centered|HRC §1]]
- **Edit distance (Levenshtein distance)** · 편집 거리 — The minimum number of single-character insertions, deletions and substitutions that turn one string into another, computed by an $O(nm)$ DP table over prefixes. · 한 문자열을 다른 문자열로 바꾸는 단일 문자 삽입·삭제·치환의 최소 횟수로, 접두사에 대한 $O(nm)$ DP 표로 계산한다. → [[02-foundations/algorithms/dynamic-programming|11.5 §4]]
- **EEG (electroencephalography)** · EEG(뇌파) — Electrical brain activity recorded from scalp electrodes. It is used to estimate worker states such as stress or attention and to decode BCI commands. · 두피 전극으로 기록하는 뇌의 전기 활동. 스트레스·주의 같은 작업자 상태 추정과 BCI 명령 해독에 쓰인다. → [[05-construction-robotics/hrc-worker-centered|HRC §1]]
- **Effective horizon** · 유효 지평 — Roughly how many steps ahead a discounted return actually looks, $1/(1-\gamma)$; $\gamma = 0.99$ gives about 100 steps. · 할인된 리턴이 실제로 내다보는 대략의 스텝 수 $1/(1-\gamma)$. $\gamma = 0.99$면 약 100스텝이다. → [[02-foundations/rl-basics|RL Basics §1]]
- **Effective sample size (ESS)** · 유효 표본 크기(ESS) — How many independent samples a correlated MCMC run is worth: $N$ samples count as about $N/\tau$, where $\tau$ grows with autocorrelation. Tune a sampler by ESS, not by acceptance rate. · 상관된 MCMC 표본이 독립 표본 몇 개의 가치인가. $N$개는 약 $N/\tau$개로 치며, $\tau$는 자기상관이 클수록 커진다. 샘플러는 수락률이 아니라 ESS로 조정한다. → [[02-foundations/probability|Probability §7]]
- **Effort–flow pair** · effort–flow 쌍 — Network theory's description of any power exchange as two variables whose product is power: force and velocity in mechanics, voltage and current in circuits. Only one can be imposed independently at a port. · 모든 일률 교환을 곱이 일률이 되는 두 변수로 기술하는 네트워크 이론의 틀. 역학에서는 힘과 속도, 회로에서는 전압과 전류이며, 한 포트에서 독립적으로 부과할 수 있는 것은 하나뿐이다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §1]]
- **Egocentric perception** · 자기중심 인지 — Perception from a head-worn camera: the wearer's body leaves view, hands and objects fill the frame, and camera motion becomes an attention signal. A different observability regime, not a worse camera angle. · 머리에 단 카메라로 하는 인지: 착용자의 몸은 화면에서 사라지고 손과 물체가 프레임을 채우며, 카메라 움직임이 주의의 신호가 된다. 나쁜 각도가 아니라 다른 관측 가능성 체제다. → [[04-robotics/egocentric-perception|Egocentric §1]]
- **Eigengrasp** · eigengrasp — One of a handful of principal hand-posture directions found by principal component analysis of recorded human grasps; their combinations span most postures a hand actually uses, giving a low-dimensional space for grasp synthesis. · 기록된 사람 파지 자세에 주성분 분석을 적용해 얻은 몇 개의 주요 손 자세 방향 중 하나. 그 조합이 손이 실제로 쓰는 자세 대부분을 덮어 파지 합성을 위한 저차원 공간을 준다. → [[04-robotics/grasping|Grasping §5]]
- **Eigenvalue / eigenvector** · 고유값/고유벡터 — Directions a matrix only stretches: $Av = \lambda v$. They govern long-run behavior ($A^k$), optimization difficulty (the condition number), and stability (poles). · 사상이 늘이기만 하는 방향; 장기 거동·최적화 난이도·안정성을 지배한다. → [[02-foundations/linear-algebra|Linear Algebra §3]]
- **ELBO (evidence lower bound)** · ELBO — $E_q[\log p(x|z)] - D_{KL}(q\|p)$: the training objective of VAEs, diffusion, and latent world models. · VAE·디퓨전·월드모델 학습의 목적함수. → [[02-foundations/information-theory|정보이론 §5]]
- **Elevation map (2.5D map)** · 고도 지도 — A robot-centric grid storing one terrain height per cell, with a per-cell variance that carries pose drift and sensor uncertainty. Most learned traversability methods run on top of one. · 셀마다 지형 높이 하나를 담는 로봇 중심 격자로, 자세 드리프트와 센서 불확실성을 셀별 분산으로 전파한다. 대부분의 학습 traversability 방법이 그 위에서 돈다. → [[04-robotics/traversability-off-road|Traversability §3]]
- **EM algorithm (expectation–maximization)** · EM(기댓값 최대화) — Fits a model with hidden variables by alternating two steps: infer expected hidden quantities under the current parameters, then re-estimate the parameters from them. Repeating never lowers the likelihood. · 은닉 변수가 있는 모델을 두 단계를 번갈아 맞추는 방법: 현재 파라미터로 은닉 양의 기댓값을 추론하고 그것으로 파라미터를 다시 추정한다. 반복해도 우도가 줄지 않는다. → [[02-foundations/probability|Probability §7]]
- **EMA of weights** · 가중치 EMA — Keeping a slowly moving exponential average of the parameters and evaluating that average instead of the live weights. Near-universal in diffusion papers, often visible only in code; distinct from an EMA teacher. · 파라미터의 느리게 움직이는 지수 이동 평균을 유지하고 살아 있는 가중치 대신 그것을 평가하는 것. 디퓨전 논문에서 거의 보편적이고 흔히 코드에서만 드러나며, EMA teacher와는 다르다. → [[02-foundations/ml-practice|ML Practice §6]]
- **EMA teacher** · EMA 교사 — A teacher built as an exponential moving average of the student's weights; the stabilizing device in self-supervision (DINO) and RL target networks. · 학생 가중치의 지수이동평균으로 만든 교사. 자기지도(DINO)와 RL 타깃 네트워크에서 학습을 안정시키는 장치다. → [[01-canonical-papers/notes/2-computer-vision/dino|DINO]]
- **Embankment** · 제방 — A raised, shaped mound of compacted earth, such as for a road or levee. Building one is a standard earthmoving task used to evaluate autonomous excavators. · 도로나 둑처럼 흙을 다져 쌓아 올린 형상 구조물. 자율 굴착기 평가에 쓰이는 대표 토공 과제다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §4]]
- **Embedding** · 임베딩 — A representation mapping discrete inputs such as tokens or image patches into a continuous vector space. · 이산 입력을 연속 벡터 공간으로 옮긴 표현.
- **Embodied question answering (EQA)** · 체화 질의응답 — A navigation task in which the agent must move through an environment to gather the views it needs to answer a question about that environment. · 환경에 대한 질문에 답하려고 에이전트가 직접 이동하며 필요한 관측을 모아야 하는 내비게이션 과제. → [[01-canonical-papers/notes/9-navigation/uni-navid|Uni-NaVid]]
- **Embodiment** · embodiment — The physical body a policy acts through: morphology, actuators and transmission, sensing, compliance, payload, limits, and coupling to the environment. It decides which actions are meaningful. · 정책이 행동하는 물리적 몸. 형태, 액추에이터와 전동 장치, 센싱, 컴플라이언스, 페이로드, 한계, 환경 결합을 포함하며, 어떤 행동이 의미 있는지를 결정한다. → [[04-robotics/robot-systems-deployment|Robot Systems §2]]
- **Embodiment gap** · embodiment 격차 — The mismatch between the body that produced a demonstration and the body that must reproduce it — different camera placement, reachable workspace, kinematics, or dynamics. It is the price paid by demonstration methods that collect data without the robot present. · 시연을 만든 몸과 그것을 재현해야 하는 몸의 불일치 — 카메라 배치, 도달 가능 작업 공간, 기구학, 동역학의 차이. 로봇 없이 데이터를 모으는 방법이 치르는 대가다. → [[04-robotics/teleoperation-demonstration|12. §4]]
- **Emergency stop (E-stop)** · 비상 정지 — A dedicated, independent means of urgently halting hazardous motion. Distinct from a safe stop, which is a controlled transition intended to reduce risk. · 위험한 운동을 급히 멈추기 위한 전용의 독립 수단. 위험을 낮추도록 의도된 제어된 전이인 안전 정지와는 구분된다. → [[04-robotics/robot-systems-deployment|Robot Systems §7]], [[04-robotics/hri-safety|HRI & Safety §6]]
- **Emergent (ability)** · emergent(창발) — Paper usage for a behaviour absent at small scale and present at large scale. There is no agreed definition, so read how the evaluation measured it. · 작은 규모에서는 없다가 큰 규모에서 나타나는 거동을 가리키는 논문 용법. 합의된 정의가 없으므로 평가가 그것을 어떻게 쟀는지 읽어야 한다. → [[01-canonical-papers/how-to-read|How to Read §2]]
- **EMG (electromyography)** · EMG(근전도) — Electrical activity of muscles recorded with surface electrodes, one of the body signals used to sense worker effort or fatigue. · 표면 전극으로 기록하는 근육의 전기 활동으로, 작업자의 힘 사용이나 피로를 센싱하는 신체 신호 중 하나다. → [[05-construction-robotics/hrc-worker-centered|HRC §1]]
- **Encoder-decoder** · 인코더-디코더 — A two-stage architecture: the encoder compresses the whole input into a representation, and the decoder emits the output one step at a time while reading that representation. It is the shape behind seq2seq, the original Transformer, U-Net and DETR, and naming it tells you what a variant dropped — an encoder-only model has no generation stage, a decoder-only one has no separate comprehension stage. · 입력 전체를 하나의 표현으로 압축하는 인코더와, 그 표현을 읽으며 출력을 한 번에 하나씩 내는 디코더로 이루어진 구조다. seq2seq, 초기 트랜스포머, U-Net, DETR이 모두 이 모양이다. 이름을 알면 변형이 무엇을 버렸는지 보인다 — 인코더만 있는 모델은 생성 단계가 없고, 디코더만 있는 모델은 별도의 이해 단계가 없다. → [[01-canonical-papers/notes/1-foundations/seq2seq|Seq2Seq]]
- **End-effector** · 말단 — The tool or gripper at the end of a robot arm, and the frame attached to it. Forward kinematics computes its pose, and contact with the world happens there. · 로봇 팔 끝의 공구나 그리퍼, 그리고 거기 붙인 프레임. 순기구학이 그 자세를 계산하고, 세계와의 접촉이 거기서 일어난다. → [[04-robotics/modern-robotics/ch04-forward-kinematics|MR ch.4]]
- **End-effector delta action** · 말단 델타 행동 — An action expressed as a small change in gripper position and orientation rather than joint commands. OXE coarsely aligns robots this way, which hides real kinematic differences between them. · 관절 명령 대신 그리퍼 위치·자세의 작은 변화로 표현한 행동. OXE는 이 방식으로 로봇들을 거칠게 정렬하며, 그 때문에 실제 기구학 차이가 가려진다. → [[01-canonical-papers/notes/4-vla/open-x-embodiment|Open X-Embodiment]]
- **End-to-end** · end-to-end — Trainable components jointly optimized under one task objective, typically from raw input to output. It does not mean nothing is frozen; frozen modules can sit inside the pipeline. · 학습 가능한 구성요소들을 하나의 과제 목적함수로 공동 최적화하는 것으로, 보통 원시 입력에서 출력까지를 가리킨다. 아무것도 얼리지 않았다는 뜻은 아니며 frozen 모듈이 안에 있을 수 있다. → [[01-canonical-papers/how-to-read|How to Read §2]]
- **Energy-based model (EBM)** · 에너지 기반 모델 — A model that scores input–output compatibility with a scalar energy (low means compatible) instead of a normalized probability. Explicit EBM policies were unstable to train; JEPA is framed as energy-based. · 입력과 출력이 얼마나 맞는지를 정규화된 확률 대신 스칼라 에너지(낮을수록 잘 맞음)로 매기는 모델. 명시적 EBM 정책은 학습이 불안정했고, JEPA는 에너지 기반으로 정식화된다. → [[01-canonical-papers/notes/4-vla/diffusion-policy|Diffusion Policy]], [[01-canonical-papers/notes/5-world-models/jepa|JEPA]]
- **Ensemble** · 앙상블 — Averaging the predictions of several separately trained models. It usually beats any single model, so a headline number may be an ensemble's: AlexNet's 15.3% came from seven CNNs. · 따로 학습한 여러 모델의 예측을 평균하는 것. 보통 단일 모델보다 낫기 때문에 대표 수치가 앙상블의 것일 수 있다. AlexNet의 15.3%는 CNN 7개의 결과다. → [[01-canonical-papers/notes/1-foundations/alexnet|AlexNet]]
- **Entity (ROS 2)** · 엔티티 — Anything in a node that can have a callback or be scheduled: publishers, subscriptions, timers, services and clients. · 노드 안에서 콜백을 가질 수 있거나 스케줄되는 것. 퍼블리셔, 서브스크립션, 타이머, 서비스, 클라이언트. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §8]]
- **Entropy** · 엔트로피 — $H(p) = -\sum_x p(x)\log p(x)$, a distribution's average surprise: how unpredictable it is, in bits (log base 2) or nats (base e). Uniform distributions maximize it; deterministic ones have zero. · 분포의 평균 놀라움으로, 얼마나 예측하기 어려운지를 비트(밑 2)나 나트(밑 e)로 잰다. 균등 분포에서 최대, 결정적 분포에서 0이다. → [[02-foundations/information-theory|Information Theory §1]]
- **Entropy bonus** · 엔트로피 보너스 — Adding $+\alpha H(\pi)$ to an RL objective so the policy is rewarded for staying undecided and does not collapse early to a deterministic mediocre habit. SAC promotes it from bonus to objective. · 정책이 이른 시점에 결정론적 습관으로 붕괴하지 않도록 목적함수에 엔트로피를 더하는 것. → [[02-foundations/rl-basics|RL Basics §8]], [[01-canonical-papers/notes/1-foundations/sac|SAC]]
- **Environment step** · environment step(환경 스텝) — One transition of the simulated or real environment; the usual x-axis of an RL learning curve. Steps are not wall-clock and not real experience: divide by the number of parallel environments and the control rate before believing a number. · 시뮬레이션·실제 환경의 전이 1회이며, RL 학습 곡선의 통상적인 가로축이다. 스텝은 벽시계 시간도 실제 경험량도 아니다 — 숫자를 믿기 전에 병렬 환경 수와 제어 주기로 나눠 보라. → [[02-foundations/rl-basics|RL Basics §10]]
- **Environment stiffness (contact stiffness)** · 환경 강성 — The stiffness $K_e$ a controller meets in contact, spanning about five orders of magnitude from foam to steel. A force controller identifies the series stiffness of tool, sensor, arm, and surface, not the material's own. · 제어기가 접촉에서 만나는 강성 $K_e$로, 폼에서 강철까지 약 다섯 자릿수에 걸친다. 힘 제어기가 식별하는 것은 재료 자체가 아니라 공구·센서·팔·표면의 직렬 강성이다. → [[04-robotics/force-compliance-control|Force & Compliance Control §1]]
- **Episode** · 에피소드 — One run of an RL task from reset to termination. On hardware every episode needs a reset, often human labor, and success rates depend on how episodes, time limits and resets are defined. · RL 과제의 리셋부터 종료까지 한 번의 실행. 하드웨어에서는 에피소드마다 흔히 사람 손이 드는 리셋이 필요하고, 성공률은 에피소드·시간 제한·리셋의 정의에 의존한다. → [[02-foundations/rl-basics|RL Basics §9]], [[02-foundations/ml-practice|ML Practice §5]]
- **Episodic map** · episodic 지도 — A map built during one episode and discarded when it ends. It supports search within the episode but accumulates nothing across repeated visits to the same building. · 한 에피소드 동안 만들고 끝나면 버리는 지도. 에피소드 안의 탐색은 돕지만 같은 건물을 다시 찾아도 쌓이는 것이 없다. → [[01-canonical-papers/notes/9-navigation/semexp|SemExp]]
- **Epoch** · 에포크 — One full pass over the training dataset. "300 epochs" times iterations-per-epoch gives the number of parameter updates. · 데이터셋 전체를 한 바퀴 도는 것. → [[02-foundations/neural-network-basics|0.7 §4]]
- **Epsilon-greedy ($\epsilon$-greedy)** · $\epsilon$-greedy — Exploration for discrete actions: act greedily with probability $1-\epsilon$ and uniformly at random otherwise, decaying $\epsilon$ over training. · 이산 행동의 탐색 방식: 확률 $1-\epsilon$로 탐욕적으로, 나머지는 균등 무작위로 행동하고 학습이 진행되면 $\epsilon$을 줄인다. → [[02-foundations/rl-basics|RL Basics §8]]
- **Epsilon metric (Ferrari–Canny metric)** · 엡실론 지표 — Grasp quality as the radius of the largest origin-centred ball inside the grasp wrench space: the worst-case external wrench magnitude the grasp resists. Positive exactly under closure, but blind to which directions the task loads. · 파지 렌치 공간 안에 들어가는 원점 중심의 가장 큰 공의 반지름으로 매긴 파지 품질로, 파지가 저항하는 최악의 외부 렌치 크기다. closure일 때만 양수지만 과제가 실제로 어느 방향에 하중을 거는지는 무시한다. → [[04-robotics/grasping|Grasping §4]]
- **Equilibrium point** · 평형점 — A state where the system stays put if left alone ($\dot x = 0$ for the chosen input). Stability, linearization, and Lyapunov arguments are all statements about a particular equilibrium. · 그대로 두면 계속 머무는 상태(선택한 입력에서 $\dot x = 0$). 안정성·선형화·리아푸노프 논증은 모두 특정 평형점에 관한 진술이다. → [[04-robotics/control-theory-ce397|Control Theory §4]]
- **ERM (eccentric rotating mass)** · ERM(편심 회전 질량) — A cheap "rumble" vibration motor spinning an off-center mass. Its force scales roughly with $mr\omega^2$, so changing speed changes frequency and amplitude together, and its envelope is slow. · 편심 질량을 돌리는 값싼 "럼블" 진동 모터. 힘이 대략 $mr\omega^2$에 비례하므로 속도를 바꾸면 주파수와 진폭이 함께 바뀌고, 포락선이 느리다. → [[04-robotics/haptics-teleoperation/tactile-display-design|24.2 Tactile Display §1]]
- **Error budget** · 오차 예산 — An accounting of error sources such as base localization, settling, arm kinematics, and workpiece pose. Independent errors add in quadrature; a centimetre budget cannot meet a millimetre tolerance without closing the loop at the task. · 베이스 위치 추정, 침하, 팔 기구학, 작업물 자세처럼 계획과 접촉 사이의 모든 오차 원천을 합산한 것. 독립 오차는 제곱합으로 더하며, 센티미터급 예산은 작업에서 루프를 닫지 않고는 밀리미터 공차를 뒷받침하지 못한다. → [[04-robotics/navigation-mobile-manipulation|Mobile Manipulation §4]]
- **Error dynamics** · 오차 동역학 — The differential equation the tracking error obeys under a given controller. Control design picks gains so it is stable and well damped, for example $\ddot e + K_d\dot e + K_p e = 0$. · 주어진 제어기 아래 추종 오차가 따르는 미분방정식. 제어 설계는 이것이 안정하고 잘 감쇠하도록 이득을 고른다. 예: $\ddot e + K_d\dot e + K_p e = 0$. → [[04-robotics/modern-robotics/ch11-robot-control|MR ch.11]]
- **ESDF (Euclidean signed distance field)** · ESDF — A distance field storing the Euclidean distance to the nearest obstacle at every point, which hands a planner both clearance and its gradient; trajectory-optimization planners want one. · 모든 지점에 가장 가까운 장애물까지의 유클리드 거리를 담는 거리장. 계획기에 여유 간격과 그 그래디언트를 함께 주므로 궤적 최적화 계획기가 원한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Essential and fundamental matrices (E, F)** · essential/fundamental 행렬 — 3×3 matrices that constrain where a point seen in one image can appear in a second view. E uses calibrated coordinates and yields relative rotation and translation up to scale; F uses raw pixels. · 한 이미지에서 본 점이 두 번째 시점의 어디에 나타날 수 있는지 제약하는 3×3 행렬. E는 보정된 좌표를 써서 스케일을 뺀 상대 회전·병진을 주고, F는 원시 픽셀 좌표를 쓴다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Estimand** · estimand — The precise quantity a study is designed to estimate, such as the mean reduction in peak contact force under one cue versus another. Naming it makes the question testable. · 연구가 추정하도록 설계된 정확한 양으로, 예를 들어 한 cue 대비 다른 cue에서 최대 접촉력의 평균 감소량이다. 이것을 정하면 질문이 검정 가능해진다. → [[04-robotics/haptics-teleoperation/experiments-readings|24.6 Experiments §2]]
- **Euler angles (roll-pitch-yaw)** · 오일러 각 — A rotation written as three successive rotations about named axes. Human-readable, but the rotation order is part of the definition and the coordinates suffer gimbal lock. · 정해진 축들에 대한 세 번의 연속 회전으로 쓰는 회전 표현. 사람이 읽기 쉽지만 회전 순서가 정의의 일부이고 짐벌 락이 생긴다. → [[02-foundations/se3-geometry|SE(3) §2]]
- **Euler's formula** · 오일러 공식 — $e^{j\theta} = \cos\theta + j\sin\theta$, the unit-circle point at angle $\theta$; multiplying by it rotates by $\theta$. It makes a sinusoid a rotation seen edge-on, which is what Fourier analysis rests on. · 각 $\theta$의 단위원 위 점이며, 이것을 곱하면 $\theta$만큼 회전한다. 사인파를 옆에서 본 회전으로 만들어 푸리에 분석의 토대가 된다. → [[02-foundations/engineering-math|Engineering Math §7]]
- **Evaluation protocol** · 평가 프로토콜 — The fixed rules for scoring a task: starting conditions, what counts as success in millimeters or newtons, trial counts and reset rules. A well-defined protocol is reusable by anyone attacking the same task. · 과제를 채점하는 고정 규칙: 시작 조건, 밀리미터나 뉴턴으로 정한 성공 기준, 시행 수, 리셋 규칙. 잘 정의된 프로토콜은 같은 과제에 도전하는 누구나 다시 쓸 수 있다. → [[06-research-practice/real-world-impact|Real-World Impact §5]]
- **Event-based haptics** · 사건 기반 햅틱 — Adding a brief open-loop high-frequency transient at contact onset, such as a pulse, decaying sinusoid, or acceleration-matched profile, so a soft but stable wall feels harder. It costs force headroom and can saturate actuators. · 접촉 순간 펄스·감쇠 사인파·가속도 정합 프로파일 같은 짧은 개루프 고주파 과도를 더해, 무르지만 안정한 벽이 더 단단하게 느껴지게 하는 것. 힘 여유를 쓰고 액추에이터를 포화시킬 수 있다. → [[04-robotics/haptics-teleoperation/haptic-rendering-algorithms|24.7 Rendering Algorithms §4]]
- **Evidence ladder (deployment ladder)** · 증거의 사다리(배치 사다리) — The wiki's five rungs of deployment evidence: simulation, lab hardware, full-scale mock-up, one active-site trial, and use by someone else. Each licenses one more claim; independent use sits on a separate axis. · 위키가 쓰는 배치 증거의 다섯 단계: 시뮬레이션, 실험실 하드웨어, 실물 크기 목업, 실제 현장 1회, 다른 사람의 사용. 단계마다 한 문장을 더 허락하며, 다른 사람의 사용은 별도의 축에 있다. → [[06-research-practice/real-world-impact|Real-World Impact §2]]
- **Evolution strategies (ES, CMA-ES)** · 진화 전략 — Gradient-free optimization that perturbs parameters, scores each variant by return, and shifts toward the better ones. World Models trains its tiny linear controller this way with CMA-ES. · 파라미터를 교란하고 각 변형을 리턴으로 채점한 뒤 좋은 쪽으로 옮겨 가는 그래디언트 없는 최적화. World Models는 작은 선형 컨트롤러를 CMA-ES로 이렇게 학습한다. → [[01-canonical-papers/notes/5-world-models/world-models|World Models]]
- **Excavator (boom, bucket)** · 굴착기 — A hydraulic digging machine: a rotating body carries a boom and arm ending in a bucket or other tool, all driven by hydraulic cylinders. · 유압 굴삭 기계로, 선회하는 차체에 붐과 암이 달리고 끝에 버킷이나 다른 도구가 붙으며 모두 유압 실린더로 구동된다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §1]]
- **Exchangeability** · 교환가능성 — The assumption that calibration cases and a new case are equally likely to arrive in any order. i.i.d. draws satisfy it, but it is weaker; conformal prediction's guarantee rests on it. · 보정 사례와 새 사례가 어떤 순서로 도착해도 확률이 같다는 가정. i.i.d. 추출이면 성립하지만 그보다 약한 가정이며, conformal prediction의 보장이 이것에 기댄다. → [[04-robotics/human-intent-prediction|Human Intent §4]]
- **Exchange argument** · 교환 논증 — A greedy proof: take an optimal solution that differs from the greedy one, change it toward the greedy choice without making it invalid or worse, and repeat until the two agree. · 그리디 증명법: 그리디 해와 다른 최적해를 잡고, 무효가 되거나 나빠지지 않게 그리디 선택 쪽으로 바꾸기를 두 해가 같아질 때까지 반복한다. → [[02-foundations/algorithms/greedy-mst|11.4 §1]]
- **Exclusions** · 제외 — Trials or runs left out of the reported numbers. Record each with its reason so readers can recover the true denominator; dropping operator-stopped runs silently inflates autonomy. · 보고한 수치에서 빠진 시행이나 실행. 독자가 실제 분모를 복원할 수 있게 각각 이유와 함께 기록한다. 운전자가 멈춘 실행을 조용히 빼면 자율성이 부풀려진다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §7]]
- **Execution layer (task executive)** · 실행 계층 — The state machine, behavior tree, or executive between the task command and the planner or controller. It decides what runs now, checks preconditions and postconditions, and handles timeouts, retries, fallbacks, and recovery. · 과제 명령과 플래너·제어기 사이의 상태기계, behavior tree, 또는 executive. 지금 무엇을 돌릴지 정하고, precondition과 postcondition을 검사하며, timeout·retry·fallback·복구를 처리한다. → [[04-robotics/robot-systems-deployment|Robot Systems §6]]
- **Executor** · executor — The object that owns one or more threads, watches the middleware through a wait set for ready messages and timers, and runs their callbacks. One thread means one callback at a time. · 스레드를 하나 이상 소유하고 wait set으로 준비된 메시지와 타이머를 감시해 콜백을 실행하는 객체. 스레드가 하나면 한 번에 콜백 하나만 돈다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §8]]
- **Existence proof** · 존재 증명 — A demonstration that something can be done at all under the paper's conditions. It does not establish reliability, throughput, or readiness for a construction site. · 논문의 조건 아래에서 어떤 일이 아예 가능하다는 것을 보인 실증. 신뢰성, 처리량, 건설 현장 준비도는 입증하지 않는다. → [[01-canonical-papers/notes/8-construction/aerial-am-2022|Aerial AM 2022]]
- **Exoskeleton** · 외골격 — A wearable device that supports or augments a worker's body, such as for lifting or overhead work. It is a worker-centered branch that need not infer worker state. · 들기나 머리 위 작업 등에서 작업자의 몸을 지지하거나 증강하는 착용형 장치. 작업자 상태 추론이 필요 없는 작업자 중심 로보틱스의 한 갈래다. → [[05-construction-robotics/hrc-worker-centered|HRC §1]]
- **Expectation (expected value)** · 기댓값 — $E[g(X)] = \int g(x)p(x)\,dx$: the average of $g$ weighted by how often each value occurs, a plain weighted sum for discrete variables. A fair die's expectation is 3.5. · $g$를 각 값이 나오는 빈도로 가중해 평균한 것으로, 이산 변수라면 그냥 가중합이다. 공정한 주사위의 기댓값은 3.5다. → [[02-foundations/engineering-math|Engineering Math §3]]
- **Expected calibration error (ECE)** · ECE — The scalar behind a reliability diagram: bin predictions by predicted probability, take each bin's gap between observed event frequency and mean prediction, and average the gaps weighted by the number of cases per bin. · reliability diagram 뒤의 스칼라. 예측을 예측 확률 구간으로 나누고, 구간마다 실제 사건 빈도와 평균 예측의 차이를 구한 뒤, 구간에 든 사례 수로 가중 평균한다. → [[04-robotics/human-intent-prediction|Human Intent §4]]
- **Expected running time** · 기대 실행 시간 — Running time averaged over an algorithm's own random choices, such as random pivots, and guaranteed for every input. It differs from average case, which assumes a distribution over inputs. · 무작위 피벗처럼 알고리즘 자신의 무작위 선택에 대해 평균한 실행 시간으로, 모든 입력에 대해 보장된다. 입력 분포를 가정하는 평균 경우와는 다르다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §3]]
- **Experience-based navigation** · 경험 기반 내비게이션 — Instead of correcting one map toward the present, a robot stores distinct visual experiences of a changing place and localizes by matching against them; failing to match lays down a new experience. · 지도 하나를 현재 쪽으로 고치는 대신, 변하는 장소의 서로 구별되는 시각 경험들을 저장하고 그것과 매칭해 위치를 추정한다. 충분히 매칭되지 않으면 새 경험으로 쌓는다. → [[04-robotics/semantic-language-navigation|Semantic navigation §7]]
- **Experimental unit** · 실험 단위 — The independent entity assigned to a condition: a seed, scene, object, participant, robot or site. Repeated frames or trials within one unit are clustered, so 1,000 staircase trials from three people give n = 3. · 조건에 배정되는 독립적 개체: 시드, 장면, 물체, 참가자, 로봇, 현장. 한 단위 안의 반복 프레임이나 시행은 묶여 있으므로 세 명의 계단법 시행 천 번은 n = 3이다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §1]]
- **Exploding gradients** · 그래디언트 폭발 — The opposite of vanishing gradients: products of Jacobians with norms above 1 grow exponentially with depth or time. Gradient clipping, rescaling the gradient norm to a ceiling, is standard in RNN and LLM training. · 그래디언트 소실의 반대로, 노름이 1보다 큰 야코비안들의 곱이 깊이나 시간에 따라 지수적으로 커진다. 그래디언트 노름을 상한으로 재스케일하는 gradient clipping이 RNN과 LLM 학습의 표준이다. → [[02-foundations/calculus-backprop|Calculus & Backprop §5]]
- **Exploration** · 탐색(exploration) — Trying actions whose outcomes are not yet known so the policy can discover reward. With sparse reward it, not the update rule, is usually the binding constraint; noise, stochastic policies and entropy bonuses provide it. · 결과를 아직 모르는 행동을 시도해 정책이 보상을 발견하게 하는 것. 희소 보상에서는 갱신 규칙이 아니라 이것이 대개 병목이며, 노이즈·확률적 정책·엔트로피 보너스로 확보한다. → [[02-foundations/rl-basics|RL Basics §8]]
- **Exploratory procedure** · 탐색 절차 — A stereotyped hand motion people use to probe an object property, such as lateral sliding for texture, pressing for hardness, or static contact for temperature. Devices should be tested with the motion the task requires. · 사람이 어떤 성질을 알아내려고 쓰는 정형화된 손동작으로, 질감에는 옆으로 문지르기, 경도에는 누르기, 온도에는 가만히 대고 있기가 있다. 장치는 과제가 요구하는 움직임으로 시험해야 한다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §1]]
- **Exponential coordinates** · 지수 좌표 — Describing a rotation by an axis and angle (a rigid motion by a screw and distance) and recovering the matrix as an exponential, $e^{[\hat\omega]\theta}$. The log map inverts it, so poses interpolate and average correctly. · 회전을 단위축과 각으로, 강체 운동을 스크류와 이동량으로 기술하고 $e^{[\hat\omega]\theta}$ 같은 지수로 행렬을 복원하는 방식. 로그 사상이 그 역이며, 덕분에 자세를 올바르게 보간·평균할 수 있다. → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR ch.3 §2]]
- **Exponential map** · 지수 사상 — Turns a small-motion generator, a skew-symmetric matrix or a screw axis times an angle, into a finite rotation or rigid transform such as $e^{[S]\theta}$; the matrix log goes back. Product-of-exponentials kinematics builds on it. · 미소 운동 생성자(반대칭 행렬, 또는 스크류 축 × 각도)를 $e^{[S]\theta}$ 같은 유한한 회전이나 강체 변환으로 바꾸는 사상. 행렬 로그가 그 역이며, 지수 곱(PoE) 기구학이 이 위에 선다. → [[02-foundations/se3-geometry|SE(3) §4]]
- **Exponential moving average (EMA)** · 지수 이동 평균 — A running average $y_n = \alpha y_{n-1} + (1-\alpha)x_n$ that forgets old values geometrically; as a filter, the simplest IIR low-pass. Larger $\alpha$ smooths more and lags more, though some papers swap $\alpha$ and $1-\alpha$. · 오래된 값을 기하급수적으로 잊는 이동 평균으로, 필터로 보면 가장 단순한 IIR 저역통과(지수 평활기)다. $\alpha$가 클수록 더 매끄럽고 더 늦지만, 문헌에 따라 $\alpha$와 $1-\alpha$를 바꿔 쓴다. → [[02-foundations/signal-processing|Signal Processing §4]], [[02-foundations/calculus-backprop|Calculus & Backprop §5]]
- **Exposure** · 노출(exposure) — How much operating time, distance or number of trials could have produced a failure. Zero collisions in 10 trials and in 10,000 trials are very different evidence about safety. · 그 실패를 낳을 수 있었던 운용 시간, 거리, 시행 수의 양. 10회 시행에서 충돌 0과 10,000회에서 충돌 0은 안전에 대해 전혀 다른 증거다. → [[06-research-practice/research-questions-claims|Research Questions §4]], [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §6]]
- **Extended abstract** · 확장 초록 — A short paper, typically a few pages, presented at a workshop. Four pages or fewer, at a workshop with no proceedings and no DOI, clears the prior-publication rules of NeurIPS, ICRA and CVPR. · 워크숍에서 발표하는 몇 쪽짜리 짧은 논문. 프로시딩도 DOI도 없는 워크숍에 4쪽 이하로 내면 NeurIPS·ICRA·CVPR의 선행 출판 규칙을 모두 통과한다. → [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **Extended Kalman filter (EKF)** · EKF — A Kalman filter for nonlinear models that linearizes the process and observation models with their Jacobians at the current estimate. Linearization error can make it inconsistent, that is, overconfident. · 비선형 모델용 칼만 필터로, 현재 추정값에서 과정·관측 모델을 야코비안으로 선형화한다. 선형화 오차 때문에 비일관, 즉 과신 상태가 될 수 있다. → [[04-robotics/state-estimation-slam|State estimation §5]]
- **Exteroception** · 외수용 감각 — Sensing the world outside the body with cameras, depth sensors, or LiDAR. It lets a legged robot see terrain before contact, but can be systematically wrong on snow, vegetation, water, or dust. · 카메라, 깊이 센서, LiDAR로 몸 바깥 세계를 감지하는 것. 레그드 로봇이 접촉 전에 지형을 보게 하지만 눈, 식생, 물, 먼지에서 체계적으로 틀릴 수 있다. → [[01-canonical-papers/notes/9-navigation/miki-perceptive-locomotion|Miki 2022]]
- **Extrapolation error (TF)** · 외삽 오류 — A TF lookup refused because the requested time is newer than the latest transform in the buffer (into the future) or older than the buffer holds (into the past). The buffer never invents transforms. · 요청 시각이 버퍼의 최신 변환보다 늦거나(future) 버퍼가 가진 것보다 오래돼서(past) TF 조회가 거부되는 것. 버퍼는 변환을 지어내지 않는다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §9]]
- **Extrinsic dexterity** · extrinsic dexterity — Reorienting or grasping an object using gravity, inertia, and environment contact, such as pushing it against a wall, instead of finger motion. A simple gripper plus environment gains much of a dexterous hand's capability. · 손가락 운동 대신 중력·관성·환경과의 접촉(벽에 대고 밀기 등)을 이용해 물체를 재정향하거나 잡는 것. 풍부한 환경의 단순한 그리퍼가 다지 손 능력의 상당 부분을 얻는다. → [[04-robotics/grasping|Grasping §5]]
- **F1 score** · F1 — The harmonic mean of precision and recall, $2PR/(P+R)$. Unlike an arithmetic mean it stays low if either one is low, so it will not hide a lopsided detector. · 정밀도와 재현율의 조화평균 $2PR/(P+R)$. 산술평균과 달리 둘 중 하나만 낮아도 낮게 유지되어 한쪽으로 쏠린 검출기를 가려 주지 않는다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Factor graph (pose graph)** · factor/pose graph — A graph whose nodes are unknowns such as poses and landmarks and whose edges are measurement constraints; the SLAM back end solves it by batch or incremental nonlinear least squares. · 노드가 pose·landmark 같은 미지수이고 간선이 측정 제약인 그래프. SLAM back end가 일괄 또는 증분 비선형 최소자승으로 푼다. → [[04-robotics/state-estimation-slam|State estimation §5]]
- **Failure taxonomy** · 실패 분류 체계 — A task-specific set of failure categories, such as sensing, estimation, planning, control, hardware, human and procedure, each tied to observable evidence and separating initiating faults from propagation and consequences. · 센싱, 추정, 계획, 제어, 하드웨어, 사람, 절차 같은 과제 맞춤 실패 범주의 묶음. 각 범주를 관찰 가능한 증거에 연결하고 시작 결함을 전파와 결과에서 분리한다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §2]]
- **False alarm and miss (false positive, false negative)** · 오경보·놓침 — The two ways a detector errs: a false alarm declares an event that is absent, a miss overlooks one that is present. Moving the decision threshold trades one for the other along the ROC curve. · 검출기가 틀리는 두 방식. 오경보는 없는 사건을 있다고 선언하고, 놓침은 있는 사건을 놓친다. 판정 문턱값을 옮기면 ROC 곡선을 따라 하나를 줄이는 대신 다른 하나가 는다. → [[02-foundations/probability|Probability §6]]
- **Falsifiability** · 반증 가능성 — The property that some possible result would show a claim or question wrong. A falsifiable question can be answered negatively; "does this improve robustness?" without a defined test cannot. · 가능한 어떤 결과가 주장이나 질문을 틀렸다고 보일 수 있는 성질. 반증 가능한 질문은 부정적으로도 답할 수 있다. 시험이 정의되지 않은 "강건성을 높이는가?"는 그럴 수 없다. → [[06-research-practice/research-questions-claims|Research Questions §1]]
- **Farthest point sampling (FPS)** · farthest point sampling — Picking points one at a time, each the point farthest from those already chosen, so the samples spread evenly over a cloud. PointNet++ uses them as neighbourhood centres. · 이미 뽑은 점들에서 가장 먼 점을 하나씩 골라 샘플이 클라우드 전체에 고르게 퍼지게 하는 방법. PointNet++는 이 점들을 이웃 그룹의 중심으로 쓴다. → [[01-canonical-papers/notes/2-computer-vision/pointnet|PointNet]]
- **Fault injection** · 결함 주입 — Deliberately adding a controlled delay, noise, dropout or freeze in a bounded, safe test to see whether it reproduces a failure. It shows a fault is sufficient to cause failure, not that it actually occurred. · 한계가 정해진 안전한 시험에서 통제된 지연, 잡음, 드롭아웃, 멈춤을 일부러 넣어 실패가 재현되는지 보는 것. 그 결함이 실패를 일으킬 수 있음을 보일 뿐, 실제로 일어났음을 보이지는 않는다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §4]]
- **FEA (finite element analysis)** · 유한요소해석(FEA) — A method that divides a continuous material into small elements and solves for its deformation. In soil simulation it sits between empirical models and grain-level DEM in detail and cost. · 연속 재료를 작은 요소로 나눠 변형을 푸는 방법. 토질 시뮬레이션에서는 세부와 비용 면에서 경험식 모델과 알갱이 수준의 DEM 사이에 놓인다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §4]]
- **Feature pyramid (multi-scale features, FPN)** · 특징 피라미드 — A set of feature maps at several resolutions, fine maps for small objects and coarse ones for large. CNN backbones supply one naturally; Swin rebuilt it for Transformers by merging patches stage by stage. · 여러 해상도의 특징 맵 묶음. 세밀한 맵은 작은 물체, 거친 맵은 큰 물체를 맡는다. CNN 백본은 자연스럽게 제공하고, Swin은 단계마다 패치를 병합해 Transformer에서 이를 재현했다. → [[01-canonical-papers/notes/2-computer-vision/swin|Swin]]
- **Feedforward** · 피드포워드 — Computing the input the model says a motion needs and applying it directly, leaving feedback to correct only the residual. It is why computed-torque control beats pure PID on robot arms. · 모델이 말하는 필요 입력을 먼저 계산해 넣고 피드백은 잔차만 고치게 하는 것. 로봇 팔에서 계산 토크 제어가 순수 PID를 이기는 이유다. → [[04-robotics/control-theory-ce397|Control Theory §7]]
- **Feed-forward network (FFN)** · feed-forward 네트워크 — The small two-layer MLP applied to each token separately inside every Transformer block, after attention mixes information across tokens. · 모든 Transformer 블록 안에서 어텐션이 토큰 간 정보를 섞은 뒤, 각 토큰에 따로 적용하는 작은 2층 MLP. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Feed-forward reconstruction (vs per-scene optimization)** · feed-forward 재구성 — Predicting cameras, depth, and 3D points in one network pass instead of iteratively optimizing each new scene as SfM, NeRF, and 3DGS do. Fast, but off-distribution inputs still get confident answers. · SfM·NeRF·3DGS처럼 장면마다 반복 최적화하지 않고, 신경망 한 번의 순전파로 카메라·깊이·3D 점을 예측하는 것. 빠르지만 학습 분포 밖 입력에도 자신 있게 답한다. → [[01-canonical-papers/notes/2-computer-vision/vggt|VGGT]]
- **Fidelity–diversity trade-off** · 충실도-다양성 트레이드오프 — In conditional generation, pushing samples to match the condition more closely makes them less varied. The guidance scale moves along this frontier; high values also cause over-saturation. · 조건부 생성에서 샘플을 조건에 더 충실하게 밀수록 다양성이 줄어드는 관계. 가이던스 스케일이 이 경계 위를 움직이며, 값이 크면 과포화도 생긴다. → [[01-canonical-papers/notes/6-diffusion/classifier-free-guidance|CFG]]
- **FID (Fréchet Inception Distance)** · FID — An image-generation metric: the distance between the feature distributions of generated and real images, with features taken from Inception, an image-classification CNN. Lower is better. · 이미지 생성 지표: 생성 이미지와 실제 이미지의 특징 분포 사이 거리이며, 특징은 이미지 분류 CNN인 Inception에서 뽑는다. 낮을수록 좋다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Fiducial marker (AprilTag)** · 피두셜 마커 — A printed pattern with a known ID and size whose pose a calibrated camera can recover precisely. On site it replaces factory fixturing but suffers from lighting changes, occlusion, and outdoor wear. · ID와 크기가 알려진 인쇄 패턴으로, 보정된 카메라가 그 자세를 정밀하게 복원한다. 현장에서 공장 고정구를 대신하지만 조명 변화, 가림, 실외 마모에 약하다. → [[01-canonical-papers/notes/8-construction/vision-guided-assembly|Feng 2015]]
- **FiLM** · FiLM — Conditioning by modulating the scale and shift of a feature map with a conditioning signal. · 조건 신호로 특징맵의 스케일·이동을 변조하는 조건화 기법. → [[01-canonical-papers/notes/4-vla/rt-1|RT-1]]
- **Filter consistency** · 필터 일관성 — A filter is consistent when its reported covariance matches its actual estimation error. An overconfident one reports too small a covariance, so tracking gates reject true detections and wrong estimates are trusted. · 필터가 보고한 공분산이 실제 추정 오차와 맞으면 일관적이다. 과신하는 필터는 공분산을 너무 작게 보고해 추적 게이트가 참인 검출을 버리고 틀린 추정을 믿게 만든다. → [[04-robotics/state-estimation-slam|State estimation §9]], [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Fine-tuning** · 파인튜닝 — Further training a pretrained model on task-specific data. · 사전학습 모델을 특정 작업 데이터로 추가 학습. → [[01-canonical-papers/notes/1-foundations/bert|BERT]], [[01-canonical-papers/notes/1-foundations/lora|LoRA]]
- **Finite-difference velocity estimate** · 속도 추정(후방차분) — Velocity computed as $(x_k-x_{k-1})/T$ from sampled positions. It amplifies quantization noise; averaging or low-pass filtering reduces noise but adds delay and phase lag, which can destabilize a haptic loop. · 샘플링된 위치로 $(x_k-x_{k-1})/T$처럼 계산한 속도. 양자화 잡음을 증폭하며, 평균이나 저역통과 필터는 잡음을 줄이지만 지연과 위상 지연을 더해 햅틱 루프를 불안정하게 만들 수 있다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|24.4 Rendering & Stability §3]]
- **FIR filter (finite impulse response)** · FIR 필터 — A filter whose output is a weighted sum of current and past inputs only, such as a moving average. Always stable and can have exactly linear phase, but sharp responses need many taps. · 출력이 현재와 과거 입력의 가중합뿐인 필터로, 이동 평균이 가장 단순한 예다. 항상 안정하고 정확한 선형 위상이 가능하지만, 날카로운 응답에는 탭이 많이 필요하다. → [[02-foundations/signal-processing|Signal Processing §4]]
- **First failure (initiating fault)** · 최초 실패 — The earliest point where a subsystem departed from its intended contract. A collision is a downstream symptom, and fixing only the symptom can leave the initiating fault in place. · 하위 시스템이 의도된 약속에서 처음 벗어난 지점. 충돌은 하류 증상이며, 증상만 고치면 시작 결함이 그대로 남을 수 있다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §1]]
- **Fisheye camera** · 어안 카메라 — A camera with a very wide-angle lens that captures a large field of view at the cost of strong distortion, useful for seeing a gripper and its surroundings in one image. · 강한 왜곡을 감수하고 매우 넓은 시야를 담는 광각 렌즈 카메라. 그리퍼와 주변을 한 영상에 함께 보는 데 쓴다. → [[01-canonical-papers/notes/7-robotics/umi|UMI]]
- **Fitts' law (index of difficulty)** · Fitts의 법칙 — Movement time grows with the index of difficulty $ID = \log_2(2D/W)$ for target distance $D$ and width $W$. It is the standard way to operationalize whether an interface reduced task difficulty. · 이동 시간은 목표 거리 $D$와 폭 $W$에 대한 난이도 지수 $ID = \log_2(2D/W)$와 함께 증가한다. 인터페이스가 과제 난이도를 낮췄는지를 조작화하는 표준 방법이다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §6]]
- **Five levels of construction HRC** · 건설 HRC 다섯 수준 — Liang et al.'s taxonomy by robot autonomy and remaining human effort: Preprogramming, Adaptive Manipulation, Imitation Learning, Improvisatory Control, Full Autonomy. It classifies published research, not deployed practice. · Liang 등이 로봇 자율과 남은 인간 노력으로 나눈 분류: Preprogramming, Adaptive Manipulation, Imitation Learning, Improvisatory Control, Full Autonomy. 배치 현황이 아니라 출판 연구를 분류한다. → [[01-canonical-papers/notes/8-construction/liang-hrc-survey|Liang HRC survey 2021]]
- **Fixed-vector bottleneck** · 고정 벡터 병목 — In seq2seq, the whole source sentence must squeeze through one fixed-size vector, so quality drops on long inputs. Attention removed it by letting the decoder read every encoder state. · seq2seq에서 원문 문장 전체가 고정 크기 벡터 하나를 통과해야 하므로 긴 입력에서 품질이 떨어지는 문제. 어텐션은 디코더가 모든 인코더 상태를 읽게 해 이를 없앴다. → [[01-canonical-papers/notes/1-foundations/seq2seq|Seq2Seq]]
- **Fixture (fixturing, jig)** · 지그(고정) — A device that holds a part at a known pose, which factory robots rely on. Site parts are where someone put them, within centimetres. · 부품을 알려진 자세로 붙잡아 두는 장치로 공장 로봇은 이것에 기댄다. 현장의 부재는 누군가 놓은 자리에 센티미터 오차로 있다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §1]]
- **FLOPs** · FLOPs — Floating-point operations: a hardware-independent measure of how much arithmetic a model costs, used to compare methods at matched compute. Not the same as wall-clock time — memory bandwidth and parallelism often dominate in practice. · 부동소수점 연산 수. 하드웨어와 무관한 연산량 척도로 연산량 정합 비교에 쓴다. 실제 소요 시간과는 다르다 — 실무에서는 메모리 대역폭과 병렬성이 지배하는 경우가 많다. → [[01-canonical-papers/notes/1-foundations/scaling-laws|Scaling Laws]]
- **Flow matching** · flow matching — A generative training method that directly regresses the velocity field of a noise-to-data probability path; the action-generation engine of π0. · 노이즈→데이터 확률 경로의 속도장을 직접 회귀하는 생성 학습법. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Floyd–Warshall algorithm** · Floyd–Warshall — All-pairs shortest paths by DP over which vertices may serve as intermediate stops, in $O(V^3)$. The loop over the intermediate vertex must be outermost; a negative diagonal entry reveals a negative cycle. · 어떤 정점을 중간 경유지로 쓸 수 있는지에 대한 DP로 모든 쌍 최단 경로를 $O(V^3)$에 구한다. 중간 정점 루프가 가장 바깥이어야 하고, 대각 성분이 음수면 음수 사이클이 있다. → [[02-foundations/algorithms/graph-algorithms|11.6 §5]]
- **Focus group** · 포커스 그룹 — A moderated group discussion with experts or stakeholders used to surface candidate factors, which a larger questionnaire then often rates. · 전문가나 이해관계자와 진행하는 집단 토론으로 후보 요인을 도출하는 방법. 도출된 요인은 흔히 더 큰 설문으로 평가한다. → [[01-canonical-papers/notes/8-construction/davila-delgado-2019|Davila Delgado 2019]]
- **Force-based digging** · 힘 기반 굴착 — Treating a dig as a force–torque trajectory rather than a position path, so the machine regulates measured interaction and depends less on a precise soil model. · 굴착을 위치 경로가 아니라 힘-토크 궤적으로 다뤄, 기계가 측정한 상호작용을 조절하고 정밀 토질 모델에 덜 의존하게 하는 방식. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §2]]
- **Force control** · 힘 제어 — Control that regulates measured contact force directly and lets position go where it must. It still has to handle tangential motion and stability, and position control is the opposite limit of infinite stiffness. · 측정된 접촉력을 직접 조절하고 위치는 필요한 곳으로 가게 두는 제어. 접선 운동과 안정성은 여전히 다뤄야 하며, 위치 제어는 강성이 무한대인 반대쪽 극한이다. → [[04-robotics/contact-force-tactile|Contact §5]]
- **Forced-choice design** · 강제선택 설계 — A procedure where the participant must pick which interval or alternative contained the stimulus instead of answering yes or no. It separates sensitivity from response bias, so it is preferred when the claim matters. · 참가자가 예/아니오 대신 자극이 어느 구간이나 선택지에 있었는지 반드시 고르게 하는 절차. 민감도를 응답 편향과 분리하므로 주장이 중요할 때 선호된다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Forward and reverse KL (mode-covering, mode-seeking)** · Forward KL·Reverse KL — The two directions of KL behave differently. Fitting $q$ by $D_{KL}(p\,\Vert\,q)$ spreads $q$ over all of $p$; $D_{KL}(q\,\Vert\,p)$, used in variational inference, locks $q$ onto one mode. · KL의 두 방향은 다르게 행동한다. $D_{KL}(p\,\Vert\,q)$로 $q$를 맞추면 $q$가 $p$ 전체를 덮으려 퍼지고(모드 커버링), 변분 추론에서 쓰는 $D_{KL}(q\,\Vert\,p)$는 $q$를 한 모드에 붙인다(모드 시킹). → [[02-foundations/information-theory|Information Theory §3]]
- **Forward dynamics** · 순동역학 — Given joint torques, solving the manipulator equation for the resulting joint accelerations, $\ddot\theta = M^{-1}(\tau - C\dot\theta - g)$. It is what a physics simulator integrates every step; inverse dynamics runs the other way. · 관절 토크가 주어졌을 때 매니퓰레이터 방정식을 관절 가속도에 대해 푸는 것, $\ddot\theta = M^{-1}(\tau - C\dot\theta - g)$. 물리 시뮬레이터가 매 스텝 적분하는 것이고, 역동역학은 반대 방향이다. → [[02-foundations/manipulator-kinematics-dynamics|Manipulator Dynamics §2]]
- **Forward kinematics (FK)** · 순기구학 — Computing the end-effector pose from the joint values, $\theta \mapsto T(\theta)$. Unlike inverse kinematics it always has exactly one answer. · 관절 값에서 말단 자세를 계산하는 것, $\theta \mapsto T(\theta)$. 역기구학과 달리 답이 항상 정확히 하나다. → [[04-robotics/modern-robotics/ch04-forward-kinematics|MR ch.4]]
- **Forward pass** · 순전파 — Running an input through the network's layers to get an output: matrix multiplies and activations, nothing more. Inference is forward passes with parameters frozen; training adds a loss, gradients and an update. · 입력을 신경망의 층들에 통과시켜 출력을 얻는 것으로, 행렬곱과 활성함수가 전부다. 추론은 파라미터를 고정한 순전파이고, 학습은 여기에 손실·그래디언트·갱신을 더한다. → [[02-foundations/neural-network-basics|0.7 §2]], [[02-foundations/neural-network-basics|0.7 §3]]
- **Forward process (noising process)** · 순방향 과정 — The fixed, unlearned chain in a diffusion model that adds a little Gaussian noise at each step until data becomes pure noise. A closed form lets training jump to any step directly. · 디퓨전 모델에서 매 스텝 가우시안 노이즈를 조금씩 더해 데이터를 순수 노이즈로 만드는 고정된(학습하지 않는) 체인. 닫힌 형태가 있어 학습 때 임의의 스텝으로 바로 건너뛸 수 있다. → [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]]
- **Foundation model** · 파운데이션 모델 — A large model pretrained on broad data and then adapted to many downstream tasks. Robot foundation models (VLAs) typically pair a pretrained VLM backbone with an action expert. · 넓은 데이터로 사전학습한 뒤 여러 하위 과제에 적응시키는 대형 모델. 로봇 파운데이션 모델(VLA)은 보통 사전학습된 VLM 백본에 action expert를 붙인다. → [[03-deep-learning/lineage|Lineage]]
- **Four-channel architecture** · 4채널 구조 — Lawrence's bilateral teleoperation architecture, sending position/velocity and force across the link in both directions, four signals in all. More channels can improve transparency under a model but also expose noise, delay, and calibration errors. · 위치·속도와 힘을 양방향으로 모두 보내 신호가 넷인 Lawrence의 양방향 원격조작 구조. 채널이 많으면 모델 아래 투명성이 좋아질 수 있지만 잡음·지연·보정 오차에도 노출된다. → [[04-robotics/haptics-teleoperation/bilateral-teleoperation|24.5 Bilateral Teleoperation §2]]
- **Free bits** · free bits — A world-model training trick that stops penalizing the KL term once it falls below a small threshold, so the latent is not squeezed into carrying no information. DreamerV3 uses it for robustness. · KL 항이 작은 문턱값 아래로 내려가면 더는 벌점을 주지 않아, 잠재변수가 정보를 전혀 싣지 않는 쪽으로 짓눌리지 않게 하는 월드모델 학습 요령. DreamerV3가 강건성을 위해 쓴다. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **Frequency response** · 주파수 응답 — $G(j\omega)$: what a linear system does to a sine wave at frequency $\omega$. Its magnitude is the amplification and its angle the phase shift; sweeping $\omega$ draws the system as a filter. · 선형 시스템이 주파수 $\omega$의 사인파에 하는 일. 크기는 증폭률, 각도는 위상 이동이며, $\omega$를 훑으면 시스템을 필터로 그린 것이 된다. → [[02-foundations/engineering-math|Engineering Math §9]]
- **Friction cone** · 마찰 원뿔 — The set of forces a contact can apply under Coulomb friction: a cone about the surface normal with half-angle $\arctan\mu$. Every closure and grasp-quality question is a question about whether these cones together span the wrenches the world can apply. · 쿨롱 마찰에서 접촉이 가할 수 있는 힘의 집합 — 법선을 축으로 반각 $\arctan\mu$인 원뿔. 모든 closure와 파지 품질 질문은 이 원뿔들이 합쳐서 세상이 가할 수 있는 렌치를 덮는가의 문제다. → [[04-robotics/grasping|15. §2]]
- **Friction pyramid** · 마찰 피라미드 — A linear approximation of the circular friction cone by flat facets, so friction limits become linear inequalities and the MPC stays a convex QP. Near the cone's edge it misjudges which forces are feasible. · 원형 마찰 원뿔을 평평한 면들로 근사한 것. 마찰 한계가 선형 부등식이 되어 MPC가 볼록 QP로 남지만, 원뿔 가장자리 근처에서는 어떤 힘이 가능한지 잘못 판정한다. → [[04-robotics/convex-mpc-legged|Convex MPC]]
- **Front end / back end** · front end / back end — In SLAM, the front end extracts features or geometric constraints and performs data association; the back end optimizes poses, landmarks and sometimes calibration as nonlinear least squares (Ceres, g2o, GTSAM). · SLAM에서 front end는 특징·기하 제약을 추출하고 data association을 수행하며, back end는 pose·landmark·때로 보정 변수를 비선형 최소자승으로 최적화한다(Ceres, g2o, GTSAM). → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Frontier** · 프런티어 — The boundary between explored free space and unexplored space on a map. Exploration chooses which frontier to visit next; VLFM scores frontiers by vision–language similarity to the goal text. · 지도에서 탐색된 빈 공간과 아직 탐색하지 않은 공간의 경계. 탐색은 다음에 갈 프런티어를 고르는 일이고, VLFM은 목표 텍스트와의 시각-언어 유사도로 프런티어를 채점한다. → [[04-robotics/semantic-language-navigation|Semantic navigation §3]]
- **Frontier exploration** · frontier 탐색 — Exploring by repeatedly driving to a frontier, a boundary cell between known free space and unknown space, until none remains. A different object from the frontier (open list) of a graph search. · 알려진 자유 공간과 미지 사이의 경계 칸인 frontier로 가기를 frontier가 남지 않을 때까지 반복하는 탐색. 그래프 탐색의 frontier(open list)와는 다른 대상이다. → [[04-robotics/planning-decision-making|Planning §2]]
- **Frozen (freezing)** · frozen(얼림) — Parameters deliberately not updated during training, so no gradient changes them. A frozen module is not unimportant, and an end-to-end system can contain one. · 학습 중 의도적으로 갱신하지 않아 그래디언트가 바꾸지 않는 파라미터. 얼렸다고 중요하지 않은 것은 아니며, end-to-end 시스템 안에도 있을 수 있다. → [[02-foundations/neural-network-basics|0.7 §6]], [[01-canonical-papers/how-to-read|How to Read §2]]
- **Full rank** · 풀랭크 — No column (or row) is a combination of the others, so the map squashes nothing flat. A square full-rank matrix is invertible; rank deficiency means information was destroyed. · 어떤 열(행)도 다른 것들의 조합이 아니어서 사상이 아무것도 뭉개지 않는다. 정방 풀랭크 행렬은 가역이다. → [[02-foundations/linear-algebra|Linear Algebra §2]]
- **Function approximation** · 함수 근사 — Representing a value function or policy with far fewer parameters than there are states — linear features, a network — so it generalizes instead of tabulating. Necessary for any state space robotics cares about, and the one leg of the deadly triad nobody proposes giving up. · 상태 수보다 훨씬 적은 파라미터로 가치 함수나 정책을 표현하는 것 — 선형 특징이나 신경망 — 이어서 나열하는 대신 일반화한다. 로보틱스가 다루는 어떤 상태 공간에도 필수이며, deadly triad에서 아무도 포기하자고 하지 않는 다리다. → [[02-foundations/rl-basics|RL Basics §3.5]]
- **Fundamental earthmoving equation (FEE)** · 기본 토공 방정식 — Classical soil-mechanics model predicting resistive force from blade geometry, depth, and soil parameters. Good enough to build training simulators, too crude for precise force prediction. · 날 기하·깊이·토질 파라미터로 저항력을 예측하는 고전 토질역학 모델. 학습 시뮬레이터를 만들기엔 충분하지만 정밀한 힘 예측에는 조악하다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §2]]

## G–L

- **GAE (generalized advantage estimation)** · GAE — An advantage estimator that sums future TD errors weighted by $(\gamma\lambda)^l$. $\lambda = 0$ gives the one-step TD error (biased, low variance); $\lambda = 1$ gives the Monte Carlo return minus $V(s)$. · 이후 TD 오차들을 $(\gamma\lambda)^l$로 가중해 더하는 어드밴티지 추정기. $\lambda = 0$이면 1스텝 TD 오차(편향, 저분산), $\lambda = 1$이면 몬테카를로 리턴에서 $V(s)$를 뺀 값이 된다. → [[02-foundations/rl-basics|RL Basics §4]]
- **GAIL (Generative Adversarial Imitation Learning)** · GAIL — Adversarial imitation: a discriminator that tells expert state–action pairs from the policy's acts as the learned reward, and the policy is trained against it with RL without first recovering an explicit reward. · 적대적 모방: 전문가와 정책의 상태–행동 쌍을 구별하는 판별기가 학습된 보상 역할을 하고, 명시적 보상을 먼저 복원하지 않은 채 정책을 그것에 대해 RL로 학습한다. → [[02-foundations/rl-basics|RL Basics §11]]
- **Gain margin** · 이득 여유 — How much the loop gain can be multiplied before the closed loop goes unstable, $g_m = 1/\lvert L(i\omega_{pc})\rvert$ at the phase-crossover frequency. Usual healthy range 2–5. Reported alone it proves little: a loop can have a huge gain margin and still pass close to the critical point. · 폐루프가 불안정해지기 전까지 루프 이득에 곱할 수 있는 배수, 위상 교차 주파수에서 $g_m = 1/\lvert L(i\omega_{pc})\rvert$. 통상 건강한 범위는 2~5. 단독으로 보고되면 증명하는 바가 적다 — 이득 여유가 커도 임계점 가까이 지나갈 수 있다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Gain scheduling** · 게인 스케줄링 — Designing linear controllers at several operating points and interpolating between them as the operating point changes, because each linearization is valid only near its own point. · 여러 운용점에서 선형 제어기를 설계하고 운용점이 바뀌면 그 사이를 보간하는 것. 각 선형화는 자기 점 근처에서만 유효하기 때문이다. → [[04-robotics/control-theory-ce397|Control Theory §9]]
- **Gait** · 보행 양식 — The pattern of which feet are on the ground when, such as trot (diagonal pairs), bound, pace, or crawl. Model-based controllers impose it; some learned policies let it emerge from the reward. · 어느 발이 언제 땅에 있는지의 패턴. trot(대각쌍), bound, pace, crawl 등이 있다. 모델 기반 제어기는 이를 부과하고, 일부 학습 정책은 보상에서 창발하게 한다. → [[04-robotics/legged-locomotion|Legged Locomotion §2]]
- **GAN (generative adversarial network)** · GAN(생성적 적대 신경망) — A generator maps noise to samples while a discriminator learns to tell them from real data, and each trains against the other. Samples are sharp and one forward pass, but training is unstable. · 생성기는 노이즈를 샘플로 사상하고 판별기는 그것을 진짜 데이터와 구별하도록 배우며, 서로를 상대로 학습한다. 샘플은 선명하고 forward pass 한 번이면 되지만 학습이 불안정하다. → [[01-canonical-papers/notes/6-diffusion/gan|GAN]]
- **Gated cross-attention** · 게이트 달린 cross-attention — New cross-attention layers inserted between frozen language-model blocks, each scaled by a tanh gate initialized at zero, so training starts exactly at the pretrained LM and blends vision in gradually. · 얼린 언어모델 블록 사이에 끼운 새 cross-attention 층으로, 0으로 초기화한 tanh 게이트를 곱한다. 학습이 사전학습 LM 그대로에서 시작해 시각 정보를 서서히 섞는다. → [[01-canonical-papers/notes/3-vlm/flamingo|Flamingo]]
- **Gate (gating)** · 게이트 — A learned multiplier, typically a sigmoid between 0 and 1, that controls how much of a signal passes. LSTM gates decide what enters and leaves the memory cell; Flamingo's tanh gates start closed at zero. · 신호가 얼마나 통과할지 조절하는 학습된 곱셈 인자로, 보통 0과 1 사이의 시그모이드다. LSTM 게이트는 메모리 셀에 들어가고 나갈 정보를 정하고, Flamingo의 tanh 게이트는 0(닫힘)에서 시작한다. → [[01-canonical-papers/notes/1-foundations/lstm|LSTM]]
- **Gating** · 게이팅 — Discarding detection–track pairs whose squared Mahalanobis distance exceeds a chi-square threshold (9.21 for a 2-D 99% gate). It rejects clutter, meaning false alarms, and makes association cheap; the gate is an ellipse, not a circle. · 제곱 마할라노비스 거리가 카이제곱 문턱(2차원 99% 게이트는 9.21)을 넘는 검출–트랙 짝을 버리는 일. 오경보인 클러터를 거르고 연관을 싸게 하며, 게이트는 원이 아니라 타원이다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Gauge freedom** · 게이지 자유도 — Relative constraints fix a map's shape but not where it sits in the world, so the whole solution can slide and rotate freely until one pose is anchored. · 상대 제약은 지도의 모양은 고정하지만 세계 어디에 놓이는지는 고정하지 않아, 한 pose를 앵커로 박기 전까지 해 전체가 자유롭게 미끄러지고 회전한다. → [[04-robotics/state-estimation-slam|State estimation §5]]
- **Gaussian distribution (normal distribution)** · 가우시안 — The bell-shaped distribution set by a mean $\mu$ and covariance $\Sigma$. Affine maps, sums of independent Gaussians, and conditioning all return Gaussians, which is why estimation and noise models lean on it. · 평균 $\mu$와 공분산 $\Sigma$로 정해지는 종 모양 분포. 아핀 사상, 독립 가우시안의 합, 조건화가 모두 다시 가우시안을 주므로 추정과 노이즈 모델이 이것에 기댄다. → [[02-foundations/probability|Probability §3]]
- **Gaussian splatting** · 가우시안 스플래팅 — Representing a scene as millions of 3D Gaussians and rasterizing them for real-time rendering. · 장면을 수백만 개의 3D 가우시안으로 표현하고 래스터라이즈하는 실시간 3D 표현. → [[01-canonical-papers/notes/2-computer-vision/3d-gaussian-splatting|3DGS]]
- **Gauss–Newton** · Gauss–Newton — Solves $\min\lVert f(x)\rVert^2$ by repeatedly replacing $f$ with its affine Taylor model and solving the resulting *linear* least squares problem: $x^+ = x - (J^\top J)^{-1}J^\top f$. Gets its curvature free from $J^\top J$ instead of forming second derivatives, which is why robotics uses it and not full Newton. · $f$를 아핀 테일러 모델로 반복해 바꾸고 그 결과인 *선형* 최소자승을 풀어 $\min\lVert f(x)\rVert^2$를 푸는 방법: $x^+ = x - (J^\top J)^{-1}J^\top f$. 2차 도함수를 만들지 않고 $J^\top J$에서 곡률을 공짜로 얻으며, 로보틱스가 완전한 뉴턴 대신 이것을 쓰는 이유다. → [[02-foundations/optimization|Optimization §3.5]]
- **Gazebo (Harmonic, Classic)** · Gazebo — The physics simulator paired with ROS 2. Gazebo Classic (up to Gazebo 11) reached end of life in January 2025; the modern line, once named Ignition, includes Harmonic, the pairing for Jazzy. · ROS 2와 짝을 이루는 물리 시뮬레이터. Gazebo Classic(Gazebo 11까지)은 2025년 1월 지원 종료됐고, 한때 Ignition이라 불린 현행 계열에 Jazzy와 짝인 Harmonic이 있다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §2]]
- **Gaze estimation** · 시선 추정 — Estimating where a person looks, as a 3D gaze ray or 2D point of regard, with error in degrees. Worn eye trackers reach about 1°; appearance-based methods need visible eyes, within a few metres. · 사람이 어디를 보는지를 3D 시선 광선이나 2D 응시점으로 추정하며 오차는 도 단위다. 착용형 아이트래커는 약 1°이고, 외형 기반 방법은 눈이 보여야 해서 수 미터 안에서만 된다. → [[04-robotics/human-pose-gaze|Pose & gaze §4]]
- **Generalist policy** · 범용 정책 — One policy trained on many tasks, and often many robots, instead of one model per task. The label usually means a reusable starting point to fine-tune, not a robot that does every task unchanged. · 과제마다 모델 하나가 아니라 여러 과제(흔히 여러 로봇)로 학습한 정책 하나. 이 이름은 보통 모든 과제를 그대로 해내는 로봇이 아니라 파인튜닝할 재사용 가능한 출발점을 뜻한다. → [[01-canonical-papers/notes/4-vla/octo|Octo]], [[01-canonical-papers/notes/4-vla/rt-1|RT-1]]
- **Generalized cylinder (trajectory tube)** · 일반화 원통 — A tube of admissible trajectories around a demonstrated path, so the robot can vary its execution inside the tube instead of replaying a single trajectory. · 시연 경로 주위의 허용 궤적 튜브. 로봇이 궤적 하나를 재생하는 대신 튜브 안에서 실행을 달리할 수 있다. → [[01-canonical-papers/notes/8-construction/liang-lfd|Liang LfD 2020]]
- **Geofence** · geofence — A virtual boundary a machine may not leave. "Autonomous" in commercial claims often means one task inside a geofence with remote supervision. · 기계가 벗어나면 안 되는 가상 경계. 상업적 "자율" 주장은 흔히 원격 감독이 있는 geofence 안의 한 과제를 뜻한다. → [[05-construction-robotics/industry-deployment|Industry & Deployment §1]]
- **Geometric degeneracy** · 퇴화 — When the measured geometry cannot constrain some motion directions: point-to-plane residuals on a flat patch leave in-plane translation and rotation about the normal free, and lidar odometry fails in corridors and tunnels. · 측정된 기하가 일부 운동 방향을 제약하지 못하는 상태. 평면 패치의 점대평면 잔차는 평면 방향 병진과 법선 둘레 회전을 못 잡고, 라이다 오도메트리는 긴 복도·터널에서 실패한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §4]], [[04-robotics/state-estimation-slam|State estimation §7]]
- **Gflops** · Gflops — Billions of floating-point operations per forward pass, used by DiT as its compute measure. DiT's FID improved almost monotonically as Gflops grew, whether from larger models or more patches. · forward pass 한 번에 드는 부동소수점 연산 수(10억 단위)로, DiT가 연산량 척도로 쓴다. 모델을 키우든 패치를 늘리든 Gflops가 늘수록 DiT의 FID가 거의 단조롭게 좋아졌다. → [[01-canonical-papers/notes/6-diffusion/dit|DiT]]
- **Gimbal lock** · 짐벌 락 — In Euler angles, when the middle rotation reaches ±90° the first and third axes align, leaving one rotation direction with no angle to describe it. A defect of the coordinates, not the physical rotation. · 오일러 각에서 가운데 회전이 ±90°가 되면 첫째와 셋째 축이 겹쳐, 한 회전 방향을 나타낼 각이 사라지는 현상. 물리적 회전이 아니라 좌표의 결함이다. → [[02-foundations/se3-geometry|SE(3) §2]]
- **Global and local costmap** · 전역·지역 costmap — Nav2's two costmaps: the global one in the map frame, sized to the map, which the planner searches; the local one, a small rolling window in odom, which the controller samples. · Nav2의 두 costmap. 전역 costmap은 map 프레임에서 지도 크기로 두고 플래너가 탐색하며, 지역 costmap은 odom 프레임의 작은 이동 창으로 제어기가 샘플링한다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §4]]
- **Global nearest neighbour association** · 전역 최근접 이웃(GNN) — Choosing the one-to-one detection–track assignment with the smallest total $\sum d^2$, solved exactly by the Hungarian method (GNN in tracking papers). Unlike greedy matching, an early cheap pair cannot force a later bad one. · 합 $\sum d^2$가 가장 작은 일대일 검출–트랙 할당을 고르는 방식으로, 헝가리안 방법이 정확히 푼다(추적 논문의 GNN). 탐욕 매칭과 달리 앞선 싼 짝이 뒤의 나쁜 짝을 강요하지 못한다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **GNSS-RTK** · GNSS-RTK — Satellite positioning (GNSS) with real-time kinematic corrections from a nearby base station, reducing position error from metre level to centimetre level. · 위성 측위(GNSS)에 근처 기준국의 실시간 이동측위(RTK) 보정을 더해 위치 오차를 미터 수준에서 센티미터 수준으로 줄이는 방식. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §3]]
- **Goal-conditioned policy** · 목표 조건 정책 — A policy told what to achieve by a goal, such as a goal image or target state, rather than by a language instruction. Octo accepts either kind; guided Diffusion Policy variants sharpen goal-following. · 언어 지시가 아니라 목표 이미지나 목표 상태 같은 목표로 무엇을 이룰지 전달받는 정책. Octo는 둘 다 받으며, 가이던스를 쓴 Diffusion Policy 변형은 목표 추종을 날카롭게 한다. → [[01-canonical-papers/notes/4-vla/octo|Octo]]
- **Goal masking** · 목표 마스킹 — Hiding the goal input during training so one policy learns both goal-directed navigation and goal-free exploration, with the absence of a goal treated as a normal input rather than a separate mode. · 학습 중 목표 입력을 가려, 정책 하나가 목표 지향 주행과 목표 없는 탐색을 모두 배우게 하는 것. 목표가 없는 경우가 별도 모드가 아니라 평범한 입력이 된다. → [[01-canonical-papers/notes/9-navigation/vint-nomad|ViNT and NoMaD]]
- **Goal-oriented semantic exploration** · 목표 지향 의미 탐색 — Choosing where to explore from the goal object's category and learned priors about how objects are arranged, instead of visiting frontiers regardless of what is being sought. · 무엇을 찾는지와 무관하게 frontier를 방문하는 대신, 목표 물체의 범주와 물체 배치에 대한 학습된 사전지식으로 탐색할 곳을 고르는 것. → [[01-canonical-papers/notes/9-navigation/semexp|SemExp]]
- **Goodhart's law** · 굿하트의 법칙 — A measure that becomes a target stops being a good measure. When one group builds the simulator, evaluator and benchmark, optimization finds their shared blind spots without anyone intending it. · 목표가 된 척도는 좋은 척도이기를 그친다. 한 집단이 시뮬레이터, 평가기, 벤치마크를 모두 만들면 최적화가 아무도 의도하지 않은 채 그 공통 사각지대를 찾아낸다. → [[06-research-practice/real-world-impact|Real-World Impact §2]]
- **Gradient** · 그래디언트 — The vector of a function's partial derivatives, $\nabla f = (\partial f/\partial x_1, \ldots)$. It points uphill, perpendicular to level sets, and says how strongly each input moves the output. · 함수의 편미분을 모은 벡터. 오르막을 가리키고 등고선에 수직이며, 각 입력이 출력을 얼마나 세게 움직이는지 알려준다. → [[02-foundations/engineering-math|Engineering Math §1]], [[02-foundations/calculus-backprop|Calculus & Backprop §1]]
- **Gradient accumulation** · 그래디언트 누적 — Summing gradients over several forward passes before one optimizer step, to mimic a large batch. It matches only for per-example losses; contrastive negatives and batch statistics still see the small microbatch. · 여러 순전파의 그래디언트를 모아 optimizer step 한 번을 밟아 큰 배치를 흉내 내는 것. 예제별로 분해되는 손실에서만 대응하며, 대조 손실의 negatives와 배치 통계는 여전히 작은 마이크로배치만 본다. → [[02-foundations/ml-practice|ML Practice §6]]
- **Gradient descent** · 경사 하강 — Repeatedly stepping against the gradient, $x_{k+1} = x_k - \alpha\nabla f(x_k)$, trusting the first-order Taylor model. On a quadratic the step must stay below $2/\lambda_{max}$, and the condition number sets how slowly it converges. · 1차 테일러 모델을 믿고 그래디언트 반대 방향으로 반복해 내딛는 방법. 이차 함수에서는 스텝이 $2/\lambda_{max}$보다 작아야 하고, 조건수가 수렴이 얼마나 느린지 정한다. → [[02-foundations/optimization|Optimization §3]]
- **Granular material** · 입상 재료 — Bulk material made of many loose particles, such as soil, gravel, or sand. Its resistance during digging or loading is uncertain and differs between materials. · 흙, 자갈, 모래처럼 많은 느슨한 입자로 된 벌크 재료. 굴착이나 적재 중의 저항이 불확실하고 재료마다 다르다. → [[01-canonical-papers/notes/8-construction/wheel-loader-rl|Wheel-loader RL 2024]]
- **Graph (vertex, edge)** · 그래프(정점, 간선) — A set of vertices joined by edges, which may be directed or weighted. Road maps, occupancy grids and task dependencies are all graphs, and the chosen representation sets the cost of every search. · 간선으로 이어진 정점들의 집합이며, 간선은 방향이나 가중치를 가질 수 있다. 도로망, 점유 격자, 작업 의존 관계가 모두 그래프이고, 어떤 표현을 고르느냐가 모든 탐색의 비용을 정한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §1]]
- **Grapple (material handler)** · 그래플 — A hydraulic claw that hangs from a material handler's boom on unpowered joints. Because the swing joint is passive, the controller cannot directly command where the load releases. · 자재 핸들러의 붐 끝에 동력 없는 관절로 매달린 유압 집게. 흔들리는 관절이 수동이라 제어기가 짐이 떨어지는 위치를 직접 명령할 수 없다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §1]]
- **Grasp map (grasp matrix)** · grasp map — The matrix that sums all contact forces, expressed in a common frame, into a single net wrench on the object. Form and force closure are built on it. · 공통 프레임으로 표현한 모든 접촉력을 더해 물체에 걸리는 알짜 렌치 하나로 만드는 행렬. form closure와 force closure가 그 위에 선다. → [[04-robotics/contact-force-tactile|Contact §4]]
- **Grasp pose (6-DoF grasp, 7-DoF grasp)** · 파지 자세(6자유도·7자유도 파지) — Where and how a gripper closes. A 6-DoF grasp gives full 3D position and orientation, a 7-DoF grasp adds gripper width, and a planar grasp approaches only from above. · 그리퍼가 어디서 어떻게 닫히는가. 6자유도 파지는 3D 위치와 방향 전체, 7자유도 파지는 여기에 그리퍼 폭을 더하고, 평면 파지는 위에서만 접근한다. → [[01-canonical-papers/notes/7-robotics/anygrasp|AnyGrasp]]
- **Grasp quality metric (analytic grasp label)** · 파지 품질 지표(해석적 라벨) — A score computed from contact mechanics under assumed friction, used to label simulated grasps so a network learns to predict grasp robustness from depth images. The label inherits the model's assumptions. · 가정한 마찰 아래 접촉 역학으로 계산한 점수. 시뮬레이션 파지에 라벨로 붙여 네트워크가 깊이 영상에서 파지 강건성을 예측하게 한다. 라벨은 모델의 가정을 물려받는다. → [[01-canonical-papers/notes/7-robotics/dex-net-2|Dex-Net 2.0]]
- **Grasp tracking (temporal grasp correspondence)** · 파지 추적(관측 간 파지 대응) — Matching a grasp in one frame to the same grasp in the next, so output stays temporally smooth and can follow a moving object instead of being recomputed from scratch each frame. · 한 프레임의 파지를 다음 프레임의 같은 파지와 대응시켜, 매 프레임 새로 계산하지 않고 매끄럽게 움직이는 물체를 따라가는 것. → [[01-canonical-papers/notes/7-robotics/anygrasp|AnyGrasp]]
- **Grasp wrench space** · 파지 렌치 공간 — The convex set of wrenches a grasp's contacts can produce with bounded contact forces. Ferrari and Canny's $\epsilon$ metric is the radius of the largest ball inside it centred at the origin, i.e. the worst-case external wrench the grasp resists. · 접촉력이 유계일 때 파지의 접촉들이 만들 수 있는 렌치의 볼록 집합. Ferrari–Canny의 $\epsilon$ 지표는 그 안에 원점을 중심으로 들어가는 가장 큰 공의 반지름, 즉 파지가 버티는 최악의 외부 렌치다. → [[04-robotics/grasping|15. §4]]
- **Grating orientation threshold** · 격자 방향 판별 역치 — The rigorous measure of tactile spatial acuity, about 0.94 mm at the fingertip. The popular two-point discrimination test leaks a non-spatial cue, so it makes skin look sharper than it is. · 촉각 공간 예민도의 엄밀한 척도로, 손끝에서 약 0.94 mm다. 널리 쓰이는 2점 식별 검사는 공간 외 단서가 새어 들어가 피부를 실제보다 예민하게 보이게 한다. → [[04-robotics/tactile-visuotactile|Tactile §3]]
- **Gravity compensation** · 중력 보상 — Commanding the joint torques $g(\theta)$ that cancel gravity at the current configuration, so the arm holds itself up. A much weaker claim than full inverse dynamics, and it does not cancel unknown contact forces. · 현재 자세에서 중력을 상쇄하는 관절 토크 $g(\theta)$를 명령해 팔이 스스로를 지탱하게 하는 것. 완전한 역동역학보다 훨씬 약한 주장이고, 미지의 접촉력은 상쇄하지 못한다. → [[02-foundations/manipulator-kinematics-dynamics|Manipulator Dynamics §2]]
- **Grübler's formula** · 그뤼블러 공식 — Counts a mechanism's degrees of freedom: dof $= m(N-1-J) + \sum_i f_i$, with $m=3$ planar or 6 spatial, $N$ links counting the ground, $J$ joints of freedom $f_i$. Assumes independent constraints. · 기구의 자유도를 센다: dof $= m(N-1-J) + \sum_i f_i$. 평면 $m=3$, 공간 6, $N$은 접지를 포함한 링크 수, $J$는 자유도 $f_i$인 관절 수. 제약이 독립이라고 가정한다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **Greedy algorithm** · 그리디 알고리즘 — An algorithm that builds its answer from irrevocable choices, each the best by a simple local rule. Easy to invent and fast, but most greedy ideas are wrong, so seek a counterexample or proof. · 단순한 국소 규칙으로 가장 좋아 보이는 것을 되돌리지 않고 골라 답을 만드는 알고리즘. 떠올리기 쉽고 빠르지만 대부분의 그리디 발상은 틀리므로, 반례를 찾거나 증명해야 한다. → [[02-foundations/algorithms/greedy-mst|11.4 §1]]
- **Greedy-choice property** · 그리디 선택 속성 — The condition that some optimal solution begins with the greedy choice. Together with optimal substructure, it is what makes a greedy algorithm correct. · 어떤 최적해가 그리디 선택으로 시작한다는 조건. 최적 부분 구조와 함께 그리디 알고리즘을 옳게 만드는 성질이다. → [[02-foundations/algorithms/greedy-mst|11.4 §1]]
- **Greedy stays ahead** · 그리디가 앞서 간다 — A greedy proof: pick a progress measure and show by induction that after every step greedy is at least as far along as any other valid solution, so it cannot finish worse. · 그리디 증명법: 진행 척도를 정하고, 매 단계 뒤 그리디가 다른 어떤 유효한 해보다 뒤처지지 않음을 귀납으로 보여 더 나쁘게 끝날 수 없음을 증명한다. → [[02-foundations/algorithms/greedy-mst|11.4 §1]]
- **Ground reaction force (GRF)** · 지면 반력 — The force the ground exerts on a foot. Convex MPC for legged robots treats these forces as its decision variables, each constrained to its foot's friction pyramid. · 지면이 발에 가하는 힘. 보행 로봇 convex MPC는 이 힘들을 결정 변수로 삼고, 각각을 그 발의 마찰 피라미드 안으로 제약한다. → [[04-robotics/convex-mpc-legged|Convex MPC]]
- **Ground truth** · ground truth(실측값) — The true value or label that predictions are scored against or trained toward. A method that uses ground-truth state at test time is using oracle information. · 예측을 채점하거나 학습 목표로 삼는 참값이나 참 라벨. 테스트 때 실측 상태를 쓰는 방법은 오라클 정보를 쓰는 것이다. → [[02-foundations/ml-practice|ML Practice §5]]
- **Group** · 군(group) — A set closed under composition in which every element has an inverse. Saying rotations form the group SO(3) says only this: rotation × rotation is a rotation, and every rotation can be undone. · 합성에 닫혀 있고 모든 원소에 역원이 있는 집합. 회전이 군 SO(3)를 이룬다는 말의 뜻은 그것이 전부다. → [[02-foundations/se3-geometry|SE(3) §1]]
- **Guidance scale (guidance weight)** · 가이던스 스케일 — The CFG multiplier $s$: $s=1$ is plain conditional sampling and $s>1$ extrapolates further toward the condition. Some papers write $w=s-1$; Stable Diffusion typically uses $s$ around 5–7.5. · CFG의 배율 $s$: $s=1$은 보통의 조건부 샘플링이고 $s>1$은 조건 쪽으로 더 외삽한다. 일부 논문은 $w=s-1$로 쓰며, Stable Diffusion은 보통 $s$를 5~7.5 정도로 둔다. → [[01-canonical-papers/notes/6-diffusion/classifier-free-guidance|CFG]]
- **Gumbel-softmax** · Gumbel-softmax — A differentiable stand-in for sampling a discrete choice: a temperature-controlled softmax of noise-perturbed logits replaces the hard sample, so gradients can flow through it. · 이산 선택 샘플링의 미분 가능한 대역. 잡음을 더한 로짓에 온도로 조절하는 softmax를 적용해 딱딱한 샘플을 대신하므로 그래디언트가 통과한다. → [[02-foundations/calculus-backprop|Calculus & Backprop §6]]
- **Habitat** · Habitat — The embodied-AI simulator used for ObjectNav, VLN-CE and the Habitat Challenge, whose repository was archived read-only in October 2023. Simulation rankings need not preserve real-robot rankings. · ObjectNav, VLN-CE, Habitat 챌린지에 쓰인 embodied AI 시뮬레이터로, 챌린지 저장소는 2023년 10월 읽기 전용으로 보관되었다. 시뮬레이션 순위가 실기계 순위를 보존한다는 보장은 없다. → [[04-robotics/semantic-language-navigation|Semantic navigation §6]]
- **Hall effect** · 홀 효과 — A magnetic field pushes moving charges sideways by the Lorentz force, producing a voltage across the conductor transverse to the current. In tactile sensing it reads how far a magnet embedded in compliant material has moved, with no electrical contact and nothing to wear out. · 자기장이 로런츠 힘으로 움직이는 전하를 옆으로 밀어, 전류에 수직인 방향으로 도체 양단에 전압을 만드는 현상. 촉각에서는 유연 재료에 박힌 자석이 얼마나 움직였는지를 읽는 데 쓰이며, 전기적 접촉이 없고 닳을 부품도 없다. → [[04-robotics/tactile-visuotactile|14. Tactile & Visuotactile §2.5]]
- **Hallucination** · 환각 — Fluent output that the input or the facts do not support, such as describing objects absent from an image. RLHF improved InstructGPT's truthfulness, yet aligned models and VLMs still hallucinate. · 입력이나 사실이 뒷받침하지 않는 유창한 출력. 이미지에 없는 물체를 묘사하는 것이 그 예다. RLHF가 InstructGPT의 진실성을 높였지만 정렬된 모델과 VLM도 여전히 환각한다. → [[01-canonical-papers/notes/3-vlm/llava|LLaVA]]
- **Hamilton–Jacobi–Bellman equation (HJB)** · HJB — The continuous-time Bellman equation: at the optimum, the running cost plus the rate of change of the cost-to-go is zero once minimized over the input. For LQR it reduces to the Riccati equation. · 연속 시간 벨만 방정식: 최적에서는 입력에 대해 최소화한 순간 비용과 cost-to-go 변화율의 합이 0이다. LQR에서는 리카티 방정식으로 줄어든다. → [[04-robotics/lqr-lqg|LQR/LQG §1]]
- **Hand–arm vibration syndrome (HAVS)** · 수부진동증후군(HAVS) — Damage from prolonged tool vibration whose sensorineural part raises vibrotactile detection thresholds, so vibration alerts tested on unexposed people may not reach exposed crews. · 장기 공구 진동 노출로 생기는 손상으로, 감각신경 성분이 진동촉각 검출 임계값을 올려 비노출자에게 시험한 진동 알림이 노출 작업조에게는 안 느껴질 수 있다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §5]]
- **Hand–eye calibration** · hand–eye 보정 — Estimating the fixed camera-to-gripper (or base) transform $X$ from $AX=XB$: $A$ is gripper motion from joint encoders, $B$ the camera motion measured from a target. Needs pose pairs with different rotation axes. · 카메라–그리퍼(또는 베이스) 고정 변환 $X$를 $AX=XB$로 추정한다. $A$는 관절 엔코더로 아는 그리퍼 운동, $B$는 타깃으로 잰 카메라 운동이다. 회전축이 다른 여러 자세 쌍이 필요하다. → [[04-robotics/geometric-perception-calibration|Geometric perception §5]]
- **Handheld demonstration interface (in-the-wild data collection)** · 휴대형 시연 인터페이스 — A handheld gripper with a camera that a person uses to demonstrate tasks anywhere without a robot; the same gripper and camera go on the robot at deployment, shrinking the embodiment gap. · 사람이 로봇 없이 어디서나 과제를 시연하는 카메라 달린 휴대형 그리퍼. 배치 때 같은 그리퍼와 카메라를 로봇에 달아 embodiment 격차를 줄인다. → [[01-canonical-papers/notes/7-robotics/umi|UMI]]
- **Handover (tactile handover)** · 전달(핸드오버) — Passing an object between robot and worker. Tactile handover uses touch sensing to decide when the other party has a secure grip. · 로봇과 작업자 사이에 물체를 주고받는 일. 촉각 전달은 상대가 확실히 잡았는지를 촉각 센싱으로 판단한다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]], [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §2]]
- **Haptic rendering** · 햅틱 렌더링 — Computing the force or tactile command that makes a virtual or remote interaction perceptible to a person. Unlike visual rendering, its closed loop includes the moving human and commonly runs near 1 kHz; sampling delay can therefore turn a mathematically passive wall into an active-feeling one. · 가상·원격 상호작용을 사람이 느끼도록 힘이나 촉각 명령을 계산하는 것. 시각 렌더링과 달리 움직이는 사람이 폐루프에 포함되고 보통 1 kHz 부근에서 실행되므로, 샘플링 지연이 수학적으로 수동적인 벽도 에너지를 만드는 것처럼 바꿀 수 있다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|Rendering & Stability]]
- **Haptics** · 햅틱 — The study and engineering of touch-mediated interaction, spanning skin sensation, kinesthesia, devices, rendering algorithms, bilateral teleoperation, and human evaluation. It is broader than tactile sensing alone. · 촉각을 매개로 한 상호작용의 과학·공학으로, 피부 감각, 근감각, 장치, 렌더링 알고리즘, 양방향 원격조작, 인간 평가를 함께 포함한다. 촉각 센싱만을 뜻하지 않는다. → [[04-robotics/haptics-teleoperation/index|24. Haptics & Teleoperation]]
- **Hard-finger contact (point contact with friction)** · hard finger(마찰 있는 점 접촉) — A contact model that transmits any force inside the friction cone but no moment. A frictionless point contact transmits only normal force; a soft finger adds a moment about the contact normal. · 마찰 원뿔 안의 힘은 전달하지만 모멘트는 전달하지 않는 접촉 모델. 무마찰 점 접촉은 법선력만, soft finger는 여기에 접촉 법선 둘레의 모멘트를 더해 전달한다. → [[04-robotics/grasping|Grasping §3]]
- **Hard real-time** · hard real-time — A timing requirement that every cycle finish within a bounded deadline, not merely on average. A haptic servo closes in about 1 ms with bounded jitter, so best-effort average timing is not enough. · 평균이 아니라 매 주기가 유계 마감 안에 끝나야 한다는 타이밍 요구. 햅틱 servo는 유계 jitter로 약 1 ms마다 닫혀야 하므로 best-effort 평균 타이밍으로는 부족하다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|24.4 Rendering & Stability §1]]
- **Hardware component (hardware interface)** · 하드웨어 컴포넌트 — The ros2_control plugin that actually talks to motors, a bus or a simulator: System (multi-joint), Actuator (single joint) or Sensor (read-only). mock_components/GenericSystem loops commands back as states. · 모터, 버스, 시뮬레이터와 실제로 통신하는 ros2_control 플러그인. System(다관절), Actuator(단일 관절), Sensor(읽기 전용) 셋이 있다. mock_components/GenericSystem은 명령을 그대로 상태로 돌려준다. → [[04-robotics/ros2/from-simulation-to-hardware|25.11 Sim to Hardware §2]]
- **Hardware-in-the-loop (HIL)** · hardware-in-the-loop — Testing that includes physical compute, controllers, or hardware interfaces around a simulated plant; software-in-the-loop exercises only the software interfaces. It validates interfaces and timing, not real-world perception, contact, or task safety. · 시뮬레이션된 플랜트 주위에 실제 컴퓨트·제어기·하드웨어 인터페이스를 넣어 시험하는 것. software-in-the-loop는 소프트웨어 인터페이스만 시험한다. 인터페이스와 타이밍은 검증하지만 실제 인지·접촉·과제 안전은 검증하지 못한다. → [[04-robotics/robot-systems-deployment|Robot Systems §9]]
- **Harris corner detector** · Harris 코너 검출기 — Scores each window with the structure tensor $M$, summed products of image gradients: $R=\det M-k(\operatorname{tr}M)^2$ is positive at corners, negative on edges, near zero when flat. Shi–Tomasi uses $\min(\lambda_1,\lambda_2)$ instead. · 창마다 영상 기울기 곱의 합인 구조 텐서 $M$으로 점수를 매긴다: $R=\det M-k(\operatorname{tr}M)^2$는 코너에서 양수, 에지에서 음수, 평탄부에서 0 근처다. Shi–Tomasi는 $\min(\lambda_1,\lambda_2)$를 쓴다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Hashable** · 해시 가능 — A Python object usable as a dict key or set element: its hash never changes and equal objects hash equally. Lists, dicts and sets are not hashable; tuples of hashable items are. · dict 키나 set 원소로 쓸 수 있는 Python 객체: 해시가 바뀌지 않고 같은 객체는 같은 해시를 낸다. list, dict, set은 해시 불가능하고, 해시 가능한 원소로 된 tuple은 해시 가능하다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Hash collision** · 충돌 — Two keys mapped to the same bucket, which is unavoidable. Separate chaining keeps a small list in each bucket; open addressing probes other slots until it finds an empty one. · 두 키가 같은 버킷에 매핑되는 것으로, 피할 수 없다. 분리 체이닝은 버킷마다 작은 리스트를 두고, 개방 주소법은 빈칸이 나올 때까지 다른 칸을 탐사한다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Hash function** · 해시 함수 — A deterministic map from a key to an integer, reduced to a bucket index. Equal keys must hash equally, and a good hash function spreads realistic keys evenly across buckets. · 키를 정수로 바꾸는 결정적 함수로, 버킷 인덱스로 줄여 쓴다. 같은 키는 같은 해시를 내야 하고, 좋은 해시 함수는 현실적인 키를 버킷에 고르게 퍼뜨린다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Hash table (hash map, hash set)** · 해시 테이블(해시 맵) — A structure that turns a key into an array index with a hash function, giving lookup, insert and delete in $O(1)$ expected time but $O(n)$ in the worst case. Python's dict and set. · 해시 함수로 키를 배열 인덱스로 바꿔 조회·삽입·삭제를 기대 $O(1)$, 최악 $O(n)$에 하는 구조. Python의 dict와 set이 그것이다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Hat and vee operators** · 대괄호(hat)·vee 연산 — The bracket or hat turns a 3-vector into the skew-symmetric matrix that performs the cross product, $[\omega]v = \omega\times v$, and a twist into a 4×4 matrix; vee undoes it, pulling the numbers back out. · 대괄호(hat)는 3차원 벡터를 외적을 수행하는 반대칭 행렬로($[\omega]v = \omega\times v$), twist를 4×4 행렬로 바꾸고, vee는 그것을 되돌려 숫자를 다시 꺼낸다. → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR ch.3 §1]], [[04-robotics/modern-robotics/ch06-inverse-kinematics|MR ch.6 §2]]
- **Head** · 헤드(head) — The last small part of a network that produces the answer from backbone features. "Pretrained backbone, fine-tuned head" means only this part was adapted to the task. · 백본 특징에서 답을 만드는 신경망의 마지막 작은 부분. "사전학습 백본, 파인튜닝된 헤드"는 이 부분만 과제에 맞춰 조정했다는 뜻이다. → [[02-foundations/neural-network-basics|0.7 §1]]
- **Head pose as gaze proxy** · 머리 자세를 시선 대용으로 — Using head orientation in place of eye gaze, the only option beyond a few metres. It follows large gaze shifts but misses eye-only glances, gait-driven head motion and habitual expert actions. · 눈의 시선 대신 머리 방향을 쓰는 것으로, 수 미터 너머에서는 유일한 선택지다. 큰 시선 이동은 따라가지만 눈만 쓰는 곁눈질, 보행이 만든 머리 움직임, 숙련자의 습관 동작은 놓친다. → [[04-robotics/human-pose-gaze|Pose & gaze §4]], [[04-robotics/egocentric-perception|Egocentric §3]]
- **Heatmap regression** · 히트맵 회귀 — Predicting an image-sized score grid for each joint, trained toward a small 2D Gaussian bump at the labelled pixel, instead of regressing coordinates. It keeps spatial alternatives for occluded joints but not calibrated uncertainty. · 좌표를 직접 회귀하는 대신 관절마다 영상 크기 점수 격자를 예측하며, 표시된 픽셀의 작은 2D 가우시안 봉우리를 목표로 학습한다. 가려진 관절의 공간적 대안은 유지하지만 보정된 불확실성은 아니다. → [[04-robotics/human-pose-gaze|Pose & gaze §2]]
- **Hessian** · 헤시안 — The matrix of second partial derivatives $H_{ij} = \partial^2 f/\partial x_i\partial x_j$, the multivariable $f''$. It describes local curvature; its eigenvalues set optimization difficulty and separate minima from saddles. · 2차 편도함수의 행렬로, 다변수판 $f''$다. 국소 곡률을 나타내며, 그 고유값이 최적화 난이도를 정하고 최솟값과 안장점을 가른다. → [[02-foundations/calculus-backprop|Calculus & Backprop §1]]
- **Hidden Markov model (HMM)** · 은닉 마르코프 모델 — A Markov chain whose state is not observed; at each step the state emits an observation with some probability. The forward algorithm filters the current state and Viterbi decodes the likeliest path, both in $O(TS^2)$. · 상태가 관측되지 않는 마르코프 체인으로, 매 스텝 현재 상태가 어떤 확률로 관측값을 낸다. 순방향 알고리즘이 현재 상태를 필터링하고 비터비가 가장 그럴듯한 경로를 복원하며, 둘 다 $O(TS^2)$다. → [[02-foundations/probability|Probability §7]]
- **Hidden state** · 은닉 상태 — The fixed-size vector a recurrent network carries from step to step as its memory of everything read so far. In seq2seq the encoder's final hidden state is the whole sentence's representation. · 순환 신경망이 지금까지 읽은 모든 것의 기억으로 스텝마다 넘겨주는 고정 크기 벡터. seq2seq에서는 인코더의 마지막 은닉 상태가 문장 전체의 표현이다. → [[01-canonical-papers/notes/1-foundations/seq2seq|Seq2Seq]]
- **Hierarchical policy (skill selection)** · 계층적 정책 — A high-level policy chooses which low-level skill to run and when, while each skill handles its own motion or contact. What it can do is bounded by the skills available. · 고수준 정책이 어떤 저수준 기술을 언제 돌릴지 고르고, 각 기술이 자기 운동이나 접촉을 처리하는 구조. 할 수 있는 일은 가진 기술로 제한된다. → [[01-canonical-papers/notes/9-navigation/anymal-parkour|ANYmal Parkour]], [[01-canonical-papers/notes/8-construction/yu-imitation|Yu 2024]]
- **High-level planner and low-level policy** · 고수준 과제 계획기와 저수준 제어 정책 — A two-layer robot stack: a planner, often a language model, picks the next skill or subtask, and a learned policy executes it. SayCan is the canonical case; end-to-end VLAs merge the layers. · 두 층의 로봇 구조: 흔히 언어모델인 계획기가 다음 skill이나 하위 과제를 고르고, 학습된 정책이 그것을 실행한다. SayCan이 대표 사례이고, end-to-end VLA는 두 층을 합친다. → [[01-canonical-papers/notes/4-vla/saycan|SayCan]]
- **History and depth (keep last, keep all)** · history와 depth — The QoS policies for how many samples the middleware retains: keep last N (depth N, the queue size) or keep all within resource limits. Under keep last, new arrivals push out the oldest. · 미들웨어가 샘플을 몇 개 보관할지 정하는 QoS 정책. keep last N(큐 크기 depth N) 또는 자원 한도 안에서 keep all. keep last에서는 새 샘플이 가장 오래된 것을 밀어낸다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §2]]
- **Holonomic constraint** · 홀로노믹 제약 — A constraint on configurations that reduces the dimension of the C-space, such as the loop closure of a four-bar linkage. Nonholonomic constraints, by contrast, restrict only velocities. · C-space의 차원을 줄이는 컨피규레이션 제약. 예: 4절 링크의 닫힌 루프. 반면 비홀로노믹 제약은 속도만 제한한다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **Homogeneity (of degree 1)** · 동차성(1차) — One of the two conditions for linearity: scaling the input scales the output by the same factor, $f(ax)=a\,f(x)$ for every scalar $a$. Setting $a=0$ shows a linear map sends $0$ to $0$. · 선형성의 두 조건 중 하나. 입력을 몇 배 하면 출력도 같은 배수만큼 커진다: 모든 스칼라 $a$에 대해 $f(ax)=a\,f(x)$. $a=0$을 넣으면 선형 사상은 $0$을 $0$으로 보낸다. → [[02-foundations/engineering-math|0.5 §4.5]]
- **Homogeneous transform** · 동차 변환 — The $4\times4$ matrix packing a rotation $R$ and translation $p$, so poses compose by matrix multiplication, $T_{AC} = T_{AB}T_{BC}$, and each offset is rotated into the right frame automatically. · 회전 $R$과 이동 $p$를 한데 묶은 $4\times4$ 행렬. 자세를 행렬곱으로 합성할 수 있고 각 오프셋이 알맞은 프레임으로 자동 회전된다. → [[02-foundations/se3-geometry|SE(3) §3]]
- **Homography (H)** · homography — A 3×3 projective map between two images of the same plane, or of any scene under pure camera rotation; one of the models RANSAC fits to verify feature matches. · 같은 평면을 찍은 두 이미지(또는 카메라가 순수 회전만 한 임의 장면) 사이의 3×3 사영 사상. RANSAC이 특징 매칭을 검증할 때 맞추는 모델 중 하나다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **HOTA** · HOTA — A tracking metric: the geometric mean of a detection score and an association score, averaged over localization thresholds, so association failures such as identity switches cannot hide behind good detection. · 추적 지표로, 검출 점수와 연관 점수의 기하평균을 위치 문턱들에 걸쳐 평균한다. 그래서 정체 전환 같은 연관 실패가 좋은 검출 뒤에 숨지 못한다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **HRC (human–robot collaboration)** · HRC(인간–로봇 협업) — A robot and worker sharing space and tasks as one changing system; the question is whether the robot can infer, communicate, allocate, and recover without adding burden. · 로봇과 작업자가 공간과 과제를 공유하는 하나의 변하는 시스템. 부담을 늘리지 않고 추론·소통·배분·복구할 수 있는지가 핵심이다. → [[05-construction-robotics/hrc-worker-centered|HRC §1]]
- **Huffman coding** · 허프만 부호 — A prefix-free binary code built by repeatedly merging the two lightest trees. It minimizes expected codeword length, which is at least the entropy and less than one bit above it. · 가장 가벼운 두 트리를 반복해서 합쳐 만드는 접두사 없는 이진 부호. 기대 부호어 길이를 최소화하며, 그 길이는 엔트로피 이상이고 엔트로피보다 1비트 미만으로 크다. → [[02-foundations/algorithms/greedy-mst|11.4 §5]]
- **Human-aware motion planning** · 인간 인지 모션 계획 — Planning robot motion that accounts for where nearby people are and will move, evaluated on measured human responses such as separation, concurrent motion, and satisfaction, not only collisions. · 주변 사람의 위치와 움직임을 고려해 로봇 동작을 계획하는 것으로, 충돌뿐 아니라 이격 거리·동시 동작·만족 같은 측정된 인간 반응으로 평가한다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Human in, on, and out of the loop** · Human in/on/out of the loop — In the loop: human input is part of normal execution. On the loop: autonomy acts while a human supervises and may intervene. Out of the loop: no runtime human role within the stated scope. · in the loop는 사람 입력이 정상 실행의 일부, on the loop는 자율성이 행동하고 사람은 감독하며 개입할 수 있음, out of the loop는 명시된 범위 안에 런타임 인간 역할이 없음을 뜻한다. → [[04-robotics/hri-safety|HRI & Safety §2]]
- **Human-in-the-loop RL** · human-in-the-loop RL(사람 참여 강화학습) — Reinforcement learning on real hardware in which a human corrects the robot during training. Corrections keep learning in productive states and cut training to hours, at the cost of operator time. · 학습 중 사람이 로봇을 교정하는 실제 하드웨어 강화학습. 교정이 학습을 생산적인 상태에 붙잡아 학습을 몇 시간으로 줄이지만, 운영자 시간이 든다. → [[01-canonical-papers/notes/7-robotics/hil-serl|HIL-SERL]]
- **Human-masked ablation** · 사람 마스킹 ablation — Masking or removing the person from an intent model's input and re-evaluating. Near-equal performance means the model learned scene priors, such as crosswalk position, rather than reading the human. · 의도 모델의 입력에서 사람을 가리거나 지우고 다시 평가하는 것. 성능이 거의 같으면 모델은 사람을 읽은 것이 아니라 횡단보도 위치 같은 장면 사전확률을 배운 것이다. → [[04-robotics/human-intent-prediction|Human Intent §8]]
- **Hungarian matching (bipartite matching)** · 헝가리안(이분) 매칭 — Finding the best one-to-one pairing between predictions and ground-truth objects before computing the loss. Duplicate predictions then go unmatched and are penalized, so NMS is no longer needed. · 손실을 계산하기 전에 예측과 정답 물체 사이의 최적 1:1 짝을 찾는 것. 중복 예측은 짝을 얻지 못해 벌점을 받으므로 NMS가 필요 없어진다. → [[01-canonical-papers/notes/2-computer-vision/detr|DETR]]
- **Hybrid A\*** · Hybrid A\* — A search that expands nodes by integrating a car model for a few steering values, keeps one continuous pose per cell, and tries a Reeds–Shepp shot near the goal. Drivable, but neither complete nor optimal. · 몇 개의 조향값으로 차 모델을 적분해 노드를 확장하고, 이산 칸마다 연속 pose 하나만 남기며, 목표 근처에서 해석적 Reeds–Shepp 연결을 시도하는 A\*. 경로는 운전 가능하지만 완전하지도 최적이지도 않다. → [[04-robotics/planning-decision-making|Planning §5.5]]
- **Hybrid position/force control** · 하이브리드 위치/힘 제어 — Raibert and Craig's architecture that splits task-frame directions with a selection matrix: position control along free directions, force control along constrained ones. It assumes the task frame and contact geometry are known. · 선택 행렬로 과제 프레임의 방향을 나누는 Raibert–Craig 아키텍처: 자유 방향은 위치 제어, 구속 방향은 힘 제어. 과제 프레임과 접촉 기하를 안다고 가정한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §3]]
- **Hydraulic actuation** · 유압 구동 — Driving joints with pressurized oil through valves and cylinders. Valves, pump pressure dynamics, dead zones, and temperature drift sit between any command and joint motion. · 밸브와 실린더를 통해 압력유로 관절을 구동하는 방식. 밸브·펌프 압력 동역학·데드존·온도 드리프트가 명령과 관절 운동 사이에 끼어 있다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §1]]
- **Hydroelastic contact** · 하이드로일래스틱 접촉 — Drake's contact model, in which rigid bodies penetrate slightly as if coated with a deformable layer, giving a contact patch with a pressure distribution instead of a point force. · Drake의 접촉 모델로, 강체가 변형되는 얇은 층을 두른 것처럼 살짝 파고들어 점 힘 대신 압력 분포를 가진 접촉면을 만든다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §3]]
- **Hyperparameter** · 하이퍼파라미터 — A value a human sets before training (learning rate, batch size, depth, width), as opposed to a parameter that gradient descent learns. Ablations vary these. · 학습 전에 사람이 정하는 값; 경사 하강이 배우는 파라미터와 구분된다. → [[02-foundations/neural-network-basics|0.7 §5]]
- **Hypothesis** · 가설 — An expected relationship that can be tested, stated before results, such as "contact-time friction updates reduce grasp failures under a matched protocol". It differs from an engineering objective, which is a capability to build. · 결과 전에 세우는 시험 가능한 기대 관계(예: "짝지은 절차에서 접촉 중 마찰 갱신이 파지 실패를 줄인다"). 구축할 능력인 엔지니어링 목표와 다르다. → [[06-research-practice/research-questions-claims|Research Questions §3]]
- **ICP (Iterative Closest Point)** · ICP — A local iterative method aligning two point clouds: match nearest neighbours → solve the least-squares SE(3) → repeat. It needs an initial guess and slides in degenerate scenes such as corridors and flat walls. · 두 포인트 클라우드를 정렬하는 국소 반복법: 최근접 이웃을 짝짓고 → 최소제곱으로 SE(3)를 풀고 → 반복. 초기 추정이 필요하고, 복도나 평평한 벽처럼 축퇴된 장면에서는 해가 미끄러진다. → [[04-robotics/geometric-perception-calibration|Geometric Perception]]
- **ICRA (IEEE International Conference on Robotics and Automation)** · ICRA — The largest robotics conference, with an annual deadline and no rebuttal for regular papers, double-anonymous from 2026. It publishes submission counts but no acceptance rate. · 가장 큰 로보틱스 학회로, 연 1회 마감이며 일반 논문에는 반박문이 없고 2026년판부터 양측 익명이다. 제출·채택 건수는 발표하지만 채택률은 발표하지 않는다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **i.i.d. (independent and identically distributed)** · i.i.d. — Every sample drawn from the same distribution, none depending on the others. The CLT and Shannon's source coding theorem assume it; strongly dependent data such as language tokens violate it. · 모든 표본이 같은 분포에서 뽑히고 서로 의존하지 않는다는 가정. CLT와 섀넌의 원천 부호화 정리가 이를 전제하며, 언어 토큰처럼 강하게 의존하는 데이터는 이를 어긴다. → [[02-foundations/information-theory|Information Theory §2]]
- **IIR filter (infinite impulse response)** · IIR 필터 — A filter that feeds back past outputs, as in $y[n] = \alpha y[n-1] + (1-\alpha)x[n]$, so one impulse echoes indefinitely. Cheap, but phase is nonlinear and higher-order designs can ring or go unstable. · 과거 출력을 되먹여 임펄스 하나의 영향이 끝없이 이어지는 필터. 싸지만 위상이 비선형이고, 고차 설계는 울리거나 불안정해질 수 있다. → [[02-foundations/signal-processing|Signal Processing §4]]
- **IJRR (International Journal of Robotics Research)** · IJRR — A leading robotics journal with rolling submission and single-anonymized review, so referees see the authors. A conference extension needs a short novelty statement; more experiments alone are not substantial. · 상시 제출과 단측 익명 심사를 하는 대표 로보틱스 저널로, 심사자가 저자를 본다. 학회 논문 확장에는 짧은 novelty statement가 필요하며 실험만 더한 것은 실질적이지 않다고 본다. → [[06-research-practice/venue-strategy|Venue Strategy §2]], [[06-research-practice/venue-strategy|Venue Strategy §5]]
- **IK solution branch** · 해의 가지 — One of the distinct joint solutions IK can have for the same end-effector pose, such as elbow-up versus elbow-down. Averaging two valid branches can give an invalid configuration. · 같은 말단 자세에 대해 IK가 가질 수 있는 서로 다른 관절 해 하나(예: 팔꿈치 위 vs 아래). 유효한 두 가지를 평균하면 무효한 컨피규레이션이 될 수 있다. → [[04-robotics/modern-robotics/ch06-inverse-kinematics|MR ch.6 §1]]
- **Image captioning** · 캡셔닝 — Generating a natural-language description of an image. A standard VLM benchmark task and, in BLIP-2, the training signal that teaches the connector to speak the LLM's input language. · 이미지를 자연어로 설명하는 문장을 생성하는 과제. VLM의 표준 벤치마크이며, BLIP-2에서는 연결자가 LLM의 입력 언어를 익히게 하는 학습 신호다. → [[01-canonical-papers/notes/3-vlm/blip-2|BLIP-2]]
- **Image domain gap** · 이미지 도메인 격차 — The difference between rendered simulator images and real camera images. Gervet et al. attribute end-to-end ObjectNav falling from 77% success in simulation to 23% in real homes to it. · 시뮬레이터 렌더링 영상과 실제 카메라 영상의 차이. Gervet 등은 end-to-end ObjectNav가 시뮬레이션 77%에서 실제 주택 23%로 떨어진 원인을 이것으로 돌린다. → [[01-canonical-papers/notes/9-navigation/gervet-real-world-objectnav|Gervet 2023]]
- **ImageNav (image-goal navigation)** · 이미지 목표 내비게이션 — Navigating to the place shown in a goal image. It differs from instruction following: the robot goes where its view matches the picture, not where a sentence points. · 목표 이미지에 찍힌 장소로 주행하는 과제. 지시 따르기와 달리, 로봇은 문장이 가리키는 곳이 아니라 시야가 사진과 맞는 곳으로 간다. → [[01-canonical-papers/notes/9-navigation/vint-nomad|ViNT and NoMaD]]
- **ImageNet (ILSVRC)** · ImageNet — A dataset of about 1.2M labeled images in 1000 classes whose yearly challenge, ILSVRC, drove the CNN era; results are quoted as top-1 or top-5 error. · 1000개 클래스에 라벨된 이미지 약 120만 장의 데이터셋. 매년 열린 대회 ILSVRC가 CNN 시대를 이끌었고, 결과는 top-1·top-5 오류율로 인용된다. → [[01-canonical-papers/notes/1-foundations/alexnet|AlexNet]]
- **Imagination rollout (learning in imagination)** · 상상된 롤아웃(상상 속 학습) — A trajectory generated by a learned world model instead of the real environment. Policies trained on such rollouts need few real steps but can exploit model errors. · 실제 환경이 아니라 학습된 월드모델이 생성한 궤적. 이런 롤아웃으로 학습한 정책은 실제 스텝이 적게 들지만 모델 오류를 악용할 수 있다. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]], [[01-canonical-papers/notes/5-world-models/world-models|World Models]]
- **Imitation learning** · 모방학습 — Learning a policy from expert demonstrations broadly (BC, DAgger, IRL included); narrowly it sometimes means BC alone. The mainstream of robot manipulation. · 전문가 시연으로 정책을 학습하는 방법 전반(BC·DAgger·IRL 포함). 좁게는 BC만 가리키기도 한다. 로봇 조작의 주류다. → [[01-canonical-papers/notes/4-vla/rt-1|RT-1]]
- **Impedance control** · 임피던스 제어 — Controlling the relation between motion and force rather than either alone: the robot renders a virtual mass-spring-damper, $M_d\ddot e + D_d\dot e + K_d e = F_{ext}$, typically by computing restoring torques from motion error. · 운동이나 힘 하나가 아니라 둘의 관계를 제어하는 것: 로봇이 가상 질량-스프링-댐퍼 $M_d\ddot e + D_d\dot e + K_d e = F_{ext}$를 구현하며, 흔히 운동 오차에서 복원 토크를 계산한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §2]]
- **Impedance display** · impedance display — A haptic device that measures motion and commands force. It must feel light and backdrivable in free space, so inertia, friction, cogging, and backlash matter. An interface causality, not a synonym for impedance control. · 운동을 측정해 힘을 명령하는 햅틱 장치. 자유공간에서 가볍고 역구동되어야 하므로 관성·마찰·cogging·backlash가 중요하다. 인터페이스의 인과성이지 임피던스 제어와 같은 말이 아니다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §1]]
- **Implicit vs explicit 3D representation** · 암시적 / 명시적 표현 — An implicit representation stores a scene as a function, like NeRF's MLP queried at any point; an explicit one stores primitives (meshes, voxels, points, Gaussians), faster to render and easier to edit. · 암시적 표현은 장면을 함수로 저장하고(임의의 점에서 질의하는 NeRF의 MLP), 명시적 표현은 메시·복셀·점·가우시안 같은 프리미티브를 직접 저장해 렌더링이 빠르고 편집이 쉽다. → [[01-canonical-papers/notes/2-computer-vision/nerf|NeRF]], [[01-canonical-papers/notes/2-computer-vision/3d-gaussian-splatting|3DGS]]
- **Importance ratio (probability ratio)** · importance ratio(중요도 비율) — The ratio $\rho_t$ of the new policy's probability of an action to the data-collecting policy's. PPO clips it to $[1-\epsilon, 1+\epsilon]$ so updates stay near the policy that gathered the data. · 새 정책이 한 행동에 주는 확률과 데이터를 모은 정책이 준 확률의 비 $\rho_t$. PPO는 이를 $[1-\epsilon, 1+\epsilon]$로 클리핑해 갱신이 데이터 수집 정책 근처에 머물게 한다. → [[02-foundations/rl-basics|RL Basics §4]]
- **Impulse response** · 임펄스 응답 — A system's output when the input is a single unit spike. For a linear time-invariant system it determines everything: every output is the input convolved with it. · 입력이 단위 임펄스 하나일 때 시스템의 출력. 선형 시불변 시스템이라면 이것이 전부를 결정한다: 모든 출력은 입력과 이것의 합성곱이다. → [[02-foundations/signal-processing|Signal Processing §1]]
- **IMU (inertial measurement unit)** · IMU(관성 측정 장치) — A chip combining accelerometer and gyroscope that reports acceleration and angular velocity at high rate. Accurate over milliseconds, but a slowly wandering bias makes integrated estimates drift within minutes. · 가속도계와 자이로스코프를 묶어 가속도·각속도를 고주기로 보고하는 칩. 밀리초 단위에서는 정확하지만 천천히 떠도는 bias 때문에 적분한 추정이 분 단위로 드리프트한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **IMU preintegration** · IMU preintegration — Summarizing the many IMU samples between two keyframes into one constraint, so the optimizer does not carry every sample. · 두 keyframe 사이의 수많은 IMU 표본을 하나의 제약으로 요약해, 최적화기가 모든 표본을 지고 가지 않게 하는 기법. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Incipient slip** · 초기 미끄러짐 — The state in which a contact patch has begun slipping at its edges while its centre still sticks, before the object has moved at all. Any sensor reporting object motion is by construction too late, which makes this the cleanest argument for a dense tactile sensor. · 물체가 아직 전혀 움직이지 않았는데 접촉면의 중심은 붙어 있고 가장자리부터 미끄러지기 시작한 상태. 물체의 운동을 보고하는 센서는 구조적으로 이미 늦었고, 그래서 조밀한 촉각 센서를 쓸 가장 깨끗한 근거가 된다. → [[04-robotics/tactile-visuotactile|14. §3]]
- **In-context learning** · in-context learning — An LLM performing a new task from examples placed in its prompt, with no gradient update. It is inference-time behaviour, not training, which is why "few-shot" in an LLM paper means something different from "few-shot" in a robot-learning paper. · 프롬프트에 넣은 예시만으로, 그래디언트 갱신 없이 새 과제를 수행하는 것. 학습이 아니라 추론 시점의 거동이며, 그래서 LLM 논문의 "few-shot"은 로봇 학습 논문의 그것과 뜻이 다르다. → [[01-canonical-papers/notes/1-foundations/gpt-3|GPT-3]]
- **Independence (conditional independence)** · 독립·조건부 독립 — $A$ and $B$ are independent if $P(A,B) = P(A)P(B)$, and conditionally independent given $C$ if that factorization holds once $C$ is known. These assumptions underlie graphical models and the Markov property. · $P(A,B) = P(A)P(B)$이면 독립, $C$를 안 뒤에 그 분해가 성립하면 $C$가 주어졌을 때 조건부 독립이다. 그래프 모델과 마르코프 성질이 기대는 인수분해 가정이다. → [[02-foundations/probability|Probability §1]]
- **Independent evaluation (third-party evaluation)** · 독립 평가(제3자 평가) — Results measured by someone other than a method's authors. Most published comparisons are the authors' own reproductions of other methods; genuinely third-party numbers are worth more and often differ substantially. · 방법의 저자가 아닌 사람이 잰 결과. 출판된 비교 대부분은 저자들이 남의 방법을 직접 재현한 것이고, 진짜 제3자 수치는 더 가치 있으며 흔히 크게 다르다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §11]]
- **Independent reference (independent ground truth)** · 독립 기준 — A reference measured separately from the alignment being evaluated. Checking positions against the same model used for registration rewards agreement with the model, not with the site. · 평가 대상 정렬과 별도로 측정한 기준. 정합에 쓴 모델로 위치를 검사하면 현장이 아니라 모델과의 일치를 보상하게 된다. → [[05-construction-robotics/site-perception|Site Perception §4]]
- **Independent variable** · 독립 변수 — The factor an experiment intentionally changes, such as vision-only versus vision plus touch. · 실험에서 의도적으로 바꾸는 요인(예: 시각만 대 시각+촉각). → [[06-research-practice/experimental-design-reproducibility|Experimental Design §1]]
- **Indoor scene datasets (Gibson, MP3D, HM3D)** · 실내 장면 데이터셋 — Collections of scanned real building interiors loaded into the Habitat simulator for navigation benchmarks. A result holds for its dataset, split, and episode definition. · Habitat 시뮬레이터에 올려 내비게이션 벤치마크에 쓰는 실제 건물 내부 스캔 모음. 결과는 그 데이터셋, 분할, 에피소드 정의에서만 성립한다. → [[01-canonical-papers/notes/9-navigation/vlfm|VLFM]]
- **Inductive bias** · 귀납 편향 — Assumptions built into an architecture before seeing data, such as a CNN's locality and translation equivariance. Less bias lets a model learn more from data but demands more data. · 데이터를 보기 전에 구조에 내장된 가정. CNN의 지역성과 평행이동 등변성이 그 예다. 편향이 적으면 데이터에서 더 많이 배울 수 있지만 더 많은 데이터가 필요하다. → [[01-canonical-papers/notes/1-foundations/vit|ViT]]
- **Inflation layer (inflation radius)** · inflation 계층 — Writes lethal-equivalent cost within the robot's inscribed radius of obstacles, then exponentially decaying cost out to inflation_radius. The decay is a potential field steering planners to mid-corridor, not a safety margin. · 장애물에서 로봇 내접 반지름 안에는 충돌 비용을, 그 밖에서 inflation_radius까지는 지수적으로 줄어드는 비용을 쓴다. 이 감소 구간은 안전 여유가 아니라 플래너를 통로 가운데로 이끄는 퍼텐셜 장이다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §5]]
- **InfoNCE** · InfoNCE — A contrastive loss treating the other samples in a batch as "classes"; a lower bound on mutual information. · 배치 안의 다른 샘플들을 "클래스"로 쓰는 대조 손실. → [[02-foundations/information-theory|정보이론 §4]]
- **Information bottleneck** · Information Bottleneck(정보 병목) — Compressing a representation as much as possible while keeping what predicts a target. Clio uses a set of tasks as the target, so detail irrelevant to those tasks is merged away. · 표적을 예측하는 정보는 남기고 표현을 최대한 압축하는 원리. Clio는 과제 집합을 표적으로 삼아 그 과제와 무관한 세부를 병합해 버린다. → [[01-canonical-papers/notes/9-navigation/clio|Clio]]
- **Information matrix** · 정보 행렬 — The inverse of a covariance matrix. A zero entry means two variables are conditionally independent given the rest; graph back ends solve the estimation cost in this information form. · 공분산 행렬의 역행렬. 성분이 0이면 나머지가 주어졌을 때 두 변수가 조건부 독립이다. 그래프 back end는 추정 비용을 이 정보 형태로 푼다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **In-hand manipulation** · in-hand manipulation — Reorienting an object after grasping it without putting it down. Finger gaiting breaks and remakes contacts, while rolling and sliding can keep contact; the changing contact modes explode combinatorially, so simulation-based RL became common. · 잡은 뒤 내려놓지 않고 물체의 방향을 바꾸는 것. finger gaiting은 접촉을 깨고 다시 만들고, 구르기·미끄러짐은 접촉을 유지할 수 있다. 접촉 모드 조합이 폭발해 시뮬레이션 기반 RL이 흔해졌다. → [[04-robotics/grasping|Grasping §5]]
- **Initial-state distribution** · 초기 상태 분포 — The range of starting conditions a policy is evaluated from, such as randomized object poses. A policy always reset to the same spot is being asked an easier question. · 정책을 평가하는 시작 조건의 범위(예: 무작위화한 물체 자세). 매번 같은 자리로 리셋되는 정책은 더 쉬운 질문을 받고 있다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §11]]
- **Inlier and outlier** · 인라이어 / outlier — Data points consistent with a fitted model within a threshold, versus points that are not, such as wrong matches. RANSAC keeps the model with the most inliers; report inlier ratio, not raw match count. · 문턱 안에서 맞춘 모델과 부합하는 데이터 점과, 틀린 매칭처럼 그렇지 않은 점. RANSAC은 인라이어가 가장 많은 모델을 남긴다. 매칭 개수가 아니라 인라이어 비율을 본다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Innovation** · innovation — The measurement minus its prediction, $z-H\hat{x}^-$: how surprising the new measurement is. Its covariance $S=HP^-H^\top+R$ sets both the Kalman gain and the tracking gate. · 측정값에서 예측값을 뺀 $z-H\hat{x}^-$로, 새 측정이 예측에 비해 얼마나 뜻밖인지다. 그 공분산 $S=HP^-H^\top+R$이 칼만 이득과 추적 게이트를 함께 정한다. → [[04-robotics/state-estimation-slam|State estimation §6]], [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Inpainting** · 인페인팅 — Filling in a missing or masked region of an image so it matches the rest. Score SDE showed it can be done with an unconditional model by steering sampling, with no retraining. · 이미지의 빠지거나 가려진 영역을 나머지와 맞게 채우는 것. Score SDE는 샘플링을 조향해 재학습 없이 무조건부 모델로도 할 수 있음을 보였다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **In-place algorithm** · 제자리(in-place) 알고리즘 — An algorithm that rearranges its input within the same array, using only small extra memory, such as quicksort's $O(\log n)$ recursion stack, instead of a copy of the input. · 입력을 같은 배열 안에서 재배치하고, 입력 복사본 대신 퀵정렬의 $O(\log n)$ 재귀 스택 같은 작은 추가 메모리만 쓰는 알고리즘. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §3]]
- **Instruction tuning** · 지시 튜닝 — Fine-tuning a pretrained model on instruction–response examples so it follows requests instead of merely continuing text. LLaVA did this with 158K GPT-4-generated multimodal samples. · 사전학습 모델을 지시-응답 예시로 파인튜닝해 텍스트를 이어 쓰는 대신 요청을 따르게 하는 것. LLaVA는 GPT-4로 만든 멀티모달 샘플 15.8만 개로 이를 수행했다. → [[01-canonical-papers/notes/3-vlm/llava|LLaVA]], [[01-canonical-papers/notes/1-foundations/instructgpt|InstructGPT]]
- **Integrated automated construction site** · 통합 자동화 현장 — The third era in Bock's construction-automation taxonomy, after single-task construction robots and robot-oriented design: automated systems integrated across the site rather than working as isolated robots. · 단일 과제 건설 로봇과 robot-oriented design 다음인 Bock 분류의 세 번째 시대. 고립된 로봇이 아니라 현장 전체에 통합된 자동화 시스템이다. → [[01-canonical-papers/notes/8-construction/bock-2015|Bock 2015]]
- **Intention-aware planning** · 의도 인식 계획 — Planning robot motion around a worker's predicted next action, with uncertainty. Prediction accuracy alone does not show safer motion until integrated and tested. · 작업자의 예측된 다음 행동에 맞춰 불확실성과 함께 로봇 동작을 계획하는 것. 예측 정확도만으로는 통합·시험 전까지 더 안전한 동작을 보인 것이 아니다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Intent prediction** · 의도 예측 — Classifying a person's latent discrete decision, such as cross or not cross, before it happens. Scored with accuracy, AUC, or F1 conditioned on time-to-event, and it feeds discrete decisions like stop, warn, or yield. · 건넌다·안 건넌다 같은 사람의 잠재적 이산 결정을 일어나기 전에 분류하는 것. time-to-event로 조건화한 정확도·AUC·F1로 평가하고, 정지·경고·양보 같은 이산 결정에 쓰인다. → [[04-robotics/human-intent-prediction|Human Intent §1]]
- **Interface package** · 인터페이스 패키지 — A dedicated ament_cmake package that holds only .msg, .srv and .action definitions, so consumers depend on the types without building your nodes and dependency cycles are avoided. Usually named something_msgs or something_interfaces. · .msg·.srv·.action 정의만 담는 전용 ament_cmake 패키지. 소비자가 노드를 빌드하지 않고 타입에만 의존하게 하고 순환 의존을 막는다. 보통 이름이 _msgs나 _interfaces로 끝난다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §7]]
- **Interference fit** · 억지 끼움 — A joint in which the inserted part is slightly larger than the hole, so it must be forced in. Timber joint assembly is an example. · 삽입하는 부품이 구멍보다 약간 커서 힘으로 밀어 넣어야 하는 결합. 목재 접합 조립이 예다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Interior-point method** · 내부점법 — A constrained solver that folds inequality constraints into a barrier term and follows the barrier's central path toward the optimum. The workhorse for LPs and QPs. · 부등식 제약을 배리어 항으로 목적함수에 넣고 배리어의 중심 경로를 따라 최적점으로 가는 제약 최적화 솔버. LP와 QP의 주력이다. → [[02-foundations/optimization|Optimization §4]]
- **Interleaved image–text data** · 이미지-텍스트가 뒤섞인 시퀀스 — Web documents where images and text alternate in one sequence. Training on them lets a VLM take few-shot prompts made of several (image, text) examples. · 이미지와 텍스트가 한 시퀀스 안에서 번갈아 나오는 웹 문서 데이터. 이것으로 학습하면 VLM이 여러 개의 (이미지, 텍스트) 예시로 된 few-shot 프롬프트를 받을 수 있다. → [[01-canonical-papers/notes/3-vlm/flamingo|Flamingo]]
- **Internal covariate shift** · 내부 공변량 이동 — BatchNorm's proposed motive: each layer's input distribution keeps shifting as earlier layers update. The explanation was later challenged; a smoother loss landscape is a rival account. · BatchNorm이 내세운 동기로, 앞 층이 갱신될 때마다 각 층의 입력 분포가 계속 바뀐다는 설명. 이후 반박되었고, 손실 지형이 매끄러워진다는 설명이 경쟁한다. → [[01-canonical-papers/notes/1-foundations/batch-norm|BatchNorm]]
- **Internal force (squeeze preload)** · 예압(내력) — Opposing contact forces that produce zero net object wrench yet keep compressive preload at the contacts, making friction available against later disturbances. A large squeeze does not by itself establish force closure. · 물체의 알짜 렌치는 0이지만 접촉에 압축 예압을 유지해 이후 외란에 대한 마찰력을 마련하는 반대 방향 접촉력. 세게 조인다고 그것만으로 force closure가 성립하지는 않는다. → [[04-robotics/contact-force-tactile|Contact §4]]
- **Internal stability** · 내부 안정성 — Every input–output pair of the closed loop is stable, not just the one the loop transfer function shows. Cancelling an unstable plant pole with a controller zero leaves $L$ looking clean while the load-disturbance-to-output map stays unstable, so a stable $L$ is not a safe system. · 루프 전달함수가 보여 주는 한 쌍뿐 아니라 폐루프의 모든 입출력 쌍이 안정한 것. 불안정한 플랜트 극점을 제어기 영점으로 지우면 $L$은 깨끗해 보이지만 부하 외란에서 출력으로 가는 사상은 여전히 불안정하다. 그러므로 안정한 $L$은 안전한 시스템이 아니다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Inter-rater agreement (inter-rater reliability)** · 판독자 간 일치 — The extent to which two annotators looking at the same evidence assign the same label. Failure categories must be defined well enough to achieve it, or counts depend on who annotated. · 같은 증거를 본 두 판독자가 같은 분류를 붙이는 정도. 실패 범주는 이것이 가능할 만큼 명확히 정의해야 하며, 그렇지 않으면 집계가 판독자에 따라 달라진다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §2]]
- **Intervention (human intervention)** · 개입 — A human modifying or taking control during an autonomous attempt. Declare in advance whether it ends the attempt, and report assisted completion separately from autonomous completion. · 자율 시도 중 사람이 수정하거나 제어를 넘겨받는 것. 개입이 시도를 끝내는지 미리 정하고, 보조 완료를 자율 완료와 따로 보고한다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §5]]
- **Intervention rate (interventions per hour)** · 개입률 — How often a human must step in to correct or recover the system, or its inverse, hours per intervention. It separates continuous operation from supervised demos. · 사람이 시스템을 바로잡거나 복구하러 개입하는 빈도, 또는 그 역수인 개입당 시간. 연속 운용과 감독 시연을 가른다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §6]], [[05-construction-robotics/industry-deployment|Industry & Deployment §3]]
- **Invariant (loop invariant)** · 불변식 — A statement that holds before and after every step of a loop or data-structure update, such as "the first true position lies in [lo, hi]". Stating it proves the code correct and fixes every boundary. · 반복문이나 자료구조 갱신의 매 단계 전후에 성립하는 명제(예: "첫 참 위치는 [lo, hi]에 있다"). 이것을 말하면 코드가 옳음이 증명되고 모든 경계가 정해진다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §6]]
- **Inverse dynamics** · 역동역학 — Reading the manipulator equation right to left: given a desired acceleration, what joint torques does it require? The basis of model-based control, and the opposite direction from forward dynamics, which a simulator runs. · 매니퓰레이터 방정식을 오른쪽에서 왼쪽으로 읽기 — 원하는 가속도에 필요한 관절 토크. 모델 기반 제어의 토대이며, 시뮬레이터가 도는 순동역학의 반대 방향이다. → [[02-foundations/manipulator-kinematics-dynamics|10. §5]]
- **Inverse kinematics (IK)** · 역기구학(IK) — Finding joint angles that achieve a desired end-effector pose. Unlike forward kinematics it may have zero, one, several, or infinitely many solutions — the classical face of action multimodality. · 원하는 말단 자세를 만드는 관절 각 찾기; 해가 0개·1개·여러 개·무한일 수 있다. → [[04-robotics/modern-robotics/ch06-inverse-kinematics|MR 6장]]
- **Inverse reachability (base placement)** · inverse reachability — Inverting a reachability map into a distribution over base poses from which a given task pose is reachable, so placements can be sampled and scored. The valid set is an annulus, narrowed further by conditioning. · 도달성 지도를 뒤집어 주어진 작업 자세에 도달할 수 있는 베이스 자세의 분포를 얻어, 후보 배치를 표본으로 뽑고 점수 매기는 것. 유효 집합은 고리 모양이고 조건수를 따지면 더 좁다. → [[04-robotics/navigation-mobile-manipulation|Mobile Manipulation §3]]
- **Inverse reinforcement learning (IRL)** · 역강화학습(IRL) — Learning the reward from demonstrations instead of copying actions. Many rewards explain the same behaviour, $r = 0$ included, so each method is a principle for choosing one; the learned reward then needs RL. · 행동을 베끼는 대신 시연에서 보상을 배우는 것. $r = 0$을 포함해 같은 거동을 설명하는 보상이 많으므로 각 방법은 하나를 고르는 원리이며, 학습된 보상은 다시 RL이 필요하다. → [[02-foundations/rl-basics|RL Basics §11]]
- **IoU (intersection over union, mIoU)** · IoU — The overlap between a predicted and a true box or mask, divided by their combined area. Detection thresholds use it; mIoU averages it over classes for segmentation. · 예측과 정답 박스·마스크의 겹친 넓이를 합친 넓이로 나눈 비율. 검출의 기준 문턱으로 쓰이며, mIoU는 분할에서 이를 클래스별로 평균한 것이다. → [[02-foundations/ml-practice|ML Practice §3]]
- **IRB (Institutional Review Board)** · IRB(기관 연구윤리위원회) — The institutional committee that reviews and approves research with human participants. · 사람 참가자를 대상으로 하는 연구를 심의하고 승인하는 기관 위원회. → [[04-robotics/hri-safety|HRI & Safety §7]]
- **IROS (IEEE/RSJ International Conference on Intelligent Robots and Systems)** · IROS — A major annual robotics conference with no rebuttal for regular papers, double-anonymous from 2026. It publishes an official acceptance rate: 46% in 2025. · 연 1회 열리는 주요 로보틱스 학회로, 일반 논문에는 반박문이 없고 2026년판부터 양측 익명이다. 공식 채택률을 발표한다(2025년 46%). → [[06-research-practice/venue-strategy|Venue Strategy §2]], [[06-research-practice/venue-strategy|Venue Strategy §3]]
- **Isaac Lab (Isaac Sim)** · Isaac Lab — NVIDIA's robot-learning framework on the Isaac Sim simulator, running thousands of GPU-parallel environments. It replaces the deprecated Isaac Gym; its deformable bodies do not report contact forces. · NVIDIA의 Isaac Sim 시뮬레이터 위에서 GPU로 수천 개 환경을 병렬 실행하는 로봇 학습 프레임워크. 지원이 끝난 Isaac Gym을 대체하며, 변형체의 접촉력은 보고하지 않는다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §2]], [[06-research-practice/simulators-benchmarks-datasets|Simulators §5]]
- **ISARC** · ISARC — The International Symposium on Automation and Robotics in Construction, the field's long-running venue, whose proceedings hold the record of the 1980s–90s single-task construction robots. · 국제 건설 자동화·로보틱스 심포지엄으로, 이 분야의 오랜 학회이며 1980~90년대 단일 작업 건설로봇의 기록이 그 회보에 있다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §3]]
- **Iteration (step)** · 이터레이션(스텝) — One parameter update, computed from one minibatch. Iterations per epoch equal dataset size divided by batch size, so "500k steps" is a count of updates. · 미니배치 하나로 계산한 파라미터 갱신 1회. 에포크당 이터레이션은 데이터 수 ÷ 배치 크기이므로, "50만 스텝"은 갱신 횟수다. → [[02-foundations/neural-network-basics|0.7 §4]]
- **Iterator invalidation** · 반복자 무효화 — In C++, an iterator, pointer or reference left dangling after its container changes, as when std::vector reallocates during push_back. Using it is undefined behaviour; keep an index instead. · C++에서 컨테이너가 바뀐 뒤 반복자·포인터·참조가 허공을 가리키게 되는 것(예: push_back 중 std::vector 재할당). 사용하면 정의되지 않은 동작이므로 대신 인덱스를 보관하라. → [[02-foundations/algorithms/interview-code|11.7 §4]]
- **Jacobian** · 야코비안 — The matrix of first partial derivatives of a vector-valued function — the local linear map it looks like. In kinematics it relates joint velocities to end-effector twist; in estimation it is what a nonlinear least squares solver linearizes at each iteration. · 벡터값 함수의 1차 편도함수 행렬 — 그 함수가 국소적으로 보이는 선형 사상. 기구학에서는 관절 속도를 말단 트위스트로 잇고, 추정에서는 비선형 최소자승 솔버가 매 반복 선형화하는 대상이다. → [[02-foundations/optimization|Optimization §3.5]]
- **Jensen–Shannon divergence (JS divergence)** · Jensen–Shannon 발산 — A symmetric, bounded distance between two distributions, built by averaging each one's KL to their midpoint mixture. The original GAN game minimizes it between real and generated data at the optimal discriminator. · 두 분포 각각에서 둘의 중간 혼합분포까지의 KL을 평균해 만든 대칭적이고 유계인 거리. 원래 GAN 게임은 판별기가 최적일 때 실제와 생성 분포 사이의 이 값을 최소화한다. → [[01-canonical-papers/notes/6-diffusion/gan|GAN]]
- **Jensen's inequality** · 옌센 부등식 — For a concave function such as log, the function of an average is at least the average of the function: $\log E[X] \ge E[\log X]$. It proves KL is non-negative and yields the ELBO. · log처럼 오목한 함수에서는 평균의 함숫값이 함숫값의 평균 이상이다. KL이 음수가 아님을 증명하고 ELBO를 유도하는 데 쓰인다. → [[02-foundations/information-theory|Information Theory §3]]
- **JEPA (Joint-Embedding Predictive Architecture)** · JEPA — Encode context and target separately and predict the target's embedding rather than its pixels, so unpredictable detail drops out. I-JEPA, V-JEPA and V-JEPA 2 are the image, video and robot-planning versions. · 문맥과 타깃을 따로 인코딩하고 타깃의 픽셀이 아니라 임베딩을 예측해, 예측 불가능한 디테일이 빠지게 하는 구조. I-JEPA·V-JEPA·V-JEPA 2가 이미지·비디오·로봇 플래닝 버전이다. → [[01-canonical-papers/notes/5-world-models/jepa|JEPA]]
- **Joint** · 관절 — A connection between two links that permits relative motion: a revolute (R) joint rotates, a prismatic (P) joint slides. Names like 2R and 6R describe an arm by its joint types. · 두 링크를 잇고 상대 운동을 허용하는 연결. 회전관절(R)은 돌고 직동관절(P)은 미끄러진다. 2R, 6R 같은 이름은 관절 종류로 팔을 기술한다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **joint_states and joint_state_publisher_gui** · joint_states와 joint_state_publisher_gui — joint_states carries each joint's name and position (sensor_msgs/msg/JointState), normally from the driver or ros2_control. joint_state_publisher_gui fakes it with one slider per joint, for debugging only. · joint_states는 각 조인트의 이름과 위치(sensor_msgs/msg/JointState)를 담으며 보통 드라이버나 ros2_control이 발행한다. joint_state_publisher_gui는 조인트마다 슬라이더로 이를 흉내 내는 디버깅 전용 도구다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §6]]
- **joint_trajectory_controller** · joint_trajectory_controller — The ros2_control controller that follows a timed multi-joint trajectory, via a topic or the FollowJointTrajectory action; MoveIt executes through it. Each point's time_from_start must be nonzero. · 시간이 정해진 다관절 궤적을 토픽이나 FollowJointTrajectory 액션으로 받아 추종하는 ros2_control 제어기로, MoveIt이 이것을 통해 실행한다. 각 점의 time_from_start는 0이 아니어야 한다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §9]]
- **Journal extension** · 저널 확장 — Turning a conference paper into a journal paper. No official "30% new material" rule exists; T-RO and IJRR require new results of substantive significance, and more experiments supporting the same claim do not qualify. · 학회 논문을 저널 논문으로 키우는 것. 공식 "신규 30%" 규칙은 없다. T-RO와 IJRR은 실질적 의의가 있는 새 결과를 요구하며, 같은 주장을 받치는 실험 추가는 해당하지 않는다. → [[06-research-practice/venue-strategy|Venue Strategy §5]]
- **JPDA (joint probabilistic data association)** · JPDA — Association that never commits: it enumerates gate-allowed joint assignments, weights them by probability, and updates each track with the weighted innovations. Robust for close targets but can pull nearby tracks together. · 확정하지 않는 연관 방식: 게이트가 허용하는 결합 할당을 열거해 확률로 가중하고, 각 트랙을 가중된 innovation으로 갱신한다. 가까운 표적에 강건하지만 가까운 트랙끼리 끌어당길 수 있다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Kalman filter** · 칼만 필터 — The recursive estimator for linear dynamics with Gaussian noise: predict by pushing mean and covariance through the model, then correct toward the measurement by the Kalman gain. Minimum mean-square error under exactly those assumptions. · 가우시안 잡음이 있는 선형 동역학의 재귀 추정기. 평균과 공분산을 모델로 밀어 예측한 뒤 칼만 이득만큼 측정 쪽으로 보정한다. 바로 그 가정 아래에서 평균제곱오차가 최소다. → [[02-foundations/probability|Probability §5]]
- **Kalman gain** · 칼만 이득 — The weight deciding how far an estimate moves toward a new measurement; it follows from predicted covariance, sensor covariance, and observation geometry rather than being set by hand. · 추정값을 새 측정 쪽으로 얼마나 옮길지 정하는 가중치. 손으로 정하는 값이 아니라 예측 공분산·센서 공분산·관측 기하에서 따라 나온다. → [[02-foundations/probability|확률 §5]], [[04-robotics/state-estimation-slam|State Estimation]]
- **Karnopp friction model** · Karnopp 모델 — A stick–slip friction model: while stuck, friction equals the applied tangential force up to a static limit; beyond it the point slides with dynamic plus viscous friction; below a small velocity threshold it sticks again. · 고착–미끄럼 마찰 모델. 붙어 있는 동안 마찰력은 정지 한계까지 가해진 접선력과 같고, 한계를 넘으면 동마찰과 점성 마찰로 미끄러지며, 속도가 작은 임계값 아래로 떨어지면 다시 붙는다. → [[04-robotics/haptics-teleoperation/haptic-rendering-algorithms|24.7 Rendering Algorithms §5]]
- **KD-tree** · KD-tree — A binary tree over points that splits space by one coordinate at a time, at the median. Nearest-neighbour queries prune far subtrees, typically $O(\log n)$ in low dimension, but pruning fails in high dimension. · 한 번에 좌표 하나씩 중앙값에서 공간을 나누는 점들의 이진 트리. 최근접점 질의가 먼 서브트리를 가지치기해 저차원에서는 보통 $O(\log n)$이지만, 고차원에서는 가지치기가 무너진다. → [[02-foundations/algorithms/data-structures|11.2 §8]]
- **Keyframe** · keyframe — One of a sparse subset of frames that a SLAM back end keeps and optimizes; the rest are marginalized, which keeps the problem bounded as the session grows. · SLAM back end가 남겨서 최적화하는 성긴 프레임 부분집합의 한 원소. 나머지는 주변화해 세션이 길어져도 문제 크기를 유한하게 유지한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Keypoint** · 키포인트 — In feature matching, a repeatable, distinctive image point, such as a corner, found again after the view changes. In human pose, a body joint location given as $(u,v)$ or $(x,y,z)$. · 특징 매칭에서는 시점이 바뀌어도 다시 찾을 수 있는 반복성·변별성 있는 점(코너 등). 사람 자세에서는 $(u,v)$나 $(x,y,z)$로 준 관절 위치다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]], [[04-robotics/human-pose-gaze|Pose & gaze §1]]
- **Kinematically matched leader arm (puppeteering rig)** · 기구학이 같은 리더 암 — A small replica of the follower's kinematics that the operator backdrives, so joint angles map across directly with no inverse kinematics and no retargeting. It records positions only; GELLO and ALOHA use this approach. · 조작자가 손으로 미는, 팔로워 기구학의 축소 복제품. 관절각이 역기구학도 리타게팅도 없이 그대로 옮겨진다. 위치만 기록하며 GELLO와 ALOHA가 이 방식을 쓴다. → [[04-robotics/teleoperation-demonstration|Teleoperation §4]]
- **Kinematics** · 기구학 — The geometry of motion without forces: how joint values map to end-effector pose and velocity. Dynamics adds mass and asks which torques produce which accelerations. · 힘을 빼고 본 운동의 기하: 관절 값이 말단 자세와 속도로 어떻게 사상되는가. 동역학은 여기에 질량을 더해 어떤 토크가 어떤 가속도를 만드는지 묻는다. → [[02-foundations/manipulator-kinematics-dynamics|Manipulator Dynamics §1]]
- **Kinesthetic teaching** · 직접 교시 — Recording a demonstration by physically guiding the robot arm by hand. It needs the robot present, which ties data collection to robot availability, workspace, and cost. · 사람이 로봇 팔을 직접 잡고 움직여 시연을 기록하는 방식. 로봇이 있어야 하므로 데이터 수집이 로봇의 가용성, 작업 영역, 비용에 묶인다. → [[01-canonical-papers/notes/7-robotics/umi|UMI]]
- **Kinodynamic planning** · kinodynamic 계획 — Planning in a state space with dynamics $\dot x=f(x,u)$, so graph or tree edges are motions the machine can execute rather than straight segments. The term originally meant planning under velocity and acceleration bounds. · $\dot x=f(x,u)$인 상태 공간에서의 계획으로, 간선이 직선 구간이 아니라 기계가 실행할 수 있는 운동이다. 원래는 속도·가속도 한계 아래의 계획을 뜻했다. → [[04-robotics/planning-decision-making|Planning §5.5]]
- **KKT conditions** · KKT 조건 — First-order optimality for constrained optimization: stationarity, primal and dual feasibility, complementary slackness. Necessary at an optimum under strong duality; sufficient only when the problem is convex. · 제약 최적화의 1차 최적성 조건. 강쌍대성 아래에서 필요조건이고, 충분해지는 것은 문제가 볼록할 때뿐이다. → [[02-foundations/optimization|최적화 §4]]
- **KL balancing** · KL 균형 — A DreamerV2 trick that splits the KL loss so the prior is pulled toward the posterior more strongly than the reverse. Representations are then not dragged toward a poorly trained prior. · KL 손실을 나눠 사전분포를 사후분포 쪽으로 당기는 힘을 그 반대보다 크게 주는 DreamerV2의 요령. 표현이 덜 학습된 사전분포 쪽으로 끌려가지 않게 한다. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **KL divergence** · KL 발산 — $E_p[\log p/q]$: the extra cost of using $q$ when the truth is $p$. The mathematics of VAE regularization, RLHF penalties, and distillation. · $E_p[\log p/q]$ — 진실이 $p$인데 $q$를 썼을 때 치르는 추가 비용. VAE 정규화, RLHF 페널티, 증류의 수학이 전부 이것이다. → [[02-foundations/information-theory|정보이론 §3]]
- **KL penalty (KL regularization)** · KL 페널티 — A term in an RL objective that penalizes the KL divergence from a reference policy, usually the pretrained one. It keeps what pretraining bought and makes reward hacking less likely. · 기준 정책(보통 사전학습 정책)으로부터의 KL 발산을 벌하는 RL 목적함수 항. 사전학습이 사 준 것을 지키고 reward hacking을 덜 일어나게 한다. → [[02-foundations/rl-basics|RL Basics §9]]
- **k-means** · k-means — A clustering heuristic that alternates assigning each point to the nearest of $k$ centres and moving each centre to the mean of its assigned points. Converges quickly, has no guarantee of finding the best clustering, and is the standard example of a method used everywhere despite being a heuristic. · 각 점을 $k$개 중심 중 가장 가까운 것에 배정하고 각 중심을 배정된 점들의 평균으로 옮기는 일을 번갈아 하는 군집화 발견법. 빠르게 수렴하지만 최선의 군집을 찾는다는 보장이 없고, 발견법인데도 어디서나 쓰이는 방법의 표준 사례다. → [[02-foundations/optimization|Optimization §3.5]]
- **Knowledge distillation (teacher–student)** · 증류(distillation) — Training a student network to reproduce a teacher's outputs, typically compressing a large model into a smaller one. DINOv2 trained ViT-g and then distilled it into smaller released backbones. · 학생 신경망이 교사의 출력을 재현하도록 학습하는 것으로, 보통 큰 모델을 작은 모델로 압축한다. DINOv2는 ViT-g를 학습한 뒤 더 작은 공개 백본으로 증류했다. → [[01-canonical-papers/notes/2-computer-vision/dino|DINO]]
- **Kruskal's algorithm** · Kruskal 알고리즘 — Minimum spanning tree by sorting edges by weight and keeping each edge that joins two different components, checked with union-find. Sorting dominates, so it costs $O(E \log E)$. · 간선을 가중치로 정렬하고 서로 다른 성분을 잇는 간선만 union-find로 확인해 남기는 최소 신장 트리 알고리즘. 정렬이 지배하므로 $O(E \log E)$다. → [[02-foundations/algorithms/greedy-mst|11.4 §6]]
- **KV cache** · KV 캐시 — Storing earlier tokens' attention keys and values during generation so they are not recomputed at every step. Attention still reads the whole prefix, so decoding becomes memory-bandwidth bound rather than free. · 생성 중 이전 토큰의 어텐션 key·value를 저장해 스텝마다 다시 계산하지 않게 하는 것. 어텐션은 여전히 접두부 전체를 읽으므로 디코딩은 공짜가 아니라 메모리 대역폭에 묶인다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Label-correcting algorithm** · 레이블 수정 탐색 — The general search loop in which a vertex re-enters the open list whenever its cost label improves. Without negative cycles it is optimal for any list order; the order yields BFS, DFS, Dijkstra or A\*. · 비용 레이블이 개선될 때마다 정점이 열린 목록에 다시 들어가는 일반 탐색 루프. 음수 사이클이 없으면 목록 순서와 무관하게 최적이며, 순서에 따라 BFS, DFS, Dijkstra, A\*가 된다. → [[02-foundations/algorithms/graph-algorithms|11.6 §7]]
- **Lagrangian (Lagrange multiplier)** · 라그랑지안 — $\mathcal{L} = f + \sum_i\lambda_i g_i + \sum_j\nu_j h_j$: the objective plus each constraint weighted by a multiplier. Setting its gradient to zero finds candidate points where no feasible descent direction remains. · 목적함수에 각 제약을 승수로 가중해 더한 함수. 그래디언트를 0으로 놓으면 실행 가능한 하강 방향이 남지 않은 후보점이 나온다. → [[02-foundations/optimization|Optimization §4]]
- **Langevin dynamics** · Langevin 동역학 — A sampler that repeatedly steps along the score (toward higher probability) while adding fresh noise. Early score-based models generated this way; predictor–corrector samplers use it as the correction step. · score 방향(확률이 높은 쪽)으로 조금씩 이동하면서 매번 새 노이즈를 더하는 샘플러. 초기 score 기반 모델이 이렇게 생성했고, predictor-corrector 샘플러는 이것을 보정 스텝으로 쓴다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **Language-conditioned policy** · 언어 조건 정책 — A policy that takes a natural-language instruction as input, so one model can perform different tasks on request. RT-1 injects the instruction embedding with FiLM; per-task policies such as ACT have none. · 자연어 지시를 입력으로 받아 요청에 따라 서로 다른 과제를 하는 정책. RT-1은 지시 임베딩을 FiLM으로 주입하며, ACT 같은 과제별 정책에는 언어 조건이 없다. → [[01-canonical-papers/notes/4-vla/rt-1|RT-1]]
- **Language grounding** · 언어 접지 — Connecting words to what the robot perceives and can actually do here and now. SayCan grounds a language model's plan in skill value functions; RT-2 grounds concepts like "smallest" or "extinct animal" in objects. · 단어를 로봇이 지금 여기서 지각하고 실제로 할 수 있는 것에 연결하는 것. SayCan은 언어모델의 계획을 skill 가치 함수에 접지하고, RT-2는 "가장 작은", "멸종한 동물" 같은 개념을 물체에 접지한다. → [[01-canonical-papers/notes/4-vla/saycan|SayCan]], [[01-canonical-papers/notes/4-vla/rt-2|RT-2]]
- **Language model** · 언어모델 — A network trained to predict the next token over a large text corpus. That one objective is the foundation the VLM and VLA literature builds on — it is the "L" in both. · 큰 텍스트 말뭉치에서 다음 토큰을 예측하도록 학습한 신경망. 그 목적함수 하나가 VLM·VLA 문헌이 딛고 선 토대이고, 둘 다의 "L"이 그것이다. → [[02-foundations/neural-network-basics|Neural Network Basics §6]]
- **Language-queryable 3D map** · 언어로 질의할 수 있는 지도 — A 3D reconstruction fused with vision–language features, so goals like "between the sofa and the TV" can be looked up by text (VLMaps); ConceptFusion also accepts image, audio or click queries. · 시각-언어 특징을 융합한 3D 재구성으로, "소파와 TV 사이" 같은 목표를 텍스트로 찾는다(VLMaps). ConceptFusion은 이미지·오디오·클릭 질의도 받는다. → [[04-robotics/semantic-language-navigation|Semantic navigation §7]]
- **Lap joint (timber joinery)** · 겹침 이음(lap joint) — A timber joint in which one member slides into its mating counterpart across two bearing faces. Parts touch before seating, so assembly is an insertion problem with wedging and jamming. · 부재를 두 지지면을 따라 짝 부재에 밀어 넣는 목재 이음. 안착 전에 부재가 닿으므로 조립이 wedging과 jamming이 있는 삽입 문제가 된다. → [[01-canonical-papers/notes/8-construction/apolinarska-timber|Apolinarska timber 2021]]
- **Laplace transform** · 라플라스 변환 — $F(s) = \int_0^\infty f(t)e^{-st}dt$, which turns differentiation into multiplication by $s$, so a linear ODE becomes algebra and a system becomes a transfer function. The Z-transform is its discrete-time twin. · 미분을 $s$ 곱하기로 바꾸는 변환. 선형 미분방정식이 대수가 되고 시스템은 전달함수가 된다. Z-변환이 그 이산 시간 쌍둥이다. → [[02-foundations/engineering-math|Engineering Math §9]]
- **LaSalle's invariance principle** · LaSalle 불변 원리 — An extension of Lyapunov's method: convergence still follows when the energy-like function only never increases, provided no trajectory other than the equilibrium can stay forever where it stops decreasing. · 리아푸노프 방법의 확장: 에너지 같은 함수가 증가하지 않기만 해도, 감소가 멈추는 곳에 평형점 외의 궤적이 영원히 머물 수 없다면 수렴이 따른다. → [[04-robotics/lqr-lqg|LQR/LQG §1]]
- **Laser profiler (laser line scanner)** · 레이저 프로파일러 — A sensor that projects a laser line and measures a 2D height profile along it. Swept by a robot arm, it measures the geometry of a joint or edge precisely. · 레이저 선을 투사해 그 선을 따라 2D 높이 단면을 재는 센서. 로봇 팔로 훑으면 이음매나 모서리의 기하를 정밀하게 잰다. → [[01-canonical-papers/notes/8-construction/lundeen-2019|Lundeen 2019]]
- **Latency budget (observation-to-action latency)** · 지연 예산(관측→행동 지연) — The time from light hitting the sensor to the command taking effect, summed over exposure, inference, transport, and command stages. A high update rate does not bound how stale the acted-on data is. · 빛이 센서에 닿은 때부터 명령이 효과를 낼 때까지의 시간으로, 노출·추론·전송·명령 단계의 합이다. 행동의 근거 데이터가 얼마나 낡았는지를 정하며, 높은 갱신 주기가 이를 제한하지 않는다. → [[04-robotics/robot-systems-deployment|Robot Systems §3]]
- **Latency matching** · 지연 정합 — Compensating at execution for the robot's control and perception delays so the policy sees the observation-to-action timing it was trained on. It handles a modelled delay; a variable delay is harder. · 실행 시 로봇의 제어·인식 지연을 보상해 정책이 학습 때의 관측-행동 시간 관계를 보게 하는 것. 모델링된 지연은 다루지만 변동하는 지연은 더 어렵다. → [[01-canonical-papers/notes/7-robotics/umi|UMI]]
- **Latent action** · 잠재 행동 — An action representation discovered without supervision from the frames of unlabeled video. · 라벨 없는 비디오의 프레임 사이에서 비지도로 발견된 행동 표현. → [[01-canonical-papers/notes/5-world-models/genie|Genie]]
- **Latent diffusion (LDM)** · 잠재 디퓨전(LDM) — Running diffusion in a pretrained autoencoder's compressed latent space instead of on pixels. The autoencoder removes imperceptible detail once, so high-resolution generation becomes much cheaper. · 픽셀 대신 사전학습된 오토인코더의 압축된 잠재 공간에서 디퓨전을 돌리는 것. 오토인코더가 안 보이는 디테일을 한 번 제거해 두므로 고해상도 생성이 훨씬 싸진다. → [[01-canonical-papers/notes/6-diffusion/latent-diffusion|Latent Diffusion]]
- **Latent dynamics model** · 잠재 동역학 모델 — A model that predicts how a compressed state evolves under actions, instead of predicting raw observations. Planning and imagination run on these latents; RSSM is the standard design. · 날 관측이 아니라 압축된 상태가 행동에 따라 어떻게 변하는지 예측하는 모델. 플래닝과 상상은 이 잠재 상태 위에서 돌며, RSSM이 표준 설계다. → [[01-canonical-papers/notes/5-world-models/planet|PlaNet]]
- **Latent variable** · 잠재변수 — A variable the model uses but never observes — the code behind an image, the compressed state behind a sensor stream. Its presence is what makes $p(x)$ intractable and forces the ELBO. · 모델이 쓰지만 관측되지 않는 변수. 이것 때문에 $p(x)$가 계산 불가가 되고 ELBO가 필요해진다. → [[02-foundations/information-theory|Information Theory §5]]
- **Launch file** · launch 파일 — A recorded, version-controlled description of a whole system (nodes, parameters, remappings, namespaces, includes), written in Python, XML or YAML and run by ros2 launch, which also supervises the processes. · 시스템 전체(노드, 파라미터, 리매핑, 네임스페이스, include)를 기술해 버전 관리하는 파일. Python·XML·YAML로 쓰고 ros2 launch가 실행하며 프로세스도 감시한다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §9]]
- **Launch substitution (LaunchConfiguration)** · substitution — A value resolved when the launch system executes actions, not when the file runs, e.g. LaunchConfiguration for a launch argument or FindPackageShare for a share path. Ordinary string operations on it do not work. · 파일이 실행될 때가 아니라 launch 시스템이 action을 실행할 때 정해지는 값. 예: launch 인자를 가리키는 LaunchConfiguration, share 경로를 주는 FindPackageShare. 평범한 문자열 연산은 통하지 않는다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §10]]
- **launch_testing** · launch_testing — ROS 2's integration-test framework: a launch file starts real nodes, unittest tests run against the live graph, and post-shutdown tests check clean exit. It catches nodes that never connect. · ROS 2 통합 테스트 프레임워크. launch 파일이 실제 노드를 띄우고, 살아 있는 그래프에 unittest 테스트를 돌리고, 종료 후 테스트로 깔끔한 종료를 확인한다. 끝내 연결되지 않는 노드를 잡는다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §11]]
- **Layer** · 층(layer) — One weight matrix and bias with its activation function, $h = \sigma(Wx + b)$. A network is a stack of layers, each feeding the next. · 가중치 행렬·편향 한 쌍과 그 활성함수, $h = \sigma(Wx + b)$. 신경망은 앞 층의 출력이 다음 층으로 들어가는 층들의 쌓음이다. → [[02-foundations/neural-network-basics|0.7 §1]]
- **LayerNorm** · LayerNorm — Normalization across the feature dimension per sample; a basic Transformer building block. · 샘플별 특징 차원 정규화. → [[01-canonical-papers/notes/1-foundations/batch-norm|BatchNorm]] (비교)
- **Lazy deletion** · 지연 삭제 — A substitute for decrease-key in heaps that lack it: push a new entry with the improved key and skip stale entries when they are popped. Nearly all Python Dijkstra and A\* code works this way. · decrease-key가 없는 힙에서 쓰는 대체법: 개선된 키로 새 항목을 넣고, 꺼낸 항목이 낡았으면 건너뛴다. 거의 모든 Python Dijkstra·A\* 코드가 이렇게 짜여 있다. → [[02-foundations/algorithms/data-structures|11.2 §4]]
- **Leaderboard** · 리더보드 — A public ranking of methods scored on a shared benchmark under a shared protocol. On-site construction manipulation has none, and no shared protocol or artifacts either. · 공유 벤치마크에서 공유 프로토콜로 채점한 방법들의 공개 순위. 현장 건설 조작에는 리더보드도, 공유 프로토콜이나 실물도 없다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §6]]
- **Leader / follower (master / slave)** · 리더/팔로워 — The device the human moves and the robot that touches the world. When the leader is a kinematic replica of the follower, joint angles map across directly — no inverse kinematics and no retargeting. · 사람이 움직이는 장치와 세상에 닿는 로봇. 리더가 팔로워의 기구학적 복제품이면 관절각이 그대로 넘어가 역기구학도 리타게팅도 필요 없다. → [[04-robotics/teleoperation-demonstration|12. §2, §4]]
- **Learning curve** · 학습 곡선 — Training and validation loss or score plotted against training time. It is the first diagnostic for overfitting; in RL papers check the x-axis units, seed count, and what the shaded band means. · 학습·검증 손실이나 점수를 학습 시간에 대해 그린 곡선. 과적합의 첫 진단 도구이며, RL 논문에서는 x축 단위, 시드 수, 음영 띠의 뜻을 확인한다. → [[02-foundations/ml-practice|ML Practice §2]], [[02-foundations/rl-basics|RL Basics §10]]
- **Learning rate** · 학습률 — The step size $\alpha$ in $W \leftarrow W - \alpha\,\partial L/\partial W$. Typical values are $10^{-4}$ to $10^{-2}$; too large and training diverges, too small and it crawls. · $W \leftarrow W - \alpha\,\partial L/\partial W$의 보폭 $\alpha$. 보통 $10^{-4}$~$10^{-2}$이며, 너무 크면 발산하고 너무 작으면 기어간다. → [[02-foundations/neural-network-basics|0.7 §3]]
- **Learning-rate schedule (warmup, cosine decay)** · 학습률 스케줄 — A plan that varies the step size over training, commonly a linear warmup followed by cosine decay. Part of the recipe, so "same architecture, different schedule" is not a fair comparison. · 학습 도중 보폭을 바꾸는 계획으로, 흔히 선형 warmup 뒤 cosine decay를 쓴다. 레시피의 일부이므로 "같은 구조, 다른 스케줄"은 공정한 비교가 아니다. → [[02-foundations/ml-practice|ML Practice §6]]
- **Least squares** · 최소제곱 — Choosing $x$ to minimize $\lVert Ax - b\rVert^2$ when $Ax = b$ has no exact solution. The answer solves the normal equations, and the residual is perpendicular to every column of $A$ (an orthogonal projection). · $Ax = b$의 정확한 해가 없을 때 $\lVert Ax - b\rVert^2$를 최소화하는 $x$를 고르는 것. 해는 정규방정식을 풀며, 잔차는 $A$의 모든 열에 수직이다(직교 투영). → [[02-foundations/linear-algebra|Linear Algebra §2]]
- **Legible motion** · 읽기 쉬운 움직임 — Robot motion whose goal an observer can read early, so a worker can anticipate where the robot is going. · 보는 사람이 목표를 일찍 알아챌 수 있는 로봇 동작으로, 작업자가 로봇이 어디로 갈지 예측하게 해 준다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Lens distortion** · 왜곡 — A real lens's departure from ideal pinhole projection, so straight lines image as curves. Modelled by extra intrinsic parameters estimated during calibration from checkerboard or target views. · 실제 렌즈가 이상적 핀홀 투영에서 벗어나 직선이 곡선으로 찍히는 현상. 체커보드나 타깃 촬영으로 보정할 때 추정하는 추가 intrinsic 파라미터로 모델링한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §5]]
- **Levenberg–Marquardt** · Levenberg–Marquardt — Gauss–Newton plus a trust penalty: $x^+ = x - (J^\top J + \lambda I)^{-1}J^\top f$. The $\lambda I$ keeps the matrix invertible when $J$ loses rank and shortens the step when the affine model cannot be trusted, fixing both of Gauss–Newton's failure modes. Damped least squares inverse kinematics is exactly this. · Gauss–Newton에 신뢰 벌점을 더한 것: $x^+ = x - (J^\top J + \lambda I)^{-1}J^\top f$. $\lambda I$는 $J$가 계수를 잃어도 행렬이 역을 갖게 하고 아핀 모델을 믿을 수 없을 때 스텝을 줄여, Gauss–Newton의 두 실패 방식을 모두 고친다. 감쇠 최소자승 역기구학이 정확히 이것이다. → [[02-foundations/optimization|Optimization §3.5]]
- **LiDAR** · LiDAR(라이다) — A sensor that measures ranges directly by laser time-of-flight, giving sparse point clouds. Weather and motion distort it, and ring spacing on the ground widens with distance, so far terrain gets few returns. · 레이저 비행시간으로 거리를 직접 재서 성긴 포인트 클라우드를 주는 센서. 날씨와 운동이 왜곡을 주고, 지면의 링 간격이 거리에 따라 벌어져 먼 지형에는 반사점이 거의 없다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2]], [[04-robotics/traversability-off-road|Traversability §3]]
- **Lidar odometry (LO, LIO)** · 라이다(-관성) 오도메트리 — Motion estimation from successive lidar scans, fused with an IMU in the lidar-inertial variant. It fails in geometrically degenerate places such as long corridors, open fields and tunnels. · 연속된 라이다 스캔으로 운동을 추정하며, 라이다-관성 변형은 IMU를 함께 융합한다. 긴 복도·트인 벌판·터널처럼 기하적으로 퇴화한 곳에서 실패한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Lifecycle manager (nav2_lifecycle_manager)** · 라이프사이클 매니저 — A node that brings a list of lifecycle nodes through configure and activate in order at startup, and down in reverse at shutdown, holding a bond with each. · 라이프사이클 노드 목록을 기동 시 순서대로 configure와 activate로 올리고 종료 시 역순으로 내리며, 각 노드와 bond를 유지하는 노드. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §8]]
- **Lifecycle node (managed node)** · 관리형(라이프사이클) 노드 — A node with a state machine: unconfigured, inactive, active, finalized, moved by configure, activate, deactivate, cleanup and shutdown transitions. Its publishers transfer nothing until active. · 상태 기계를 가진 노드. unconfigured, inactive, active, finalized 상태를 configure, activate, deactivate, cleanup, shutdown 전이로 오간다. active가 되기 전에는 퍼블리셔가 아무것도 보내지 않는다. → [[04-robotics/ros2/services-actions-parameters|25.3 Services & Actions §8]]
- **Likelihood ratio test** · 우도비 검정 — Choosing between two hypotheses by comparing $p(y \mid H_1)/p(y \mid H_0)$ with a threshold. Rules differ only in the threshold: prior odds for MAP, a fixed false-alarm rate for Neyman–Pearson. · 두 가설 중 하나를 우도비와 문턱값을 비교해 고르는 검정. 규칙은 문턱값만 다르다: MAP은 사전 확률 비, 네이만–피어슨은 고정된 오경보율. → [[02-foundations/probability|Probability §6]]
- **Likert scale** · 리커트 척도(1~5점 척도) — A questionnaire rating on ordered points, such as 1 (strongly disagree) to 5 (strongly agree). Mean ratings can rank factors but are not causal estimates. · 1(전혀 아니다)부터 5(매우 그렇다)처럼 순서 있는 점수로 매기는 설문 척도. 평균 점수로 요인 순위를 매길 수 있지만 인과 추정은 아니다. → [[01-canonical-papers/notes/8-construction/davila-delgado-2019|Davila Delgado 2019]]
- **Limitations section** · Limitations — The part of a paper stating the boundary of validity: untested environments, data and hardware dependencies, interventions and resets, failure modes. It should predict where the claim may stop holding, not promise future work. · 유효성의 경계를 밝히는 논문의 부분: 시험하지 않은 환경, 데이터·하드웨어 의존성, 개입과 리셋, 실패 방식. 미래 연구를 약속하는 대신 주장이 어디서 성립하지 않을지 예측해야 한다. → [[06-research-practice/scientific-writing-peer-review|Scientific Writing §6]]
- **Limit cycle** · 극한주기 — An isolated closed trajectory in state space that a nonlinear system returns to after a small disturbance, with no external clock setting its period. A steady walking gait is one, which is why passive dynamic walkers work on gravity alone and why gait stability is judged by a return map rather than by staying near a point. · 작은 교란 뒤에 비선형계가 되돌아오는, 상태 공간 안의 고립된 닫힌 궤적이며 주기를 정해 주는 외부 시계가 없다. 정상 보행 걸음새가 그것이고, 그래서 수동 동보행기가 중력만으로 걷고 걸음새 안정성을 한 점 근처에 머무는 것이 아니라 복귀 사상으로 판정한다. → [[04-robotics/legged-locomotion|18. Legged Locomotion §1.5]]
- **Linear inverted pendulum model (LIPM)** · 선형 도립진자 모형 — A walking model that treats the robot as a point mass held at constant height $z$, so its balance dynamics are linear with $\omega_0=\sqrt{g/z}$. The capture point is computed from it. · 로봇을 일정 높이 $z$에 있는 점질량으로 보는 보행 모형으로, 균형 동역학이 $\omega_0=\sqrt{g/z}$로 선형이 된다. capture point가 이 모형에서 계산된다. → [[04-robotics/legged-locomotion|Legged Locomotion §1.5]]
- **Linearity (superposition)** · 선형성 — A map $f$ is linear when it is both additive, $f(x+y)=f(x)+f(y)$, and homogeneous, $f(ax)=a\,f(x)$; together, $f(ax+by)=a\,f(x)+b\,f(y)$. Matrices, differentiation, expectation and LTI systems are linear; $2x+1$, $x^2$ and ReLU are not. · 사상 $f$가 가법적 $f(x+y)=f(x)+f(y)$이고 동차적 $f(ax)=a\,f(x)$이면 선형이다. 합치면 $f(ax+by)=a\,f(x)+b\,f(y)$. 행렬, 미분, 기댓값, LTI 시스템은 선형이고 $2x+1$, $x^2$, ReLU는 아니다. → [[02-foundations/engineering-math|0.5 §4.5]], [[02-foundations/linear-algebra|Linear Algebra §1]]
- **Linearization** · 선형화 — Replacing nonlinear dynamics $\dot x=f(x,u)$ by their first-order Taylor expansion at an operating point, with $A=\partial f/\partial x$ and $B=\partial f/\partial u$. Every linear-control guarantee then holds only near that point. · 비선형 동역학 $\dot x=f(x,u)$를 운용점에서의 1차 테일러 전개로 바꾸는 것, $A=\partial f/\partial x$, $B=\partial f/\partial u$. 그러면 모든 선형 제어 보장은 그 점 근처에서만 성립한다. → [[04-robotics/control-theory-ce397|Control Theory §2]]
- **Linear map** · 선형 사상 — A function with $W(ax + by) = aWx + bWy$; every matrix is one. Linear layers, attention projections, and embedding lookups are linear maps: each row a pattern matched, each column a direction mixed. · $W(ax + by) = aWx + bWy$를 만족하는 함수이며 모든 행렬이 그렇다. 선형 층, 어텐션 투영, 임베딩 조회가 모두 이것이고, 행은 맞춰 보는 패턴, 열은 섞이는 방향이다. → [[02-foundations/linear-algebra|Linear Algebra §1]]
- **Linear probe (linear evaluation, k-NN evaluation)** · linear probing(선형 평가) — Judging a pretrained backbone by freezing it and training only a linear classifier (or running k-NN) on its features. MAE is strong after fine-tuning but weaker under linear probing than DINO. · 사전학습 백본을 얼리고 그 특징 위에 선형 분류기만 학습(또는 k-NN)해 품질을 재는 평가. MAE는 파인튜닝 후에는 강하지만 linear probing에서는 DINO보다 약하다. → [[01-canonical-papers/notes/1-foundations/mae|MAE]]
- **Linear quadratic Gaussian (LQG)** · LQG — LQR with Gaussian noise and partial observation; the optimal controller is a Kalman filter feeding an LQR. Nominally optimal, but it has no guaranteed robustness margins. · 가우시안 잡음과 부분 관측을 더한 LQR. 최적 제어기는 칼만 필터가 LQR에 추정값을 공급하는 구조다. 공칭 최적이지만 보장된 강건성 여유는 없다. → [[04-robotics/lqr-lqg|LQR/LQG §4]]
- **Linear quadratic regulator (LQR)** · LQR — The optimal controller for linear dynamics and quadratic cost $\int(x^\top Qx + u^\top Ru)\,dt$: a constant state feedback $u=-Kx$ with $K=R^{-1}B^\top P$, where $P$ solves the Riccati equation offline. · 선형 동역학과 이차 비용 $\int(x^\top Qx + u^\top Ru)\,dt$에 대한 최적 제어기: $K=R^{-1}B^\top P$인 상수 상태 피드백 $u=-Kx$이고, $P$는 리카티 방정식을 오프라인에서 푼 해다. → [[04-robotics/lqr-lqg|LQR/LQG]]
- **Linear temporal logic (LTL)** · LTL(시간 논리) — Logic judged over whole runs of discrete steps, adding next ($\mathsf{X}$), eventually ($\mathsf{F}$), always ($\mathsf{G}$), and until ($\mathsf{U}$) to Boolean propositions. Safety patterns say something bad never happens; liveness patterns say something good keeps happening. · 한 순간이 아니라 이산 단계로 된 실행 전체에 대해 참·거짓을 판정하는 논리로, 불리언 명제에 다음($\mathsf{X}$)·언젠가($\mathsf{F}$)·항상($\mathsf{G}$)·까지($\mathsf{U}$)를 더한다. 안전성 패턴은 나쁜 일이 결코 없음을, 활성 패턴은 좋은 일이 계속 일어남을 말한다. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Link** · 링크 — One rigid body of a mechanism, connected to others by joints. Grübler's formula counts the fixed ground as a link too. · 기구를 이루는 강체 하나로, 관절로 다른 링크와 연결된다. 그뤼블러 공식은 고정된 접지도 링크로 센다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **Linked list** · 연결 리스트 — A sequence of nodes, each pointing to the next (and, if doubly linked, the previous). Inserting or removing at a node you hold is $O(1)$, but reaching the $i$-th element is $O(n)$. · 노드마다 다음(이중이면 이전) 노드를 가리키는 시퀀스. 이미 쥔 노드에서의 삽입·삭제는 $O(1)$이지만, $i$번째 원소에 가려면 $O(n)$이 든다. → [[02-foundations/algorithms/data-structures|11.2 §2]]
- **Liveliness and lease duration (QoS)** · liveliness와 lease duration — How a publisher is judged still alive: it must assert liveliness within the lease duration, or subscriptions get a liveliness-changed event, which catches a publisher that died quietly. · 퍼블리셔가 살아 있다고 판정하는 방식. lease duration 안에 살아 있음을 알려야 하고, 그렇지 않으면 서브스크립션이 liveliness changed 이벤트를 받아 조용히 죽은 퍼블리셔를 잡는다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §2]]
- **LLM-as-a-judge** · LLM 심판 평가 — Using a strong language model to score other models' outputs. It is cheap but can be circular: LLaVA was judged by GPT-4, the same model that wrote its training data. · 강한 언어모델로 다른 모델의 출력을 채점하는 평가. 싸지만 순환적일 수 있다: LLaVA는 학습 데이터를 쓴 GPT-4에게 채점받았다. → [[01-canonical-papers/notes/3-vlm/llava|LLaVA]]
- **Load factor** · 적재율 — The number of stored keys per bucket in a hash table, $\alpha = n/m$. Lookups slow down as it grows, so tables resize past a threshold and re-insert every key. · 해시 테이블에서 버킷당 저장된 키 수, $\alpha = n/m$. 커질수록 조회가 느려지므로 테이블은 임계값을 넘으면 크기를 늘리고 모든 키를 다시 넣는다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Logistic regression** · 로지스틱 회귀 — A linear model passed through a sigmoid to output a probability, trained with cross-entropy. The simplest classifier that is still a neural network — one layer, no hidden units — and the standard baseline optimization papers report against. · 선형 모델을 시그모이드에 통과시켜 확률을 내고 교차 엔트로피로 학습하는 방법. 여전히 신경망인 가장 단순한 분류기이고 — 층 하나, 은닉 유닛 없음 — 최적화 논문들이 기준선으로 보고하는 표준이다. → [[02-foundations/neural-network-basics|Neural Network Basics §3]]
- **Log-odds** · 로그 승산(log-odds) — $\ell=\log\frac{p}{1-p}$, mapping probability 0.5 to 0 and 0 or 1 to minus or plus infinity. Bayesian occupancy updates become additions; because it is unbounded, implementations clamp it so the map can still change. · $\ell=\log\frac{p}{1-p}$로, 확률 0.5를 0으로, 0이나 1 근처를 음·양의 무한대 쪽으로 보낸다. 베이즈 점유 갱신이 덧셈이 되며, 유계가 아니므로 지도가 계속 바뀔 수 있게 구현은 범위를 클램핑한다. → [[04-robotics/planning-decision-making|Planning §2]]
- **Log-sum-exp** · Log-sum-exp — Computing $\log\sum_i e^{x_i}$ as $x_{max} + \log\sum_i e^{x_i - x_{max}}$, factoring out the largest term. Exact, and it cannot overflow; stable softmax and cross-entropy implementations use it. · 가장 큰 항을 밖으로 빼서 $\log\sum_i e^{x_i}$를 계산하는 항등식. 근사가 아니라 정확하고 오버플로가 나지 않으며, 안정적인 softmax와 교차 엔트로피 구현이 이것을 쓴다. → [[02-foundations/engineering-math|Engineering Math §6]]
- **Longest common subsequence (LCS)** · 최장 공통 부분 수열 — The longest sequence appearing in order, not necessarily contiguously, in two sequences, found by an $O(nm)$ DP. It underlies diff and can compare the action sequences of two demonstrations. · 두 시퀀스 모두에 순서대로(연속일 필요는 없이) 나타나는 가장 긴 수열로, $O(nm)$ DP로 구한다. diff의 기반이며 두 시연의 행동 시퀀스 비교에도 쓴다. → [[02-foundations/algorithms/dynamic-programming|11.5 §4]]
- **Longest increasing subsequence (LIS)** · 최장 증가 부분 수열 — The longest strictly increasing subsequence of a sequence. A DP over "ends at index i" gives $O(n^2)$; keeping the smallest tail per length with binary search gives $O(n \log n)$. · 수열에서 가장 긴 엄격히 증가하는 부분 수열. "인덱스 i에서 끝나는" DP는 $O(n^2)$이고, 길이별 가장 작은 꼬리를 이진 탐색으로 유지하면 $O(n \log n)$이다. → [[02-foundations/algorithms/dynamic-programming|11.5 §5]]
- **Long-horizon task** · 장기 과제 — A task that takes many decisions over minutes, such as searching an unseen house or chaining several skills. It needs memory of what was already ruled out, which a purely reactive policy lacks. · 처음 보는 집 탐색이나 여러 기술 연결처럼 수 분에 걸쳐 많은 결정이 필요한 과제. 이미 배제한 곳을 기억해야 하는데 순수 반응형 정책에는 그 기억이 없다. → [[01-canonical-papers/notes/9-navigation/semexp|SemExp]]
- **Loop closure** · loop closure(루프 폐쇄) — Recognizing that the robot has returned to a previously mapped place, and adding that constraint so accumulated drift can be corrected. A *false* closure corrupts the whole map. · 이전에 지도화한 장소로 돌아왔음을 인식해 제약을 추가하고 누적 drift를 보정하는 것; 잘못된 closure 하나가 지도 전체를 망친다. → [[04-robotics/state-estimation-slam|State Estimation §7]]
- **Loop transfer function** · 루프 전달함수 — $L = PC$, the product of plant and controller taken once around the loop. Closed-loop stability is read off this open-loop object, because changing the controller moves $L$ directly while its effect on the closed-loop response is tangled. · 플랜트와 제어기를 곱해 루프를 한 바퀴 돈 것, $L = PC$. 폐루프 안정성을 이 개루프 대상에서 읽는다. 제어기를 바꾸면 $L$이 곧장 움직이지만 폐루프 응답에 미치는 영향은 뒤엉켜 있기 때문이다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Loosely vs tightly coupled** · loosely / tightly coupled — Loosely coupled fusion combines finished estimates from each subsystem; tightly coupled fusion jointly uses lower-level measurements, preserving more information at the cost of model and implementation complexity. · loosely coupled는 하위 시스템이 완성한 추정값을 융합하고, tightly coupled는 저수준 측정을 공동으로 써 정보를 더 보존하는 대신 모델·구현이 복잡해진다. → [[04-robotics/state-estimation-slam|State estimation §8]]
- **LoRA** · LoRA — Parameter-efficient fine-tuning that learns only a low-rank update $\Delta W = BA$ on top of frozen weights. · 얼린 가중치에 저랭크 업데이트만 학습하는 파라미터 효율 파인튜닝. → [[01-canonical-papers/notes/1-foundations/lora|LoRA]]
- **Loss function** · 손실함수 — The single number measuring how wrong a prediction is (MSE for continuous outputs, cross-entropy for categories). Training minimizes it; it is not the same as the evaluation metric. · 예측이 얼마나 틀렸는지를 재는 숫자 하나(연속 출력이면 MSE, 범주면 교차 엔트로피). 학습이 최소화하는 대상이며, 평가 지표와 같지 않다. → [[02-foundations/neural-network-basics|0.7 §3]]
- **Lower bound and upper bound (bisect_left, bisect_right)** · lower bound와 upper bound — In a sorted array, the first index whose value is at least x, and the first whose value exceeds x. Their difference counts the copies of x. · 정렬된 배열에서 값이 x 이상인 첫 인덱스와 x보다 큰 첫 인덱스. 둘의 차가 x의 개수다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §6]]
- **Low-pass filter** · 저역통과 필터 — A filter that passes slow changes and attenuates fast ones, the usual remedy for sensor noise. Causal smoothing costs phase lag, so aggressive low-passing of a velocity estimate can destabilize the controller that uses it. · 느린 변화는 통과시키고 빠른 변화는 약화시키는 필터로, 센서 노이즈에 흔히 쓴다. 인과적 평활은 위상 지연을 치르므로 속도 추정을 과하게 거르면 그것을 쓰는 제어기가 불안정해질 수 있다. → [[02-foundations/signal-processing|Signal Processing §4]]
- **LRA (linear resonant actuator)** · LRA(선형 공진 액추에이터) — A vibrotactile actuator, like a voice coil, offering faster and richer waveform control than an ERM, but shaped by its own transfer function: strong near resonance, weak away from it. · voice coil처럼 ERM보다 빠르고 풍부한 파형 제어를 주는 진동 촉각 액추에이터. 다만 자기 전달함수에 따라 공진 근처에서는 강하고 멀어지면 약하다. → [[04-robotics/haptics-teleoperation/tactile-display-design|24.2 Tactile Display §1]]
- **LSTM (long short-term memory)** · LSTM — A recurrent network with a gated memory cell whose self-connection lets error flow back unchanged, so it learns dependencies over long sequences. The forget gate was added in 1999. · 게이트가 달린 메모리 셀을 가진 순환 신경망. 셀의 자기 연결로 오차가 변형 없이 거꾸로 흘러 긴 시퀀스의 의존성을 학습한다. forget 게이트는 1999년에 추가됐다. → [[01-canonical-papers/notes/1-foundations/lstm|LSTM]]
- **LTI system (linear time-invariant)** · LTI(선형 시불변) 시스템 — A system where scaled or summed inputs give scaled or summed outputs and a delayed input gives a delayed output. It is fully described by its impulse response and acts by convolution. · 입력을 스케일하거나 더하면 출력도 그렇게 되고, 입력을 늦추면 출력도 똑같이 늦어지는 시스템. 임펄스 응답으로 완전히 기술되며 합성곱으로 작동한다. → [[02-foundations/signal-processing|Signal Processing §1]]
- **Lyapunov function** · 리아푸노프 함수 — A scalar energy-like function of the state that is positive everywhere except at the equilibrium and decreases along every closed-loop trajectory. Finding one proves stability without solving the dynamics. · 평형점을 뺀 모든 곳에서 양수이고 모든 폐루프 궤적을 따라 줄어드는, 에너지 같은 상태의 스칼라 함수. 이런 함수를 찾으면 동역학을 풀지 않고도 안정성이 증명된다. → [[04-robotics/lqr-lqg|LQR/LQG §1]]

## M–P

- **Mahalanobis distance** · 마할라노비스 거리 — $d^2 = (x-\mu)^\top\Sigma^{-1}(x-\mu)$: distance in units of the covariance, equal to Euclidean distance after whitening. A displacement against the correlation pattern counts as more surprising; trackers gate measurements with it. · 공분산 단위로 잰 거리로, 백색화한 뒤의 유클리드 거리와 같다. 상관 패턴을 거스르는 변위일수록 더 놀랍다고 친다. 추적기가 측정값을 게이팅할 때 쓴다. → [[02-foundations/probability|Probability §6]]
- **Manifold hypothesis** · 다양체 가설 — The assumption that real data such as images occupy a low-dimensional surface inside their huge raw space. It is the implicit justification for latent spaces in VAEs and latent diffusion. · 이미지 같은 실제 데이터가 거대한 원시 공간 안의 저차원 곡면 위에 놓여 있다는 가정. VAE와 잠재 디퓨전이 잠재 공간을 쓰는 암묵적 근거다. → [[02-foundations/linear-algebra|Linear Algebra §6]]
- **Manipulation primitive** · 조작 원시동작 — A basic manipulation action such as grasp, insert, push, drill, or track a seam. Decomposing a construction task into primitives shows which sensing and control mode it needs. · 파지·삽입·밀기·드릴·이음선 추적 같은 기본 조작 동작. 건설 작업을 원시동작으로 분해하면 필요한 센싱과 제어 모드가 보인다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Manipulator equation** · 매니퓰레이터 방정식 — $M(\theta)\ddot\theta + C(\theta,\dot\theta)\dot\theta + g(\theta) = \tau$, with an added $J^\top\mathcal{F}_{\text{ext}}$ when something is touched. Every model-based manipulation control claim is a statement about one of these terms. · 무언가에 닿으면 $J^\top\mathcal{F}_{\text{ext}}$가 더해지는 로봇 팔의 운동 방정식. 모델 기반 조작 제어의 모든 주장은 이 항들 중 하나에 관한 진술이다. → [[02-foundations/manipulator-kinematics-dynamics|10. §2]]
- **MAP estimation (maximum a posteriori)** · MAP — Maximize log-likelihood plus log-prior. A zero-mean Gaussian prior on the weights adds an L2 penalty to the loss, so weight decay is a prior in disguise; a Laplace prior gives sparsity. · 로그 우도에 로그 사전을 더해 최대화하는 추정. 가중치에 평균 0인 가우시안 사전을 두면 손실에 L2 벌점이 붙어 weight decay가 변장한 사전이 되고, 라플라스 사전은 희소성을 준다. → [[02-foundations/probability|Probability §4]]
- **mAP (mean average precision)** · mAP — The detection metric averaging, over classes, the area under each class's precision–recall curve. It is computed at IoU 0.5 (PASCAL VOC) or averaged over IoU 0.50–0.95 (COCO); check which before comparing. · 클래스마다 정밀도–재현율 곡선 아래 넓이(평균 정밀도)를 구해 클래스에 걸쳐 평균한 검출 지표. IoU 0.5 기준(PASCAL VOC)이거나 IoU 0.50–0.95 평균(COCO)이니, 비교 전에 확인한다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Marginalization** · 주변화 — Removing variables such as old frames from an estimation problem while folding their information into the survivors through a Schur complement. The result is denser, which is why sliding windows cap their length. · 오래된 프레임 같은 변수를 추정 문제에서 없애되, 그 정보를 Schur 보수로 남은 변수에 접어 넣는 일. 결과가 더 조밀해지므로 슬라이딩 윈도우가 길이를 제한한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Marginal vs joint coverage (randomization)** · 개별 포함률과 동시 포함률 — Each randomized parameter may cover most of its real range, yet the chance that all real parameters fall inside every range at once is much smaller, like $0.8^6 \approx 26\%$. · 무작위화한 파라미터마다 실제 범위 대부분을 덮어도, 모든 실제 파라미터가 동시에 모든 범위 안에 들 확률은 $0.8^6 \approx 26\%$처럼 훨씬 작다. → [[05-construction-robotics/sim-to-real|Sim-to-Real §4]]
- **Markov chain** · 마르코프 체인 — A sequence of states where the next one depends only on the current one, not on how you arrived. The Markov property is what makes an MDP's value functions well defined, and "no Markov chain needed" in a generative-model paper means it samples in one pass instead of running a chain to equilibrium. · 다음 상태가 도착 경로가 아니라 현재 상태에만 의존하는 상태의 열. MDP의 가치 함수가 잘 정의되게 만드는 것이 이 마르코프 성질이고, 생성 모델 논문의 "마르코프 연쇄가 필요 없다"는 평형까지 연쇄를 돌리는 대신 한 번에 표집한다는 뜻이다. → [[02-foundations/rl-basics|RL Basics §1]]
- **Masked autoencoder (MAE)** · MAE(마스크 오토인코더) — Self-supervised image pretraining that hides 75% of patches, encodes only the visible ones, and has a light decoder reconstruct the missing pixels. The decoder is discarded afterwards. · 패치의 75%를 가리고 보이는 패치만 인코딩한 뒤, 가벼운 디코더가 가려진 픽셀을 복원하게 하는 이미지 자기지도 사전학습. 학습 후 디코더는 버린다. → [[01-canonical-papers/notes/1-foundations/mae|MAE]]
- **Masked language modeling (MLM)** · masked LM(MLM) — Hiding about 15% of tokens and training the model to predict them from context on both sides. It is BERT's pretraining task and the idea MAE carried to image patches. · 토큰의 약 15%를 가리고 양쪽 문맥으로 그것을 예측하도록 학습하는 것. BERT의 사전학습 과제이며, MAE가 이미지 패치로 옮긴 발상이다. → [[01-canonical-papers/notes/1-foundations/bert|BERT]]
- **Masking** · 마스킹 — A competing signal in the same sensory channel making a stimulus harder to detect. Power tools put broadband vibration into the band vibrotactile alerts use, so site alert wearables fail without any electronic fault. · 같은 감각 채널의 경쟁 신호 때문에 자극을 검출하기 어려워지는 현상. 동력 공구가 진동촉각 알림이 쓰는 대역에 광대역 진동을 넣으므로, 현장 알림 웨어러블은 전자적 고장 없이도 실패한다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §5]]
- **Mass (inertia) matrix $M(\theta)$** · 질량 행렬 — Symmetric, positive definite, and — the part that matters — a function of the configuration. The same arm can be several times harder to accelerate in one pose than another, so a controller gain tuned in one pose is detuned in the next. · 대칭이고 양정치이며, 무엇보다 *자세의 함수*다. 같은 팔이 자세에 따라 몇 배씩 가속하기 어려워지므로, 한 자세에서 튜닝한 게인은 다른 자세에서 어긋난다. → [[02-foundations/manipulator-kinematics-dynamics|10. §3]]
- **Mass–spring–damper** · 질량-스프링-댐퍼 — The canonical second-order system $m\ddot q + b\dot q + kq = u$. Its natural frequency and damping ratio come straight from the coefficients, and impedance control renders a virtual one. · 대표적인 2차 시스템 $m\ddot q + b\dot q + kq = u$. 고유 진동수와 감쇠비가 계수에서 바로 나오고, 임피던스 제어는 가상의 질량-스프링-댐퍼를 구현한다. → [[04-robotics/control-theory-ce397|Control Theory §2]]
- **Master method (master theorem)** · 마스터 방법 — A rule for $T(n) = aT(n/b) + O(n^d)$ that compares $a$ with $b^d$: smaller gives $\Theta(n^d)$, equal gives $\Theta(n^d \log n)$, larger gives $\Theta(n^{\log_b a})$. · $T(n) = aT(n/b) + O(n^d)$에서 $a$와 $b^d$를 비교하는 규칙: 작으면 $\Theta(n^d)$, 같으면 $\Theta(n^d \log n)$, 크면 $\Theta(n^{\log_b a})$. → [[02-foundations/algorithms/complexity-recursion|11.1 §3]]
- **Maximum-entropy IRL (MaxEnt IRL)** · 최대 엔트로피 IRL — Inverse RL that, among trajectory distributions matching the expert's feature counts, picks the maximum-entropy one, so trajectory probability grows exponentially with reward. Each gradient step needs a full planning pass. · 전문가의 특징 합을 맞추는 궤적 분포 중 엔트로피가 최대인 것을 고르는 역강화학습으로, 궤적 확률이 보상에 지수적으로 커진다. 그래디언트 한 스텝마다 계획을 한 번 통째로 풀어야 한다. → [[02-foundations/rl-basics|RL Basics §11]]
- **Maximum-entropy RL** · maximum-entropy 강화학습 — RL that maximizes expected return plus the policy's entropy, weighted by a temperature $\alpha$. Paying the policy to stay random keeps exploring until the critic gives a reason to commit. · 기대 수익에 온도 $\alpha$로 가중한 정책 entropy를 더해 최대화하는 강화학습. 무작위로 남는 데 보상을 주므로 critic이 근거를 줄 때까지 탐색이 유지된다. → [[01-canonical-papers/notes/1-foundations/sac|SAC]]
- **MCMC (Markov chain Monte Carlo)** · MCMC — Sampling a distribution you cannot draw from directly by running a Markov chain designed to have it as its stationary distribution, then using the states after mixing as samples. · 직접 뽑을 수 없는 분포를 정상 분포로 갖도록 체인을 설계해 돌리고, 혼합 시간 이후의 상태를 표본으로 쓰는 샘플링 방법. → [[02-foundations/probability|Probability §7]]
- **MDN-RNN (mixture density network)** · MDN-RNN — A recurrent network that outputs a mixture of Gaussians over the next latent state rather than one guess, so it can represent several possible futures. World Models' memory module; a temperature knob scales its randomness. · 다음 잠재 상태를 한 값이 아니라 가우시안 혼합으로 출력해 여러 가능한 미래를 표현하는 순환 신경망. World Models의 기억 모듈이며, 온도 조절로 무작위성을 키우거나 줄인다. → [[01-canonical-papers/notes/5-world-models/world-models|World Models]]
- **MDP (Markov Decision Process)** · MDP — $(\mathcal{S},\mathcal{A},p,r,\gamma)$: states, actions, transition kernel, reward, discount. The formalism every RL paper writes its problem in; robotics usually faces the partially observed version (POMDP). · $(\mathcal{S},\mathcal{A},p,r,\gamma)$ — 상태, 행동, 전이 커널, 보상, 할인. 모든 RL 논문이 문제를 써 넣는 형식이고, 로보틱스는 대개 부분 관측 버전(POMDP)을 마주한다. → [[02-foundations/rl-basics|RL Basics §1]]
- **Memoization (top-down DP)** · 메모이제이션 — Caching each recursive call's result the first time it is computed, so overlapping calls are solved once. Cost becomes distinct arguments times work per call; bottom-up tabulation fills the same table with loops. · 재귀 호출의 결과를 처음 계산할 때 저장해 겹치는 호출을 한 번만 푸는 것. 비용은 서로 다른 인자 수 × 호출당 작업이 되며, 상향식 테이블 채우기는 같은 표를 반복문으로 채운다. → [[02-foundations/algorithms/complexity-recursion|11.1 §5]], [[02-foundations/algorithms/dynamic-programming|11.5 §2]]
- **Mental model** · 멘탈 모델 — The operator's internal picture of how the system behaves, which sets what they expect it to do next. · 시스템이 어떻게 움직이는지에 대한 조작자의 내적 그림으로, 다음에 무엇을 기대할지를 정한다. → [[04-robotics/hri-safety|HRI & Safety §4]]
- **Merge sort** · 병합 정렬 — Sort each half recursively, then merge the two sorted halves by repeatedly taking the smaller front element. It takes $\Theta(n \log n)$ on every input and $O(n)$ extra space, and it is stable. · 절반씩 재귀로 정렬한 뒤, 두 정렬된 절반의 맨 앞 원소 중 작은 것을 반복해서 꺼내 병합한다. 모든 입력에서 $\Theta(n \log n)$, 추가 공간 $O(n)$이며 안정 정렬이다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §2]]
- **Message (.msg)** · 메시지(message) — A typed data structure defined in a .msg file, one field per line (type then name). Field types are enforced by generated code; the meaning and units live only in the definition's comments. · .msg 파일에 한 줄에 필드 하나(타입, 이름)로 정의되는 타입 있는 데이터 구조. 필드 타입은 생성 코드가 강제하지만, 의미와 단위는 정의의 주석에만 있다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §4]]
- **Method of adjustment** · 조정법 — A threshold procedure where the participant tunes the stimulus until it is just perceptible or matches a reference. Fastest but most variable; the mean gives the PSE and the standard deviation estimates the difference threshold. · 참가자가 자극을 직접 조절해 겨우 지각되게 하거나 기준과 맞추는 임계값 절차. 가장 빠르지만 분산이 가장 크며, 평균이 PSE를, 표준편차가 차이 임계값을 준다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Method of constant stimuli** · 항상자극법 — A threshold procedure presenting 5–9 fixed intensities many times in random order and recording yes or no. Least biased and most trial-hungry, it yields the whole psychometric function. · 고정된 5–9개 강도를 무작위 순서로 여러 번 제시해 예/아니오를 기록하는 임계값 절차. 편향이 가장 적고 시행이 가장 많이 들며, 심리측정 함수 전체를 준다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Method of limits** · 극한법 — A threshold procedure using ascending and descending stimulus series, taking the average point where the sensation appears or disappears. Fast, but habituation and expectation bias the transition points. · 상승·하강 자극 계열을 제시하고 감각이 나타나거나 사라지는 지점의 평균을 임계값으로 삼는 절차. 빠르지만 습관화와 기대가 전이점을 편향시킨다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Metropolis–Hastings** · Metropolis–Hastings — An MCMC recipe: propose a move, accept it with probability $\min(1, r)$ where $r$ compares target and proposal densities, otherwise repeat the current state. Needs the target only up to a constant. · MCMC 방법 하나: 이동을 제안하고, 목표 밀도와 제안 밀도를 비교한 비 $r$로 $\min(1, r)$ 확률에 받아들이며, 아니면 현재 상태를 반복한다. 목표 분포를 상수배까지만 알면 된다. → [[02-foundations/probability|Probability §7]]
- **MHT (multiple hypothesis tracking)** · MHT — Tracking that keeps several association histories alive across frames, lets later data decide between them, and prunes the hypothesis tree to stay tractable. · 여러 연관 이력을 프레임을 넘어 살려 두고 뒤의 데이터가 고르게 하며, 감당할 수 있도록 가설 트리를 가지치기하는 추적 방식. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Minimal realization** · 최소 실현 — A state-space model with no hidden modes that cancel out of its transfer function. Only then do the transfer function's poles equal all eigenvalues of $A$; a cancelled unstable eigenvalue is invisible in $G(s)$. · 전달함수에서 상쇄되어 사라지는 숨은 모드가 없는 상태공간 모델. 이때만 전달함수의 극점이 $A$의 고유값 전부와 같으며, 상쇄된 불안정 고유값은 $G(s)$에 보이지 않는다. → [[02-foundations/engineering-math|Engineering Math §9]]
- **Minimax** · minimax — An objective optimized in opposite directions by two parties — one minimizes what the other maximizes. GANs are the familiar case (generator against discriminator); robust control and game-theoretic planning use the same form to optimize against a worst case rather than an opponent. · 두 편이 서로 반대 방향으로 최적화하는 목적함수 — 한쪽이 최대화하는 것을 다른 쪽이 최소화한다. GAN이 익숙한 사례이고(생성기 대 판별기), 강건 제어와 게임이론적 계획은 상대가 아니라 최악의 경우에 대해 최적화하려고 같은 형태를 쓴다. → [[02-foundations/optimization|Optimization §1]]
- **Minimum spanning tree (MST)** · 최소 신장 트리 — The $V - 1$ edges of least total weight that connect every vertex of a weighted undirected graph without a cycle. It is not a shortest-path tree; Prim or Kruskal finds it. · 가중 무방향 그래프의 모든 정점을 사이클 없이 잇는, 총 가중치가 최소인 간선 $V - 1$개. 최단 경로 트리와 다르며, Prim이나 Kruskal로 구한다. → [[02-foundations/algorithms/greedy-mst|11.4 §6]]
- **Mixed-effects model (hierarchical model)** · 위계·혼합 모델 — A regression that gives each participant their own baseline, so repeated trials from one person are not counted as independent people. Twenty participants with 30 trials each is not $n=600$. · 참가자마다 자기 기준선을 주어 한 사람의 반복 시행을 독립된 사람으로 세지 않게 하는 회귀 모델. 20명이 각 30회 시행했다고 $n=600$이 되지는 않는다. → [[04-robotics/haptics-teleoperation/experiments-readings|24.6 Experiments §2]]
- **Mixed precision (fp16, bf16)** · 혼합 정밀도 — Computing in 16-bit numbers while keeping a 32-bit master copy of the weights, roughly halving memory. fp16 needs loss scaling to avoid underflow; bf16 keeps fp32's exponent range and does not. · 가중치의 32비트 마스터 사본을 두고 16비트로 계산해 메모리를 대략 절반으로 줄이는 것. fp16은 손실 스케일링이 필요하고, fp32의 지수 범위를 가진 bf16은 필요 없다. → [[02-foundations/ml-practice|ML Practice §6]]
- **MLE (maximum likelihood estimation)** · MLE — $\arg\max_\theta \log p(x|\theta)$: the origin of both cross-entropy and MSE losses. · 교차 엔트로피와 MSE 손실의 기원. → [[02-foundations/probability|확률 §4]]
- **Mobile manipulation** · 모바일 조작 — Manipulation by an arm mounted on a moving base. The navigation goal becomes a set of base poses from which the whole task is reachable and well conditioned, not a point on a map. · 이동하는 베이스에 실린 팔로 하는 조작. 내비게이션 목표가 지도 위의 점이 아니라 과제 전체가 도달 가능하고 조건이 좋은 베이스 자세들의 집합이 된다. → [[04-robotics/navigation-mobile-manipulation|Mobile Manipulation §1]]
- **Mock-up (full-scale mock-up)** · 목업 — A built test setup that imitates part of a building, from a lab frame to a full-scale demonstration building. It is not an active construction site. · 실험실 틀부터 실규모 실증 건물까지, 건물 일부를 흉내 내어 만든 시험 설비. 가동 중인 건설 현장이 아니다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §3]]
- **Mode** · 모드 — One eigenvector direction of $\dot x = Ax$ that grows or decays like $e^{\lambda t}$; the free response is a sum of modes. The eigenvalue's real part sets growth or decay, its imaginary part oscillation. · $\dot x = Ax$에서 $e^{\lambda t}$처럼 성장하거나 감쇠하는 고유벡터 방향 하나. 자유 응답은 모드의 합이다. 고유값의 실수부가 성장·감쇠를, 허수부가 진동을 정한다. → [[04-robotics/control-theory-ce397|Control Theory §3]]
- **Mode collapse** · 모드 붕괴 — A generator covering only a few modes of the data, producing little variety even though the data is diverse. It is a structural failure of GAN training. · 데이터가 다양한데도 생성기가 몇 개의 모드만 덮어 다양성이 거의 없는 샘플을 내는 현상. GAN 학습의 구조적 실패다. → [[01-canonical-papers/notes/6-diffusion/gan|GAN]]
- **Model bias (model-based RL)** · 모델 편향 — Errors in a learned dynamics model that compound over imagined rollouts, so the policy can exploit predictions rather than reality. Managed with short imagined horizons and value bootstrapping. · 학습된 동역학 모델의 오차가 상상 롤아웃에서 누적되어 정책이 현실이 아니라 예측을 이용하게 되는 것. 짧은 상상 지평과 가치 부트스트래핑으로 관리한다. → [[02-foundations/rl-basics|RL Basics §5]]
- **Model checking** · 모델 검사 — Verifying whether every behavior of a given design satisfies a temporal-logic formula, returning either yes or a counterexample run. · 주어진 설계의 모든 거동이 시간 논리 식을 만족하는지 검증해, 예 또는 반례 실행을 돌려주는 것. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Model exploitation** · 모델 악용 — An agent trained inside a learned model finding and abusing the model's errors, so it scores well in imagination but fails in reality. World Models manages it by raising the dream temperature. · 학습된 모델 안에서 훈련된 에이전트가 모델의 오류를 찾아 악용해, 상상에서는 점수가 높지만 현실에서는 실패하는 현상. World Models는 꿈의 온도를 높여 이를 관리한다. → [[01-canonical-papers/notes/5-world-models/world-models|World Models]]
- **Model-form error (vs parameter error)** · 모델 형식 오차 — A simulator missing a phenomenon, state, or event entirely, as opposed to having wrong parameter values. Randomizing parameters cannot fix it; contact is where it often dominates. · 파라미터 값이 틀린 것과 달리 시뮬레이터에 현상·상태·사건 자체가 빠진 오차. 파라미터 랜덤화로 고칠 수 없고 접촉에서 특히 지배적이기 쉽다. → [[05-construction-robotics/sim-to-real|Sim-to-Real §1]]
- **Model-free RL** · 모델 프리 RL — RL that updates a policy or value function without planning through an explicit transition model. Experience may still come from simulation or stored data; the contrast with model-based RL is the explicit model. · 명시적 전이 모델을 통한 계획 없이 정책이나 가치 함수를 갱신하는 RL. 경험은 시뮬레이션이나 저장 데이터에서 올 수도 있으며, 모델 기반 RL과의 구분점은 명시적 모델이다. → [[02-foundations/rl-basics|RL Basics §5]]
- **Modular learning (modular navigation pipeline)** · 모듈형 학습 — A classical mapping, planning, and control pipeline with learned modules for semantic perception and exploration. In real homes it kept 90% ObjectNav success while end-to-end learning fell to 23%. · 고전적 지도·계획·제어 파이프라인에 의미 감지와 탐색을 학습 모듈로 넣은 방식. 실제 주택에서 ObjectNav 성공률 90%를 지켰고 end-to-end 학습은 23%로 떨어졌다. → [[01-canonical-papers/notes/9-navigation/gervet-real-world-objectnav|Gervet 2023]]
- **Momentum** · 모멘텀 — Accumulating a velocity from past gradients and stepping along it, which averages out the oscillation gradient descent shows across ill-conditioned valleys. Adam combines it with per-coordinate step sizes. · 과거 그래디언트로 속도를 누적해 그 방향으로 내딛는 것. 나쁜 조건의 골짜기에서 경사 하강이 보이는 진동을 평균해 없앤다. Adam은 이것을 좌표별 스텝 크기와 결합한다. → [[02-foundations/optimization|Optimization §3]]
- **Monitored standstill (safety-rated monitored stop)** · monitored standstill — A collaboration method in which the robot halts while a person is in the shared space and resumes when they leave. ISO 10218-2:2025 renamed the older safety-rated monitored stop. · 사람이 공유 공간에 있는 동안 로봇이 멈추고 나가면 재개하는 협동 방법. ISO 10218-2:2025가 이전의 safety-rated monitored stop을 이 이름으로 바꿨다. → [[04-robotics/hri-safety|HRI & Safety §6]]
- **Monocular depth estimation** · 단안 깊이 추정 — Predicting per-pixel depth from a single image with a network. The output is often defined only up to unknown scale and shift, frequently as inverse depth $1/Z$, which stays bounded for distant points. · 이미지 한 장에서 네트워크로 픽셀별 깊이를 예측한다. 출력은 흔히 미지의 스케일·오프셋까지만 정해지고, 먼 점에서도 유계인 역깊이 $1/Z$로 나오는 경우가 많다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2]]
- **Monotonic stack (monotonic deque)** · 단조 스택 — A stack or deque that keeps, in sorted order, only the candidates that could still be an answer and discards the rest for good. It gives next-greater-element and sliding-window maximum in $O(n)$ total. · 아직 답이 될 수 있는 후보만 정렬된 상태로 남기고 나머지는 영원히 버리는 스택이나 덱. 다음 큰 원소와 슬라이딩 윈도 최댓값을 총 $O(n)$에 준다. → [[02-foundations/algorithms/data-structures|11.2 §2]]
- **Monte Carlo estimate** · 몬테카를로 추정 — Approximating an expectation by averaging a function over samples, $E[g(X)] \approx \frac1N\sum_i g(x_i)$. Many expected losses and returns are estimated this way, so they carry sampling uncertainty that should be reported. · 표본에 대해 함수를 평균 내어 기댓값을 근사하는 것. 많은 기대 손실과 리턴이 이렇게 추정되므로 보고해야 할 표본 불확실성을 지닌다. → [[02-foundations/engineering-math|Engineering Math §3]]
- **Monte Carlo return** · 몬테카를로 리턴 — Estimating value from the complete return observed after an episode ends, with no bootstrapping. Unbiased but high variance, and nothing is learned from an episode until it finishes. · 에피소드가 끝난 뒤 관측한 완결 리턴으로 가치를 추정하는 것으로, 부트스트랩하지 않는다. 무편향이지만 분산이 크고, 에피소드가 끝나야 거기서 무엇이든 배운다. → [[02-foundations/rl-basics|RL Basics §3.5]], [[02-foundations/rl-basics|RL Basics §4]]
- **MOTA (multiple object tracking accuracy)** · MOTA — $1-\sum(\mathrm{FN}+\mathrm{FP}+\mathrm{IDSW})/\sum\mathrm{GT}$ over all frames. Misses and false positives dominate it, so identity switches (a track jumping to another object) barely move the score. · 모든 프레임에 걸친 $1-\sum(\mathrm{FN}+\mathrm{FP}+\mathrm{IDSW})/\sum\mathrm{GT}$. 놓침과 오검출이 지배해서 정체 전환(트랙이 다른 물체로 옮겨 붙음)은 점수를 거의 움직이지 않는다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Motion artifact** · 동작 아티팩트 — Noise in a biosignal such as EEG caused by body or electrode movement rather than the activity of interest. Sweat, helmets, and movement on a site make it worse than in a lab. · 관심 활동이 아니라 몸이나 전극의 움직임 때문에 EEG 같은 생체신호에 생기는 잡음. 땀, 안전모, 움직임이 있는 현장에서는 실험실보다 심해진다. → [[01-canonical-papers/notes/8-construction/liu-jebelli-bci|BCI teleoperation 2021]]
- **Motion blur** · 모션 블러 — Smear from camera or object motion during exposure. At 1/60 s, a head turning at 90°/s smears a 21-pixels-per-degree image by about 32 pixels. · 노출 동안 카메라나 물체가 움직여 생기는 번짐. 1/60초 노출에서 90°/s로 도는 머리는 도당 21픽셀인 영상을 약 32픽셀 번지게 한다. → [[04-robotics/egocentric-perception|Egocentric §3]]
- **Motion capture (mocap)** · 모션 캡처 — Tracking body or object positions with external cameras, usually with markers, at high accuracy. In HRC studies it supplies objective measures such as idle time and separation distance. · 외부 카메라와 보통 마커로 몸이나 물체의 위치를 높은 정확도로 추적하는 것. HRC 연구에서 유휴 시간, 이격 거리 같은 객관 지표를 제공한다. → [[01-canonical-papers/notes/8-construction/lasota-shah|Lasota & Shah 2015]]
- **Motion scaling** · 모션 스케일링 — Mapping large operator motion to small robot motion: at 10:1 a 40 mm hand motion becomes 4 mm and 1 mm tremor 0.1 mm. Force scaling does the reverse, amplifying small forces. · 사람의 큰 운동을 로봇의 작은 운동으로 사상하는 것. 10:1이면 40 mm 손 운동은 4 mm, 1 mm 손떨림은 0.1 mm가 된다. 힘 스케일링은 반대로 작은 힘을 증폭해 조작자가 느끼게 한다. → [[04-robotics/teleoperation-demonstration|Teleoperation §5]]
- **Motor imagery** · 운동 심상 — Imagining a movement, such as moving the left hand, without performing it. The resulting EEG patterns can be classified into discrete commands for a brain–computer interface. · 왼손을 움직이는 것처럼 실제로 하지 않고 동작을 상상하는 것. 그때의 EEG 패턴을 분류해 뇌-컴퓨터 인터페이스의 이산 명령으로 쓴다. → [[01-canonical-papers/notes/8-construction/liu-jebelli-bci|BCI teleoperation 2021]]
- **move_group** · move_group — MoveIt's integrator node: it reads the URDF, SRDF, joint states and TF, owns the planning scene, plans on request, and executes by acting as a client of a controller's FollowJointTrajectory action. · MoveIt의 통합 노드. URDF, SRDF, 조인트 상태, TF를 읽고, planning scene을 소유하며, 요청에 따라 계획하고, 제어기의 FollowJointTrajectory 액션 클라이언트로서 실행한다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §2]]
- **MoveIt 2** · MoveIt 2 — The standard plugin-based ROS 2 motion-planning framework for arms: IK, collision-free path search and time parameterization. It is not a controller, not a grasp planner, and not contact-aware. · 팔을 위한 표준 플러그인 기반 ROS 2 운동 계획 프레임워크로, IK, 충돌 없는 경로 탐색, 시간 파라미터화를 한다. 제어기도, 파지 계획기도, 접촉을 다루는 시스템도 아니다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §1]]
- **MPC (Model Predictive Control)** · MPC — Solving a finite-horizon optimal control problem each cycle and applying only the first input. · 매 주기 유한 지평 최적 제어를 풀고 첫 입력만 적용하는 제어. → [[04-robotics/mpc|MPC]], [[02-foundations/optimization|최적화 §5]]
- **MPJPE (mean per-joint position error)** · MPJPE — The Euclidean distance between predicted and true 3D joints, averaged over joints, in millimetres. Usually root-relative, so it says nothing about where the person is, and it is not an uncertainty model. · 예측 3D 관절과 참 관절 사이 유클리드 거리를 관절에 걸쳐 평균한 값(mm). 보통 루트 상대라 사람이 어디 있는지는 말하지 않고, 불확실성 모델도 아니다. → [[04-robotics/human-pose-gaze|Pose & gaze §3]]
- **MPPI (Model Predictive Path Integral)** · MPPI — Nav2's default controller in Jazzy: it samples a batch of noisy perturbations of the previous best trajectory, scores them with critics, and blends them into one velocity command. Handles moving obstacles well. · Jazzy Nav2의 기본 제어기. 이전 최적 궤적에 노이즈를 준 샘플 묶음을 굴려 critic으로 점수 매긴 뒤 하나의 속도 명령으로 섞는다. 움직이는 장애물을 잘 다룬다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §6]]
- **MuJoCo** · MuJoCo — Google DeepMind's open-source physics simulator for fast, stable articulated dynamics. Its soft contact model permits small constraint violations by design, so its contact forces are not what a load cell would read. · Google DeepMind의 오픈소스 물리 시뮬레이터로, 빠르고 안정적인 관절 동역학이 강점이다. 부드러운 접촉 모델이 작은 제약 위반을 설계상 허용하므로 그 접촉력은 로드셀이 읽을 값이 아니다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §2]], [[06-research-practice/simulators-benchmarks-datasets|Simulators §3]]
- **Multi-armed bandit** · 멀티암드 밴딧 — The stripped-down setting where exploration and exploitation first collide: repeated choices among fixed actions with unknown payoffs, no state and no transitions. Strategies here ($\epsilon$-greedy, upper confidence bounds, Thompson sampling) are where the exploration vocabulary of RL comes from. · 탐색과 활용이 처음 충돌하는, 군더더기를 걷어낸 설정: 보상을 모르는 고정된 행동들 사이의 반복 선택이며 상태도 전이도 없다. 여기서의 전략들($\epsilon$-탐욕, 상한 신뢰구간, 톰프슨 표집)이 RL 탐색 어휘의 출처다. → [[02-foundations/rl-basics|RL Basics §8]]
- **Multicast** · 멀티캐스트 — Sending one packet to a group address that many hosts listen on. ROS 2 discovery uses it, while data travels unicast to per-participant ports, so a firewall can allow discovery yet block data. · 여러 호스트가 듣는 그룹 주소로 패킷 하나를 보내는 방식. ROS 2 discovery가 이것을 쓰고 데이터는 참여자별 포트로 유니캐스트되므로, 방화벽이 discovery는 통과시키고 데이터는 막을 수 있다. → [[04-robotics/ros2/from-simulation-to-hardware|25.11 Sim to Hardware §5]]
- **Multidimensional scaling (MDS)** · 다차원 척도법 — Placing stimulus patterns as points on a map whose distances match how different users judge them, revealing which tactile patterns people actually distinguish. · 사용자가 느끼는 차이가 거리와 맞도록 자극 패턴들을 지도 위의 점으로 배치해, 사람들이 실제로 구별하는 촉각 패턴을 드러내는 방법. → [[04-robotics/haptics-teleoperation/tactile-display-design|24.2 Tactile Display §3]]
- **Multi-head attention** · 멀티헤드 어텐션 — Running attention in several low-dimensional subspaces in parallel to learn different relations. · 어텐션을 여러 저차원 부분공간에서 병렬 수행해 서로 다른 관계를 학습. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Multimodal demonstrations** · 다봉 시연 — Demonstrations in which experts solve the same situation in different ways, like passing an obstacle on either side. A mean-regressing policy averages them and drives through the middle; action chunking and expressive heads address it. · 전문가들이 같은 상황을 서로 다르게 푸는 시연(장애물을 양쪽으로 지나가기). 평균 회귀 정책은 이를 평균해 한가운데로 돌진하며, 행동 청킹과 표현력 있는 헤드가 처방이다. → [[02-foundations/rl-basics|RL Basics §6]]
- **Multimodal fusion** · 멀티모달 융합 — Combining inputs from different sensors, such as images, force/torque, and proprioception, into one representation. What it usually buys is sample efficiency, not a capability that was impossible without it. · 영상, 힘/토크, 고유수용감각처럼 서로 다른 센서 입력을 하나의 표현으로 합치는 것. 보통 사는 것은 샘플 효율이지, 없으면 불가능했던 새 능력이 아니다. → [[01-canonical-papers/notes/7-robotics/vision-and-touch|Vision and Touch 2019]]
- **Multimodality** · 다봉성(multimodality) — A task or future that admits several valid solutions. Regressing the mean of two valid actions or paths yields an invalid one, which motivates action chunking, generative policies, and multi-sample forecasts. · 과제나 미래에 타당한 해가 여럿 있는 성질. 타당한 두 행동이나 경로의 평균을 회귀하면 무효한 결과가 나오며, 이것이 action chunking, 생성형 정책, 다중 샘플 예측의 동기다. → [[04-robotics/teleoperation-demonstration|Teleoperation §6]], [[04-robotics/human-intent-prediction|Human Intent §6]]
- **Multi-object tracking (MOT)** · 다중 물체 추적 — Following an unknown, changing number of objects by running one filter per track and deciding each frame which detections belong to which track, which are new objects, and which are clutter. · 수를 모르고 변하는 여러 물체를 트랙마다 필터 하나로 따라가며, 매 프레임 어느 검출이 어느 트랙 것이고 무엇이 새 물체이며 무엇이 클러터인지 정하는 일. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Multi-source BFS (brushfire)** · 다중 출발 BFS — BFS started from many sources at distance 0. Seeded with the obstacle cells of an occupancy grid, it is the brushfire distance transform: every free cell's distance to its nearest obstacle in one pass. · 여러 출발점을 거리 0으로 두고 시작하는 BFS. 점유 격자의 장애물 셀로 시작하면 brushfire 거리 변환이 되어, 한 번에 모든 빈 셀에서 가장 가까운 장애물까지의 거리를 준다. → [[02-foundations/algorithms/graph-algorithms|11.6 §2]]
- **Multi-task pretraining** · 멀티태스크 사전학습 — Training one policy on demonstrations from several tasks before adapting it to specific ones, so behaviour learned for one task can be reused by the others. · 여러 과제의 시연으로 정책 하나를 먼저 학습한 뒤 특정 과제에 적응시키는 것. 한 과제에서 배운 행동을 다른 과제가 재사용할 수 있다. → [[01-canonical-papers/notes/8-construction/ext|ExT]]
- **Multi-view association** · 다중 시점 결합 — Deciding which 2D detections in different frames belong to the same 3D object and fusing them into one map node. Rows of identical objects are the case where it fails. · 서로 다른 프레임의 2D 검출 중 어느 것이 같은 3D 물체인지 정해 지도 노드 하나로 융합하는 것. 똑같은 물체가 줄지어 있는 경우가 실패 사례다. → [[01-canonical-papers/notes/9-navigation/conceptgraphs|ConceptGraphs]]
- **Multi-view stereo (MVS)** · MVS(다시점 스테레오) — Computing dense depth and geometry from many images whose camera poses are already known, usually after SfM. It is the densification stage of the classical reconstruction pipeline. · 카메라 자세를 이미 아는 여러 이미지에서 조밀한 깊이와 형상을 계산하는 것으로, 보통 SfM 다음에 온다. 고전 재구성 파이프라인의 조밀화 단계다. → [[01-canonical-papers/notes/2-computer-vision/vggt|VGGT]]
- **Mutual information** · 상호 정보량 — $I(X;Y) = H(X) - H(X \mid Y)$: how much knowing $Y$ reduces uncertainty about $X$, zero exactly when they are independent. Contrastive objectives such as InfoNCE maximize a lower bound on it. · $Y$를 알면 $X$에 대한 불확실성이 얼마나 줄어드는가를 잰 양으로, 둘이 독립일 때만 0이다. InfoNCE 같은 대조 목적함수는 이것의 하한을 최대화한다. → [[02-foundations/information-theory|Information Theory §4]]
- **Mutually exclusive callback group** · mutually exclusive 콜백 그룹 — A callback group whose callbacks never run in parallel with each other, as if it had its own single-threaded executor. It is the default group type. · 소속 콜백끼리 절대 병렬로 돌지 않는 콜백 그룹으로, 자기만의 단일 스레드 executor를 가진 것처럼 동작한다. 기본 그룹이 이 종류다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §9]]
- **Name resolution (absolute, relative, private names)** · 이름 해석 — How a topic name becomes a full name: /a/b is absolute and ignores the namespace; a/b is prefixed by the namespace; ~/b adds namespace and node name. Resolution mismatches cause silent non-connection. · 토픽 이름이 전체 이름이 되는 규칙. /a/b는 절대 이름이라 네임스페이스를 무시하고, a/b는 네임스페이스가 붙으며, ~/b는 네임스페이스와 노드 이름이 붙는다. 어긋나면 조용히 연결되지 않는다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §3]]
- **Namespace** · 네임스페이스 — A prefix such as /robot1 applied to a node's relative names, so the same node can be launched twice into two namespaces without its topics colliding. · /robot1 같은 접두어로, 노드의 상대 이름 앞에 붙는다. 같은 노드를 두 네임스페이스에 두 번 띄워도 토픽이 충돌하지 않게 한다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §3]]
- **NASA-TLX** · NASA-TLX — A questionnaire of six self-reported workload dimensions: mental, physical, and temporal demand, perceived performance, effort, and frustration. It complements objective data but does not reveal workload's physical cause; report raw or weighted scoring. · 정신적·신체적·시간적 요구, 지각된 수행, 노력, 좌절의 여섯 자기보고 workload 차원을 재는 설문. 객관 데이터를 보완할 뿐 workload의 물리적 원인을 드러내지 않으며, raw와 weighted 중 어느 채점을 썼는지 보고한다. → [[04-robotics/haptics-teleoperation/experiments-readings|24.6 Experiments §2]]
- **Natural frequency ($\omega_n$)** · 고유 진동수 — The number that sets how fast a second-order response is, from $s^2 + 2\zeta\omega_n s + \omega_n^2$; for a mass-spring-damper $\omega_n=\sqrt{k/m}$. With the damping ratio it fixes settling time. · 2차 응답이 얼마나 빠른지를 정하는 수로, $s^2 + 2\zeta\omega_n s + \omega_n^2$에서 온다. 질량-스프링-댐퍼에서는 $\omega_n=\sqrt{k/m}$이다. 감쇠비와 함께 정착 시간을 정한다. → [[04-robotics/control-theory-ce397|Control Theory §5]]
- **Natural language supervision** · 언어 감독 — Learning visual concepts from the text that naturally accompanies images on the web rather than from a fixed, hand-labeled class list, so the label set becomes open-ended. · 사람이 붙인 고정 클래스 목록 대신 웹에서 이미지와 자연스럽게 함께 오는 텍스트로 시각 개념을 배우는 것. 라벨 집합이 열린 텍스트가 된다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **Natural language understanding (NLU)** · 자연어 이해(NLU) — Turning a spoken or written instruction into structured task information that later stages map to building components and robot skills. Understanding the words alone does not identify the physical component. · 말이나 글로 된 지시를 구조화된 과제 정보로 바꾸는 단계. 이후 단계가 이를 건물 부재와 로봇 스킬에 대응시킨다. 말을 이해하는 것만으로 실제 부재가 정해지지는 않는다. → [[01-canonical-papers/notes/8-construction/park-nl|Park NL 2024]]
- **Nav2** · Nav2 — The ROS 2 navigation framework (successor to ROS 1's move_base): swappable planner, controller, smoother and behavior plugins in lifecycle-managed servers, coordinated by a behavior tree that encodes recovery. · ROS 2 내비게이션 프레임워크(ROS 1 move_base의 후속). 라이프사이클로 관리되는 서버 안의 교체 가능한 플래너·제어기·스무더·행동 플러그인을, 복구 정책을 담은 행동 트리가 조율한다. → [[04-robotics/ros2/navigation-nav2|25.9 Nav2 §1]]
- **Nav-graph** · nav-graph — A benchmark representation in which the agent teleports between panorama nodes along known navigable edges. It hides three assumptions: known topology, oracle short-range navigation, and perfect localization. · 에이전트가 알려진 이동 가능 간선을 따라 파노라마 노드 사이를 순간이동하는 벤치마크 표현. 알려진 위상, 오라클 근거리 주행, 완벽한 위치 추정이라는 세 가정을 감춘다. → [[01-canonical-papers/notes/9-navigation/vln-ce|VLN-CE]]
- **Navigate-then-manipulate** · 이동한 뒤 조작 — Moving the base first and then running a separate arm motion, as opposed to whole-body control that commands base and arm together as one chain. Easier, and far more common than papers' phrasing suggests. · 베이스를 먼저 옮기고 그다음 별도의 팔 동작을 하는 방식으로, 베이스와 팔을 하나의 사슬로 함께 명령하는 전신 제어와 대비된다. 더 쉽고, 논문 표현이 시사하는 것보다 훨씬 흔하다. → [[04-robotics/navigation-mobile-manipulation|Mobile Manipulation §6]]
- **Navigation function** · 내비게이션 함수 — A potential constructed to have a single minimum, located at the goal, so that following it does not trap the robot in the local minima a plain potential field has. · 최솟값이 목표 한 곳에만 있도록 만든 퍼텐셜로, 이를 따라가면 단순한 퍼텐셜장의 국소 최솟값에 로봇이 갇히지 않는다. → [[04-robotics/planning-decision-making|Planning §5]]
- **Near miss** · near miss — An event where no harm occurred but a plausible hazardous trajectory or event did. Counting near misses gives safety evidence that collision counts alone cannot. · 해는 없었지만 그럴듯하게 위험한 궤적이나 사건이 있었던 경우. near miss를 세면 충돌 횟수만으로는 얻지 못하는 안전 증거를 얻는다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §5]]
- **Negative cycle** · 음수 사이클 — A cycle whose edge weights sum to less than zero, so going around it again keeps lowering a path's cost. Bellman–Ford detects one reachable from the source with one extra round of relaxation. · 간선 가중치 합이 0보다 작은 사이클로, 한 바퀴 더 돌 때마다 경로 비용이 계속 낮아진다. Bellman–Ford가 완화를 한 번 더 돌려 출발점에서 도달 가능한 것을 검출한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §5]]
- **Negative result** · 부정적 결과 — A finding that a predicted advantage did not appear. It is informative when question, implementation quality, conditions, exposure and failure mechanism are documented; "it did not work" alone does not show the idea cannot work. · 예측한 이점이 나타나지 않았다는 결과. 질문, 구현 품질, 조건, 노출, 실패 기전이 문서화되면 유익하다. "안 됐다"만으로는 그 아이디어가 불가능하다는 증거가 아니다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §8]]
- **Negatives (contrastive)** · negatives(대조 음성 예제) — The other examples in a batch that a contrastive loss such as CLIP's must score as non-matches. Their number depends on batch size, so accumulated microbatches give fewer negatives than one true large batch. · CLIP 같은 대조 손실이 짝이 아니라고 점수 매겨야 하는 배치 안의 다른 예제들. 개수가 배치 크기에 달려 있어, 누적한 마이크로배치는 진짜 큰 배치 하나보다 negatives가 적다. → [[02-foundations/ml-practice|ML Practice §6]]
- **NeRF** · NeRF — An implicit 3D scene representation: an MLP from (position, view direction) to (color, density), rendered volumetrically. · (위치, 시선) → (색, 밀도) MLP와 볼륨 렌더링으로 장면을 표현하는 암시적 3D. → [[01-canonical-papers/notes/2-computer-vision/nerf|NeRF]]
- **Neural network** · 신경망 — A composition of matrix multiplications with a nonlinearity between them; the weights and biases are the parameters fitted to data. · 행렬곱 사이에 비선형성을 끼워 합성한 것; 가중치와 편향이 데이터에 맞춰지는 파라미터다. → [[02-foundations/neural-network-basics|0.7 §1]]
- **Neural trajectories** · 신경 궤적 — Robot training data produced by a video generation model rather than recorded on a robot. GR00T N1 uses them in its data pyramid; they carry the video model's physics errors. · 로봇에서 기록한 것이 아니라 비디오 생성 모델이 만들어 낸 로봇 학습 데이터. GR00T N1이 데이터 피라미드에 쓰며, 비디오 모델의 물리 오류를 함께 들여온다. → [[01-canonical-papers/notes/4-vla/gr00t-n1|GR00T N1]]
- **NeurIPS (Conference on Neural Information Processing Systems)** · NeurIPS — The flagship machine-learning conference, with multi-round reviewer discussion and an official acceptance rate (24.52% in 2025). Its workshop papers are non-archival. · 기계학습 분야 대표 학회로, 여러 라운드의 심사 토론이 있고 공식 채택률을 발표한다(2025년 24.52%). 워크숍 논문은 non-archival이다. → [[06-research-practice/venue-strategy|Venue Strategy §3]], [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **Newton's method** · 뉴턴법 — Stepping to the minimum of the local quadratic model, $x_{k+1} = x_k - H^{-1}\nabla f$. Exact in one step on a quadratic and very fast near an optimum, but forming and inverting $H$ costs $O(n^3)$. · 국소 이차 모델의 최솟점으로 내딛는 방법. 이차 함수에서는 한 스텝에 정확하고 최적점 근처에서 매우 빠르지만, $H$를 만들고 역을 구하는 데 $O(n^3)$이 든다. → [[02-foundations/optimization|Optimization §3]]
- **NFE (number of function evaluations)** · 네트워크 평가 횟수(NFE) — How many times a sampler calls the network to make one sample, the real cost of diffusion inference. DDPM needs about 1000, DDIM 10–50, and CFG doubles the count per step. · 샘플 하나를 만드는 동안 샘플러가 네트워크를 호출하는 횟수로, 디퓨전 추론의 실제 비용이다. DDPM은 약 1000, DDIM은 10~50번이며, CFG는 스텝당 횟수를 두 배로 만든다. → [[01-canonical-papers/notes/6-diffusion/ddim|DDIM]], [[01-canonical-papers/notes/6-diffusion/classifier-free-guidance|CFG]]
- **NIST Assembly Task Boards** · NIST Assembly Task Boards — Physical benchmark boards for robotic assembly, such as peg insertion, gear meshing, connectors and nut threading, scored by time to complete against tabulated human handling times. Real hardware only. · 핀 삽입, 기어 맞물림, 커넥터, 너트 체결 같은 로봇 조립용 물리 벤치마크 보드. 표로 정리된 사람 작업 시간 대비 완료 시간으로 채점하며, 실제 하드웨어 전용이다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §6]]
- **NMS (non-maximum suppression)** · NMS — The post-processing step that deletes overlapping duplicate detections, keeping the highest-scoring box in each cluster. It is a hand-written rule sitting after a learned detector, which is exactly what [[01-canonical-papers/notes/2-computer-vision/detr|DETR]] set out to remove. · 겹치는 중복 검출을 지우고 군집마다 최고 점수 박스만 남기는 후처리. 학습된 검출기 뒤에 붙은 수작업 규칙이고, [[01-canonical-papers/notes/2-computer-vision/detr|DETR]]이 없애려 한 것이 정확히 이것이다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Node** · 노드(node) — A participant in the ROS 2 graph that uses a client library to talk to other nodes; ideally it does one logical thing. One process can host several nodes. · 클라이언트 라이브러리로 다른 노드와 통신하는 ROS 2 그래프의 참여자로, 논리적으로 한 가지 일을 하는 것이 좋다. 한 프로세스가 노드 여러 개를 담을 수도 있다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §2]]
- **Noise schedule** · 노이즈 스케줄 — The sequence $\beta_t$ fixing how much noise each diffusion step adds, and hence the signal level $\bar\alpha_t$ remaining at step $t$. Samplers and step-skipping depend on it. · 디퓨전의 각 스텝이 노이즈를 얼마나 더할지 정하는 수열 $\beta_t$로, 스텝 $t$에 남은 신호 수준 $\bar\alpha_t$도 이것으로 정해진다. 샘플러와 스텝 건너뛰기가 이에 의존한다. → [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]], [[01-canonical-papers/notes/6-diffusion/ddim|DDIM]]
- **Noisy-rational human model (noisy expert)** · noisy-rational 모델 — Modeling a person as choosing command $u$ with probability proportional to $\exp(\beta Q(x,u))$: better commands are exponentially likelier but never certain, and $\beta$ sets assumed rationality. Used to update a belief over the user's goal. · 사람이 명령 $u$를 $\exp(\beta Q(x,u))$에 비례하는 확률로 고른다고 보는 모델. 더 좋은 명령이 지수적으로 더 그럴듯하지만 확실하지는 않고, $\beta$가 가정한 합리성을 정한다. 사용자 목표에 대한 믿음 갱신에 쓰인다. → [[04-robotics/hri-safety|HRI & Safety §3.5]]
- **Nonholonomic constraint** · 비홀로노믹 제약 — A constraint on velocities but not positions: a car cannot slide sideways yet can still reach any pose. It restricts paths, not the reachable set. · 위치가 아니라 속도를 제한하는 제약: 자동차는 옆으로 미끄러질 수 없지만 어떤 자세에도 도달할 수 있다. 도달 가능 집합이 아니라 경로를 제한한다. → [[04-robotics/modern-robotics/ch13-wheeled-mobile-robots|MR 13장]]
- **Nonlinear least squares** · 비선형 최소자승 — Minimizing $\lVert f(x)\rVert^2$ for a stack of nonlinear residuals $f$. Bundle adjustment, pose-graph SLAM, calibration, and inverse kinematics take this form, solved by Gauss–Newton or Levenberg–Marquardt from a good initial guess. · 비선형 잔차 더미 $f$에 대해 $\lVert f(x)\rVert^2$를 최소화하는 문제. 번들 조정, 포즈그래프 SLAM, 보정, 역기구학이 이 꼴이며, 좋은 초기값에서 Gauss–Newton이나 Levenberg–Marquardt로 푼다. → [[02-foundations/optimization|Optimization §3.5]]
- **Nonlinear MPC (NMPC)** · 비선형 MPC(NMPC) — MPC with nonlinear dynamics or constraints, solved by sequential quadratic programming or DDP-style methods. Unlike linear MPC it brings back local optima and sensitivity to initialization. · 비선형 동역학이나 제약을 쓰는 MPC로, SQP나 DDP류 방법으로 푼다. 선형 MPC와 달리 국소 최적과 초기화 민감성이 돌아온다. → [[04-robotics/mpc|MPC §4]]
- **Non-saturating loss** · non-saturating loss — The standard GAN generator objective: maximize $\log D(G(z))$ instead of minimizing $\log(1-D(G(z)))$. It keeps gradients strong early, when the discriminator easily rejects fakes. · 표준 GAN 생성기 목적함수: $\log(1-D(G(z)))$를 최소화하는 대신 $\log D(G(z))$를 최대화한다. 판별기가 가짜를 쉽게 가려내는 초기에도 그래디언트를 살려 둔다. → [[01-canonical-papers/notes/6-diffusion/gan|GAN]]
- **No-regret online learning** · no-regret 온라인 학습 — A guarantee that, over many rounds, a learner's average loss approaches that of the best fixed policy in hindsight. DAgger's reduction to it turns imitation error growth from quadratic to linear in the horizon. · 여러 라운드에 걸쳐 학습자의 평균 손실이 사후적으로 가장 좋은 고정 정책의 손실에 가까워진다는 보장. DAgger는 이것으로 환원해 모방 오차의 증가를 지평에 대해 이차에서 일차로 바꾼다. → [[01-canonical-papers/notes/4-vla/dagger|DAgger]]
- **Norm** · 노름 — A vector's length. $\lVert x\rVert_2$ is the root of the sum of squares; $\lVert x\rVert_1$, the sum of absolute values, favors sparsity; the Frobenius norm treats a matrix's entries as one vector. · 벡터의 길이. $\lVert x\rVert_2$는 제곱합의 제곱근이고, 절댓값의 합인 $\lVert x\rVert_1$은 희소한 해를 유도하며, 프로베니우스 노름은 행렬 성분 전체를 긴 벡터 하나로 본다. → [[02-foundations/linear-algebra|Linear Algebra §1]]
- **Normal equations** · 정규방정식 — $A^\top A x = A^\top b$, the linear system whose solution is the least squares fit. Forming $A^\top A$ squares the condition number, so dense solvers prefer a QR factorization of $A$ — but sparse normal-Cholesky back ends in SLAM and bundle adjustment do form them, because the sparsity and speed are worth the numerical price. · $A^\top A x = A^\top b$, 그 해가 최소자승 적합인 선형계. $A^\top A$를 만들면 조건수가 제곱되므로 조밀 솔버는 $A$의 QR 분해를 선호한다. 다만 SLAM과 번들 조정의 희소 normal-Cholesky 백엔드는 희소성과 속도가 그 수치적 대가를 갚기 때문에 실제로 만든다. → [[02-foundations/optimization|Optimization §3.5]]
- **Novelty** · 신규성(novelty) — How a contribution differs from the nearest prior work. It is a relation to prior work, not a property; a reviewer's "novelty is unclear" asks for a traceable difference and evidence that it matters. · 기여가 가장 가까운 선행연구와 어떻게 다른가. 성질이 아니라 선행연구와의 관계이며, 심사자의 "novelty가 불분명하다"는 추적 가능한 차이와 그것이 중요하다는 증거를 요구한다. → [[06-research-practice/research-questions-claims|Research Questions §3]], [[06-research-practice/scientific-writing-peer-review|Scientific Writing §7]]
- **Novel view synthesis** · 새 시점 합성 — Rendering an image of a scene from a camera viewpoint that none of the input photos used. It is the task NeRF and 3D Gaussian splatting are evaluated on. · 입력 사진 어디에도 없던 카메라 시점에서 장면 이미지를 렌더링하는 과제. NeRF와 3D Gaussian splatting이 평가받는 과제다. → [[01-canonical-papers/notes/2-computer-vision/nerf|NeRF]]
- **NP** · NP — The class of yes/no problems whose "yes" answers come with a certificate that can be checked in polynomial time. The name means nondeterministic polynomial, not "not polynomial"; every problem in P is in NP. · "예"라는 답에 다항 시간에 검사할 수 있는 증거가 딸린 예/아니오 문제의 클래스. 이름은 "다항이 아님"이 아니라 비결정적 다항(nondeterministic polynomial)이고, P의 모든 문제는 NP에 속한다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **NP-complete** · NP-완전 — A problem that is both NP-hard and in NP, such as subset sum. A polynomial-time algorithm for any one of them would give one for every problem in NP. · NP-난해이면서 NP에도 속하는 문제(예: 부분집합 합). 그중 하나라도 다항 시간 알고리즘이 있으면 NP의 모든 문제에 대해 생긴다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **NP-hard** · NP-난해 — At least as hard as every problem in NP, since each reduces to it in polynomial time. In practice: skip exact polynomial algorithms and use small-n search, pseudo-polynomial DP, or approximation. · NP의 모든 문제가 다항 시간에 환원되므로 그 어느 문제만큼은 어려운 문제. 실무적 뜻: 정확한 다항 알고리즘을 찾지 말고 작은 n의 탐색, 의사 다항 DP, 근사를 쓴다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **Null space** · 영공간 — All inputs a matrix sends to zero, $\{x : Ax = 0\}$. A nontrivial null space means information is destroyed; for a redundant arm it is the joint motion that leaves the tool velocity unchanged. · 행렬이 0으로 보내는 입력 전부. 자명하지 않은 영공간은 정보가 파괴된다는 뜻이고, 여유자유도 팔에서는 도구 속도를 바꾸지 않는 관절 운동이다. → [[02-foundations/linear-algebra|Linear Algebra §2]]
- **Null-space projection** · 영공간 투영 — Filtering a secondary torque through a projector so it moves a redundant arm only in ways that produce no task-space force, letting it avoid joint limits or people without disturbing the main task. · 부차 토크를 투영자로 걸러 여유 자유도 팔을 작업 공간 힘을 만들지 않는 방식으로만 움직이게 하는 것. 주 과제를 교란하지 않고 관절 한계나 사람을 피할 수 있다. → [[04-robotics/force-compliance-control|Force & Compliance Control §4]]
- **Numerical inverse kinematics** · 수치 IK — Solving IK by Newton–Raphson on the pose error, $\Delta\theta = J^\dagger e$, recomputing pose and Jacobian each step. It follows the branch near its initial guess, so one failure does not prove no solution exists. · 자세 오차에 대한 뉴턴-랩슨으로 IK를 푸는 것, $\Delta\theta = J^\dagger e$. 매 스텝 자세와 야코비안을 다시 계산한다. 초기값 근처의 가지를 따라가므로 한 번의 실패가 해가 없다는 증거는 아니다. → [[04-robotics/modern-robotics/ch06-inverse-kinematics|MR ch.6 §2]]
- **Nyquist criterion** · 나이퀴스트 판별법 — The closed loop is stable when the plot of $L(i\omega)$ has the right encirclement relationship to the point $-1$ — the point where $1 + L = 0$ and the closed loop blows up. Unlike gain and phase margins it stays correct when the curve crosses the critical region several times. · $L(i\omega)$의 궤적이 점 $-1$에 대해 올바른 감김 관계를 가질 때 폐루프가 안정하다는 판별법. $-1$은 $1 + L = 0$이 되어 폐루프가 발산하는 점이다. 이득·위상 여유와 달리 곡선이 임계 영역을 여러 번 지나가도 여전히 옳다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Nyquist–Shannon sampling theorem (Nyquist frequency)** · 나이퀴스트–섀넌 — A signal with no content above $B$ Hz is perfectly recoverable from samples taken faster than $2B$ Hz. Content above half the sampling rate, the Nyquist frequency, aliases instead. · $B$ Hz 위에 성분이 없는 신호는 $2B$ Hz보다 빠르게 샘플링하면 완벽히 복원할 수 있다는 정리. 샘플링 주파수의 절반(나이퀴스트 주파수)보다 높은 성분은 에일리어싱된다. → [[02-foundations/signal-processing|Signal Processing §2]]
- **Object detection** · 검출(detection) — Finding every object of known classes in an image and outputting a class label and bounding box for each. Two-stage, one-stage, and set-prediction detectors are the main families. · 이미지에서 알려진 클래스의 물체를 모두 찾아 각각의 클래스 라벨과 박스를 출력하는 과제. 2단계, 1단계, 집합 예측 검출기가 주요 계열이다. → [[01-canonical-papers/notes/2-computer-vision/faster-r-cnn|Faster R-CNN]]
- **Object granularity (task-driven granularity)** · 물체 입도 — The level of detail at which a map treats something as one object, such as a bookshelf versus each book. Clio argues the right level is set by the task, not the object. · 지도가 무엇을 물체 하나로 볼지 정하는 세부 수준. 책장 하나인가, 책 한 권씩인가. Clio는 올바른 수준이 물체가 아니라 과제에 달렸다고 본다. → [[01-canonical-papers/notes/9-navigation/clio|Clio]]
- **ObjectNav (object-goal navigation)** · ObjectNav — In an unseen environment, given only an object category, the agent must reach any instance and emit STOP within 1.0 m, with the object viewable from there. Scored by success rate and SPL. · 처음 보는 환경에서 물체 범주만 받고 아무 인스턴스로 가서, 1.0 m 이내이면서 그 물체가 보이는 곳에서 STOP을 내야 하는 과제. 성공률과 SPL로 채점한다. → [[04-robotics/semantic-language-navigation|Semantic navigation §2]]
- **Object queries (learned queries)** · 물체 쿼리 — A fixed set of learned vectors that cross-attend to image features, each emitting one prediction such as a class and box or "no object". BLIP-2's Q-Former reuses the idea. · 이미지 특징에 cross-attention하는 학습된 벡터의 고정 집합으로, 각 쿼리가 (클래스, 박스)나 "물체 없음" 같은 예측 하나를 낸다. BLIP-2의 Q-Former가 이 발상을 재사용한다. → [[01-canonical-papers/notes/2-computer-vision/detr|DETR]]
- **Observability** · 가관측성 — Whether the sensors can eventually reveal every state; the transpose twin of controllability, and the reason a robot can run an observer instead of instrumenting everything. · 센서가 결국 모든 상태를 드러낼 수 있는가. 가제어성의 전치 쌍둥이이고, 로봇이 모든 것을 계측하는 대신 관측기를 돌릴 수 있는 이유다. → [[04-robotics/control-theory-ce397|제어 이론 §6]]
- **Observation model (measurement model)** · 관측 모델 — The equation $z_t=h(x_t)+v_t$ giving what a sensor should read for a proposed state, with measurement noise $v_t$. Its likelihood $p(z_t\mid x_t)$ drives the filter's correction step. · 가정한 상태에서 센서가 읽어야 할 값을 주는 식 $z_t=h(x_t)+v_t$이고, $v_t$는 측정 잡음이다. 그 우도 $p(z_t\mid x_t)$가 필터의 보정 단계를 이끈다. → [[04-robotics/state-estimation-slam|State estimation §3]]
- **Observation space** · 관측 공간 — What the policy receives as input (joint states, a heightmap, images); the action space is what it emits. Both change the problem more than the algorithm does, so they are part of the result. · 정책이 입력으로 받는 것(관절 상태, 높이맵, 이미지)이며, 행동 공간은 정책이 내보내는 것이다. 둘 다 알고리즘보다 문제를 크게 바꾸므로 결과의 일부다. → [[02-foundations/rl-basics|RL Basics §10]]
- **Observer (state observer)** · 관측기 — An estimator that runs a copy of the model and corrects it with the measurement residual, $\dot{\hat x} = A\hat x + Bu + L(y - C\hat x)$, to recover unmeasured states. The Kalman filter is its stochastic version. · 모델의 복사본을 돌리고 측정 잔차로 보정해($\dot{\hat x} = A\hat x + Bu + L(y - C\hat x)$) 측정되지 않은 상태를 복원하는 추정기. 칼만 필터가 그 확률적 버전이다. → [[04-robotics/control-theory-ce397|Control Theory §8]]
- **Obstacle inflation** · 팽창(inflation) — Growing occupied cells by the robot's radius so a planner can treat the robot as a point. Exact only for a circular robot; a decaying cost beyond it adds a preferred margin. · 계획기가 로봇을 점으로 다룰 수 있도록 점유 칸을 로봇 반경만큼 키우는 것. 원형 로봇에서만 정확하고, 그 바깥에 감쇠하는 비용을 더하면 계획기가 지키려는 여유가 생긴다. → [[04-robotics/planning-decision-making|Planning §2]]
- **Occlusion** · 가림 — Part of a scene being hidden from a sensor by another object or by the robot's own body. Perception must reconstruct what it cannot see or treat it as unknown. · 다른 물체나 로봇 자신의 몸 때문에 장면 일부가 센서에서 가려지는 것. 인식은 보이지 않는 부분을 복원하거나 미지로 다뤄야 한다. → [[01-canonical-papers/notes/9-navigation/anymal-parkour|ANYmal Parkour]]
- **Occupancy grid** · 점유 격자(occupancy grid) — A map divided into cells, each storing the probability that the cell is occupied. A cell reads as free, occupied, or unknown, and unknown is not free. · 지도를 칸으로 나누고 각 칸에 점유되어 있을 확률을 담는 표현. 칸은 비어 있음·점유됨·미지 중 하나이며, 미지는 비어 있음이 아니다. → [[04-robotics/planning-decision-making|Planning §2]]
- **OCR (optical character recognition)** · OCR — Reading text that appears inside images, such as documents, signs, and screens. A common weak spot of VLMs with few or low-resolution visual tokens. · 문서·표지판·화면처럼 이미지 안에 있는 글자를 읽는 것. 시각 토큰이 적거나 해상도가 낮은 VLM의 흔한 약점이다. → [[01-canonical-papers/notes/3-vlm/qwen-vl|Qwen-VL]]
- **Octile distance** · 옥타일 거리 — The exact cost on an empty 8-connected grid with straight steps 1 and diagonal steps $\sqrt 2$: $\max(\Delta x, \Delta y) + (\sqrt 2 - 1)\min(\Delta x, \Delta y)$. It is a consistent A\* heuristic there; Manhattan distance would overestimate. · 직선 1, 대각 $\sqrt 2$인 빈 8-연결 격자에서의 정확한 비용: $\max(\Delta x, \Delta y) + (\sqrt 2 - 1)\min(\Delta x, \Delta y)$. 그런 격자에서 일관적인 A\* 휴리스틱이며, 맨해튼 거리는 과대추정한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §6]]
- **OEM (original equipment manufacturer)** · OEM(원 장비 제조사) — The company that builds the machine, such as Caterpillar or Komatsu. OEM-integrated autonomy is built into the manufacturer's own machines, unlike retrofit kits. · Caterpillar·Komatsu처럼 기계를 만드는 회사. OEM 통합 자율성은 개조 키트와 달리 제조사 자체 기계에 내장된다. → [[05-construction-robotics/industry-deployment|Industry & Deployment §1]]
- **Offline RL** · 오프라인 RL — RL from a fixed logged dataset with rewards and no new interaction. It can stitch behaviour better than any single demonstrator but risks unstable value extrapolation, so methods penalize actions the data does not support. · 보상이 달린 고정 기록 데이터로, 새 상호작용 없이 하는 RL. 어느 단일 시연자보다 나은 행동을 꿰맬 수 있지만 가치 외삽이 불안정해, 데이터가 뒷받침하지 않는 행동에 비관주의를 더한다. → [[02-foundations/rl-basics|RL Basics §6]], [[02-foundations/rl-basics|RL Basics §3.5]]
- **Off-site construction (prefabrication)** · 오프사이트 시공(프리팹) — Making building components in a factory and bringing them to site for installation, as opposed to building them in place on site. · 건물 부재를 현장에서 짓는 대신 공장에서 만들어 현장으로 가져와 설치하는 방식. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §4]]
- **OMPL (sampling-based planners)** · OMPL — MoveIt's default planner library (RRTConnect and relatives): fast in high dimensions and clutter, probabilistically complete, and not deterministic, so the same request yields a different path each run. · MoveIt의 기본 플래너 라이브러리(RRTConnect 등). 고차원과 복잡한 장면에서 빠르고 확률적으로 완전하지만 결정적이지 않아, 같은 요청에도 매번 다른 경로가 나온다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §5]]
- **One-stage vs two-stage detector** · 1단계 / 2단계 검출기 — A two-stage detector first proposes regions and then classifies and refines each (accuracy first); a one-stage detector predicts all boxes in a single pass (speed first). · 2단계 검출기는 먼저 영역을 제안하고 각 영역을 분류·정제하며(정확도 우선), 1단계 검출기는 한 번의 순전파로 모든 박스를 예측한다(속도 우선). → [[01-canonical-papers/notes/2-computer-vision/faster-r-cnn|Faster R-CNN]], [[01-canonical-papers/notes/2-computer-vision/yolo|YOLO]]
- **Online adaptation vs generalization** · 적응 vs 일반화 — Two different traversability claims: adaptation learns terrain in the field from seconds or minutes of the robot's own data; generalization works on new environments or robots without it. They need different evidence. · 서로 다른 두 traversability 주장: 적응은 현장에서 로봇 자신의 데이터 몇 초·몇 분으로 지형을 배우고, 일반화는 그런 데이터 없이 새 환경이나 로봇에서 작동한다. 요구되는 증거가 다르다. → [[04-robotics/traversability-off-road|Traversability §2]]
- **On-policy vs off-policy** · 온폴리시/오프폴리시 — On-policy methods (PPO) must learn from data their *current* policy just generated and discard it after; off-policy methods (Q-learning, SAC) can learn from data collected by a different or older policy, so they can reuse a replay buffer. · 온폴리시는 현재 정책이 방금 만든 데이터로만 학습하고 버린다; 오프폴리시는 다른/과거 정책의 데이터도 재사용할 수 있다. → [[02-foundations/rl-basics|RL Basics §3]]
- **Open list and closed set** · 열린 목록과 닫힌 집합 — In best-first search, the priority queue of discovered vertices awaiting expansion, and the set already expanded. A closed set is safe only when a vertex's first pop is final, as in Dijkstra or consistent A\*. · 최선 우선 탐색에서 발견되어 확장을 기다리는 정점의 우선순위 큐와, 이미 확장한 정점의 집합. 닫힌 집합은 Dijkstra나 일관적 A\*처럼 처음 꺼낸 값이 최종일 때만 안전하다. → [[02-foundations/algorithms/graph-algorithms|11.6 §6]]
- **OpenReview** · OpenReview — An online platform that hosts submissions, reviews and author responses for many machine-learning venues. CoRL runs its review process on it. · 여러 기계학습 venue의 제출, 심사, 저자 응답을 담는 온라인 플랫폼. CoRL이 이곳에서 심사를 진행한다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **OpenVLA** · OpenVLA — A fully open 7B VLA (SigLIP and DINOv2 vision, Llama-2 language) that emits binned action tokens and fine-tunes with LoRA. It is the standard open baseline new VLAs report against. · 완전히 공개된 7B VLA(SigLIP·DINOv2 시각, Llama-2 언어)로, 구간 나눈 행동 토큰을 출력하고 LoRA로 파인튜닝한다. 새 VLA들이 비교 기준으로 삼는 표준 오픈 베이스라인이다. → [[01-canonical-papers/notes/4-vla/openvla|OpenVLA]]
- **Open-vocabulary** · open-vocabulary — Accepting goals or queries as free-form language rather than a fixed category list, as in HM3D-OVON's 379 categories versus six-category ObjectNav. · 고정된 범주 목록이 아니라 자유 형식 언어로 목표나 질의를 받는 것. 6범주 ObjectNav와 379범주 HM3D-OVON의 차이가 그 예다. → [[04-robotics/semantic-language-navigation|Semantic navigation §6]]
- **Open weights** · 오픈 가중치 — Releasing a trained model's parameters so anyone can run and fine-tune it, without necessarily releasing the training data or recipe. Open weights do not mean open data. · 학습된 모델의 파라미터를 공개해 누구나 실행·파인튜닝할 수 있게 하는 것. 학습 데이터나 레시피 공개를 뜻하지는 않는다. 오픈 가중치는 오픈 데이터가 아니다. → [[01-canonical-papers/notes/3-vlm/qwen-vl|Qwen-VL]]
- **Open X-Embodiment (OXE)** · Open X-Embodiment(OXE) — A large pooled dataset of robot manipulation episodes from many robots. Its shared schema records vision, proprioception and language but has no force or tactile column. · 여러 로봇의 조작 에피소드를 모은 대형 데이터셋. 공유 스키마가 시각, 고유수용감각, 언어는 기록하지만 힘이나 촉각 열은 없다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §7]]
- **Operational definition** · 조작적 정의 — A concrete measurement rule that turns a vague term into something checkable, such as what counts as soil variation, an intervention or success. · 모호한 용어를 확인 가능한 것으로 바꾸는 구체적 측정 규칙(예: 무엇을 토질 변동, 개입, 성공으로 칠 것인가). → [[06-research-practice/research-questions-claims|Research Questions §6]]
- **Operational-space control (OSC)** · 작업공간(operational space) 제어 — Khatib's method of writing control directly in end-effector coordinates using the operational-space inertia, $\mathcal{F} = \Lambda\ddot x_d + \mu + p$ with $\tau = J^\top\mathcal{F}$, so commanded task behavior is the same in every pose. · 작업 공간 관성을 써서 제어를 말단 좌표에서 직접 쓰는 Khatib의 방법, $\mathcal{F} = \Lambda\ddot x_d + \mu + p$, $\tau = J^\top\mathcal{F}$. 명령한 작업 거동이 모든 자세에서 같아진다. → [[04-robotics/force-compliance-control|Force & Compliance Control §4]]
- **Operational space (task space)** · 작업 공간 — Working in end-effector coordinates rather than joint coordinates. Contact happens here, so force control is formulated here; the operational-space inertia $\Lambda = (JM^{-1}J^\top)^{-1}$ is the apparent mass the end-effector shows the world in each direction, and it is direction-dependent even when the arm is not. · 관절 좌표가 아니라 말단 좌표에서 다루는 공간. 접촉이 여기서 일어나므로 힘 제어도 여기서 정식화된다. 작업 공간 관성 $\Lambda$는 말단이 각 방향에서 세상에 보이는 겉보기 질량이며, 팔은 대칭이어도 방향에 따라 다르다. → [[02-foundations/manipulator-kinematics-dynamics|10. §6]]
- **Optical flow** · optical flow(광학 흐름) — The per-pixel apparent motion field between consecutive frames. Two-stream video networks feed it in as the motion input; it is expensive to compute and brittle on low-texture surfaces. · 연속 프레임 사이의 픽셀별 겉보기 운동장. two-stream 비디오 네트워크가 움직임 입력으로 넣으며, 계산이 비싸고 저텍스처 표면에서 불안정하다. → [[04-robotics/video-action-understanding|Video §3]]
- **Optical tactile sensor (vision-based tactile sensor)** · 광학(비전 기반) 촉각 센서 — A camera imaging a deformable gel (GelSight, DIGIT) or internal pins (TacTip) from behind. Its raw output is an image at camera rate; geometry and force must be reconstructed or calibrated from it. · 변형되는 젤(GelSight, DIGIT)이나 내부 핀(TacTip)을 뒤에서 카메라로 찍는 센서. 원시 출력은 카메라 주기의 이미지이고, 기하와 힘은 거기서 복원하거나 보정해야 한다. → [[04-robotics/tactile-visuotactile|Tactile §2]]
- **Optimal control** · 최적 제어 — Choosing inputs over time to minimize a cost subject to the dynamics, instead of placing poles by hand. LQR is its exactly solvable linear-quadratic case; MPC re-solves a finite-horizon version online. · 동역학을 지키면서 비용을 최소화하도록 시간에 따른 입력을 고르는 것으로, 극점을 손으로 놓는 방식과 대비된다. LQR은 정확히 풀리는 선형-이차 사례이고, MPC는 유한 지평판을 온라인으로 다시 푼다. → [[04-robotics/lqr-lqg|LQR/LQG]]
- **Optimal substructure** · 최적 부분 구조 — The property that an optimal solution contains optimal solutions to smaller instances of the same problem, usually proved by cut-and-paste. DP and greedy both need it; the longest simple path lacks it. · 최적해가 같은 문제의 더 작은 사례에 대한 최적해를 품는 성질로, 보통 잘라 붙이기 논법으로 증명한다. DP와 그리디 모두 필요로 하며, 최장 단순 경로에는 이 성질이 없다. → [[02-foundations/algorithms/dynamic-programming|11.5 §1]]
- **Optimal-transport path (OT path, straight-line path)** · 최적 수송(직선) 경로 — The flow-matching conditional path $x_t=(1-t)x_0+tx_1$, a straight line from noise to one data point with constant velocity. The learned overall field need not stay straight, so fewer steps are possible, not guaranteed. · 노이즈에서 데이터 한 점까지 일정한 속도로 가는 직선인 flow matching 조건부 경로 $x_t=(1-t)x_0+tx_1$. 학습된 전체 속도장이 직선으로 남는다는 보장은 없어서, 적은 스텝은 가능할 뿐 보장되지 않는다. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Oracle information** · 오라클 정보 — Information a method quietly uses during evaluation that deployment will not have: ground-truth state, perfect calibration, or human resets. · 방법이 평가 중에 조용히 쓰지만 배포 환경에는 없을 정보: 실측 상태, 완벽한 캘리브레이션, 사람의 리셋. → [[02-foundations/ml-practice|ML Practice §5]]
- **ORB** · ORB — A fast binary feature: FAST corners plus a rotated BRIEF descriptor of 256 intensity comparisons, matched by Hamming distance (XOR, then count set bits). Not scale-invariant alone, so it is extracted over an image pyramid. · 빠른 이진 특징: FAST 코너와 밝기 비교 256개의 회전 BRIEF 기술자를 쓰고, 해밍 거리(XOR 후 켜진 비트 수)로 매칭한다. 자체로는 스케일 불변이 아니어서 이미지 피라미드 위에서 추출한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Ornstein–Uhlenbeck noise (OU noise)** · Ornstein–Uhlenbeck 노이즈 — Temporally correlated exploration noise that drifts from its last value instead of being drawn fresh each step, so a continuous-action machine does not jitter. · 매 스텝 새로 뽑지 않고 직전 값에서 흘러가는 시간 상관 탐색 노이즈로, 연속 행동 기계가 떨지 않게 한다. → [[02-foundations/rl-basics|RL Basics §8]]
- **Orthogonal matrix** · 직교행렬 — Columns unit-length and mutually perpendicular, so $Q^\top Q = I$ and multiplying by $Q$ is a pure rotation/reflection that changes no lengths. The $U$ and $V$ of an SVD, and every rotation matrix. · 열이 길이 1이고 서로 수직이어서 $Q^\top Q = I$이고, $Q$를 곱하는 것은 길이를 바꾸지 않는 순수 회전/반사다. SVD의 $U$와 $V$가 그렇고, 모든 회전 행렬이 그렇다. → [[02-foundations/linear-algebra|Linear Algebra §4]]
- **Overfitting** · 과적합 — Fitting the training data so closely that new data suffers: training loss keeps falling while validation loss rises. The failure that splits, regularization and early stopping exist to manage. · 학습 데이터에 너무 맞춰 새 데이터에서 나빠지는 것: 학습 손실은 계속 내려가는데 validation 손실은 오른다. 데이터 분할·정규화·조기 종료가 관리하려는 실패다. → [[02-foundations/ml-practice|ML Practice §2]]
- **Overlapping subproblems** · 겹치는 부분 문제 — A natural recursion asking the same smaller question many times, which is what makes caching pay: a few thousand distinct subproblems can hide a recursion tree with billions of nodes. · 자연스러운 재귀가 같은 작은 질문을 여러 번 묻는 것으로, 캐시가 이득이 되는 이유다: 서로 다른 부분 문제 몇천 개가 수십억 노드짜리 재귀 트리 뒤에 숨어 있을 수 있다. → [[02-foundations/algorithms/dynamic-programming|11.5 §1]]
- **Overshoot** · 오버슈트 — How far a step response passes its final value before settling, as a fraction. For a second-order system $M_p = e^{-\pi\zeta/\sqrt{1-\zeta^2}}$, about 44% at $\zeta = 0.25$. · 계단 응답이 정착하기 전에 최종값을 넘어서는 정도(비율). 2차 시스템에서 $M_p = e^{-\pi\zeta/\sqrt{1-\zeta^2}}$이며 $\zeta = 0.25$면 약 44%다. → [[04-robotics/control-theory-ce397|Control Theory §5]]
- **Package** · 패키지 — The unit of ROS 2 code that declares what it is and what it needs in package.xml, so tools can build it, find its executables, and install its dependencies. · package.xml에 자신이 무엇이고 무엇이 필요한지 선언하는 ROS 2 코드 단위. 덕분에 도구가 빌드하고, 실행 파일을 찾고, 의존성을 설치할 수 있다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §1]]
- **package.xml** · package.xml — A package's manifest: name, version, maintainer, licence, build type, and dependency tags (depend, build_depend, exec_depend, test_depend) whose values are rosdep keys. · 패키지 선언 파일. 이름, 버전, 관리자, 라이선스, build type, 그리고 값이 rosdep key인 의존 태그(depend, build_depend, exec_depend, test_depend)를 담는다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §5]]
- **Paired comparison (paired design)** · 짝지은 비교 — Evaluating both methods on the same scenes, tasks or participants so nuisance variation cancels in the difference. The analysis must then respect the pairing. · 두 방법을 같은 장면, 과제, 참가자에서 평가해 방해 변동이 차이에서 상쇄되게 하는 설계. 분석도 그 짝을 반영해야 한다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §2]]
- **PA-MPJPE (Procrustes-aligned MPJPE)** · PA-MPJPE — MPJPE after Procrustes alignment also removes global rotation and scale. Good PA-MPJPE with poor MPJPE means limb shape is right but orientation or scale is wrong. · Procrustes 정렬로 전역 회전과 스케일까지 제거한 뒤의 MPJPE. PA-MPJPE는 좋은데 MPJPE가 나쁘면 사지 형상은 맞고 방향이나 스케일이 틀린 것이다. → [[04-robotics/human-pose-gaze|Pose & gaze §3]]
- **Paper arc** · 논문 arc — A planned sequence of papers that build one argument, reusing hardware, data and evaluation protocol. A negative result becomes the next paper's motivation instead of a dead end. · 하나의 논증을 쌓는 계획된 논문 순서로, 하드웨어·데이터·평가 프로토콜을 재사용한다. 부정적 결과가 막다른 길이 아니라 다음 논문의 동기가 된다. → [[07-research-program/paper-arc|Paper Arc §1]]
- **Paper transfer (ICRA–IROS transfer)** · 이관 — Moving a paper between IEEE RAS venues: one rejected at ICRA may go to IROS with an author response file, and an accepted RA-L paper may present at one RAS conference within 270 days. · IEEE RAS venue 사이에서 논문을 옮기는 것. ICRA에서 떨어진 논문은 저자 응답 파일과 함께 IROS로 갈 수 있고, 채택된 RA-L 논문은 270일 안에 RAS 학회 한 곳에서 발표할 수 있다. → [[06-research-practice/venue-strategy|Venue Strategy §2]], [[06-research-practice/venue-strategy|Venue Strategy §4]]
- **Parallel-jaw gripper** · 평행 조 그리퍼 — A two-finger gripper whose jaws close toward each other. Its compliant pads act as soft-finger contacts, so two antipodal contacts can force-close a box; results do not transfer to suction or multi-finger hands. · 두 조가 서로를 향해 닫히는 두 손가락 그리퍼. 순응 패드가 soft finger 접촉으로 작용해 대척 접촉 둘로 상자를 force closure할 수 있다. 결과가 흡착이나 다지 핸드로 이전되지는 않는다. → [[04-robotics/grasping|Grasping §3]]
- **Parallel simulation (GPU-parallel simulation)** · 병렬 시뮬레이션 — Running thousands of simulated environments at once to generate experience quickly. It raises sample throughput, not simulator validity; all environments can repeat the same wrong physics. · 수천 개의 시뮬레이션 환경을 동시에 돌려 경험을 빠르게 만드는 것. 샘플 생산량을 높일 뿐 시뮬레이터 타당성은 높이지 않으며, 모든 환경이 같은 잘못된 물리를 반복할 수 있다. → [[05-construction-robotics/sim-to-real|Sim-to-Real §2]]
- **Parameter descriptor** · descriptor — Metadata attached to a parameter: description, allowed range, and whether it is read-only or dynamically typed. A read-only parameter can be set at startup but not afterwards. · 파라미터에 붙는 메타데이터. 설명, 허용 범위, 읽기 전용 여부, 동적 타입 여부를 담는다. 읽기 전용 파라미터는 기동 시에만 설정할 수 있다. → [[04-robotics/ros2/services-actions-parameters|25.3 Services & Actions §6]]
- **Parameter file (ros__parameters)** · 파라미터 파일 — A YAML file whose top-level key is the node's fully qualified name (or a wildcard key matching every node), then the literal key ros__parameters. A wrong node name silently sets nothing. · 최상위 키가 노드의 완전한 이름(또는 모든 노드에 맞는 와일드카드 키)이고 그 아래에 글자 그대로 ros__parameters 키가 오는 YAML 파일. 노드 이름이 틀리면 아무것도 설정되지 않고 조용하다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §11]]
- **Parameter (model parameter)** · 파라미터 — A number fitted to data by gradient descent, such as every entry of the weight matrices and bias vectors. "7B parameters" is a count of these. · 경사 하강이 데이터에 맞춰 정하는 숫자로, 가중치 행렬과 편향 벡터의 모든 성분이 그렇다. "7B 파라미터"는 이것의 개수다. → [[02-foundations/neural-network-basics|0.7 §2]], [[02-foundations/neural-network-basics|0.7 §5]]
- **Parametric body model** · 파라메트릭 신체 모델 — A model mapping a few shape parameters $\beta$ and pose parameters $\theta$ to a full body mesh (the SMPL family; MANO for hands), enabling surface, volume, contact and occlusion reasoning. · 적은 수의 형상 파라미터 $\beta$와 자세 파라미터 $\theta$를 전신 메시로 사상하는 모델(SMPL 계열, 손은 MANO). 표면·부피·접촉·가림 추론을 가능하게 한다. → [[04-robotics/human-pose-gaze|Pose & gaze §1]]
- **Parkour (agile locomotion)** · 파쿠르(민첩 로코모션) — A legged robot crossing consecutive obstacles at speed by walking, jumping, climbing, and crouching. It sits between locomotion, which keeps the body upright, and navigation, which picks the route. · 레그드 로봇이 걷기·뛰기·오르기·웅크리기로 연속 장애물을 빠르게 넘는 것. 몸을 세우는 로코모션과 경로를 고르는 내비게이션 사이에 있다. → [[01-canonical-papers/notes/9-navigation/anymal-parkour|ANYmal Parkour]]
- **Particle filter** · 파티클 필터 — A Bayes filter representing the belief by weighted samples: move each through the motion model, weight by measurement fit, resample by weight. Handles multimodal beliefs, but resampling can collapse diversity (particle depletion). · 가중 표본으로 belief를 표현하는 베이즈 필터: 표본을 운동 모델에 통과시키고, 측정 부합도로 가중하고, 가중치에 비례해 재표집한다. 다봉 belief를 다루지만 재표집이 다양성을 없앨 수 있다(파티클 고갈). → [[04-robotics/state-estimation-slam|State estimation §5]]
- **Partition (quicksort)** · 파티션 — Rearranging a range around a pivot so smaller elements end up on one side and larger ones on the other. Lomuto and Hoare are the two classic schemes; a three-way version handles many duplicates. · 피벗을 기준으로 작은 원소는 한쪽, 큰 원소는 다른 쪽으로 가도록 범위를 재배치하는 단계. Lomuto와 Hoare가 고전적인 두 방식이며, 중복이 많으면 삼분할 파티션을 쓴다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §3]]
- **Passivity** · 수동성 — A system is passive if it can store and dissipate energy but never create it; interconnecting passive systems keeps the whole passive, hence stable. A communication delay destroys passivity, which is why delayed force feedback can oscillate at any gain. · 에너지를 저장·소산할 뿐 만들어내지 않는 성질. 수동 시스템끼리 연결해도 수동성이 유지되어 안정하다. 통신 지연이 이 성질을 깨뜨리며, 그래서 지연된 힘 피드백은 어떤 게인에서도 진동할 수 있다. → [[04-robotics/teleoperation-demonstration|12. §3]]
- **Passivity observer/controller (PO/PC)** · PO/PC — An observer tracks exchanged energy; when it detects net generated energy, a controller injects only enough damping to remove it. This is an online stability safeguard, not a guarantee of transparent feel. · 관측기가 교환 에너지를 추적하고 순 에너지 생성이 감지되면 제어기가 그것을 제거할 만큼만 감쇠를 주입한다. 온라인 안정성 안전장치이지 투명한 느낌의 보장은 아니다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|Rendering & Stability]]
- **Patch embedding (patchify)** · 패치 토큰화 — Cutting an image into fixed patches (16×16 pixels in ViT) and linearly projecting each to a vector, so the image becomes a token sequence a Transformer can read. · 이미지를 고정 크기 패치(ViT에서는 16×16 픽셀)로 자르고 각 패치를 선형 투영해 벡터로 만드는 것. 이미지가 Transformer가 읽을 수 있는 토큰 시퀀스가 된다. → [[01-canonical-papers/notes/1-foundations/vit|ViT]]
- **Patchify (patch size)** · 패치 토큰화(패치 크기) — Cutting an image or latent into fixed-size square patches that become Transformer tokens. A smaller patch size means more tokens and more compute; DiT tests sizes 2, 4 and 8. · 이미지나 잠재 표현을 고정 크기 정사각형 패치로 잘라 Transformer 토큰으로 만드는 것. 패치가 작을수록 토큰과 연산이 늘며, DiT는 2·4·8 크기를 시험한다. → [[01-canonical-papers/notes/6-diffusion/dit|DiT]]
- **Path compression** · 경로 압축 — A union-find optimization: during find, point every node on the path directly at the root. With union by size or rank, $m$ operations cost $O(m\,\alpha(n))$, effectively constant each. · union-find 최적화: find 중에 경로 위 모든 노드가 루트를 직접 가리키게 한다. 크기·랭크 기준 합치기와 함께 쓰면 연산 $m$개가 $O(m\,\alpha(n))$, 사실상 연산당 상수다. → [[02-foundations/algorithms/data-structures|11.2 §7]]
- **PCA (principal component analysis)** · PCA — Center the data and project it onto the top eigenvectors of its covariance, the directions of largest variance, which are also the top right singular vectors of the data matrix. · 데이터를 중심화하고 공분산의 상위 고유벡터, 즉 분산이 가장 큰 방향으로 투영하는 방법. 이 방향은 데이터 행렬의 상위 오른쪽 특이벡터와 같다. → [[02-foundations/linear-algebra|Linear Algebra §4]]
- **PCK (percentage of correct keypoints)** · PCK(올바른 키포인트 비율) — The fraction of joints whose error falls under a threshold, such as 150 mm in 3D or half the head size in 2D. A hit rate at one tolerance, not an average distance. · 오차가 문턱(3D에서 150 mm, 2D에서 머리 크기의 절반 등) 아래인 관절의 비율. 평균 거리가 아니라 한 허용치에서의 적중률이다. → [[04-robotics/human-pose-gaze|Pose & gaze §3]]
- **P (complexity class)** · P — The class of yes/no problems solvable in time polynomial in the input length in bits, $O(n^k)$ for a fixed $k$. Asking whether a graph path exists is in P, because BFS answers it. · 입력 비트 길이에 대한 다항 시간, 즉 고정된 $k$에 대해 $O(n^k)$에 풀리는 예/아니오 문제의 클래스. 그래프에 경로가 있는지는 BFS로 답하므로 P에 속한다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **Peer review** · 동료 심사 — Evaluation of a paper by independent experts before a venue accepts it. Preprints have none, workshops usually a light version, conferences a single round, journals revision rounds. · 게재지가 논문을 받아들이기 전에 독립 전문가들이 평가하는 것. 프리프린트에는 없고, 워크숍은 대개 가볍고, 학회는 한 라운드, 저널은 개정 라운드가 있다. → [[01-canonical-papers/how-to-read|How to Read §4.6]]
- **Peg-in-hole insertion** · peg-in-hole — The classic assembly task of inserting a part into a close-fitting hole. Small misalignments cause wedging or jamming, which a passive remote-centre compliance device solves without any sensor. · 부재를 꼭 맞는 구멍에 끼우는 고전적 조립 과제. 작은 정렬 오차가 wedging이나 jamming을 일으키며, 수동 RCC 장치가 센서 없이 이를 해결한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §5]]
- **Penalty-based rendering** · 벌점 기반 렌더링 — Haptic rendering that pushes the device point out along the nearest surface normal with force $Kd$ for penetration depth $d$. Stateless and cheap, but it lacks locality, jumps at edges, and pops through thin objects. · 침투 깊이 $d$에 대해 힘 $Kd$로 장치 점을 가장 가까운 표면 법선을 따라 밀어내는 햅틱 렌더링. 상태가 없고 싸지만 국소성이 없고, 모서리에서 힘이 튀며, 얇은 물체를 뚫고 나간다. → [[04-robotics/haptics-teleoperation/haptic-rendering-algorithms|24.7 Rendering Algorithms §2]]
- **Perceiver Resampler** · Perceiver Resampler — Flamingo's module that compresses a variable number of visual features into a fixed small set of visual tokens for the language model. · 가변 개수의 시각 특징을 언어모델에 넣을 고정된 소수의 시각 토큰으로 압축하는 Flamingo의 모듈. → [[01-canonical-papers/notes/3-vlm/flamingo|Flamingo]]
- **Percentage-point gain** · 퍼센트 포인트 증가 — The difference between two rates: 0% to 95% is a 95-point gain. It is not a relative increase, which is undefined when the baseline is 0%. · 두 비율의 차이. 0%에서 95%로 가면 95포인트 증가다. 상대 증가율이 아니며, 기준이 0%이면 상대 증가율은 정의되지 않는다. → [[01-canonical-papers/notes/7-robotics/mobile-aloha|Mobile ALOHA]]
- **Perceptive locomotion** · 지각 로코모션 — Legged control that also uses exteroception, typically a terrain height map, to adapt the gait before contact. It gains speed and efficiency over blind control only if it copes with wrong perception. · 외수용 감각, 보통 지형 높이 지도를 함께 써서 접촉 전에 보행을 맞추는 제어. 틀린 인식에 대처할 수 있을 때에만 맹목 제어보다 빠르고 효율적이다. → [[01-canonical-papers/notes/9-navigation/miki-perceptive-locomotion|Miki 2022]]
- **Permutation invariance (point sets)** · 순서 무관성 — An output that does not change when input elements are reordered. A point cloud lists points in no meaningful order, so PointNet pools per-point features symmetrically. · 입력 원소 순서를 바꿔도 출력이 변하지 않는 성질. 포인트 클라우드의 점에는 의미 있는 순서가 없어 PointNet은 점별 특징을 대칭 풀링한다. → [[05-construction-robotics/site-perception|Site Perception §3]]
- **Perplexity** · perplexity — The exponential of cross-entropy, $2^H$ in bits or $e^H$ in nats: the language model is as confused as if choosing among that many options. · 교차 엔트로피의 지수(비트면 $2^H$, 나트면 $e^H$). 언어 모델이 그만큼 많은 선택지 중에서 고르는 것처럼 헷갈린다는 뜻이다. → [[02-foundations/information-theory|Information Theory §2]]
- **Person following** · 사람 추종 — A navigation task in which the robot keeps up with a moving person. It matters for robots that must accompany a worker rather than only avoid one. · 로봇이 움직이는 사람을 따라가는 내비게이션 과제. 작업자를 피하기만 하지 않고 동행해야 하는 로봇에 중요하다. → [[01-canonical-papers/notes/9-navigation/uni-navid|Uni-NaVid]]
- **Phase lag** · 위상 지연 — The frequency-dependent delay a filter adds. Causal smoothing generally introduces it, so a heavily filtered signal reaches a feedback loop late; ask what a filter costs in phase, not only what noise it removes. · 필터가 더하는 주파수 의존 지연. 인과적 평활은 대개 이를 만들어 강하게 거른 신호가 피드백 루프에 늦게 도착한다. 필터가 잡음을 얼마나 없애는지뿐 아니라 위상에서 무엇을 치르게 하는지 물어야 한다. → [[02-foundations/signal-processing|Signal Processing §4]]
- **Phase margin** · 위상 여유 — The extra phase lag, in degrees at the gain-crossover frequency, a feedback loop can absorb before its correction arrives so late that it amplifies the error instead of cancelling it. Every filter added to tame sensor noise spends some of it. · 이득 교차 주파수에서 피드백 루프가 견딜 수 있는 여분의 위상 지연(도). 실제 허용 시간 지연은 이 값(rad)을 교차 주파수로 나눈 지연 여유다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Photometric stereo** · 광도 스테레오 — Recovering a surface's shape by lighting it from several known directions and reading how its brightness changes. GelSight uses it to turn a camera view of pressed gel into a contact height map. · 알려진 여러 방향에서 표면을 비추고 밝기 변화로 형상을 복원하는 기법. GelSight는 이를 써서 눌린 젤의 카메라 영상을 접촉 높이 지도로 바꾼다. → [[01-canonical-papers/notes/7-robotics/gelsight|GelSight]]
- **Physical AI** · physical AI — Umbrella term for AI that perceives and acts in the physical world, such as robots and autonomous vehicles. These notes also use it for filling scarce real data with simulation and world models. · 로봇이나 자율주행차처럼 물리 세계를 지각하고 그 안에서 행동하는 AI를 아우르는 말. 이 노트들에서는 귀한 실데이터를 시뮬레이션과 월드모델로 채우는 전략의 이름이기도 하다. → [[01-canonical-papers/notes/4-vla/gr00t-n1|GR00T N1]], [[01-canonical-papers/notes/5-world-models/cosmos|Cosmos]]
- **Physical plausibility** · 물리적 그럴듯함 — Generated video looking physically believable, which is weaker than being physically correct. Video world models often break causality or conservation while looking plausible, the key risk when using them as training grounds. · 생성된 비디오가 물리적으로 그럴듯해 보이는 것으로, 물리적으로 정확한 것보다 약한 조건이다. 비디오 월드모델은 그럴듯해 보이면서 인과나 보존 법칙을 자주 깨며, 이것이 훈련장으로 쓸 때의 핵심 위험이다. → [[01-canonical-papers/notes/5-world-models/sora|Sora]], [[01-canonical-papers/notes/5-world-models/genie|Genie]]
- **Physics simulator** · 시뮬레이터 — Software that predicts motion and contact under a modeled physics. Its outputs are predictions under its assumptions, so a simulated contact force is a modeling choice, not a measurement. · 모델링된 물리 아래에서 운동과 접촉을 예측하는 소프트웨어. 출력은 그 가정 아래의 예측이므로, 시뮬레이션한 접촉력은 측정이 아니라 모델링 선택이다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §1]], [[06-research-practice/simulators-benchmarks-datasets|Simulators §10]]
- **Physiological computing** · 생리 컴퓨팅 — Estimating a worker's state, such as stress, fatigue, or attention, from body signals like EEG or heart rate. A biosignal correlate is not a causal state estimate. · EEG나 심박 같은 신체 신호로 스트레스·피로·주의 같은 작업자 상태를 추정하는 것. 바이오신호 상관관계는 인과적 상태 추정이 아니다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **PID control** · PID — The controller most hardware runs, $u = K_p e + K_i\int e\,dt + K_d\dot e$: proportional acts on present error, integral removes steady offset, derivative adds damping but amplifies sensor noise. · 대부분의 하드웨어가 돌리는 제어기, $u = K_p e + K_i\int e\,dt + K_d\dot e$. 비례는 현재 오차에 반응하고, 적분은 정상 상태 오프셋을 없애며, 미분은 감쇠를 더하지만 센서 잡음을 증폭한다. → [[04-robotics/control-theory-ce397|Control Theory §7]]
- **Piezoelectric** · 압전 — A material that produces charge when stressed, and deforms when a field is applied — PVDF, ceramics, ferroelectric crystals. The charge leaks away through any real input impedance, so a piezoelectric sensor measures *change*: vibration, texture and slip onset, never a held grip force. · 응력을 받으면 전하를 만들고 전기장을 걸면 변형하는 재료 — PVDF, 세라믹, 강유전 결정. 전하가 실재하는 입력 임피던스로 빠져나가므로 압전 센서는 *변화*를 잰다. 진동·질감·미끄러짐의 시작이지 유지되는 파지력이 아니다. → [[04-robotics/tactile-visuotactile|14. Tactile & Visuotactile §2.5]]
- **Piezoresistive** · 압전저항 — A material whose resistance changes as it is strained, read with a bridge circuit. The cheapest tactile transduction to get working and the standard first array, at the cost of drift, hysteresis and noise that grows with array size. · 변형될 때 저항이 달라지는 재료를 브리지 회로로 읽는 방식. 가장 싸게 동작시킬 수 있고 표준적인 첫 배열이지만, 드리프트와 이력현상, 그리고 배열이 커질수록 자라는 잡음을 대가로 치른다. → [[04-robotics/tactile-visuotactile|14. Tactile & Visuotactile §2.5]]
- **Pilot (paid pilot)** · pilot(유료 시범 운용) — A limited trial deployment on a customer's site, sometimes paid. "Deployed" in a commercial claim may mean a pilot rather than continuous operation across machines and sites. · 고객 현장에서의 제한된 시범 배치로, 유료일 때도 있다. 상업 주장의 "배치"는 여러 기계·현장의 연속 운용이 아니라 pilot일 수 있다. → [[05-construction-robotics/industry-deployment|Industry & Deployment §3]]
- **Pilot valve (electric pilot valve)** · 파일럿 밸브 — A small valve whose output moves a larger main hydraulic valve. Electric pilot valves let a computer, rather than cab levers, command the machine. · 출력이 더 큰 주 유압 밸브를 움직이는 작은 밸브. 전자 파일럿 밸브는 운전석 레버 대신 컴퓨터가 기계를 명령하게 해 준다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §4]]
- **Pinhole camera model** · 핀홀 카메라 모델 — Ideal projection from similar triangles, $u=f_xX/Z+c_x$, with focal lengths in pixels and principal point $(c_x,c_y)$. Dividing by $Z$ makes it nonlinear, non-invertible, and destroys absolute scale. · 닮은꼴 삼각형에서 나온 이상적 투영 $u=f_xX/Z+c_x$. 초점 거리는 픽셀 단위이고 $(c_x,c_y)$는 주점이다. $Z$로 나누므로 비선형·비가역이며 절대 스케일이 사라진다. → [[04-robotics/geometric-perception-calibration|Geometric perception §1]]
- **Planning pipeline (request and response adapters)** · 계획 파이프라인 — MoveIt's three-stage chain: request adapters check or fix the request, the planner finds a path, response adapters add timing and validate. An error naming an adapter means the planner never ran. · MoveIt의 세 단계 사슬. request adapter가 요청을 검사·수정하고, 플래너가 경로를 찾고, response adapter가 시간을 붙이고 검증한다. 오류에 adapter 이름이 나오면 플래너는 돌지도 않은 것이다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §4]]
- **Planning scene (collision objects)** · planning scene — move_group's model of the robot state plus world: collision objects you add, objects attached to the robot after grasping, and an octomap from depth sensors. Plans are only as good as this model. · 로봇 상태와 세계에 대한 move_group의 모형. 직접 추가한 collision object, 파지 후 로봇에 부착한 물체, 깊이 센서로 만든 octomap이 들어간다. 계획은 이 모형만큼만 옳다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §6]]
- **Plant** · 플랜트 — Control-theory word for the system being controlled (a motor, an arm, a heater), as distinct from the controller and the sensor. · 제어 이론에서 제어 대상 시스템(모터, 팔, 히터)을 부르는 말로, 제어기·센서와 구별된다. → [[04-robotics/control-theory-ce397|Control Theory §1]]
- **pluginlib** · pluginlib — The ROS library that loads a C++ class from a shared library at runtime by its registered name, rather than compiling it into the program. ros2_control, MoveIt and Nav2 plugins all load this way. · 프로그램에 컴파일해 넣지 않고, 등록된 이름으로 공유 라이브러리에서 C++ 클래스를 실행 중에 로드하는 ROS 라이브러리. ros2_control, MoveIt, Nav2 플러그인이 모두 이렇게 로드된다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §7]]
- **PnP (Perspective-n-Point)** · PnP — Estimating a camera's pose from $n$ known 3D points and their 2D image projections; a PnP pose is one of the models RANSAC fits to verify matches. · 알려진 3D 점 $n$개와 그 2D 투영으로부터 카메라 pose를 추정하는 문제. PnP pose는 RANSAC이 매칭 검증에 맞추는 모델 중 하나다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Point cloud** · 포인트 클라우드 — A set of 3D points from lidar or from back-projecting a depth image with intrinsics, $X=(u-c_x)Z/f_x$. Every cloud lives in some frame, so extrinsic calibration decides whether clouds line up. · 라이다에서 오거나 깊이 이미지를 intrinsics로 역투영($X=(u-c_x)Z/f_x$)해 얻는 3D 점 집합. 모든 클라우드는 어떤 프레임에 살므로 extrinsic 보정이 서로 맞는지를 좌우한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §3]]
- **Point map** · 포인트맵 — An image-shaped output that stores, for every pixel, the 3D point it sees in a common coordinate frame. VGGT predicts one per input image, anchored to the first frame. · 픽셀마다 그 픽셀이 보는 3D 점을 공통 좌표계로 저장한 이미지 모양의 출력. VGGT는 입력 이미지마다 이것을 첫 프레임 기준으로 예측한다. → [[01-canonical-papers/notes/2-computer-vision/vggt|VGGT]]
- **PointNav (point-goal navigation)** · 점 목표 내비게이션 — Navigating to a goal given as coordinates. Classical geometric mapping and planning can solve it, so it serves as the baseline that semantic navigation methods must beat. · 좌표로 주어진 목표까지 주행하는 과제. 고전적 기하 지도와 계획으로 풀 수 있어, 의미 기반 내비게이션 방법이 넘어야 할 기준선이 된다. → [[01-canonical-papers/notes/9-navigation/gervet-real-world-objectnav|Gervet 2023]]
- **Pole** · 극점 — A root of a transfer function's denominator. Always an eigenvalue of the state-space $A$, but not every eigenvalue is a pole: uncontrollable or unobservable modes cancel and never show up in the transfer function. Left half-plane (continuous) or inside the unit circle (discrete) means stable. · 전달함수 분모의 근. $A$의 고유값이긴 하지만 역은 아니다 — 비가제어·비가관측 모드는 상쇄되어 전달함수에 나타나지 않는다. → [[04-robotics/control-theory-ce397|제어 이론 §5]], [[02-foundations/engineering-math|0.5 §9]]
- **Policy** · 정책 — The agent's decision rule: a mapping from state or observation to a distribution over actions, written $\pi(a \mid s)$. RL and imitation learning both produce one. · 에이전트의 결정 규칙: 상태나 관측을 행동 분포로 보내는 사상으로, $\pi(a \mid s)$로 쓴다. RL과 모방학습 모두 이것을 만든다. → [[02-foundations/rl-basics|RL Basics §1]]
- **Policy gradient** · 정책 그래디언트 — Differentiating the RL objective itself via the log-derivative trick, then raising the log-probability of actions in proportion to the return that followed. Unbiased but high variance, hence baselines, advantages and GAE. · 로그 미분 트릭으로 RL 목적함수 자체를 미분한 뒤, 뒤따른 리턴에 비례해 행동의 로그 확률을 올리는 것. 무편향이지만 분산이 커서 baseline·advantage·GAE가 따라붙는다. → [[02-foundations/rl-basics|RL Basics §4]]
- **Policy iteration** · 정책 반복 — Alternating policy evaluation, computing $V^\pi$ for the current policy, with policy improvement, acting greedily on it, until the policy stops changing. Needs the transition model. · 현재 정책의 $V^\pi$를 구하는 정책 평가와 그에 대해 탐욕적으로 행동하는 정책 개선을 정책이 더 바뀌지 않을 때까지 번갈아 하는 것. 전이 모델이 필요하다. → [[02-foundations/rl-basics|RL Basics §3]]
- **POMDP (partially observable MDP)** · POMDP — An MDP in which the agent cannot see the state, only observations such as images and proprioception. Practical robot policies condition on observation histories or a recurrent state. · 에이전트가 상태를 보지 못하고 이미지·고유수용감각 같은 관측만 받는 MDP. 실제 로봇 정책은 관측 이력이나 순환 상태를 조건으로 삼는다. → [[02-foundations/rl-basics|RL Basics §1]]
- **Pooling** · 풀링 — Downsampling a feature map by summarizing each neighbourhood (max or average). Max-pooling is also what makes [[01-canonical-papers/notes/2-computer-vision/pointnet|PointNet]] permutation-invariant — the same operation used for a different reason. · 각 이웃을 요약(최댓값 또는 평균)해 특징 맵을 줄이는 연산. max-pooling은 [[01-canonical-papers/notes/2-computer-vision/pointnet|PointNet]]을 순열 불변으로 만드는 장치이기도 하다 — 같은 연산, 다른 이유.
- **Port (one-port, two-port)** · 포트(1-포트, 2-포트) — A force–velocity pair through which power enters or leaves a system. A haptic handle is a one-port; a teleoperator is a two-port with one port facing the human and one facing the environment. · 일률이 시스템으로 들어오거나 나가는 힘·속도 쌍. 햅틱 손잡이는 1-포트이고, 원격조작기는 사람을 향한 포트와 환경을 향한 포트를 가진 2-포트다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|24.4 Rendering & Stability §4]], [[04-robotics/haptics-teleoperation/bilateral-teleoperation|24.5 Bilateral Teleoperation §1]]
- **Positional encoding** · 위치 인코딩 — Injecting order information into attention, which is otherwise permutation-blind; also used for high-frequency representation in NeRF. · 순열에 눈이 먼 어텐션에 순서 정보를 주입하는 방법. NeRF에서는 고주파 표현을 위해서도 쓴다.
- **Position quantization** · 양자화(quantization) — Loss of information about where the device lies within one encoder interval $\Delta$. It caps passively renderable stiffness near $2f_c/\Delta$ independently of the sample rate; faster sampling does not improve encoder resolution. · 장치가 encoder 간격 $\Delta$ 안의 어디에 있는지에 대한 정보 손실. 샘플링 주기와 무관하게 수동적으로 렌더링할 수 있는 강성을 $2f_c/\Delta$ 부근으로 제한하며, 빠른 샘플링이 encoder 해상도를 개선하지 않는다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|24.4 Rendering & Stability §3]]
- **Positive (semi)definite $A \succeq 0$** · 양의 (준)정부호 — The matrix version of "$\ge 0$": $x^\top A x \ge 0$ in every direction $x$. Covariance matrices are PSD; a Hessian that is PSD everywhere means the function is convex. · 행렬판 "$\ge 0$": 모든 방향에서 $x^\top A x \ge 0$. 공분산은 PSD이고, 헤시안이 어디서나 PSD면 볼록 함수다. → [[02-foundations/linear-algebra|Linear Algebra §3]]
- **Positive span** · 양의 결합으로 생성 — Vectors positively span a space when every vector in it is a nonnegative combination of them. Force closure needs the contact friction cones to positively span wrench space, because contacts push but never pull. · 공간의 모든 벡터가 주어진 벡터들의 음이 아닌 결합이면 그 벡터들이 공간을 양의 결합으로 생성한다고 한다. 접촉은 밀 수만 있으므로 force closure는 마찰 원뿔들이 렌치 공간을 이렇게 생성하기를 요구한다. → [[04-robotics/modern-robotics/ch12-grasping|MR ch.12]]
- **Positive transfer (negative transfer)** · 양의 전이(positive transfer) — When training on data from other tasks or robots improves performance on yours; negative transfer is when it hurts. OXE found positive cross-robot transfer in small-data regimes but not with large robot-specific datasets. · 다른 과제나 로봇의 데이터로 학습한 것이 내 과제 성능을 높이는 것이며, 오히려 해치면 음의 전이다. OXE는 소규모 데이터 영역에서는 로봇 간 양의 전이를 찾았지만 대규모 로봇별 데이터에서는 찾지 못했다. → [[01-canonical-papers/notes/4-vla/open-x-embodiment|Open X-Embodiment]]
- **Posterior** · 사후 분포 — The belief over hypotheses or parameters after seeing data, $P(\theta \mid x)$, proportional to likelihood times prior. When it is intractable, variational methods or MCMC approximate it. · 데이터를 본 뒤 가설이나 파라미터에 대한 믿음으로, 우도 × 사전에 비례한다. 계산이 불가능하면 변분 방법이나 MCMC로 근사한다. → [[02-foundations/probability|Probability §1]]
- **Posterior collapse** · 사후분포 붕괴 — A VAE failure where a powerful decoder ignores the latent variable, so the encoder's posterior simply matches the prior and the latent carries no information. · 강력한 디코더가 잠재변수를 무시해, 인코더의 사후분포가 그냥 사전분포와 같아지고 잠재변수가 아무 정보도 싣지 않게 되는 VAE의 실패. → [[01-canonical-papers/notes/6-diffusion/vae|VAE]]
- **Potential-based shaping** · 포텐셜 기반 shaping — The one reward-shaping form provably guaranteed not to change the optimal policy: add $F = \gamma\Phi(s')-\Phi(s)$ for a state function $\Phi$. Other shaping can change what is optimal. · 최적 정책을 바꾸지 않음이 증명된 유일한 shaping 형태: 상태 함수 $\Phi$에 대해 $F = \gamma\Phi(s')-\Phi(s)$를 더한다. 다른 shaping은 무엇이 최적인지를 바꿔 놓을 수 있다. → [[02-foundations/rl-basics|RL Basics §7]]
- **Potential field** · 퍼텐셜장 — A planning method that adds an attractive field pulling toward the goal to repulsive fields pushing away from obstacles, then follows the gradient. Gives an action for every state rather than one path, and is cheap and reactive — but a plain potential field has local minima where the robot stops short of the goal. · 목표로 끌어당기는 장과 장애물에서 밀어내는 장을 더한 뒤 그 기울기를 따라가는 계획 방법. 경로 하나가 아니라 모든 상태에 대한 행동을 주며 값싸고 반응적이지만, 단순한 퍼텐셜장에는 로봇이 목표 앞에서 멈춰 서는 국소 최솟값이 있다. → [[04-robotics/planning-decision-making|5. Planning §5]]
- **Power and force limiting (PFL)** · 동력 및 힘 제한(PFL) — A collaboration method that permits contact but bounds force and pressure per body region and per contact type, quasi-static (body part trapped) or transient (it can recoil). It caps speed and payload by design. · 접촉을 허용하되 신체 부위별, 그리고 준정적(끼임)·과도(튕겨 나갈 수 있음) 접촉 유형별 힘·압력 한계로 제한하는 협동 방법. 설계상 속도와 가반하중이 묶인다. → [[04-robotics/hri-safety|HRI & Safety §6]]
- **Power law** · 거듭제곱 법칙 — A relation $L \propto N^{-\alpha}$. On log-log axes it is a straight line with slope $-\alpha$, which is how scaling-law plots are read. · $L \propto N^{-\alpha}$ 꼴의 관계. log-log 축에서 기울기 $-\alpha$인 직선이 되며, 스케일링 법칙 그림은 이렇게 읽는다. → [[01-canonical-papers/notes/1-foundations/scaling-laws|Scaling Laws]]
- **Power-preserving scaling** · 일률 보존 스케일링 — Choosing the force scale equal to the position scale, $s_f=s_x$, so leader and follower power match. Amplifying force beyond that draws energy from the actuators, so passivity must then be argued separately. · 힘 스케일을 위치 스케일과 같게 $s_f=s_x$로 두어 리더와 팔로워의 일률이 같게 하는 것. 그보다 힘을 키우면 액추에이터에서 에너지가 나오므로 수동성을 따로 논증해야 한다. → [[04-robotics/haptics-teleoperation/bilateral-teleoperation|24.5 Bilateral Teleoperation §3]]
- **PPE (personal protective equipment)** · PPE(개인 보호구) — Helmets, gloves, eyewear, and similar gear workers wear on site. It degrades sensing and interfaces, such as EEG under helmets or touch through gloves. · 작업자가 현장에서 착용하는 안전모·장갑·보안경 등. 헬멧 아래 EEG나 장갑 너머 촉각처럼 센싱과 인터페이스를 저하시킨다. → [[05-construction-robotics/hrc-worker-centered|HRC §3]]
- **PPO** · PPO — A policy gradient that imitates a trust region by clipping the policy ratio; the algorithm inside RLHF. · 정책 비율을 클리핑해 신뢰 영역을 흉내 내는 정책 그래디언트. → [[02-foundations/rl-basics|RL 기초 §4]]
- **Precision** · 정밀도 — $TP/(TP+FP)$: of the cases flagged positive, the fraction that really are. It trades against recall through the decision threshold and depends on the base rate. · $TP/(TP+FP)$: 양성으로 표시한 것 중 진짜 양성의 비율. 결정 문턱값을 통해 재현율과 맞바뀌며 기저율에 의존한다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Precision weighting (cue combination)** · 정밀도 가중 — Combining two noisy estimates weighted by inverse variance, so the more reliable cue counts more. A model under particular Gaussian noise assumptions, not a universal law of sensory dominance. · 두 잡음 섞인 추정치를 분산의 역수로 가중해 결합하여 더 신뢰할 만한 cue에 더 큰 무게를 주는 것. 특정 가우시안 잡음 가정 아래의 모델이지 감각 우세의 보편 법칙이 아니다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §4]]
- **Predeclared primary outcome (preregistration)** · 주요 결과 사전 선언 — Stating the main outcome and analysis before running the experiment, so many metrics and conditions cannot be cherry-picked afterward. Preregistration is the formal, publicly filed version. · 실험 전에 주요 결과와 분석을 정해 두어, 많은 지표와 조건 중에서 나중에 골라 보고하지 못하게 하는 것. preregistration은 이를 공개적으로 등록하는 형식적 방식이다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §4]]
- **Prediction horizon** · 지평 — The number of future steps $N$ an MPC optimizes over at each solve. Longer horizons look further ahead but enlarge the problem, and only the first input is applied before re-solving. · MPC가 매 풀이에서 최적화하는 미래 스텝 수 $N$. 길수록 멀리 보지만 문제가 커지며, 첫 입력만 적용하고 다시 푼다. → [[04-robotics/mpc|MPC §2]]
- **Predictor–corrector sampler** · predictor-corrector 샘플러 — A diffusion sampler that alternates a numerical step of the reverse SDE (predictor) with Langevin steps that pull the sample back toward the right distribution (corrector). · 역방향 SDE의 수치 적분 스텝(predictor)과 샘플을 올바른 분포 쪽으로 되돌리는 Langevin 스텝(corrector)을 번갈아 하는 디퓨전 샘플러. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **Prefix-LM attention** · prefix-LM 어텐션 — An attention pattern that is fully bidirectional over the image-and-prompt prefix and causal only over the generated answer. PaliGemma uses it. · 이미지와 프롬프트로 된 접두부 안에서는 완전 양방향이고 생성되는 답변에서만 인과적인 어텐션 패턴. PaliGemma가 이것을 쓴다. → [[01-canonical-papers/notes/3-vlm/paligemma|PaliGemma]]
- **Pre-norm vs post-norm** · pre-norm / post-norm — Where LayerNorm sits in a Transformer block: post-norm (2017) normalizes after adding the residual; pre-norm normalizes the sublayer input first. Modern models use pre-norm, which trains stably without relying on warmup. · Transformer 블록에서 LayerNorm의 위치. post-norm(2017)은 residual을 더한 뒤 정규화하고, pre-norm은 서브레이어 입력을 먼저 정규화한다. 현대 모델은 warmup에 기대지 않고 안정적으로 학습되는 pre-norm을 쓴다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Preprint** · 프리프린트 — A paper posted by its authors, usually on arXiv, with no peer review. It may be entirely sound, but cite it as a preprint and check whether a published version with different numbers exists. · 저자가 보통 arXiv에 올린, 심사를 거치지 않은 논문. 완전히 타당할 수 있지만 프리프린트라고 밝혀 인용하고, 숫자가 다른 출판 판본이 있는지 확인한다. → [[01-canonical-papers/how-to-read|How to Read §4.6]]
- **Prim's algorithm** · Prim 알고리즘 — Minimum spanning tree grown from one vertex by repeatedly adding the lightest edge leaving the tree. It is Dijkstra's loop keyed by the cheapest edge into the tree, $O(E \log V)$ with a heap. · 한 정점에서 시작해 트리를 떠나는 가장 가벼운 간선을 반복해서 더하는 최소 신장 트리 알고리즘. 키를 트리로 들어오는 가장 싼 간선으로 바꾼 Dijkstra 루프이며, 힙으로 $O(E \log V)$다. → [[02-foundations/algorithms/greedy-mst|11.4 §6]]
- **Prior** · 사전 분포 — The belief over hypotheses or parameters before seeing data, $P(\theta)$. It dominates when events are rare, and in MAP estimation a Gaussian prior on weights becomes weight decay. · 데이터를 보기 전 가설이나 파라미터에 대한 믿음. 사건이 드물 때 결과를 지배하며, MAP 추정에서 가중치의 가우시안 사전은 weight decay가 된다. → [[02-foundations/probability|Probability §1]]
- **Priority queue** · 우선순위 큐 — A structure that repeatedly hands back the element with the smallest (or largest) key, usually a binary heap. Python heapq is a min-heap, C++ std::priority_queue a max-heap; it drives Dijkstra and A\*. · 가장 작은(또는 큰) 키의 원소를 반복해서 돌려주는 구조로, 보통 이진 힙으로 구현한다. Python heapq는 최소 힙, C++ std::priority_queue는 최대 힙이며, Dijkstra와 A\*를 움직인다. → [[02-foundations/algorithms/data-structures|11.2 §4]]
- **Privileged teacher–student distillation** · 특권 교사–학생 증류 — Train a teacher policy with RL using simulator-only ground truth (terrain, friction, contact forces), then distill it by supervised learning into a student that sees only onboard proprioceptive history. · 시뮬레이터에만 있는 참값(지형·마찰·접촉력)을 쓰는 교사 정책을 RL로 학습한 뒤, 온보드 고유수용감각 이력만 보는 학생으로 지도학습을 통해 증류하는 것. → [[04-robotics/legged-locomotion|Legged Locomotion §2]]
- **Probabilistic completeness** · 확률적 완전성 — The planning property that the probability of finding a solution approaches one as computation and samples grow. Not a guarantee of speed or of finite-time optimality. · 계산과 표본이 늘 때 해를 찾을 확률이 1에 가까워진다는 계획 알고리즘의 성질. 속도의 보장도, 유한 시간 최적성의 보장도 아니다. → [[04-robotics/planning-decision-making|Planning]]
- **Probability density function (PDF)** · PDF — For a continuous variable, a density $p(x)$ whose integral over an interval is a probability; $p(x)$ itself is not a probability and can exceed 1. Its discrete counterpart, the PMF, is a probability. · 연속 변수의 밀도로, 구간에 대해 적분해야 확률이 된다. $p(x)$ 자체는 확률이 아니며 1을 넘을 수 있다. 이산판인 PMF는 그 값이 곧 확률이다. → [[02-foundations/probability|Probability §2]]
- **Probability-flow ODE** · 확률 흐름 ODE — A deterministic ODE whose distributions at every time match those of the diffusion SDE. It enables exact likelihoods and fast deterministic sampling; DDIM is a discretization of it. · 매 시점의 분포가 디퓨전 SDE와 같은 결정론적 ODE. 정확한 우도 계산과 빠른 결정론적 샘플링을 가능하게 하며, DDIM이 그 이산화다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **Probability path** · 확률 경로 — A family of distributions $p_t$ that moves from noise at $t=0$ to data at $t=1$. Flow matching chooses the path first, then learns the velocity field that follows it; diffusion is one such choice. · $t=0$의 노이즈에서 $t=1$의 데이터로 옮겨 가는 분포족 $p_t$. flow matching은 경로를 먼저 정하고 그것을 따르는 속도장을 학습하며, 디퓨전도 그런 선택지 중 하나다. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Proceedings** · 프로시딩 — The published collection of papers accepted at a conference or workshop. A workshop that publishes proceedings makes its papers count as publications; a transferred RA-L paper is not in the conference proceedings. · 학회나 워크숍에서 채택된 논문을 묶어 출판한 것. 프로시딩을 내는 워크숍의 논문은 출판으로 취급되며, 이관된 RA-L 논문은 학회 프로시딩에 들어가지 않는다. → [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **Process-level twin** · 공정 수준 트윈 — A digital twin covering resources, task dependencies, humans, and multiple robots across a project. Reliable semantics and uncertainty at project scale remain the open problem. · 프로젝트 전반의 자원·과제 의존성·인간·멀티로봇을 다루는 디지털 트윈. 프로젝트 규모의 신뢰할 만한 의미론과 불확실성이 남은 과제다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §2]]
- **Process model (motion model)** · 과정 모델(운동 모델) — The equation $x_t=f(x_{t-1},u_t)+w_t$ predicting how the state evolves under input $u_t$, with $w_t$ absorbing model error. Wheel slip violates it, which is different from sensor noise. · 입력 $u_t$ 아래 상태가 어떻게 변하는지 예측하는 식 $x_t=f(x_{t-1},u_t)+w_t$로, $w_t$가 모델 오차를 흡수한다. 바퀴 미끄럼은 이 모델을 위반하며, 센서 잡음과는 다르다. → [[04-robotics/state-estimation-slam|State estimation §3]]
- **Product of experts** · product-of-experts(전문가 곱) — Combining models by multiplying their scores, so a candidate must satisfy every model and any one can veto it. SayCan multiplies a language model's usefulness score by a skill's affordance score. · 모델들의 점수를 곱해 결합하는 방식으로, 후보가 모든 모델을 만족해야 하고 어느 하나라도 거부할 수 있다. SayCan은 언어모델의 유용성 점수와 skill의 affordance 점수를 곱한다. → [[01-canonical-papers/notes/4-vla/saycan|SayCan]]
- **Product of exponentials (PoE)** · 지수 곱 공식(PoE) — Forward kinematics as $T(\theta) = e^{[\mathcal{S}_1]\theta_1}\cdots e^{[\mathcal{S}_n]\theta_n}M$: start at the home pose $M$ and let each joint's screw axis, written in the fixed frame, move everything downstream. No per-link frames needed. · 순기구학을 $T(\theta) = e^{[\mathcal{S}_1]\theta_1}\cdots e^{[\mathcal{S}_n]\theta_n}M$로 쓰는 것: 홈 자세 $M$에서 시작해 고정 프레임에서 쓴 각 관절의 스크류 축이 하류 전체를 움직인다. 링크별 프레임이 필요 없다. → [[04-robotics/modern-robotics/ch04-forward-kinematics|MR ch.4]]
- **Progress monitoring** · 공정 모니터링 — Comparing site photos or scans against the design model and schedule to infer which components are installed, missing, or deviating. · 현장 사진이나 스캔을 설계 모델·공정표와 비교해 어떤 부재가 설치·누락·편차 상태인지 추론하는 것. → [[05-construction-robotics/site-perception|Site Perception §2]]
- **Progress score (partial credit)** · 진행 점수(부분 점수) — Scoring a long-horizon task by how far a policy got, such as stages or subtasks completed. A high progress score and a zero success rate are compatible, so look for both. · 긴 지평 과제를 정책이 얼마나 갔는지(완료한 단계·하위 과제 수)로 채점하는 것. 높은 진행 점수와 0% 성공률은 함께 있을 수 있으므로 둘 다 확인한다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §11]]
- **Promptable segmentation** · 프롬프트 가능한 분할 — Segmenting whatever a prompt indicates (a point, box, or rough mask) instead of a fixed class list. SAM returns three candidate masks because a single click can be ambiguous. · 고정 클래스 목록 대신 프롬프트(점, 박스, 대략의 마스크)가 가리키는 대상을 분할하는 과제. 클릭 하나는 모호할 수 있어 SAM은 후보 마스크 3개를 낸다. → [[01-canonical-papers/notes/2-computer-vision/sam|SAM]]
- **Prompt engineering** · 프롬프트 엔지니어링 — Tuning the wording of a prompt to improve a model's output, such as CLIP's "a photo of a {class}" template. Reported zero-shot numbers can depend on it. · 모델 출력을 개선하려고 프롬프트 문구를 조정하는 것. CLIP의 "a photo of a {class}" 템플릿이 예다. 보고된 zero-shot 수치가 여기에 좌우될 수 있다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **Prompt sensitivity** · 프롬프트 민감도 — How much a language-conditioned model's behavior changes when the same request is worded differently. An independent π0 evaluation found one toilet-lid task at 100% or 0% success depending on phrasing. · 같은 요청을 다른 문구로 줬을 때 언어 조건 모델의 행동이 얼마나 달라지는가. π0의 독립 평가에서는 변기 뚜껑 과제 하나가 문구에 따라 성공률 100%와 0%로 갈렸다. → [[01-canonical-papers/notes/4-vla/pi0|π0]]
- **Proportional valve (servo valve)** · 비례 밸브(서보 밸브) — A hydraulic valve whose opening varies continuously with an electric command, giving flow or pressure control instead of on/off switching. Adding such valves is how a manual machine becomes computer-controlled. · 전기 명령에 따라 개도가 연속적으로 바뀌어 on/off 대신 유량이나 압력을 제어하는 유압 밸브. 이런 밸브를 달아 수동 기계를 컴퓨터로 제어하게 만든다. → [[01-canonical-papers/notes/8-construction/heap|HEAP]]
- **Proprioception** · 고유수용감각 — A robot's sensing of its own body state, such as IMU accelerations, wheel odometry and shocks. In traversability learning it supplies labels: literally how driving over the terrain felt. · 로봇이 IMU 가속도, 바퀴 오도메트리, 충격처럼 자기 몸 상태를 감지하는 것. traversability 학습에서는 지형을 달린 느낌 그대로를 라벨로 공급한다. → [[04-robotics/traversability-off-road|Traversability §2]]
- **Provenance (state provenance)** · 출처(provenance) — Where a piece of twin state came from: measured, inferred, planned, or manually entered. Each carries different trust and staleness and must not be treated equally. · 트윈 상태 하나가 어디서 왔는가: 측정·추론·계획·수기 입력. 각각 신뢰도와 낡음이 달라 동등하게 취급하면 안 된다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §3]]
- **Proxy (god object)** · proxy — A second point constrained to stay on virtual surfaces while the device point may penetrate; the rendered force is a spring between them. It remembers the entry side, follows edges smoothly, and eases friction. · 장치 점은 침투해도 되지만 가상 표면 위에 머물도록 제약된 두 번째 점으로, 렌더링되는 힘은 둘 사이의 스프링이다. 어느 쪽으로 들어왔는지 기억하고, 모서리를 매끄럽게 따라가며, 마찰 구현을 쉽게 한다. → [[04-robotics/haptics-teleoperation/haptic-rendering-algorithms|24.7 Rendering Algorithms §3]]
- **Pruning** · 가지치기 — Rejecting a partial solution at an internal node of a search tree, which removes the whole subtree below it. It rarely improves the worst-case bound but can cut practical search time by orders of magnitude. · 탐색 트리의 내부 노드에서 부분 해를 기각해 그 아래 부분 트리 전체를 없애는 것. 최악 경우 상한은 거의 개선하지 못하지만 실제 탐색 시간은 몇 자릿수 줄일 수 있다. → [[02-foundations/algorithms/complexity-recursion|11.1 §6]]
- **PSE (point of subjective equality)** · 주관적 동등점(PSE) — The stimulus value a person judges equal to a reference. In the method of adjustment it is the mean of the participant's settings. · 사람이 기준 자극과 같다고 판단하는 자극 값. 조정법에서는 참가자가 맞춘 값들의 평균이다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Pseudo-inverse** · 유사역행렬 — Written $A^\dagger$, the stand-in for an inverse when $A$ is not square. Tall with independent columns: $A^\dagger = (A^\top A)^{-1}A^\top$, a left inverse giving the least-squares solution. Wide with independent rows: $A^\dagger = A^\top(AA^\top)^{-1}$, a right inverse giving the minimum-norm solution — which is what $J^\dagger$ computes for a redundant arm. Also called the Moore–Penrose inverse. · $A^\dagger$로 쓰며, $A$가 정사각이 아닐 때 역행렬을 대신한다. 키 크고 열이 독립이면 $A^\dagger = (A^\top A)^{-1}A^\top$로 왼쪽 역원이자 최소자승해를 준다. 넓고 행이 독립이면 $A^\dagger = A^\top(AA^\top)^{-1}$로 오른쪽 역원이자 최소 노름 해를 주는데, 여유자유도 팔에서 $J^\dagger$가 계산하는 것이 그것이다. Moore–Penrose 역행렬이라고도 한다. → [[02-foundations/linear-algebra|Linear Algebra §4.5]]
- **Pseudo-label** · 의사 라벨 — Training a student on unlabeled data using a teacher model's predictions as labels. · 교사 모델의 예측을 라벨 삼아 무라벨 데이터로 학생을 학습. → [[01-canonical-papers/notes/2-computer-vision/depth-anything|Depth Anything]]
- **Pseudo-polynomial time** · 의사 다항 시간 — Running time polynomial in the numeric value of an input number rather than in its bit length, like the $O(nW)$ knapsack DP. Such an algorithm is fast only while the numbers stay small. · 입력 수의 비트 길이가 아니라 수치 값에 대해 다항인 실행 시간(예: $O(nW)$ 배낭 DP). 이런 알고리즘은 수가 작을 때만 빠르다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **Psychometric function** · 심리측정 함수 — The curve mapping physical stimulus intensity to response probability. Its midpoint can define the point of subjective equality (PSE); in two-alternative comparisons the JND is often half the 25%–75% gap. · 물리 자극 세기를 반응 확률로 사상하는 곡선. 중간점이 주관적 등가점(PSE)을 정의할 수 있고, 2대안 비교에서 JND는 흔히 75% 지점과 25% 지점 사이 간격의 절반으로 읽는다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §2]]
- **Psychophysics** · 심리물리학 — The branch of experimental psychology that measures what people can detect and discriminate, with standardized procedures. A robot claim that an operator felt or noticed something needs it. · 사람이 무엇을 검출하고 변별할 수 있는지를 표준화된 절차로 재는 실험심리학의 분과. 조작자가 무언가를 느꼈거나 알아챘다는 로봇 주장에는 이것이 필요하다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §1]]
- **Publisher and subscription** · 퍼블리셔와 서브스크립션 — The two topic endpoints a node creates: a publisher sends typed messages on a topic name; a subscription runs a callback per arriving message. They connect only if name, type and QoS match. · 노드가 만드는 토픽의 두 끝점. 퍼블리셔는 한 타입의 메시지를 토픽 이름으로 보내고, 서브스크립션은 도착한 메시지마다 콜백을 실행한다. 이름·타입·QoS가 맞아야만 연결된다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §5]]
- **Publish–subscribe (pub–sub)** · publish–subscribe — A pattern where producers publish to a named topic and consumers subscribe to that name, with no list of consumers held by the producer. Cost: nothing warns you when an expected connection is missing. · 생산자는 이름 붙은 토픽에 publish하고 소비자는 그 이름을 구독하며, 생산자가 소비자 목록을 갖지 않는 패턴. 대가로, 기대한 연결이 없어도 아무것도 경고하지 않는다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §1]]
- **p-value** · p-값 — The probability, assuming the null hypothesis holds, of a statistic at least as extreme as observed. Not the probability the null is true, not an effect size; large p is not evidence of no difference. · 귀무가설이 참이라고 가정하고 계산한, 관측된 것 이상으로 극단적인 검정 통계량이 나올 확률. 귀무가설이 참일 확률도 효과 크기도 아니며, 큰 p가 차이 없음의 증거도 아니다. → [[02-foundations/probability|Probability §6]]
- **PWM (pulse-width modulation)** · PWM — Switching a supply on and off rapidly so the duty cycle approximately sets average terminal voltage. It does not set force: motor torque follows current, $\tau_m=k_ti$, which also depends on resistance, inductance, back-EMF, and load. · 전원을 빠르게 켜고 꺼 duty가 평균 단자 전압을 근사적으로 정하게 하는 방식. 힘을 정하지는 않는다. 모터 토크는 전류 $\tau_m=k_ti$를 따르고, 전류는 저항·인덕턴스·역기전력·부하에도 달려 있다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §3]]

## Q–S

- **Q-Former** · Q-Former — A connector module distilling an image into a few tokens using learned queries. · 학습된 쿼리로 이미지를 소수 토큰으로 증류하는 연결 모듈. → [[01-canonical-papers/notes/3-vlm/blip-2|BLIP-2]]
- **Q-function (action-value function)** · Q 함수 — $Q^\pi(s,a)$: the expected return from taking action $a$ in state $s$ and following policy $\pi$ afterwards. Acting greedily on the optimal $Q^*$ is optimal. · $Q^\pi(s,a)$: 상태 $s$에서 행동 $a$를 한 뒤 정책 $\pi$를 따를 때의 기대 리턴. 최적 $Q^*$에 대해 탐욕적으로 행동하면 최적이다. → [[02-foundations/rl-basics|RL Basics §2]]
- **Q-learning** · Q-learning — Model-free, off-policy TD learning of $Q^*$: nudge $Q(s,a)$ toward $r + \gamma \max_{a'} Q(s',a')$ while acting under an exploratory policy. The max makes continuous actions awkward. · $Q^*$를 배우는 모델 프리·오프폴리시 TD 학습: 탐색 정책으로 행동하면서 $Q(s,a)$를 $r + \gamma \max_{a'} Q(s',a')$ 쪽으로 민다. max 때문에 연속 행동에서 어색하다. → [[02-foundations/rl-basics|RL Basics §3]]
- **QMDP (hindsight optimization)** · QMDP — A POMDP approximation scoring each action as $\sum_g b(g)Q_g(x,a)$, as if the hidden goal were revealed right after it. It lets a robot help before it is sure, but gives purely information-gathering actions no value. · 각 행동을 숨은 목표가 그 직후 드러난다고 가정하고 $\sum_g b(g)Q_g(x,a)$로 점수 매기는 POMDP 근사. 확신 전에도 도울 수 있게 하지만, 정보만 얻는 행동에는 가치를 주지 않는다. → [[04-robotics/hri-safety|HRI & Safety §3.5]]
- **QoS (Quality of Service) profile** · QoS 프로파일 — A set of delivery policies (history, depth, reliability, durability, deadline, lifespan, liveliness) set independently on each publisher, subscription, service and client. Incompatible profiles silently prevent a connection. · 퍼블리셔·서브스크립션·서비스·클라이언트마다 따로 정하는 전달 정책(history, depth, reliability, durability, deadline, lifespan, liveliness)의 묶음. 호환되지 않으면 조용히 연결되지 않는다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §2]]
- **QP (quadratic program)** · QP(이차 계획) — Minimize a convex quadratic cost under linear equality and inequality constraints. Linear MPC, trajectory smoothing, and inverse dynamics are QPs, and small structured QPs can be solved at millisecond scale. · 선형 등식과 부등식 제약 아래에서 볼록 이차 비용을 최소화하는 문제. 선형 MPC, 궤적 평활화, 역동역학이 QP이며, 작고 구조가 있는 QP는 밀리초 단위로 풀 수 있다. → [[02-foundations/optimization|Optimization §5]]
- **Quadratic attention cost** · 어텐션의 제곱 비용 — Self-attention builds a $T \times T$ score table, so compute and memory grow as $O(T^2)$ in the number of tokens. Windowed, sparse, and linear attention variants exist to avoid it. · self-attention은 $T \times T$ 점수표를 만들므로 연산과 메모리가 토큰 수에 대해 $O(T^2)$로 늘어난다. 창·희소·선형 어텐션 변형은 이를 피하려고 존재한다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]], [[01-canonical-papers/notes/2-computer-vision/swin|Swin]]
- **Quadrature encoder** · quadrature encoder — A position sensor that outputs counts and direction; angle requires counts per revolution and transmission calibration. Its finite resolution quantizes position, and differencing counts into velocity amplifies that quantization. · count와 방향을 출력하는 위치 센서로, 각도를 얻으려면 회전당 count 수와 전달비 보정이 필요하다. 유한 해상도가 위치를 양자화하고, count 차분으로 얻는 속도는 그 양자화를 증폭한다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §4]]
- **Quadruped** · 4족 로봇 — A four-legged walking robot such as ANYmal, Spot, or A1. Learned controllers for them are usually trained in simulation and then transferred to hardware. · ANYmal, Spot, A1 같은 네 발 보행 로봇. 학습 제어기는 보통 시뮬레이션에서 학습한 뒤 하드웨어로 옮긴다. → [[01-canonical-papers/notes/9-navigation/lee-quadruped-terrain|Lee 2020]]
- **Quantization (4-bit quantization)** · 양자화 — Storing model weights with fewer bits, such as 4 instead of 16, to cut memory and speed up inference. OpenVLA reports 4-bit quantization preserves task performance, which matters for on-robot compute. · 모델 가중치를 16비트 대신 4비트처럼 더 적은 비트로 저장해 메모리를 줄이고 추론을 빠르게 하는 것. OpenVLA는 4비트 양자화가 과제 성능을 보존한다고 보고하며, 로봇 탑재 연산에서 중요하다. → [[01-canonical-papers/notes/4-vla/openvla|OpenVLA]]
- **Quasi-repetitive task** · 준반복 과제 — A task repeated many times with small variations in geometry or conditions, like installing similar panels. It suits learning from demonstration but defeats pure replay. · 형상이나 조건이 조금씩 달라지며 여러 번 반복되는 과제로, 비슷한 패널 설치가 예다. 시연 학습에는 맞지만 단순 재생으로는 풀리지 않는다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §2]]
- **Quaternion** · 쿼터니언 — A four-number rotation representation $(w, x, y, z)$ with no singularities, cheap composition, and smooth interpolation (slerp). Double cover: $q$ and $-q$ are the same rotation, so raw differences between quaternions mislead. · 네 숫자 $(w, x, y, z)$로 쓰는 회전 표현. 특이점이 없고 합성이 싸며 매끄러운 보간(slerp)이 된다. 이중 덮개 때문에 $q$와 $-q$가 같은 회전이라 성분끼리의 단순 차이는 오해를 낳는다. → [[02-foundations/se3-geometry|SE(3) §2]]
- **Queue (FIFO)** · 큐 — An access policy in which elements leave in the order they arrived, first in first out. It drives BFS; in Python use collections.deque rather than a list. · 원소가 들어온 순서대로 나가는(선입선출) 접근 방식. BFS를 움직이며, Python에서는 list가 아니라 collections.deque를 쓴다. → [[02-foundations/algorithms/data-structures|11.2 §2]]
- **Quickselect** · Quickselect — Finding the k-th smallest element by partitioning around a random pivot and continuing only on the side that holds position k. It is $O(n)$ expected and $\Theta(n^2)$ worst case; C++ std::nth_element does selection. · 무작위 피벗으로 파티션한 뒤 위치 k가 있는 쪽만 계속 처리해 k번째로 작은 원소를 찾는 방법. 기대 $O(n)$, 최악 $\Theta(n^2)$이며, C++ std::nth_element가 선택을 수행한다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §7]]
- **Quicksort** · 퀵정렬 — Partition around a pivot, then sort both sides recursively. With random pivots it takes $O(n \log n)$ expected time on every input and sorts in place, but it is unstable and $\Theta(n^2)$ worst case. · 피벗을 기준으로 파티션한 뒤 양쪽을 재귀로 정렬한다. 무작위 피벗이면 모든 입력에서 기대 $O(n \log n)$이고 제자리 정렬이지만, 안정적이지 않고 최악은 $\Theta(n^2)$이다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §3]]
- **Radiance field** · radiance field — A function giving the color and density at every 3D point, with color depending on viewing direction. NeRF stores it in an MLP; 3D Gaussian splatting stores it as Gaussians. · 모든 3D 점의 색과 밀도를 주는 함수로, 색은 보는 방향에 따라 달라진다. NeRF는 이를 MLP에, 3D Gaussian splatting은 가우시안 집합에 저장한다. → [[01-canonical-papers/notes/2-computer-vision/nerf|NeRF]]
- **RA-L (IEEE Robotics and Automation Letters)** · RA-L — A robotics journal with a hard 8-page limit that often carries a paper's first version; an accepted paper can be presented at a RAS conference, since the joint RA-L+ICRA deadline ended with ICRA 2022. The exception to journal-means-later-version. · 8쪽 제한이 있는 로보틱스 저널로, 흔히 논문의 최초 판본을 싣는다. RA-L+ICRA 공동 마감은 ICRA 2022로 끝났고, 채택 논문은 RAS 학회에서 발표할 수 있다. "저널은 나중 판본"이라는 통념의 예외다. → [[01-canonical-papers/how-to-read|How to Read §4.6]]
- **Random variable** · 확률변수 — A quantity whose value is set by chance and described by a distribution: a PMF if discrete, a PDF if continuous, and a CDF $F(x) = P(X \le x)$ in either case. · 값이 우연으로 정해지는 양으로, 분포로 기술한다. 이산이면 PMF, 연속이면 PDF, 어느 쪽이든 CDF $F(x) = P(X \le x)$로 나타낸다. → [[02-foundations/probability|Probability §2]]
- **Rank** · 랭크 — The number of independent columns of a matrix, equal to the number of independent rows: the dimension of what the map can express. It counts nonzero singular values; rank deficiency destroys information. · 행렬의 독립인 열의 수이자 독립인 행의 수로, 사상이 표현할 수 있는 차원이다. 0이 아닌 특이값의 수와 같으며, 랭크가 부족하면 정보가 파괴된다. → [[02-foundations/linear-algebra|Linear Algebra §2]]
- **RANSAC (random sample consensus)** · RANSAC — Robust fitting: repeatedly fit a model (F, E, H or PnP pose) to a random minimal sample and keep the one with most inliers. Repeated structure can still produce consistently wrong matches that pass. · 강건 맞춤: 무작위 최소 표본에 모델(F, E, H, PnP pose)을 맞추기를 반복하고 인라이어가 가장 많은 것을 남긴다. 반복 구조는 일관되게 틀린 매칭을 만들어 통과할 수 있다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Rapidly-exploring random tree (RRT)** · RRT — A sampling-based planner that grows a tree from the start: sample a random point, find the nearest node, and extend toward it if collision-free. It survives high dimensions, but paths come out jagged. · 시작점에서 트리를 키우는 표본 기반 계획기. 무작위 점을 뽑고, 가장 가까운 노드를 찾고, 짧은 구간이 충돌 없으면 그쪽으로 뻗는다. 고차원에서도 통하지만 경로가 들쭉날쭉하게 나온다. → [[04-robotics/planning-decision-making|Planning §5]]
- **Rasterization (alpha blending)** · 래스터라이제이션(알파 블렌딩) — Projecting primitives onto the screen, sorting them by depth, and compositing their colors by opacity. It is the GPU-friendly path that lets 3DGS render in real time. · 프리미티브를 화면에 투영하고 깊이 순으로 정렬한 뒤 불투명도로 색을 합성하는 것. 3DGS가 실시간으로 렌더링하게 해 주는 GPU 친화적 경로다. → [[01-canonical-papers/notes/2-computer-vision/3d-gaussian-splatting|3DGS]]
- **Ratio test** · 비율 검사 — Lowe's match filter: accept a nearest-neighbour match only if $d_1/d_2<0.8$, the distance ratio of the best to the second-best candidate. Two near-equal candidates usually signal repeated structure. · Lowe의 매칭 필터: 가장 가까운 후보와 두 번째 후보까지의 거리 비 $d_1/d_2<0.8$일 때만 받아들인다. 거의 같은 후보가 둘이면 대개 반복 구조다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **Reachability map** · 도달성 지도(reachability map) — A voxelized record of which end-effector positions an arm can reach from a fixed base. A capability map also records from which directions each voxel is reachable, because an arm's reach is strongly anisotropic. · 고정된 베이스에서 팔의 말단이 어느 위치에 도달할 수 있는지를 복셀로 기록한 것. 팔의 도달은 방향에 크게 의존하므로 능력 지도(capability map)는 각 복셀에 어느 방향에서 도달할 수 있는지도 기록한다. → [[04-robotics/navigation-mobile-manipulation|Mobile Manipulation §2]]
- **Reactive synthesis** · 반응형 합성 — Building a controller that satisfies a formula against every sequence of environment inputs, choosing each output from the past alone. The formula is realizable if one exists, merely satisfiable if some run meets it. · 모든 환경 입력 열에 대해 식을 만족하되 각 출력을 과거만 보고 고르는 제어기를 만드는 것. 그런 제어기가 있으면 식은 실현 가능(realizable)하고, 어떤 실행 하나만 만족하면 충족 가능(satisfiable)할 뿐이다. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Reading depth** · 읽기 깊이 — How deeply to engage one paper: the term is not foreign; you can restate problem, method I/O and results; you can follow code and tensor shapes; you can critique assumptions and design variants. · 논문 한 편에 들이는 깊이: 용어가 낯설지 않다 / 문제·방법 입출력·결과를 내 말로 말한다 / 코드와 텐서 모양을 따라간다 / 가정을 비판하고 변형을 설계한다. → [[01-canonical-papers/how-to-read|How to Read §1]]
- **Readout token** · readout 토큰 — A learned input token that attends to all other tokens and feeds a small output head. In Octo, adding a new output means adding readouts and a head, not modifying the backbone. · Transformer 입력에 더하는 학습된 토큰으로, 다른 모든 토큰을 참조해 작은 출력 헤드에 공급한다. Octo에서 새 출력을 더하는 일은 백본 수정이 아니라 readout과 헤드를 더하는 일이다. → [[01-canonical-papers/notes/4-vla/octo|Octo]]
- **Reality gap** · reality gap — The set of mismatches between simulator and world (dynamics, contact/material, sensing, task distribution, software/hardware) that a sim-trained policy meets at deployment. Sim-to-real is the practice of managing it. · 시뮬레이터와 현실 사이의 불일치 묶음(동역학, 접촉·재료, 센싱, 과제 분포, 소프트웨어·하드웨어)으로, 시뮬레이션에서 학습한 정책이 배치 시점에 마주하는 것이다. sim-to-real은 그것을 관리하는 실무다. → [[05-construction-robotics/sim-to-real|Sim-to-Real §1]]
- **Real-time factor** · 실시간 계수 — The ratio of simulated time to wall time: 1.0 is real speed, 0.05 is twenty times slower. Timestamp accuracy under sim time degrades as it grows. · 시뮬레이션 시간과 벽시계 시간의 비. 1.0은 실제 속도, 0.05는 20배 느린 것이다. 이것이 커질수록 시뮬레이션 시간의 타임스탬프 정확도가 떨어진다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §10]]
- **Rebar** · 철근 — Steel reinforcing bars placed inside concrete to carry tension, tied into grids, mesh, or cages before the concrete is poured. The mesh is non-rigid and shifts. · 콘크리트 속에서 인장력을 받도록 넣는 보강용 강봉으로, 타설 전에 격자·메시·철근망으로 묶는다. 메시는 비강체라 어긋난다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §1]]
- **Rebar tying** · 철근 결속 — Joining rebar into a grid by wrapping wire around each crossing. It is repeated thousands of times but only lightly contact-rich, and has site-verified robot results. · 철근 교차점마다 철사를 감아 격자로 묶는 일. 수천 번 반복되지만 접촉은 가볍고, 현장 검증된 로봇 결과가 있다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Rebuttal (author response)** · 반박문(rebuttal) — The authors' written reply to reviews before the decision. ICRA and IROS have none for regular papers and RSS and CoRL allow one page; answer by agreeing, clarifying, revising or rebutting, citing exact locations. · 결정 전에 저자가 심사에 서면으로 답하는 것. ICRA·IROS 일반 논문에는 없고 RSS·CoRL은 1쪽이다. 동의, 명료화, 수정, 증거에 기반한 반박으로 답하고 바뀐 위치를 정확히 밝힌다. → [[06-research-practice/scientific-writing-peer-review|Scientific Writing §7]], [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Recall (recall@k)** · 재현율 — $TP/(TP+FN)$: of the real positives, the fraction caught; the same as the true positive rate. In retrieval, recall@k is the fraction of queries whose answer is in the top $k$ results. · $TP/(TP+FN)$: 실제 양성 중 잡아낸 비율로, 참양성률과 같다. 검색에서 recall@k는 정답이 상위 $k$개 결과 안에 있는 비율이다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Receding horizon** · receding horizon — The execution structure of repeatedly re-planning while pushing the horizon forward; what makes a planner closed-loop. · 지평을 앞으로 밀며 계획을 반복 갱신하는 실행 구조. → [[01-canonical-papers/notes/4-vla/diffusion-policy|Diffusion Policy]]
- **Receptive field** · 수용 영역 — How much of the input a single output unit can see. Stacking two $3\times3$ convolutions gives the same $5\times5$ receptive field as one $5\times5$ convolution with fewer weights — the whole argument of [[01-canonical-papers/notes/1-foundations/vgg|VGG]]. · 하나의 출력 단위가 입력의 얼마만큼을 볼 수 있는가. $3\times3$ 합성곱 둘을 쌓으면 $5\times5$ 하나와 같은 수용 영역을 더 적은 가중치로 얻는다 — [[01-canonical-papers/notes/1-foundations/vgg|VGG]]의 논증 전부다.
- **Reconstruction (backtrace, parent pointers)** · 복원 — Recovering the actual choice, path or alignment, not just its cost, by storing the winning choice or parent per cell or walking back through the finished table. A rolling array cannot reconstruct. · 비용뿐 아니라 실제 선택·경로·정렬을 되찾는 것으로, 칸마다 이긴 선택이나 부모를 저장하거나 완성된 표를 거꾸로 따라간다. 롤링 배열로는 복원할 수 없다. → [[02-foundations/algorithms/dynamic-programming|11.5 §1]]
- **Reconstruction objective** · 복원 기반 학습 — Training a representation or world model to reproduce its input pixels, as VAE-style world models do. It spends capacity on detail no plan needs, the critique JEPA answers by predicting embeddings instead. · VAE식 월드모델처럼 표현이나 월드모델이 입력 픽셀을 다시 만들어 내도록 학습하는 것. 어떤 계획도 필요 없는 디테일에 용량을 쓰며, JEPA는 대신 임베딩을 예측하는 것으로 이 비판에 답한다. → [[01-canonical-papers/notes/5-world-models/jepa|JEPA]], [[01-canonical-papers/notes/5-world-models/planet|PlaNet]]
- **Recovery** · 회복 — The system returning to progress without an external reset under the declared intervention policy. Report time to recovery and success after recovery separately from interventions and resets. · 선언한 개입 정책 아래에서 외부 리셋 없이 시스템이 다시 진행하는 것. 회복 시간과 회복 후 성공을 개입·리셋과 따로 보고한다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §5]]
- **Recovery behavior** · recovery behavior — What the execution layer does when a step fails, such as replan, retry, request help, or safe stop. When a paper says the robot recovered, this layer rather than the policy often did it. · 단계가 실패했을 때 실행 계층이 하는 일로, 재계획·재시도·도움 요청·안전 정지 등이다. 논문이 로봇이 "회복했다"고 하면 정책이 아니라 이 계층이 한 경우가 많다. → [[04-robotics/robot-systems-deployment|Robot Systems §6]]
- **Recurrence (recurrence relation)** · 점화식 — An equation giving an algorithm's cost on size $n$ in terms of its cost on smaller inputs plus the work outside the recursive calls, such as merge sort's $T(n) = 2T(n/2) + n$. · 크기 $n$에서의 비용을 더 작은 입력에서의 비용과 재귀 호출 밖의 작업으로 나타낸 식. 예: 병합 정렬의 $T(n) = 2T(n/2) + n$. → [[02-foundations/algorithms/complexity-recursion|11.1 §3]]
- **Recursion limit (RecursionError)** · 재귀 한도 — CPython's default cap of 1000 stack frames, past which it raises RecursionError, even in memoized code. Fix it by filling bottom-up in a loop or using an explicit stack, not by raising the limit far. · CPython이 기본으로 두는 1000 스택 프레임 한도로, 넘으면 메모이제이션한 코드에서도 RecursionError가 난다. 한도를 크게 올리기보다 반복문으로 상향식으로 채우거나 명시적 스택을 써서 고친다. → [[02-foundations/algorithms/complexity-recursion|11.1 §5]]
- **Recursion tree** · 재귀 트리 — A drawing of a recursive algorithm with one node per call, holding the non-recursive work. Summing each level, then the levels, solves any recurrence; the master method is its regular case. · 호출마다 노드 하나를 두고 재귀 밖의 작업을 적은 그림. 층별로 더한 뒤 층들을 더하면 어떤 점화식이든 풀리며, 마스터 방법은 그 규칙적인 경우다. → [[02-foundations/algorithms/complexity-recursion|11.1 §3]]
- **Recursive feasibility** · recursive feasibility — The MPC property that a feasible plan now guarantees a feasible plan at the next step, typically secured by ending the horizon inside an invariant terminal set. · 지금 실행 가능한 계획이 있으면 다음 스텝에도 실행 가능한 계획이 보장된다는 MPC 성질. 보통 지평의 끝을 불변 종단 집합 안에 두어 확보한다. → [[04-robotics/mpc|MPC]]
- **Recursive Newton–Euler algorithm (RNEA)** · 재귀 뉴턴-오일러 — The force-balance method that computes manipulator dynamics link by link in $O(n)$ time. It is how simulators and controllers actually evaluate the equation, while the Lagrangian derivation serves analysis. · 링크별 힘 평형으로 매니퓰레이터 동역학을 $O(n)$ 시간에 계산하는 방법. 시뮬레이터와 제어기가 실제로 계산하는 방식이고, 라그랑주 유도는 해석에 쓰인다. → [[04-robotics/modern-robotics/ch08-dynamics|MR ch.8]]
- **Reduction (polynomial-time reduction)** · 환원 — A polynomial-time translation of instances of one problem into instances of another, so a fast algorithm for the second would solve the first. NP-hardness is defined through it. · 한 문제의 사례를 다른 문제의 사례로 다항 시간에 옮기는 것으로, 두 번째 문제의 빠른 알고리즘이 첫 번째 문제를 풀게 한다. NP-난해성이 이것으로 정의된다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **Redundancy (kinematic)** · 여유자유도 — An arm with more joints than its task needs, such as seven joints for a 6-D pose. Its Jacobian has a null space of joint motions that leave the tool still, spendable on secondary objectives. · 과제에 필요한 것보다 관절이 많은 팔(예: 6차원 자세에 7관절). 그러면 야코비안에 도구를 움직이지 않는 관절 운동의 영공간이 생겨 부차 목표에 쓸 수 있다. → [[04-robotics/modern-robotics/ch06-inverse-kinematics|MR ch.6 §4]]
- **Reentrant callback group** · reentrant 콜백 그룹 — A callback group whose callbacks may run in parallel, including several concurrent calls of the same callback, when a multi-threaded executor supplies the threads. · 멀티 스레드 executor가 스레드를 줄 때, 소속 콜백이 같은 콜백의 동시 실행까지 포함해 병렬로 돌 수 있는 콜백 그룹. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §9]]
- **Reflected inertia** · 반사 관성 — Motor inertia felt at a transmission's output, growing roughly as $N^2$ with ratio $N$ while torque and friction grow as $N$. A high ratio makes a device strong but heavy and hard to backdrive. · 비율 $N$인 전달장치의 출력에서 느껴지는 모터 관성으로, 토크와 반사 마찰이 대략 $N$배일 때 대략 $N^2$배로 커진다. 큰 비율은 장치를 강하지만 무겁게 느껴지고 역구동하기 어렵게 만든다. → [[04-robotics/haptics-teleoperation/device-design-kinematics|24.3 Device Design §3]], [[04-robotics/teleoperation-demonstration|Teleoperation §4.5]]
- **Region proposal network (RPN)** · 영역 제안(RPN) — A small network sliding over backbone features that scores each anchor for objectness and regresses its box, producing a few hundred candidate regions for the second stage. · 백본 특징 위를 미끄러지며 앵커마다 물체성 점수와 박스 회귀를 예측해, 두 번째 단계에 넘길 수백 개의 후보 영역을 만드는 작은 신경망. → [[01-canonical-papers/notes/2-computer-vision/faster-r-cnn|Faster R-CNN]]
- **Registration** · registration — Estimating the rigid transform $T\in SE(3)$ that aligns two geometries: scan to scan, point cloud to part model, or site scan to BIM. First ask which points represent the same surface. · 두 기하를 정렬하는 강체 변환 $T\in SE(3)$를 추정하는 일: 스캔끼리, 점군과 부품 모델, 현장 스캔과 BIM. 먼저 어떤 점끼리 같은 표면을 나타내는지 물어야 한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §4]]
- **Regression fixture (bag test)** · 회귀 fixture — A recorded bag of a component's inputs kept as a test case: replay it in CI and assert the output within tolerance, since replay is not bit-identical to the live run. · 컴포넌트 입력을 기록한 bag을 테스트 케이스로 보관하는 것. 재생은 실제 실행과 비트 단위로 같지 않으므로, CI에서 재생해 허용 오차 안에서 출력을 검사한다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §9]]
- **Regularization** · 정규화(regularization) — Any method aimed at reducing harmful overfitting, through different mechanisms: weight decay, dropout, data augmentation, early stopping, or simply more data. Not the same as normalization layers. · 해로운 과적합을 줄이려는 방법 전반으로, 작동 기제는 제각각이다: weight decay, dropout, 데이터 증강, early stopping, 또는 더 많은 데이터. 정규화 층(normalization)과는 다르다. → [[02-foundations/ml-practice|ML Practice §2]]
- **REINFORCE** · REINFORCE — The plain policy-gradient estimator: raise each action's log-probability in proportion to the return that followed. Unbiased but with very high variance, hence baselines, reward-to-go and critics. · 가장 기본적인 정책 그래디언트 추정기: 각 행동의 로그 확률을 뒤따른 리턴에 비례해 올린다. 무편향이지만 분산이 매우 커서 베이스라인·reward-to-go·크리틱이 필요하다. → [[02-foundations/rl-basics|RL Basics §4]]
- **Related work** · Related work(관련연구) — The section placing a paper among prior work. Write it as a taxonomy grouped by assumption, representation, supervision, interface or evaluation, ending each group with the distinction your work tests, not as a citation list. · 논문을 선행연구 속에 위치시키는 절. 인용 목록이 아니라 가정·표현·감독·인터페이스·평가로 묶은 분류 체계로 쓰고, 각 묶음을 이 연구가 시험하는 구분으로 끝낸다. → [[06-research-practice/scientific-writing-peer-review|Scientific Writing §3]]
- **Relative-trajectory action** · 상대 궤적 행동 — Expressing predicted motion in the current gripper frame rather than as absolute world poses, so it does not matter where the demonstrator stood or where the robot base ends up. · 예측 운동을 절대 월드 자세가 아니라 현재 그리퍼 프레임 기준으로 표현하는 것. 시연자가 어디 섰는지, 로봇 베이스가 어디 놓이는지와 무관해진다. → [[01-canonical-papers/notes/7-robotics/umi|UMI]]
- **Relative vs metric depth** · 상대 깊이 / 미터 깊이 — Relative (affine-invariant) depth gets the scene's depth ordering and shape up to an unknown scale and shift; metric depth is in meters and needs domain fine-tuning or scale calibration. · 상대(affine-invariant) 깊이는 미지의 스케일·시프트를 뺀 깊이 순서와 형상만 맞추고, 미터 깊이는 실제 미터 단위로 도메인 파인튜닝이나 스케일 보정이 필요하다. → [[01-canonical-papers/notes/2-computer-vision/depth-anything|Depth Anything]]
- **Relaxation (edge relaxation)** · 완화 — The shortest-path update step: if reaching $v$ through $u$ is cheaper than $v$'s current label, lower the label and record $u$ as its parent. Dijkstra, Bellman–Ford and A\* all repeat it in different orders. · 최단 경로의 갱신 단계: $u$를 거쳐 $v$에 가는 것이 $v$의 현재 레이블보다 싸면 레이블을 낮추고 $u$를 부모로 기록한다. Dijkstra, Bellman–Ford, A\*는 이를 서로 다른 순서로 반복한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §7]]
- **Reliability (reliable, best effort)** · reliability(신뢰성) — The QoS policy deciding whether lost messages are retransmitted until acknowledged (reliable) or simply dropped (best effort). A best-effort publisher cannot serve a reliable subscription. · 잃어버린 메시지를 확인 응답까지 재전송할지(reliable), 그냥 버릴지(best effort) 정하는 QoS 정책. best effort 퍼블리셔는 reliable 서브스크립션과 연결되지 않는다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §3]]
- **Remapping** · 리매핑 — Rewiring a node's topic or node name from outside, at launch or on the command line, without touching its source. It is why generic topic names in node code are a feature. · 소스를 건드리지 않고 launch나 커맨드라인에서 노드의 토픽 이름이나 노드 이름을 바꿔 배선하는 것. 노드 코드의 일반적인 토픽 이름이 장점이 되는 이유다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §11]]
- **Remote centre compliance (RCC)** · RCC — A passive mechanical device that places the compliance centre at the tip of a held part, so lateral misalignment produces lateral motion and angular misalignment produces rotation about the tip, instead of each error generating the other. It solves insertion with no sensor and no latency. · 컴플라이언스 중심을 잡고 있는 부재의 끝점에 놓는 수동 기계 장치. 횡방향 오차는 횡방향 운동을, 각도 오차는 끝점 둘레의 회전을 만들어 두 오차가 서로를 생성하지 않게 한다. 센서도 지연도 없이 삽입을 푼다. → [[04-robotics/force-compliance-control|13. §5]]
- **REP 105 frames (map, odom, base_link)** · REP 105 프레임 — The standard mobile-robot frame chain map → odom → base_link: odometry publishes odom → base_link (smooth but drifting), localization publishes the map → odom correction (drift-free but jumping). · 이동 로봇 표준 프레임 사슬 map → odom → base_link. 오도메트리가 odom → base_link(매끄럽지만 드리프트)를, 위치 추정이 map → odom 보정(드리프트 없지만 점프)을 발행한다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §11]]
- **Reparameterization trick** · reparameterization trick(재매개변수화) — Rewriting a sample $z \sim \mathcal{N}(\mu,\sigma^2)$ as $z = \mu + \sigma\epsilon$ with $\epsilon \sim \mathcal{N}(0,1)$, moving the randomness into an external input so gradients can flow to $\mu$ and $\sigma$. Sampling has no derivative; this makes the surrounding function differentiable again. · 샘플 $z \sim \mathcal{N}(\mu,\sigma^2)$를 $z = \mu + \sigma\epsilon$으로 다시 써서 무작위성을 외부 입력으로 옮기는 것. 샘플링에는 도함수가 없지만 이렇게 하면 $\mu, \sigma$로 그래디언트가 흐른다. → [[02-foundations/calculus-backprop|Calculus §5]], [[01-canonical-papers/notes/6-diffusion/vae|VAE]]
- **Replanning** · 재계획 — Recomputing a motion or task plan when sensing shows the world differs from what the plan assumed, such as as-built geometry that does not match the drawing. · 감지 결과 세계가 계획의 가정과 다를 때, 예를 들어 시공 형상이 도면과 다를 때 운동이나 과제 계획을 다시 계산하는 것. → [[01-canonical-papers/notes/8-construction/lundeen-2019|Lundeen 2019]]
- **Replay buffer** · 리플레이 버퍼 — A store of past transitions that an off-policy algorithm samples from repeatedly, so experience is reused instead of discarded after one update. · off-policy 알고리즘이 반복해 샘플하는 과거 전이 저장소 — 경험을 한 번 쓰고 버리지 않는다. → [[02-foundations/rl-basics|RL Basics §3]], [[01-canonical-papers/notes/1-foundations/sac|SAC]]
- **Representation collapse** · 표현 붕괴(collapse) — A failure of embedding-prediction training where the encoder outputs nearly the same vector for everything, making prediction trivially easy and the features useless. JEPA prevents it with EMA target encoders and architectural asymmetry. · 인코더가 모든 입력에 거의 같은 벡터를 내 예측은 자명하게 쉬워지고 특징은 쓸모없어지는, 임베딩 예측 학습의 실패. JEPA는 EMA 타깃 인코더와 구조적 비대칭으로 이를 막는다. → [[01-canonical-papers/notes/5-world-models/jepa|JEPA]]
- **Reproduction bundle** · 재현 번들 — Code released together with sensor configuration, calibration parameters, a recorded log, simulator setup and hardware specification. It decides whether another group can build on robotics work rather than merely cite it. · 코드와 함께 센서 구성, 보정 파라미터, 기록 로그, 시뮬레이터 설정, 하드웨어 사양을 묶어 공개한 것. 다른 집단이 로보틱스 연구를 인용만 하는지 실제로 이어 쓸 수 있는지를 가른다. → [[06-research-practice/real-world-impact|Real-World Impact §3]]
- **Reprojection error** · reprojection error(재투영 오차) — The pixel distance between an estimated 3D point projected through the estimated camera model and its detection. The standard calibration and reconstruction quality metric — but it does not certify accuracy outside the calibrated volume. · 추정된 3D 점을 추정된 카메라 모델로 투영했을 때 그 검출 위치와의 픽셀 거리. 보정·재구성의 표준 품질 지표이지만, 보정된 부피 **밖**의 정확도까지 보증하지는 않는다. → [[04-robotics/geometric-perception-calibration|Geometric Perception]]
- **Request-versus-offered rule (QoS compatibility)** · request 대 offered 규칙 — A subscription requests the minimum quality it accepts and a publisher offers the maximum it provides; they connect only if every requested policy is no stricter than the offer. · 서브스크립션은 받아들일 최소 품질을 요청하고 퍼블리셔는 줄 수 있는 최대 품질을 제공한다. 요청한 모든 정책이 제공보다 엄격하지 않을 때만 연결된다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §3]]
- **Research artifact** · 산출물(artifact) — A reusable output behind a result: code, data, model, configuration, calibration, raw logs, hardware files or analysis scripts. Each has an ongoing cost, and code alone rarely reproduces a robot experiment. · 결과를 뒷받침하는 재사용 가능한 산출물: 코드, 데이터, 모델, 설정, 보정, 원시 로그, 하드웨어 파일, 분석 스크립트. 각각 유지 비용이 들며, 코드만으로는 로봇 실험이 거의 재현되지 않는다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §7]], [[06-research-practice/real-world-impact|Real-World Impact §3]]
- **Research gap** · research gap — Missing evidence that matters: an unexplained failure, incompatible assumptions, poor generalization or unrealistic evaluation. "Nobody has done this" is not a gap until linked to a predicted failure mechanism. · 중요한 빠진 증거: 설명되지 않은 실패, 양립 불가능한 가정, 나쁜 일반화, 비현실적 평가. "아무도 안 했다"는 예상 실패 기전과 연결되기 전까지 gap이 아니다. → [[06-research-practice/research-questions-claims|Research Questions §2]]
- **Research question** · research question(연구 질문) — An uncertain relationship that evidence can resolve, naming intervention, comparator, outcome, conditions and test distribution. A topic only names an area; a good question can be answered negatively. · 증거가 해소할 수 있는 불확실한 관계로, 개입, 비교 대상, 결과, 조건, 시험 분포를 명시한다. topic은 영역의 이름일 뿐이며, 좋은 질문은 부정적으로도 답할 수 있다. → [[06-research-practice/research-questions-claims|Research Questions §1]]
- **Reset** · 리셋 — Restoring the environment or robot for a new attempt, such as a human straightening an object between trials. Hidden resets exaggerate autonomy, so count and report them. · 새 시도를 위해 환경이나 로봇을 원상태로 되돌리는 것(예: 시행 사이에 사람이 물체를 바로 놓기). 숨긴 리셋은 자율성을 과장하므로 세어서 보고한다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §5]]
- **Residual connection** · 잔차 연결 — Adding a layer's input to its output, $x + F(x)$. The derivative is $I + \partial F/\partial x$, so the gradient always has one path that is never multiplied down — the reason very deep networks train at all. · 층의 입력을 출력에 더하는 것, $x + F(x)$. 도함수가 $I + \partial F/\partial x$이므로 그래디언트에 결코 줄어들지 않는 경로가 하나 생긴다 — 아주 깊은 신경망이 애초에 학습되는 이유다. → [[01-canonical-papers/notes/1-foundations/resnet|ResNet]], [[02-foundations/calculus-backprop|Calculus §5]]
- **Residual learning** · 잔차 학습 — Keeping a model-based controller and learning only a correction on top of it. It shrinks the search space, but safety still depends on how the residual is bounded. · 모델 기반 제어기를 유지하고 그 위의 보정량만 학습하는 방식. 탐색 공간을 줄이지만 안전은 잔차를 어떻게 제한하느냐에 달려 있다. → [[05-construction-robotics/sim-to-real|Sim-to-Real §2]]
- **Residual model (learned residual dynamics)** · 학습 잔차 — A learned model fitted to the part of the true dynamics that a physics-based model leaves unexplained. It corrects repeatable mismatch, but only where its training observations constrain the correction. · 물리 기반 모델이 설명하지 못하고 남긴 실제 동역학의 몫에 맞춘 학습 모델. 반복되는 불일치를 고치지만, 학습 관측이 그 보정을 제약하는 범위 안에서만 근거가 있다. → [[04-robotics/contact-force-tactile|Contact §3]]
- **ResNet** · ResNet — A CNN built from residual blocks, output $F(x) + x$, which made networks of 100+ layers trainable. It won ILSVRC 2015 and remains the standard vision baseline. · 출력이 $F(x) + x$인 residual 블록으로 쌓은 CNN으로, 100층이 넘는 신경망을 학습 가능하게 했다. ILSVRC 2015에서 우승했고 지금도 비전의 표준 베이스라인이다. → [[01-canonical-papers/notes/1-foundations/resnet|ResNet]]
- **Response bias** · 응답 편향 — A tendency to answer yes or no regardless of the stimulus. Yes/no procedures mix it with sensitivity, how well a person actually tells stimulus from no stimulus. · 자극과 상관없이 "예"나 "아니오"로 답하는 경향. 예/아니오 절차는 이것을 민감도, 곧 자극 유무를 실제로 구별하는 능력과 뒤섞는다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Retargeting** · 리타게팅 — Mapping human motion onto a robot that does not share the human's kinematics — by end-effector pose, by joint correspondence, or relative to the task frame. The choice decides whether the resulting demonstrations sit in well-conditioned parts of the robot's workspace. · 사람과 기구학이 다른 로봇에 사람의 운동을 사상하는 것 — 말단 자세로, 관절 대응으로, 또는 과제 프레임 기준으로. 이 선택이 시연이 로봇 작업 공간의 조건 좋은 영역에 놓이는지를 결정한다. → [[04-robotics/teleoperation-demonstration|12. §5]]
- **Retrofit autonomy** · retrofit 자율성 — An autonomy kit added to an existing machine rather than built in by the manufacturer. Evidence to seek is supported machines and tasks plus installation and support burden. · 제조사가 내장하는 대신 기존 기계에 덧붙이는 자율 키트. 지원 기계·과제와 설치·지원 부담이 확인할 증거다. → [[05-construction-robotics/industry-deployment|Industry & Deployment §1]]
- **Return** · 리턴 — The discounted sum of rewards from a time step onward, $G_t = \sum_{k\ge 0}\gamma^k r_{t+k}$. RL maximizes its expectation, and it satisfies $G_t = r_t + \gamma G_{t+1}$, the root of the Bellman equation. · 한 시점부터의 할인된 보상 합 $G_t = \sum_{k\ge 0}\gamma^k r_{t+k}$. RL은 그 기댓값을 최대화하며, $G_t = r_t + \gamma G_{t+1}$을 만족하는 것이 벨만 방정식의 뿌리다. → [[02-foundations/rl-basics|RL Basics §1]], [[02-foundations/rl-basics|RL Basics §2]]
- **Reversed spatial relationship** · 역전된 공간 관계 — The construction–manufacturing contrast: a construction robot travels to and registers against a large static structure, while in a factory a fixtured product arrives at a fixed robot. · 건설과 제조의 대조. 건설 로봇은 크고 고정된 구조물로 이동해 그에 대해 위치를 정합하고, 공장에서는 고정구에 물린 제품이 고정된 로봇으로 온다. → [[01-canonical-papers/notes/8-construction/vision-guided-assembly|Feng 2015]]
- **Reverse process** · 역방향 과정 — The learned chain in a diffusion model that removes noise step by step, turning pure noise back into data. Each step is a Gaussian whose mean comes from the network's noise prediction. · 디퓨전 모델에서 노이즈를 한 스텝씩 제거해 순수 노이즈를 데이터로 되돌리는 학습된 체인. 각 스텝은 네트워크의 노이즈 예측에서 평균을 얻는 가우시안이다. → [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]]
- **Reverse-time SDE** · 역시간 SDE — The SDE that runs a noising process backward in time. A classical result says its only unknown is the score, so learning the score makes generation a matter of integrating it numerically. · 노이즈 주입 과정을 시간상 거꾸로 돌리는 SDE. 고전적 결과에 따르면 미지수는 score 하나뿐이어서, score를 배우면 생성은 이것을 수치 적분하는 일이 된다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **Revise and resubmit** · revise & resubmit — A journal decision asking authors to revise the paper and submit it for another review round. It gives a dialogue with reviewers that ICRA and IROS, which have no rebuttal, do not. · 저자에게 논문을 고쳐 다시 심사받도록 요청하는 저널의 결정. 반박문이 없는 ICRA·IROS와 달리 심사자와 대화할 기회가 생긴다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Reward** · 보상 — The scalar signal $r(s,a)$ an RL agent receives each step. Unlike a supervised target, someone authors it, usually as a weighted sum of terms, and the policy optimizes what was written, not what was meant. · RL 에이전트가 매 스텝 받는 스칼라 신호 $r(s,a)$. 지도학습의 타깃과 달리 누군가 써 넣은 것이고 보통 항들의 가중합이며, 정책은 의도가 아니라 써 놓은 것을 최적화한다. → [[02-foundations/rl-basics|RL Basics §7]]
- **Reward hacking** · reward hacking(보상 해킹) — The policy maximizes the reward as written rather than the intent behind it (velocity reward met by vibrating in place). Symptom: the reward curve rises while the behavior is wrong. The diagnostic is "what is the cheapest way to earn this reward?" · 정책이 의도가 아니라 적힌 대로 보상을 최대화하는 것(속도 보상을 제자리 진동으로 채우는 식). 증상은 보상 곡선이 오르는데 거동은 틀린 것이고, 진단 질문은 "이 보상을 버는 가장 싼 방법이 무엇인가"다. → [[02-foundations/rl-basics|RL Basics §7]]
- **Reward model** · 보상 모델 — A network trained on human comparisons, usually with a Bradley–Terry loss, to score behaviour; a policy is then optimized against it. It is a proxy, least reliable where it saw no data. · 사람의 비교 데이터로, 보통 Bradley–Terry 손실로 학습해 거동에 점수를 매기는 신경망이며, 이후 정책을 그것에 대해 최적화한다. 대리물이므로 데이터를 못 본 곳에서 가장 믿을 수 없다. → [[02-foundations/rl-basics|RL Basics §11]]
- **Reward shaping** · 보상 셰이핑(reward shaping) — Replacing a sparse reward with a dense per-step signal so learning is feasible, at the cost of optimizing a proxy for the goal. In robotics the reward is a weighted sum of task, accuracy, smoothness, effort and constraint terms — the weights are hyperparameters that fight each other. · 희소 보상을 스텝마다의 촘촘한 신호로 바꿔 학습을 가능하게 하되, 목표 자체가 아니라 그 대리를 최적화하게 되는 것. 로봇에서 보상은 과제·정확도·부드러움·노력·제약 항의 가중합이고, 그 가중치들이 서로 싸우는 하이퍼파라미터다. → [[02-foundations/rl-basics|RL Basics §7]]
- **Reward-to-go** · reward-to-go — Weighting an action's log-probability gradient only by rewards that come after it, rather than by the whole episode's return. A variance reduction for policy gradients. · 행동의 로그 확률 그래디언트를 에피소드 전체 리턴이 아니라 그 행동 이후의 보상만으로 가중하는 것. 정책 그래디언트의 분산 감소책이다. → [[02-foundations/rl-basics|RL Basics §4]]
- **Riccati equation** · 리카티 방정식 — The algebraic equation whose stabilizing solution $P$ gives the optimal LQR gain $K = R^{-1}B^\top P$, computed once offline. · 안정화 해 $P$가 최적 LQR 이득을 주는 대수 방정식. → [[04-robotics/lqr-lqg|LQR/LQG]]
- **Right-half-plane zero** · 우반평면 영점 — A zero of the plant with positive real part. It forces the output to move the wrong way first and costs phase without costing gain, so it caps achievable bandwidth. A time delay behaves like one: the first-order Padé approximation of delay $\tau$ puts a zero at $2/\tau$. · 실수부가 양인 플랜트 영점. 출력이 처음에 반대 방향으로 움직이게 만들고, 이득은 그대로 둔 채 위상만 쓰므로 달성 가능한 대역폭을 제한한다. 시간 지연이 이처럼 굴어서, 지연 $\tau$의 1차 파데 근사는 $2/\tau$에 영점을 놓는다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **RLDS (Reinforcement Learning Datasets)** · RLDS — A standard episode format for storing robot and RL trajectories (steps of observations, actions and metadata). Open X-Embodiment converted all its datasets to it, and later robot corpora follow the convention. · 로봇·RL 궤적(관측·행동·메타데이터로 이루어진 스텝)을 저장하는 표준 에피소드 형식. Open X-Embodiment가 모든 데이터셋을 이 형식으로 바꿨고, 이후 로봇 코퍼스들이 이 규약을 따른다. → [[01-canonical-papers/notes/4-vla/open-x-embodiment|Open X-Embodiment]]
- **RLFT (RL fine-tuning)** · RLFT — Continuing with RL from a policy already pretrained by behavior cloning, usually with a KL term back to that policy. Pretraining makes exploration feasible; RL fixes what demonstrations could not cover. The same shape as pretrain → RLHF. · 행동 복제로 사전학습된 정책에서 RL을 이어가는 것; 사전학습 → RLHF와 같은 모양. → [[02-foundations/rl-basics|RL Basics §9]], [[01-canonical-papers/notes/8-construction/ext|ExT]]
- **RLHF** · RLHF — The three-stage recipe: supervised fine-tuning on demonstrations, then a reward model learned from human preferences, then RL (usually PPO) against that reward. · 시연으로 지도 미세조정 → 인간 선호로 보상 모델 학습 → 그 보상에 대해 RL(보통 PPO)로 정렬하는 3단계 레시피. → [[01-canonical-papers/notes/1-foundations/instructgpt|InstructGPT]]
- **RMSNorm** · RMSNorm — A LayerNorm variant that only divides by the root-mean-square of the features, with no mean subtraction and no shift. Common in LLM backbones at comparable quality. · 평균 차감과 시프트 없이 특징의 제곱평균제곱근으로 나누기만 하는 LayerNorm 변형. 품질은 비슷하며 LLM 백본에서 흔하다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **rmw (ROS middleware interface)** · rmw — The thin ROS layer that adapts ROS 2 to one specific middleware product, e.g. rmw_fastrtps_cpp (the default) or rmw_cyclonedds_cpp. Networked hosts should use the same implementation. · ROS 2를 특정 미들웨어 제품에 맞추는 얇은 층. 예: 기본인 rmw_fastrtps_cpp, rmw_cyclonedds_cpp. 네트워크로 묶인 호스트는 같은 구현을 써야 한다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §5]], [[04-robotics/ros2/from-simulation-to-hardware|25.11 Sim to Hardware §5]]
- **RNN (recurrent neural network)** · RNN(순환 신경망) — A network that reads a sequence one step at a time, updating a hidden state at each step. It cannot parallelize across time and suffers vanishing gradients over long spans. · 시퀀스를 한 스텝씩 읽으며 스텝마다 은닉 상태를 갱신하는 신경망. 시간 축으로 병렬화할 수 없고 긴 구간에서 그래디언트 소실을 겪는다. → [[01-canonical-papers/notes/1-foundations/lstm|LSTM]]
- **robomimic** · robomimic — An open benchmark and dataset suite from a controlled study of offline learning from human manipulation demonstrations. It separates the effects of data quality, algorithm, observation space and stopping criteria. · 사람의 조작 시연으로부터의 오프라인 학습을 통제해 연구한 결과로 공개된 벤치마크·데이터셋 모음. 데이터 품질·알고리즘·관측 공간·중단 기준의 효과를 분리한다. → [[01-canonical-papers/notes/4-vla/robomimic|robomimic]]
- **Robot middleware** · 로봇 미들웨어 — The software layer between a robot's separate processes that handles data exchange, discovery, independent restart, and a shared checked type language, so each part can be written and restarted on its own. · 로봇의 여러 프로세스 사이에서 데이터 교환, 서로 찾기(discovery), 독립 재시작, 공통의 검사 가능한 타입 체계를 맡는 소프트웨어 층. 덕분에 각 부분을 따로 쓰고 따로 재시작할 수 있다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §1]]
- **Robot-oriented design (co-design)** · robot-oriented design(공동설계) — Designing the building element itself for robotic reachability, tolerance, and assembly. It can remove difficulty at design time rather than solve it in perception or control. · 로봇의 도달성·공차·조립에 맞게 건축 요소 자체를 설계하는 것. 인식이나 제어로 풀기보다 설계 단계에서 어려움을 제거할 수 있다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §3]]
- **robot_state_publisher** · robot_state_publisher — The node that reads the URDF from its robot_description parameter, subscribes to joint_states, and publishes fixed joints to /tf_static and movable joints to /tf; it also republishes the URDF on a transient-local topic. · robot_description 파라미터로 URDF를 읽고 joint_states를 구독해, 고정 조인트는 /tf_static에, 움직이는 조인트는 /tf에 발행하는 노드. URDF를 transient local 토픽으로 다시 발행하기도 한다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §6]]
- **ROC curve** · ROC 곡선 — True positive rate plotted against false positive rate as the decision threshold sweeps. Summarizes a detector across every operating point at once, which is its value, and is computed within the columns of the confusion matrix, which is why it never sees the class balance. · 결정 문턱값을 훑으며 참양성률을 거짓양성률에 대해 그린 곡선. 모든 동작점에서의 검출기를 한 번에 요약하는 것이 그 값어치이고, 혼동행렬의 각 열 안에서 계산되므로 클래스 균형을 결코 보지 못한다. → [[02-foundations/ml-practice|ML Practice §3]]
- **Rodrigues' formula** · 로드리게스 공식 — The closed form of a rotation exponential, $e^{[\hat\omega]\theta} = I + \sin\theta\,[\hat\omega] + (1-\cos\theta)[\hat\omega]^2$. The infinite series collapses to three terms because $[\hat\omega]^3 = -[\hat\omega]$. · 회전 지수의 닫힌 형태, $e^{[\hat\omega]\theta} = I + \sin\theta\,[\hat\omega] + (1-\cos\theta)[\hat\omega]^2$. $[\hat\omega]^3 = -[\hat\omega]$이므로 무한급수가 세 항으로 접힌다. → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR ch.3 §2]]
- **RoI pooling** · RoI pooling — Pooling the backbone features inside each proposed region into a fixed-size grid, so a shared head can classify and refine regions of any size. · 제안된 각 영역 안의 백본 특징을 고정 크기 격자로 풀링해, 크기가 제각각인 영역을 공유 헤드가 분류·정제할 수 있게 하는 연산. → [[01-canonical-papers/notes/2-computer-vision/faster-r-cnn|Faster R-CNN]]
- **Role allocation** · 역할 배분 — Deciding which subtasks the human does and which the robot does, possibly changing over time with worker state or task phase. · 어느 하위 작업을 사람이, 어느 것을 로봇이 맡을지 정하는 것으로, 작업자 상태나 작업 단계에 따라 바뀔 수 있다. → [[05-construction-robotics/hrc-worker-centered|HRC §2]]
- **Rollout** · 롤아웃 — Running a policy forward for a sequence of steps, in the real world, a simulator, or a learned model's imagination. Robot results are reported over rollouts: ask how many, and what the median looks like. · 정책을 실제 세계·시뮬레이터·학습된 모델의 상상 속에서 일련의 스텝 동안 실행하는 것. 로봇 결과는 롤아웃에 걸쳐 보고되므로, 몇 번인지와 중앙값이 어떤지 물어라. → [[02-foundations/rl-basics|RL Basics §5]], [[02-foundations/ml-practice|ML Practice §5]]
- **Root-relative pose** · 루트 상대 자세 — 3D joint positions expressed relative to a root joint, usually the pelvis placed at the origin. Its error describes limb configuration only; absolute 3D localization of the person is a separate, harder problem. · 루트 관절(보통 원점에 둔 골반) 기준으로 표현한 3D 관절 위치. 오차는 사지 배치만 말하며, 사람의 절대 3D 위치추정은 별개의 더 어려운 문제다. → [[04-robotics/human-pose-gaze|Pose & gaze §3]]
- **Root-sum-square uncertainty (RSS)** · 제곱합의 제곱근 불확실성 — Combining independent zero-mean error sources as $\sqrt{\sum \sigma_i^2}$. The largest term dominates, and it is invalid for systematic offsets or correlated errors. · 독립이고 평균 0인 오차 원천을 $\sqrt{\sum \sigma_i^2}$로 합치는 것. 가장 큰 항이 지배하며, 체계적 오프셋이나 상관 오차에는 쓸 수 없다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §3]]
- **RoPE (rotary position embedding)** · RoPE — Encoding position by rotating query and key vectors through angles proportional to position, so attention depends on relative position. M-RoPE extends it across text, image, and video axes. · query와 key 벡터를 위치에 비례한 각도만큼 회전시켜 위치를 넣는 방식으로, 어텐션이 상대 위치에 의존하게 된다. M-RoPE는 이를 텍스트·이미지·비디오 축으로 확장한다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **ros2_control** · ros2_control — A framework that puts a named-interface seam between controllers and hardware, so the same controller and YAML run on a simulator, mock hardware or a real robot by changing one plugin line in the URDF. · 제어기와 하드웨어 사이에 이름 붙은 인터페이스라는 이음매를 두는 프레임워크. URDF의 plugin 줄 하나만 바꾸면 같은 제어기와 YAML이 시뮬레이터, mock 하드웨어, 실제 로봇에서 돈다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §5]]
- **ROS 2 (Robot Operating System 2)** · ROS 2 — Not an operating system: a robot middleware of libraries, message definitions, build tooling and command-line tools that runs on Linux and lets separately written programs find each other and exchange data. · 운영체제가 아니라 Linux 위에서 도는 로봇 미들웨어로, 라이브러리·메시지 정의·빌드 도구·CLI 도구의 묶음이다. 따로 작성된 프로그램들이 서로를 찾고 데이터를 주고받게 한다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §2]]
- **ros2 topic echo** · ros2 topic echo — Prints messages on a topic by creating a temporary subscriber. It adapts its QoS to the publishers it finds, so it can show data your own node cannot receive. · 임시 구독자를 만들어 토픽의 메시지를 찍는다. 찾은 퍼블리셔에 맞춰 QoS를 조정하므로, 내 노드는 받지 못하는 데이터도 보여 줄 수 있다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §7]]
- **ros2 topic hz, bw and delay** · ros2 topic hz·bw·delay — Three rate tools: hz measures how often messages arrive, bw how much bandwidth they use, delay the gap between a message's header stamp and its arrival. A growing delay reveals a filling queue. · 세 가지 측정 도구. hz는 도착 빈도, bw는 대역폭, delay는 메시지 헤더 스탬프와 수신 시각의 차이를 잰다. 계속 커지는 delay는 어딘가 큐가 차고 있다는 뜻이다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §3]]
- **ros2 topic info --verbose** · ros2 topic info --verbose — Lists every publisher and subscription on a topic with node name, type, type hash and full QoS profile; the one command that distinguishes name, type and QoS mismatches. · 토픽의 모든 퍼블리셔·서브스크립션을 노드 이름, 타입, 타입 해시, 전체 QoS 프로파일과 함께 보여 준다. 이름·타입·QoS 불일치를 가르는 단 하나의 명령이다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §7]]
- **rosbag2 (bag)** · rosbag2(bag) — ROS 2's recorder and player: ros2 bag record subscribes to chosen topics and writes a bag directory (metadata plus storage files, MCAP by default); ros2 bag play republishes them. · ROS 2의 기록·재생 도구. ros2 bag record는 고른 토픽을 구독해 bag 디렉터리(메타데이터와 저장 파일, 기본 MCAP)를 쓰고, ros2 bag play는 그것을 다시 발행한다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §6]]
- **rosdep** · rosdep — A tool that reads every package.xml, maps each platform-independent rosdep key to the system package name (e.g. an apt package on Ubuntu), and installs what is missing. It is a mapping, not a package manager. · 모든 package.xml을 읽고 플랫폼 독립적인 rosdep key를 시스템 패키지 이름(Ubuntu에서는 apt 패키지)으로 바꿔 빠진 것을 설치하는 도구. 패키지 관리자가 아니라 대응표다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §5]]
- **ROS_DOMAIN_ID** · ROS_DOMAIN_ID — An integer environment variable that partitions discovery: nodes see only nodes with the same value. Different values on two shells or hosts give invisible, error-free separation; on Linux choose 0 to 101. · discovery를 구획하는 정수 환경 변수로, 같은 값을 가진 노드끼리만 서로 보인다. 두 셸이나 호스트의 값이 다르면 오류 없이 서로 안 보인다. Linux에서는 0~101에서 고른다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §5]], [[04-robotics/ros2/from-simulation-to-hardware|25.11 Sim to Hardware §5]]
- **ros_gz_bridge** · ros_gz_bridge — A node that republishes topics between Gazebo Transport and ROS 2, per topic and type pair. A topic you do not bridge, commonly /clock, simply does not exist on the ROS side. · Gazebo Transport와 ROS 2 사이에서 토픽과 타입 쌍마다 토픽을 다시 발행하는 노드. 브리지하지 않은 토픽은(흔히 /clock) ROS 쪽에 아예 존재하지 않는다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §3]]
- **ROS time (ROSTime)** · ROSTime — The node clock to use for anything published or compared with message stamps. It equals system time until use_sim_time is set, then returns the latest value from the /clock topic. · 발행하거나 메시지 스탬프와 비교하는 모든 것에 써야 할 노드 시계. use_sim_time이 설정되기 전에는 시스템 시간과 같고, 설정되면 /clock 토픽의 최신 값을 돌려준다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §10]]
- **Rotation matrix** · 회전 행렬 — A $3\times3$ matrix with $R^\top R = I$ and $\det R = +1$: its columns are the rotated axes, its inverse is its transpose, and composition order matters. Determinant $-1$ means a reflection bug. · $R^\top R = I$이고 $\det R = +1$인 $3\times3$ 행렬. 열은 회전된 축들이고, 역행렬은 전치이며, 합성 순서가 중요하다. 행렬식이 $-1$이면 실수로 반사를 만든 것이다. → [[02-foundations/se3-geometry|SE(3) §1]]
- **rqt** · rqt — ROS's Qt GUI framework with plugins such as rqt_graph (connections), rqt_console (filter logs), rqt_plot (values over time) and rqt_bag (browse recordings). It shows command-line data with a time axis. · ROS의 Qt GUI 프레임워크로 rqt_graph(연결), rqt_console(로그 필터), rqt_plot(시간에 따른 값), rqt_bag(기록 탐색) 같은 플러그인이 있다. CLI의 데이터를 시간축과 함께 보여 준다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §3]]
- **RSSM** · RSSM — A recurrent state-space model carrying both deterministic and stochastic state; the standard world-model backbone. · 결정론적+확률적 상태를 함께 갖는 순환 상태공간 모델. → [[01-canonical-papers/notes/5-world-models/planet|PlaNet]], [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **RSS (Robotics: Science and Systems)** · RSS — A selective robotics conference with double-blind review and a one-page rebuttal for only a subset of papers. It publishes no official acceptance rate. · 이중 블라인드 심사를 하고 일부 논문에만 1쪽 반박문을 허용하는 선별적 로보틱스 학회. 공식 채택률을 발표하지 않는다. → [[06-research-practice/venue-strategy|Venue Strategy §2]], [[06-research-practice/venue-strategy|Venue Strategy §3]]
- **RT_PREEMPT (real-time kernel)** · RT_PREEMPT — A Linux kernel patch set that bounds scheduling latency, so a ready thread runs within a known time. It does nothing about allocation, page faults or blocking inside your code. · 스케줄링 지연에 상한을 주어 실행 준비된 스레드가 정해진 시간 안에 돌게 하는 Linux 커널. 코드 안의 메모리 할당, 페이지 폴트, 블로킹은 해결하지 않는다. → [[04-robotics/ros2/from-simulation-to-hardware|25.11 Sim to Hardware §6]]
- **Rule of three** · 3의 법칙(rule of three) — If zero failures occur in n trials, the 95% upper bound on the true failure rate is about 3/n. Valid for n above about 30; at n = 10 the exact bound is 26%. · n회 시행에서 실패가 0이면 참 실패율의 95% 상한이 약 3/n이다. n이 약 30 이상일 때 맞는 근사이며, n = 10이면 정확한 상한은 26%다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §4]]
- **RViz and the Fixed Frame** · RViz와 Fixed Frame — RViz is ROS's 3D visualizer. Its Fixed Frame is the reference the world is drawn in and should not move with the robot (map or odom); each display's status names what went wrong. · RViz는 ROS의 3D 시각화 도구. Fixed Frame은 세계를 그리는 기준 프레임으로 로봇과 함께 움직이면 안 되며(map이나 odom), 각 display의 상태가 무엇이 잘못됐는지 알려 준다. → [[04-robotics/ros2/debugging-data-reproducibility|25.10 Debugging & Data §4]]
- **SA and RA afferents (slowly and rapidly adapting)** · SA·RA 구심신경 — Skin receptor classes: slowly adapting units keep firing during sustained stimuli, rapidly adapting units fire mainly at change; type I has small receptive fields, type II large. SA-I Merkel, RA-I Meissner, SA-II Ruffini, RA-II Pacinian. · 피부 수용기 부류. SA는 지속 자극 동안 계속 발화하고 RA는 주로 자극이 변할 때 발화하며, I형은 수용장이 작고 II형은 크다. SA-I은 Merkel, RA-I은 Meissner, SA-II는 Ruffini, RA-II는 Pacinian이다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §1]]
- **SAC (Soft Actor-Critic)** · SAC — An off-policy actor–critic that maximizes return plus policy entropy, using a replay buffer, twin critics, and a tanh-squashed Gaussian actor. It is the standard continuous-control off-policy baseline. · 수익과 정책 entropy의 합을 최대화하는 off-policy actor–critic으로, replay buffer, twin critic, tanh로 누른 Gaussian actor를 쓴다. 연속 제어의 표준 off-policy 베이스라인이다. → [[01-canonical-papers/notes/1-foundations/sac|SAC]]
- **Saddle point** · 안장점 — A point with zero gradient where the surface curves up in some directions and down in others, so it is not a minimum. Evidence suggests high-loss critical points in neural networks are overwhelmingly saddles. · 그래디언트가 0이지만 어떤 방향으로는 위로, 다른 방향으로는 아래로 휘어 최솟점이 아닌 점. 신경망에서 손실이 높은 임계점은 압도적으로 안장점이라는 근거가 보고되었다. → [[02-foundations/optimization|Optimization §3]]
- **Safety filter / envelope** · 안전 필터·엔벨로프 — A layer that clips or vetoes unsafe commands before they reach the actuator (often an MPC). Unlike a reward penalty it is a hard mechanism, which is why reward penalties alone are not a safety guarantee. · 안전하지 않은 명령을 액추에이터 앞에서 자르거나 거부하는 층; 보상 페널티와 달리 하드 장치다. → [[02-foundations/rl-basics|RL Basics §9]], [[04-robotics/mpc|MPC]]
- **Sample efficiency** · 샘플 효율 — How much experience a method needs to reach a given performance. Check the unit: environment steps, wall-clock time or real machine hours; only the last is scarce for robots. · 방법이 주어진 성능에 도달하는 데 필요한 경험의 양. 단위를 확인하라: environment step, 벽시계 시간, 실기계 시간 중 로봇에게 희소한 것은 마지막뿐이다. → [[02-foundations/rl-basics|RL Basics §10]]
- **Saturation (actuator saturation)** · 포화 — An actuator reaching its force, torque, or rate limit, so the commanded input is clipped. No linear stability result survives it; it causes integral windup, and handling it natively is MPC's reason to exist. · 액추에이터가 힘·토크·속도 한계에 닿아 명령 입력이 잘리는 것. 어떤 선형 안정성 결과도 살아남지 못하고 적분 와인드업을 일으키며, 이를 태생적으로 다루는 것이 MPC의 존재 이유다. → [[04-robotics/control-theory-ce397|Control Theory §9]]
- **Scale ambiguity** · 스케일 모호성 — Because projection divides by depth, a single image cannot tell a large far object from a small near one. Metric scale must come from calibrated stereo, lidar, known object size or priors. · 투영이 깊이로 나누기 때문에 이미지 한 장으로는 크고 먼 물체와 작고 가까운 물체를 구분할 수 없다. 미터 스케일은 보정된 스테레오, 라이다, 알려진 물체 크기, 사전지식에서 와야 한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §1]], [[04-robotics/geometric-perception-calibration|Geometric perception §6]]
- **Scaling law** · 스케일링 법칙 — The empirical regularity that loss follows a power law in parameters, data, and compute. · 손실이 파라미터·데이터·연산의 거듭제곱 법칙을 따른다는 경험 법칙. → [[01-canonical-papers/notes/1-foundations/scaling-laws|Scaling Laws]]
- **Scan matching** · 스캔 매칭 — Estimating the motion between two range scans by aligning their geometry. It underlies LiDAR odometry and the registration of scans to a map or a BIM model. · 두 거리 스캔의 기하를 맞춰 그 사이의 운동을 추정하는 것. LiDAR 오도메트리와 스캔을 지도나 BIM 모델에 정합하는 일의 기반이다. → [[01-canonical-papers/notes/8-construction/cho-slam|Cho SLAM 2018]]
- **Scan-to-BIM** · Scan-to-BIM — Registering point clouds or images to a design model, then inferring installed, missing, or deviating components, or reconstructing an as-built BIM from scans. · 포인트 클라우드나 이미지를 설계 모델에 정합한 뒤 설치·누락·편차 부재를 추론하거나, 스캔으로 as-built BIM을 복원하는 것. → [[05-construction-robotics/site-perception|Site Perception §2]]
- **Scene bias** · 장면 편향 — When a single frame carries nearly all label information, $I(y;x_1)\approx I(y;x_{1:T})$, so a video model can score well with no temporal reasoning. Diagnose with single-frame or shuffled-frame baselines. · 한 프레임이 레이블 정보 거의 전부를 담아 $I(y;x_1)\approx I(y;x_{1:T})$인 상태로, 비디오 모델이 시간 추론 없이도 높은 점수를 낼 수 있다. 단일 프레임·프레임 셔플 베이스라인으로 진단한다. → [[04-robotics/video-action-understanding|Video §2]]
- **Scene graph (3D scene graph)** · 장면 그래프 — An object-centric map whose nodes are objects fused from multiple views and whose edges are relations, often inferred by an LLM. Cheaper and more composable than dense feature fields, and queryable. · 노드가 다시점 융합된 물체이고 간선이 관계(흔히 LLM이 추론)인 물체 중심 지도. 조밀한 특징 필드보다 싸고 조합 가능하며 질의할 수 있다. → [[04-robotics/semantic-language-navigation|Semantic navigation §7]]
- **Schur complement** · Schur 보수 — For a block matrix with blocks $A$, $B$, $C$, the matrix $S = C - B^\top A^{-1}B$ left behind when the $A$ block is eliminated. In a SLAM back end it *is* marginalization: dropped keyframes vanish while their evidence folds into the survivors — losing no information but making the remaining block denser, which is why sliding windows are capped. · 블록 $A$, $B$, $C$를 가진 블록 행렬에서 $A$ 블록을 소거했을 때 남는 행렬 $S = C - B^\top A^{-1}B$. SLAM 백엔드에서는 이것이 *곧* 주변화다. 버린 keyframe은 사라지지만 그 증거는 살아남은 것들 안으로 접혀 들어간다 — 정보를 잃지 않는 대신 남은 블록이 조밀해지고, 슬라이딩 윈도우에 상한을 두는 이유가 그것이다. → [[04-robotics/state-estimation-slam|3. State Estimation & SLAM §8]]
- **Scope (of a claim)** · 범위 — The population, environment, embodiment, sensors, data regime, task, intervention policy and horizon a result applies to. Assumptions are not weaknesses by default; hidden assumptions are. · 결과가 적용되는 모집단, 환경, embodiment, 센서, 데이터 체제, 과제, 개입 정책, 평가 지평. 가정은 그 자체로 약점이 아니고, 숨긴 가정이 약점이다. → [[06-research-practice/research-questions-claims|Research Questions §5]]
- **Score function (score)** · score — The gradient of log-density with respect to the data, $\nabla_x \log p(x)$; it points toward higher probability. Score-based diffusion learns it at every noise level and generates by following it. · 데이터에 대한 로그 밀도의 그래디언트 $\nabla_x \log p(x)$로, 확률이 높아지는 방향을 가리킨다. score 기반 디퓨전은 모든 노이즈 수준에서 이것을 배우고 그 방향을 따라 생성한다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **Screw axis** · 스크류 축 — Every nonzero rigid-body velocity is a rotation about an axis combined with translation along it; the screw axis $\mathcal{S}$ encodes that axis, and its pitch is distance advanced per radian turned (zero for pure rotation). · 0이 아닌 모든 강체 속도는 축 둘레의 회전과 그 축을 따른 병진의 결합이다. 스크류 축 $\mathcal{S}$가 그 축을 담고, 피치는 1라디안 도는 동안 나아가는 거리다(순수 회전이면 0). → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR ch.3 §3]]
- **Scripted controller** · 스크립트 제어기 — A hand-coded controller that performs a task by fixed rules. In simulation it generates cheap, labeled demonstrations alongside RL expert policies and teleoperation. · 고정 규칙으로 과제를 수행하도록 손으로 짠 제어기. 시뮬레이션에서 RL 전문가 정책, 원격조작과 함께 값싸고 라벨된 시연을 만든다. → [[01-canonical-papers/notes/8-construction/ext|ExT]]
- **SDE (stochastic differential equation)** · 확률미분방정식(SDE) — An equation for a quantity that drifts deterministically while being kicked by random noise, $dx=f(x,t)dt+g(t)dw$. Score SDE describes diffusion's noising as one and generation as its reverse. · 결정론적으로 흘러가면서 무작위 노이즈에 차이는 양의 방정식 $dx=f(x,t)dt+g(t)dw$. Score SDE는 디퓨전의 노이즈 주입을 SDE로, 생성을 그 역으로 기술한다. → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **SDF (Simulation Description Format)** · SDF — Gazebo's native XML format for robots and worlds. It can express things URDF cannot, such as closed kinematic loops; Gazebo converts URDF to SDF internally, reading extra information from gazebo tags. · 로봇과 월드를 위한 Gazebo 고유의 XML 형식. 닫힌 기구 루프처럼 URDF가 못 하는 것을 표현하며, Gazebo는 URDF를 내부에서 SDF로 바꾸면서 gazebo 태그의 추가 정보를 읽는다. → [[04-robotics/ros2/simulation-and-control|25.7 Simulation & ros2_control §4]]
- **SE(3)** · SE(3) — The group of rigid-body poses in 3D, a rotation plus a translation, written as $4\times4$ homogeneous transforms. Robot actions, camera poses, and 3D reconstructions are expressed in it. · 3차원 강체 자세(회전 + 이동)의 군으로, $4\times4$ 동차 변환으로 쓴다. 로봇 행동, 카메라 자세, 3D 복원이 여기서 표현된다. → [[02-foundations/se3-geometry|SE(3) §3]]
- **Seam tracking** · seam tracking(이음선 추적) — Sensing the joint line between parts to be welded and steering the torch along it, since real joint geometry varies from the plan. · 용접할 부재 사이의 이음선을 센싱해 토치를 그 선을 따라 움직이는 것으로, 실제 이음 기하가 계획과 다르기 때문에 필요하다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §2]]
- **Seed (random seed)** · 랜덤 시드 — The starting value of the random number generator for a run. Deep learning results wobble across seeds, so serious reporting states the number of runs and an uncertainty measure. · 한 실행의 난수 생성기 시작값. 딥러닝 결과는 시드에 따라 흔들리므로, 진지한 보고는 실행 횟수와 불확실성 지표를 명시한다. → [[02-foundations/ml-practice|ML Practice §4]]
- **Seen / unseen split** · seen/unseen 분할 — Reporting results separately for conditions present in training and for new ones (instructions, objects, scenes). The gap is the honest content of a generalization claim, such as RT-1's 97% versus 76%. · 학습에 있던 조건과 새로운 조건(지시문·물체·장면)의 결과를 따로 보고하는 것. 그 격차가 일반화 주장의 정직한 내용이다(RT-1의 97% 대 76%). → [[02-foundations/ml-practice|ML Practice §1]], [[01-canonical-papers/how-to-read|How to Read §4.5]]
- **Segmentation mask** · 마스크 — A per-pixel map marking which pixels belong to one region or object. SAM outputs masks, not class names. · 어떤 픽셀이 한 영역이나 물체에 속하는지 표시한 픽셀 단위 지도. SAM은 클래스 이름이 아니라 마스크를 출력한다. → [[01-canonical-papers/notes/2-computer-vision/sam|SAM]]
- **Segmentation (semantic, instance, panoptic)** · 분할(segmentation) — Labeling images at the pixel level: semantic segmentation gives each pixel a class, instance segmentation separates individual objects, and panoptic segmentation does both. · 이미지를 픽셀 단위로 라벨링하는 과제. 의미 분할은 픽셀마다 클래스를, 인스턴스 분할은 개별 물체를 구분하고, panoptic 분할은 둘을 함께 한다. → [[01-canonical-papers/notes/2-computer-vision/u-net|U-Net]]
- **Selection matrix** · 선택 행렬 — In hybrid position/force control, the diagonal matrix of ones and zeros that assigns each task-frame direction to either the position controller or the force controller, so the two never address the same axis. Its premise is that the task frame and contact geometry are known. · 하이브리드 위치/힘 제어에서 과제 프레임의 각 방향을 위치 제어기와 힘 제어기 중 하나에 배정하는 0과 1의 대각 행렬. 둘이 같은 축을 건드리지 않게 한다. 전제는 과제 프레임과 접촉 기하를 안다는 것이다. → [[04-robotics/force-compliance-control|13. §3]]
- **Self-attention** · self-attention — Attention where a sequence attends to itself (Q, K, V drawn from the same sequence). · 한 시퀀스가 자기 자신을 참조하는 어텐션. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Self-distillation** · 자기 증류 — Self-supervised training where a student matches the output distribution of a teacher that is only an EMA copy of itself, across different augmented views, with no labels and no negatives. · 라벨도 음의 쌍도 없이, 서로 다른 증강 뷰에 대해 학생이 자기 자신의 EMA 복사본인 교사의 출력 분포를 맞추는 자기지도 학습. → [[01-canonical-papers/notes/2-computer-vision/dino|DINO]]
- **Self-supervised learning (SSL)** · 자기지도 학습 — Learning representations from unlabeled data using a target built from the data itself, such as reconstructing masked patches (MAE) or matching augmented views (DINO). · 가려진 패치 복원(MAE)이나 증강 뷰 맞추기(DINO)처럼 데이터 자체로 만든 목표를 써서 라벨 없는 데이터에서 표현을 학습하는 것. → [[01-canonical-papers/notes/2-computer-vision/dino|DINO]], [[01-canonical-papers/notes/1-foundations/mae|MAE]]
- **Self-supervised traversability** · 자기지도 traversability — Learning where a robot can go from labels its own experience produces, such as bumper hits, IMU shaking, or how well it tracked its commanded velocity, with no human annotation. · 범퍼 충돌, IMU 흔들림, 명령 속도를 얼마나 잘 추종했는가처럼 로봇 자신의 경험이 만든 라벨로 갈 수 있는 곳을 학습하는 것. 사람 주석이 필요 없다. → [[01-canonical-papers/notes/9-navigation/badgr|BADGR]], [[01-canonical-papers/notes/9-navigation/wild-visual-navigation|WVN]]
- **Semantic generalization** · 의미적 일반화 — Handling new objects, concepts or instructions by drawing on pretrained knowledge, as distinct from acquiring new motor skills. RT-2's web pretraining gave semantic generalization; dexterity stayed bounded by robot data. · 새 운동 기술을 얻는 것과 구별되는, 사전학습 지식을 끌어와 새 물체·개념·지시를 다루는 능력. RT-2의 웹 사전학습은 의미적 일반화를 줬지만 손재주는 로봇 데이터 범위에 머물렀다. → [[01-canonical-papers/notes/4-vla/rt-2|RT-2]]
- **Semantic grounding (semantic gap)** · 의미 접지 — Matching a design-model entity, such as a BIM wall, to the observed physical object and to executable robot skills. The gap between design objects and robot actions is the hard interface. · BIM 벽 같은 설계 모델 객체를 관측된 실제 객체와 실행 가능한 로봇 skill에 대응시키는 것. 설계 객체와 로봇 행동 사이의 의미 격차가 어려운 인터페이스다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §3]]
- **Semantic map** · 의미 지도 — A map whose cells also carry object-category labels, so a navigation policy can pick exploration goals by meaning; SemExp made it the backbone of modular ObjectNav. · 셀에 물체 범주 라벨도 담아 내비게이션 정책이 의미로 탐색 목표를 고르게 한 지도. SemExp가 이것을 모듈형 ObjectNav의 중추로 만들었다. → [[04-robotics/semantic-language-navigation|Semantic navigation §3]]
- **Semantic prior (arrangement prior)** · 배치 사전지식 — Learned knowledge of how objects are usually arranged, used to decide where to search: a toilet is sought in different places than a sofa. Home-learned priors may not hold on sites. · 물체들이 서로에 대해 보통 어떻게 놓이는지에 대한 학습된 지식으로, 어디를 볼지 정하는 데 쓴다. 변기는 소파와 다른 곳에서 찾는다. 주택에서 배운 사전지식은 현장에서 성립하지 않을 수 있다. → [[01-canonical-papers/notes/9-navigation/semexp|SemExp]]
- **Sense–plan–act** · Sense–plan–act — The early deliberative architecture: sensing builds a world model, a planner reasons on it, and a controller executes. The controller waits on the planner and never sees sensors, so the robot cannot react while thinking. · 초기 숙고형 아키텍처. 센싱이 세계 모델을 만들고 플래너가 추론하고 제어기가 실행한다. 제어기는 플래너를 기다리고 센서를 직접 보지 못하므로 로봇은 생각하는 동안 반응하지 못한다. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Sensitivity function ($S$)** · 감도 함수 — The transfer function $S = 1/(1+PC)$ from disturbance to output error; $\lvert S\rvert < 1$ means feedback is attenuating at that frequency and $> 1$ means it is amplifying. Its peak $M_s$ is the inverse of the stability margin, $s_m = 1/M_s$. · 외란에서 출력 오차로 가는 전달함수 $S = 1/(1+PC)$. $\lvert S\rvert < 1$이면 그 주파수에서 피드백이 감쇠시키는 것이고 $> 1$이면 증폭시키는 것이다. 최댓값 $M_s$는 안정 여유의 역수다: $s_m = 1/M_s$. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Sensor data QoS profile** · Sensor data 프로파일 — A predefined profile (keep last 5, best effort, volatile) for high-rate streams like camera frames, lidar and IMU where the newest sample matters and losses are acceptable. It causes most reliability mismatches. · 카메라, 라이다, IMU처럼 최신 샘플이 중요하고 손실을 감수하는 고속 스트림용 미리 정의된 프로파일(keep last 5, best effort, volatile). reliability 불일치 대부분의 원인이다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §5]]
- **Separation principle** · 분리 원리 — For linear-Gaussian models the estimator and controller may be designed independently and remain jointly optimal — with the caveat that LQG has no guaranteed robustness margins. · 선형-가우시안 모델에서는 추정기와 제어기를 독립 설계해도 결합이 최적으로 남는다는 원리. 다만 LQG에는 보장된 강건성 여유가 없다는 단서가 붙는다. → [[04-robotics/lqr-lqg|LQR/LQG]], [[04-robotics/control-theory-ce397|제어 이론 §8]]
- **Series elastic actuator (SEA)** · 직렬 탄성 액추에이터 — An actuator with a deliberate spring between motor and load, adding compliance. Its behavior with its control software is hard to model analytically, which is why Hwangbo 2019 learned an actuator network. · 모터와 부하 사이에 의도적으로 스프링을 둔 액추에이터로, 컴플라이언스를 더한다. 제어 소프트웨어를 포함한 거동을 해석적으로 모델링하기 어려워 Hwangbo 2019가 액추에이터 네트워크를 학습했다. → [[04-robotics/legged-locomotion|Legged Locomotion §3]]
- **Service (.srv)** · 서비스(service) — Request–response between nodes: a client sends a request and a server computes and returns a response, defined in a .srv file. There should be one server per service name, and handlers must return quickly. · 노드 사이의 요청–응답. 클라이언트가 요청을 보내고 서버가 계산해 응답을 돌려주며, .srv 파일로 정의한다. 서비스 이름당 서버는 하나여야 하고, 처리기는 빨리 돌아와야 한다. → [[04-robotics/ros2/services-actions-parameters|25.3 Services & Actions §2]]
- **Set cover** · 집합 덮개 — Choosing the fewest subsets whose union covers a universe, such as viewpoints that together see every surface patch. It is NP-hard; greedy stays within about $\ln n$ of optimal, essentially the best possible. · 합집합이 전체 집합을 덮는 가장 적은 부분집합 고르기(예: 모든 표면 패치를 함께 보는 시점들). NP-난해이며, 그리디가 최적의 약 $\ln n$배 이내로 사실상 최선이다. → [[02-foundations/algorithms/greedy-mst|11.4 §7]]
- **Settling time** · 정착 시간 — How long a response takes to stay within a band, commonly 2%, of its final value. For a second-order system $t_s \approx 4/(\zeta\omega_n)$. · 응답이 최종값의 일정 범위(흔히 2%) 안에 머물기까지 걸리는 시간. 2차 시스템에서 $t_s \approx 4/(\zeta\omega_n)$이다. → [[04-robotics/control-theory-ce397|Control Theory §5]]
- **SfM (Structure from Motion)** · SfM — The classical pipeline recovering camera poses and sparse 3D structure from many photographs. · 여러 사진에서 카메라 자세와 성긴 3D 구조를 복원하는 고전 파이프라인. → [[01-canonical-papers/notes/2-computer-vision/vggt|VGGT]] (이를 feed-forward로 대체)
- **SFT (supervised fine-tuning)** · SFT(지도 파인튜닝) — Fine-tuning a pretrained model on demonstration outputs with ordinary supervised loss. It is stage one of RLHF, before the reward model and PPO. · 사전학습 모델을 시연 출력에 대해 일반 지도 손실로 파인튜닝하는 것. RLHF에서 보상 모델과 PPO 앞의 첫 단계다. → [[01-canonical-papers/notes/1-foundations/instructgpt|InstructGPT]]
- **Shared autonomy** · 공유 자율성 — A structure in which a human and a robot divide authority and decision-making during normal execution; distinct from mere human monitoring, and it must state arbitration and override. · 사람과 로봇이 정상 실행 중 권한과 의사결정을 나누는 구조. 사람이 그저 감시만 하는 것과는 다르며, 중재(arbitration)와 override 방식을 반드시 명시해야 한다. → [[04-robotics/hri-safety|HRI & Safety]]
- **Shared embedding space (joint embedding)** · 공유 임베딩 공간 — One vector space into which separate encoders map different modalities, so matching image and text land close together and similarity can be compared directly. · 서로 다른 인코더가 각 모달리티를 사상해 넣는 하나의 벡터 공간. 짝이 맞는 이미지와 텍스트가 가까이 놓여 유사도를 직접 비교할 수 있다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **Shifted window attention** · 창 어텐션 + 이동 창 — Computing attention only inside local windows, which keeps cost linear in image size, and shifting the window grid every other layer so information crosses window borders. · 국소 창 안에서만 어텐션을 계산해 비용을 이미지 크기에 선형으로 유지하고, 한 층 걸러 창 격자를 이동시켜 정보가 창 경계를 넘게 하는 방식. → [[01-canonical-papers/notes/2-computer-vision/swin|Swin]]
- **Short loading cycle** · 단거리 적재 사이클 — The short, repeated fill–reverse–drive–dump cycle a wheel loader runs between a pile and a nearby truck or hopper, where cycle time dominates productivity. · 휠로더가 더미와 가까운 트럭·호퍼 사이에서 반복하는 채우기–후진–주행–붓기의 짧은 사이클로, 사이클 시간이 생산성을 좌우한다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §4]]
- **Shotcrete** · 숏크리트 — Concrete sprayed through a hose at high velocity onto a surface instead of poured into formwork; robotic shotcrete printing builds structures this way. · 거푸집에 붓는 대신 호스로 고속 분사해 표면에 뿜어 붙이는 콘크리트. 로봇 숏크리트 프린팅은 이렇게 구조물을 만든다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §2]]
- **SIFT (scale-invariant feature transform)** · SIFT — Detects points across scale as extrema of a difference of Gaussians, then describes each in its dominant orientation with a 128-number gradient-histogram vector compared by Euclidean distance. · 가우시안 차분(difference of Gaussians)의 극값으로 스케일에 걸쳐 점을 검출하고, 지배 방향 좌표계에서 기울기 히스토그램 128차원 벡터로 기술해 유클리드 거리로 비교한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2.5]]
- **SigLIP** · SigLIP — A CLIP-style image–text encoder trained with a pairwise sigmoid loss instead of CLIP's batch-wide softmax, so the loss no longer couples every pair in the batch. It is the vision encoder in PaliGemma. · CLIP의 배치 전체 softmax 대신 쌍별 sigmoid 손실로 학습한 CLIP식 이미지-텍스트 인코더. 손실이 배치의 모든 쌍을 묶지 않는다. PaliGemma의 비전 인코더다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **Signal detection theory** · 신호 검출 분석 — Analysis that separates sensitivity, how well a person tells stimulus from no stimulus, from response criterion, how willing they are to say yes when unsure. It needs false alarms recorded as well as hits. · 민감도(자극이 있을 때와 없을 때를 얼마나 잘 구별하는가)와 반응 기준(확신이 없을 때 "있다"고 답하려는 경향)을 분리하는 분석. hit뿐 아니라 false alarm도 기록해야 한다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §3]]
- **Signal temporal logic (STL)** · signal temporal logic — Temporal logic for continuous signals with time intervals and real-valued predicates. A robustness score says how much a signal satisfies or violates a formula, e.g. +0.5 m for a 2.5 m approach against 2 m. · 시간 구간과 실수값 술어를 붙인 연속 신호용 시간 논리. robustness 점수가 신호가 식을 얼마나 여유 있게 만족하거나 위반하는지 말한다. "항상 2 m 초과"에 대해 가장 가까운 거리 2.5 m는 +0.5 m다. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Sim-to-real transfer** · sim-to-real — The modeling, training, adaptation, and evaluation practices that keep a policy trained in simulation useful on the real machine when simulator assumptions fail. · 시뮬레이터 가정이 깨져도 시뮬레이션에서 학습한 정책이 실기계에서 쓸모 있도록 하는 모델링·학습·적응·평가 방법의 묶음. → [[05-construction-robotics/sim-to-real|Sim-to-Real §1]]
- **Simulation-free training** · 시뮬레이션 없는 학습 — Training a continuous-time generative model without integrating its ODE or SDE during training, by regressing a known per-sample target. Flow matching and diffusion's noise prediction are both simulation-free. · 샘플별로 알려진 타깃을 회귀해, 학습 중에 ODE나 SDE를 적분하지 않고 연속 시간 생성 모델을 학습하는 것. flow matching과 디퓨전의 노이즈 예측이 모두 시뮬레이션 없는 학습이다. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Simulation–lab–site ladder** · 시뮬레이션–실험실–현장 사다리 — The wiki's distinction between simulation, laboratory or mock-up, and active construction site evidence. Few contact-rich manipulation papers reach the site rung. · 시뮬레이션, 실험실·목업, 가동 중인 건설 현장 증거를 구분하는 위키의 틀. 접촉이 많은 조작 논문 중 현장 단에 도달한 것은 드물다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §3]]
- **Single-anonymized review (single-blind review)** · 단측 익명 심사 — Review in which referees see the authors' names but authors do not see the referees'. IJRR and most civil-engineering journals use it; ICRA and IROS did until their 2026 editions. · 심사자는 저자 이름을 보지만 저자는 심사자를 모르는 심사. IJRR과 토목 저널 대부분이 쓰며, ICRA·IROS는 2026년판 전까지 이 방식이었다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Single-linkage clustering** · 단일 연결 군집화 — Clustering by running Kruskal on pairwise distances and stopping before the long edges; with a distance threshold it is Euclidean cluster extraction for point clouds. One chain of short hops can merge two objects. · 점 쌍 거리에 Kruskal을 돌리다가 긴 간선 전에 멈추는 군집화로, 거리 임계값을 쓰면 포인트 클라우드의 유클리드 군집 추출이다. 짧은 도약의 사슬 하나가 두 물체를 합칠 수 있다. → [[02-foundations/algorithms/greedy-mst|11.4 §6]]
- **Single rigid body model** · 단일 강체 — Approximating a legged robot as one rigid body pushed by foot forces, ignoring leg dynamics. With small roll and pitch it linearizes so MPC becomes a convex QP, at the cost of leg-swing momentum. · 다리 동역학을 무시하고 보행 로봇을 발 힘이 미는 강체 하나로 근사하는 것. 작은 롤·피치 가정과 함께 선형화되어 MPC가 볼록 QP가 되지만, 다리를 휘두르는 운동량을 잃는다. → [[04-robotics/convex-mpc-legged|Convex MPC]]
- **Singularity (kinematic)** · 특이점 — A configuration where the Jacobian loses rank, so the end-effector cannot move instantaneously in some direction; nearby, small task motions demand huge joint velocities. A planar 2R arm is singular when straight or folded. · 야코비안이 랭크를 잃어 말단이 어떤 방향으로 순간적으로 움직일 수 없는 컨피규레이션. 근처에서는 작은 작업 운동이 거대한 관절 속도를 요구한다. 평면 2R 팔은 완전히 뻗거나 접힐 때 특이하다. → [[04-robotics/modern-robotics/ch05-velocity-kinematics|MR ch.5 §4]]
- **Situation awareness** · 상황 인식 — Correctly perceiving and understanding what the system and the site are doing right now. An operator can lose it behind a reassuring interface and miss a failure. · 시스템과 현장이 지금 무엇을 하고 있는지 올바르게 지각하고 이해하는 상태. 안심시키는 인터페이스 뒤에서 이를 잃으면 조작자는 고장을 놓칠 수 있다. → [[04-robotics/hri-safety|HRI & Safety §4]]
- **Skew-symmetric matrix** · 반대칭 행렬 — A matrix with $A^\top = -A$. Small rotations are generated by one, $R \approx I + [\hat\omega\theta]_\times$, because differentiating $R^\top R = I$ forces it; its off-diagonal entries hold the rotation axis components. · $A^\top = -A$인 행렬. $R^\top R = I$를 미분하면 생성자가 반드시 이 꼴이 되므로 미소 회전은 $R \approx I + [\hat\omega\theta]_\times$로 생성되며, 비대각 성분이 회전축 성분을 담는다. → [[02-foundations/se3-geometry|SE(3) §4]]
- **Skew-symmetry property** · 반대칭 성질 — With a suitable choice of the non-unique Coriolis matrix $C$, $\dot M - 2C$ is skew-symmetric, so $x^\top(\dot M - 2C)x = 0$ for every $x$. Most manipulator stability proofs rely on this identity. · 유일하지 않은 코리올리 행렬 $C$를 알맞게 고르면 $\dot M - 2C$가 반대칭이 되어 모든 $x$에 대해 $x^\top(\dot M - 2C)x = 0$이다. 매니퓰레이터 안정성 증명 대부분이 이 항등식을 쓴다. → [[02-foundations/manipulator-kinematics-dynamics|Manipulator Dynamics §2]]
- **Skilled operator baseline** · 숙련 운전자 베이스라인 — Comparing an autonomous machine's throughput, such as tons or m³ per hour, against an experienced human operator on the same work, rather than reporting absolute numbers. · 자율 기계의 처리량(톤 또는 m³/시간)을 절대치로만 보고하지 않고 같은 작업의 숙련 운전자와 비교하는 것. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §6]]
- **Skill library** · skill library — A fixed set of pretrained robot behaviors (pick, place, go to) that a planner sequences. The system can only do what the library contains; adding a capability means training a new skill. · 계획기가 순서를 짜는, 사전학습된 로봇 행동(집기·놓기·이동)의 고정된 집합. 시스템은 라이브러리에 있는 것만 할 수 있고, 능력을 더하려면 새 skill을 학습해야 한다. → [[01-canonical-papers/notes/4-vla/saycan|SayCan]]
- **Skip connection** · skip connection(스킵 연결) — Adding or concatenating an input to an output, creating a gradient highway. · 입력을 출력에 더하거나 이어붙여 그래디언트 고속도로를 만드는 연결. → [[01-canonical-papers/notes/1-foundations/resnet|ResNet]], [[01-canonical-papers/notes/2-computer-vision/u-net|U-Net]]
- **Sliding window (two pointers)** · 슬라이딩 윈도 — Scanning a sequence with left and right indices that only move forward, keeping a running quantity for the window between them. Total work is linear even though the inner loop looks nested. · 앞으로만 움직이는 왼쪽·오른쪽 인덱스로 시퀀스를 훑으며 둘 사이 창의 누적값을 유지하는 기법. 안쪽 루프가 중첩처럼 보여도 총 작업은 선형이다. → [[02-foundations/algorithms/complexity-recursion|11.1 §2]], [[02-foundations/algorithms/data-structures|11.2 §2]]
- **Slowly vs fast adapting afferents (SA, FA)** · 느린 적응 / 빠른 적응 수용기 — Skin receptors split by adaptation rate: SA I and II carry sustained pressure and stretch; FA I and II carry vibration and slip onset. Tactile processing splits the same way into slow and transient signals. · 적응 속도로 나뉜 피부 수용기: SA I·II는 지속 압력과 신장을, FA I·II는 진동과 미끄러짐 개시를 전한다. 촉각 신호처리도 같은 방식으로 느린 신호와 순간적 신호로 갈린다. → [[04-robotics/tactile-visuotactile|Tactile §3]]
- **SNR · dB** · SNR·dB — Signal-to-noise ratio is signal power divided by noise power; the decibel is the log scale it is quoted on, where +6 dB is about 2× in amplitude. One extra bit of an ADC buys roughly 6 dB. · SNR은 신호 전력 ÷ 잡음 전력, dB는 그것을 적는 로그 척도(+6 dB ≈ 진폭 2배). ADC 비트 하나가 약 6 dB다. → [[02-foundations/signal-processing|Signal Processing §2]]
- **SO(3)** · SO(3) — The group of all 3D rotation matrices, $R^\top R = I$ with $\det R = +1$. A rotation times a rotation is a rotation and every rotation can be undone, but order matters. · 모든 3차원 회전 행렬($R^\top R = I$, $\det R = +1$)의 군. 회전끼리 곱하면 회전이고 모든 회전은 되돌릴 수 있지만, 순서가 중요하다. → [[02-foundations/se3-geometry|SE(3) §1]]
- **Soft-finger contact** · soft-finger 접촉 — A contact model that, besides pushing within its friction cone, can also resist twisting moments. Two soft-finger contacts can achieve spatial force closure; two hard point contacts cannot. · 마찰 원뿔 안에서 미는 것에 더해 비틀림 모멘트에도 저항할 수 있는 접촉 모델. soft-finger 접촉 둘은 공간 force closure를 이룰 수 있지만 hard point 접촉 둘은 못 한다. → [[04-robotics/modern-robotics/ch12-grasping|MR ch.12]]
- **Softmax** · Softmax — $\text{softmax}(z)_i = e^{z_i}/\sum_j e^{z_j}$: turns any score vector into positive probabilities that sum to 1, a smooth argmax. It appears in attention, classification losses, and policies. · 임의의 점수 벡터를 양수이고 합이 1인 확률로 바꾸는 매끄러운 argmax. 어텐션, 분류 손실, 정책에 쓰인다. → [[02-foundations/engineering-math|Engineering Math §10]]
- **Soft prompt** · 소프트 프롬프트 — Continuous vectors placed in a language model's input in place of word tokens. BLIP-2 feeds its 32 query outputs to a frozen LLM as soft visual prompts. · 단어 토큰 대신 언어모델 입력에 넣는 연속 벡터. BLIP-2는 32개 쿼리 출력을 소프트 시각 프롬프트로 얼린 LLM에 넣는다. → [[01-canonical-papers/notes/3-vlm/blip-2|BLIP-2]]
- **Software citation** · 소프트웨어 인용 — Citing a tool by the reference entry its project provides, often a `@misc` with name, team and URL. PyBullet, Drake, Isaac Sim and Genesis have no peer-reviewed paper, so inventing a venue fabricates a citation. · 프로젝트가 제시하는 참고문헌 항목(흔히 이름·팀·URL을 담은 `@misc`)으로 도구를 인용하는 것. PyBullet·Drake·Isaac Sim·Genesis에는 심사받은 논문이 없으므로 venue를 지어내면 조작된 인용이 된다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §2]]
- **Software license** · 라이선스 — The terms under which code or data may be used and redistributed, such as Apache-2.0, BSD-3 or CC BY. Read the actual license files: Isaac Sim's needs components under a separate NVIDIA agreement. · 코드나 데이터를 쓰고 재배포할 수 있는 조건(Apache-2.0, BSD-3, CC BY 등). 실제 라이선스 파일을 읽어야 한다. Isaac Sim은 별도 NVIDIA 계약이 적용되는 구성요소를 요구한다. → [[06-research-practice/simulators-benchmarks-datasets|Simulators §2]], [[06-research-practice/simulators-benchmarks-datasets|Simulators §8]]
- **Soil bin** · 모래통(토조) — A contained box of sand or soil for repeatable digging tests. A single dig cycle in one bin supports almost no claim about real sites. · 반복 가능한 굴착 시험을 위해 모래·흙을 담은 통. 한 통에서의 굴착 사이클 한 번은 실제 현장에 대해 거의 아무 주장도 지지하지 못한다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §6]]
- **Soil cohesion and internal friction angle** · 점착력과 내부 마찰각 — Two soil strength parameters: cohesion is resistance from particles sticking together, and the internal friction angle sets resistance from particles sliding past each other. Earthmoving simulators sample them to vary soil. · 두 토질 강도 파라미터. 점착력은 입자끼리 붙어서 생기는 저항, 내부 마찰각은 입자끼리 미끄러질 때의 저항을 정한다. 토공 시뮬레이터는 이를 샘플링해 토질을 바꾼다. → [[01-canonical-papers/notes/8-construction/egli-rl|Egli RL]]
- **SOTA (state of the art)** · SOTA — The best reported result on a specific benchmark at a specific time. Impressive but fragile: benchmark-specific, often compute-confounded, and no guarantee of general superiority. · 특정 벤치마크·특정 시점에 보고된 최고 결과. 인상적이지만 취약하다: 벤치마크 특정적이고 연산량과 교락되기 쉬우며 일반적 우월성을 보장하지 않는다. → [[02-foundations/ml-practice|ML Practice §4]], [[01-canonical-papers/how-to-read|How to Read §2]]
- **Sourcing a setup file** · setup 파일 source — Running source on a setup.bash so the current shell gets PATH, AMENT_PREFIX_PATH, PYTHONPATH and other ROS variables. Environment variables are per process, so every new terminal must source again. · setup.bash를 source해서 현재 셸에 PATH, AMENT_PREFIX_PATH, PYTHONPATH 등 ROS 변수를 넣는 것. 환경 변수는 프로세스마다 따로라서 새 터미널마다 다시 해야 한다. → [[04-robotics/ros2/what-ros2-is|25.1 What ROS 2 Is §7]]
- **Spacetime patches** · 시공간 패치 — Tokens made by cutting a compressed video latent into small blocks across height, width and time, extending ViT's image patches to video. Sora uses them to handle variable duration, resolution and aspect ratio. · 압축된 비디오 잠재 표현을 높이·너비·시간 방향으로 작은 블록으로 잘라 만든 토큰으로, ViT의 이미지 패치를 비디오로 확장한 것. Sora는 이것으로 가변 길이·해상도·종횡비를 다룬다. → [[01-canonical-papers/notes/5-world-models/sora|Sora]]
- **Sparse reward** · 희소 보상 — A reward given only on success, such as +1 when the bucket is full. Honest, but a random policy may never see it; a dense (shaped) reward signals every step but optimizes a proxy. · 성공했을 때만 주는 보상(버킷이 차면 +1). 목표에 정직하지만 무작위 정책은 영영 못 볼 수 있다. 촘촘한(shaped) 보상은 매 스텝 신호를 주는 대신 대리물을 최적화하게 한다. → [[02-foundations/rl-basics|RL Basics §7]]
- **Spatial memory** · 공간 기억 — Keeping several recorded experiences of a place instead of one current estimate of it. A map converges on a single model of where things are, so a changed appearance is an error to be corrected away; an experience-based representation stores the variations as distinct modes and matches the present against whichever one fits. The distinction only pays off in deployments long enough for the same place to look several ways. · 한 장소에 대한 현재 추정 하나가 아니라 기록된 여러 경험을 함께 들고 있는 것. 지도는 무엇이 어디 있는지에 대한 모델 하나로 수렴하므로 달라진 겉모습은 고쳐 없앨 오류가 된다. 경험 기반 표현은 그 변이들을 서로 구별되는 모드로 저장하고, 현재를 그중 맞는 것에 정합시킨다. 같은 장소가 여러 모습을 갖기에 충분히 긴 운용에서만 값을 한다. → [[04-robotics/semantic-language-navigation|19. Semantic Navigation §7]]
- **Spectral bias** · 스펙트럼 편향 — Neural networks fit low frequencies first, so a network fed raw coordinates learns an over-smooth function. It is why NeRF lifts its inputs to Fourier features. · 신경망이 낮은 주파수부터 맞추는 경향. 원시 좌표를 넣은 신경망은 지나치게 매끄러운 함수를 배우며, NeRF가 입력을 푸리에 특징으로 올리는 이유다. → [[02-foundations/signal-processing|Signal Processing §3]]
- **Specular surface** · 반사면 — A shiny surface, such as metal, glazing, or water, that reflects light like a mirror. Cameras and depth sensors often misread it or return no data at all. · 금속, 유리, 물처럼 빛을 거울같이 반사하는 표면. 카메라와 깊이 센서가 흔히 잘못 읽거나 아예 데이터를 내지 못한다. → [[01-canonical-papers/notes/9-navigation/miki-perceptive-locomotion|Miki 2022]]
- **Speed and separation monitoring (SSM)** · 속도·이격 감시(SSM) — A collaboration method that keeps a protective separation distance and slows or stops as it closes. The distance sums human approach, robot reaction and stopping travel, intrusion distance, and position uncertainties, so sensing latency dominates. · 보호 이격 거리를 유지하고 거리가 좁혀지면 감속·정지하는 협동 방법. 그 거리는 사람의 접근, 로봇의 반응·정지 이동, 침입 거리, 위치 불확실성의 합이므로 센싱 지연이 크게 좌우한다. → [[04-robotics/hri-safety|HRI & Safety §6]]
- **Spherical harmonics (SH)** · 구면 조화(spherical harmonics) — Basis functions over directions on a sphere. Storing a few coefficients per 3D Gaussian lets its color change with the viewing direction. · 구 위의 방향에 대한 기저 함수. 3D 가우시안마다 계수 몇 개를 저장하면 보는 방향에 따라 색이 달라지게 할 수 있다. → [[01-canonical-papers/notes/2-computer-vision/3d-gaussian-splatting|3DGS]]
- **Spin** · spin — Handing control of a node to an executor, which blocks and runs its callbacks until shutdown. A node that is created but never spun appears in the graph yet never runs a callback. · 노드의 제어권을 executor에 넘기는 것으로, 종료까지 블로킹하며 콜백을 실행한다. 만들기만 하고 spin하지 않은 노드는 그래프에는 보이지만 콜백을 하나도 돌리지 않는다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §5]]
- **SPL (success weighted by path length)** · SPL — $\frac{1}{N}\sum_i S_i\,\ell_i/\max(p_i,\ell_i)$: success discounted by how much longer the walked path $p_i$ was than the shortest $\ell_i$. SPL alone cannot separate wandering from failing, so report success rate too. · $\frac{1}{N}\sum_i S_i\,\ell_i/\max(p_i,\ell_i)$: 걸은 경로 $p_i$가 최단 경로 $\ell_i$보다 길수록 성공을 깎는다. SPL만으로는 헤매는 것과 실패하는 것을 구분할 수 없어 성공률도 함께 보고한다. → [[04-robotics/semantic-language-navigation|Semantic navigation §2]]
- **SQP (sequential quadratic programming)** · SQP — Solving a nonlinear constrained problem by building and solving a QP model at each iterate. The workhorse of nonlinear MPC. · 반복점마다 QP 모델을 만들어 풀면서 비선형 제약 문제를 푸는 방법. 비선형 MPC의 주력이다. → [[02-foundations/optimization|Optimization §4]]
- **Squashed Gaussian policy** · squashed Gaussian — A continuous-action policy that samples from a Gaussian and passes the sample through tanh to keep actions bounded; with the reparameterization trick it gives low-variance gradients. · Gaussian에서 샘플한 값을 tanh에 통과시켜 행동 범위를 제한하는 연속 행동 정책. reparameterization trick과 함께 분산이 낮은 그래디언트를 준다. → [[01-canonical-papers/notes/1-foundations/sac|SAC]]
- **SRDF (Semantic Robot Description Format)** · SRDF — The semantic layer MoveIt adds on top of the URDF: planning groups, named poses, end effectors, virtual joints and the disabled collision pairs. Usually generated by the MoveIt Setup Assistant. · MoveIt이 URDF 위에 더하는 의미 계층. planning group, 이름 붙은 자세, end effector, virtual joint, 충돌 검사를 끈 링크 쌍을 담는다. 보통 MoveIt Setup Assistant가 생성한다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §3]]
- **Stability margin ($s_m$)** · 안정 여유 — The shortest distance from the Nyquist curve to the critical point $-1$ — the one margin that bounds the distance itself rather than a direction. Guarantees the other two ($g_m \ge 1/(1-s_m)$, $\varphi_m \ge 2\arcsin(s_m/2)$) but is not implied by them; healthy range 0.5–0.8. · 나이퀴스트 곡선에서 임계점 $-1$까지의 최단 거리 — 방향이 아니라 거리 자체를 제약하는 유일한 여유다. 나머지 둘을 보장하지만($g_m \ge 1/(1-s_m)$, $\varphi_m \ge 2\arcsin(s_m/2)$) 그 역은 성립하지 않는다. 건강한 범위는 0.5~0.8. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Stability (of a linear system)** · 안정성 — Every mode decays: all eigenvalues (poles) have negative real part in continuous time, or magnitude below 1 in discrete time. Sampling maps the left half-plane into the unit disc, so the conditions agree. · 모든 모드가 감쇠하는 것: 연속 시간에서는 모든 고유값(극점)의 실수부가 음수, 이산 시간에서는 크기가 1보다 작다. 샘플링은 좌반평면을 단위원 안으로 보내므로 두 조건은 일치한다. → [[02-foundations/engineering-math|Engineering Math §8]], [[02-foundations/linear-algebra|Linear Algebra §5]]
- **Stable Diffusion** · Stable Diffusion — The latent diffusion recipe trained on LAION-5B subsets and released with open weights. Its open release created the fine-tuning, LoRA and ControlNet ecosystem around image generation. · LAION-5B 부분집합으로 학습해 가중치까지 공개한 잠재 디퓨전 레시피. 공개 릴리스가 이미지 생성을 둘러싼 파인튜닝·LoRA·ControlNet 생태계를 만들었다. → [[01-canonical-papers/notes/6-diffusion/latent-diffusion|Latent Diffusion]]
- **Stable sort** · 안정 정렬 — A sort that keeps items with equal keys in their input order. Merge sort and Python's sort are stable; quicksort and C++ std::sort are not, so use std::stable_sort when ties matter. · 키가 같은 항목들의 입력 순서를 유지하는 정렬. 병합 정렬과 Python 정렬은 안정적이고 퀵정렬과 C++ std::sort는 아니므로, 동점 순서가 중요하면 std::stable_sort를 쓴다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §2]]
- **Stackelberg game** · Stackelberg(선도자–추종자) 게임 — A leader–follower game in which the leader chooses its action knowing the follower will best-respond to it. In HRI the robot leads, optimizing through a learned model of how the human will react. · 선도자가 추종자의 최적 반응을 알고 행동을 고르는 게임. HRI에서는 로봇이 선도자가 되어 사람이 어떻게 반응할지에 대한 학습된 모델을 거쳐 최적화한다. → [[04-robotics/hri-safety|HRI & Safety §3.5]]
- **Stack (LIFO)** · 스택 — An access policy in which the most recently added element leaves first, last in first out. It drives DFS, parsing and undo, and an explicit stack replaces the call stack when recursion is too deep. · 가장 최근에 넣은 원소가 먼저 나가는(후입선출) 접근 방식. DFS, 파싱, 되돌리기를 움직이며, 재귀가 너무 깊으면 명시적 스택이 호출 스택을 대신한다. → [[02-foundations/algorithms/data-structures|11.2 §2]]
- **Staircase method (adaptive method)** · 계단법(적응적 방법) — A threshold procedure whose up-down rule steps the stimulus toward threshold after each response, so it oscillates there; averaging reversal points estimates the threshold at a fraction of constant-stimuli cost. · 응답마다 상하 규칙으로 자극을 임계값 쪽으로 옮겨 그 주위에서 진동하게 하는 임계값 절차. 반전점들의 평균으로 항상자극법 비용의 몇 분의 일에 임계값을 추정한다. → [[06-research-practice/psychophysics-human-measurement|Psychophysics §2]]
- **Staircase procedure (adaptive up–down method)** · staircase — A psychophysical method that adjusts the next stimulus up or down according to the previous response, converging quickly on an approximate threshold. Its convergence depends on the step rule and on lapses. · 직전 반응에 따라 다음 자극을 올리거나 내려 대략의 임계값에 빠르게 수렴하는 심리물리 방법. 수렴은 스텝 규칙과 실수에 달려 있다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §3]]
- **Stale state** · 낡은 상태(stale state) — An estimate that has stopped updating but is still consumed as current, such as a pose frozen for 2.1 s that a planner trusts. Recording acquisition, arrival and consumption timestamps exposes it. · 갱신이 멈췄는데도 최신인 것처럼 소비되는 추정값(예: 계획기가 믿은, 2.1 s 동안 멈춘 pose). 취득·도착·소비 시각을 기록하면 드러난다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §1]], [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §3]]
- **Standard deviation (SD)** · 표준편차 — How spread out individual values are around their mean, in the same units, such as a seed-to-seed spread of 4 percentage points. It describes single-run variability, not the precision of an average. · 개별 값이 평균 주위에 퍼진 정도를 같은 단위로 나타낸 값(예: 시드 간 4%p). 개별 실행의 변동을 나타낼 뿐 평균의 정밀도가 아니다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §3]]
- **Standard error (SE, SEM)** · 표준오차 — The uncertainty of an average, the standard deviation divided by $\sqrt{n}$: an SD of 4 points over 3 seeds gives about 2.3. It is not a confidence interval. · 평균의 불확실성으로, 표준편차를 $\sqrt{n}$으로 나눈 값이다(표준편차 4%p, 시드 3개면 약 2.3%p). 신뢰구간이 아니다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §3]]
- **State estimation** · 상태 추정 — Inferring variables a robot needs but cannot observe perfectly, such as pose and velocity, by combining a motion model, control inputs, noisy sensor observations and their uncertainty. · 운동 모델, 제어 입력, 잡음 섞인 센서 관측과 불확실성을 결합해 로봇에 필요하지만 완벽히 관측할 수 없는 변수(pose, 속도 등)를 추론하는 일. → [[04-robotics/state-estimation-slam|State estimation §1]]
- **State feedback** · 상태 피드백 — A control law that computes the input from the full state, $u=-Kx$, making the closed loop $\dot x=(A-BK)x$. If the system is controllable, the gain can place those eigenvalues anywhere. · 전체 상태에서 입력을 계산하는 제어 법칙 $u=-Kx$로, 폐루프가 $\dot x=(A-BK)x$가 된다. 시스템이 가제어이면 이득으로 그 고유값을 어디든 놓을 수 있다. → [[04-robotics/control-theory-ce397|Control Theory §7]]
- **State freshness** · 상태 신선도 — How recently and at what rate and latency the twin's state was updated. Stale state can make a robot act on a wall that was never built. · 트윈 상태가 얼마나 최근에, 어떤 주기와 지연으로 갱신됐는가. 낡은 상태는 로봇이 지어지지 않은 벽을 두고 행동하게 만들 수 있다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §3]]
- **State lattice (motion primitives)** · 상태 격자(state lattice) — A search graph whose nodes are discretized states such as $(x,y,\theta)$ and whose edges are precomputed feasible motion primitives that start and end exactly on lattice states. Optimality holds only relative to that set. · 노드가 $(x,y,\theta)$ 같은 이산화된 상태이고 간선이 격자 상태에서 정확히 시작하고 끝나는 미리 계산한 실행 가능 프리미티브인 탐색 그래프. 최적성은 그 프리미티브 집합에 대해서만 성립한다. → [[04-robotics/planning-decision-making|Planning §5.5]]
- **State-space model** · 상태공간 모델 — $\dot x = Ax + Bu$, $y = Cx$: a system written as matrices acting on a state vector, solved by the matrix exponential $e^{At}$. Stability, controllability, and observability become questions about these matrices. · 상태 벡터에 작용하는 행렬로 쓴 시스템으로, 행렬 지수 $e^{At}$로 푼다. 안정성, 가제어성, 가관측성이 모두 이 행렬들에 대한 질문이 된다. → [[02-foundations/linear-algebra|Linear Algebra §5]]
- **Static and dynamic transforms (/tf_static, /tf)** · 정적 변환과 동적 변환 — A static transform never changes (e.g. base to camera mount) and is sent once on transient-local /tf_static; a dynamic one changes (odom to base_link, moving joints) and is restamped continuously on /tf. · 정적 변환은 변하지 않아(예: 베이스와 카메라 마운트) transient local인 /tf_static으로 한 번 보내고, 동적 변환은 변해서(odom→base_link, 움직이는 조인트) /tf로 계속 새 스탬프와 함께 보낸다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §8]]
- **Statics duality** · 정역학 쌍대성 — $\tau = J^\top(\theta)\mathcal{F}$: the same Jacobian that maps joint velocities to end-effector twist maps an end-effector wrench back to joint torques. It follows from matching power at both ends. · $\tau = J^\top(\theta)\mathcal{F}$: 관절 속도를 말단 twist로 보내는 같은 야코비안이 말단 렌치를 관절 토크로 되돌린다. 양 끝의 일률을 맞추면 나온다. → [[04-robotics/modern-robotics/ch05-velocity-kinematics|MR ch.5 §3]]
- **Static vs dynamic tactile sensing** · 정적 / 동적 촉각 센싱 — Whether a sensor holds a steady DC reading or reports only change. Piezoelectric sensors read a held grip back to zero and suit slip and texture; barometric and capacitive sensors report static force indefinitely. · 센서가 일정한 DC 값을 유지하는가, 변화만 보고하는가. 압전 센서는 쥔 힘을 스스로 0으로 되돌려 읽어 미끄러짐·질감에 맞고, 기압식·정전용량식은 정적 힘을 무한정 보고한다. → [[04-robotics/tactile-visuotactile|Tactile §2.5]]
- **Stationarity · WSS** · 정상성·WSS — Wide-sense stationary: mean and autocorrelation depend only on the time gap $\tau$, not on *when* you look. The assumption under spectral analysis and most noise models. · 광의의 정상성: 평균과 자기상관이 언제 보느냐가 아니라 시간 간격에만 의존한다. 스펙트럼 분석과 대부분의 잡음 모델의 전제. → [[02-foundations/probability|Probability §5]]
- **Stationary distribution** · 정상 분포 — A distribution $\pi$ over a Markov chain's states that one more step leaves unchanged, $\pi P = \pi$. A finite irreducible, aperiodic chain has exactly one and converges to it from any start. · 한 스텝을 더 가도 변하지 않는 마르코프 체인 상태 위의 분포, $\pi P = \pi$. 유한하고 기약이며 비주기인 체인은 이것을 정확히 하나 가지며 어느 출발점에서든 그리로 수렴한다. → [[02-foundations/probability|Probability §7]]
- **Statistical significance** · 통계적 유의성 — A test's judgment that a difference is unlikely under chance variation alone. It differs from practical importance: a significant gain can be too small to matter; non-significance does not prove no effect. · 차이가 우연한 변동만으로는 나오기 어렵다는 검정의 판단. 실질적 중요성과 다르다. 유의한 개선도 너무 작아 무의미할 수 있고, 유의하지 않다고 효과가 없다는 증거는 아니다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §4]]
- **STCR (Single-Task Construction Robot)** · STCR(단일 작업 건설로봇) — Japanese contractor-built robots of the 1980s–90s that each automated one task, such as spraying, tiling, or rebar. Their lesson: the environment, not the mechanism, is the problem. · 1980~90년대 일본 시공사가 만든, 뿜칠·타일·철근 같은 한 작업씩 자동화한 로봇. 교훈은 문제가 기구가 아니라 환경이라는 것이다. → [[05-construction-robotics/lineage|Lineage §1]]
- **Steady-state error (offset)** · 정상 상태 오프셋 — The error left after transients die out. Proportional control alone leaves an offset against a constant load, and integral action removes it. · 과도 응답이 사라진 뒤 남는 오차. 비례 제어만으로는 일정한 부하에 대해 오프셋이 남고, 적분 동작이 이를 없앤다. → [[04-robotics/control-theory-ce397|Control Theory §7]]
- **Stereo disparity** · 시차 — The horizontal pixel shift of a point between two cameras; depth is $Z=fb/d$ for baseline $b$. Disparity shrinks for far points, so depth error grows as $Z^2$. · 두 카메라 사이에서 한 점이 가로로 밀린 픽셀 수로, 기선 $b$에 대해 깊이는 $Z=fb/d$다. 먼 점일수록 시차가 작아져 깊이 오차가 $Z^2$로 자란다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2]]
- **Stiffness** · 강성 — Force per unit displacement, $F = K\Delta x$ in N/m. High closed-loop stiffness against a stiff surface turns a small position error into a large force. · 단위 변위당 힘, $F = K\Delta x$ (N/m). 단단한 표면에 높은 폐루프 강성으로 맞서면 작은 위치 오차가 큰 힘이 된다. → [[04-robotics/force-compliance-control|Force & Compliance Control §1]]
- **Stochastic gradient descent (SGD)** · 확률적 그래디언트 — Gradient descent using unbiased but noisy gradient estimates from minibatches. The noise can help escape saddles but demands step-size decay or adaptive step sizes. · 미니배치에서 얻은 불편이지만 시끄러운 그래디언트 추정으로 하는 경사 하강. 잡음이 안장점 탈출을 도울 수 있지만 스텝 크기 감소나 적응형 스텝이 필요하다. → [[02-foundations/optimization|Optimization §3]]
- **Stochastic policy** · 확률적 정책 — A policy that outputs a probability distribution over actions rather than one action. In continuous control it can learn its own standard deviation, which is how PPO explores. · 행동 하나가 아니라 행동에 대한 확률 분포를 내는 정책. 연속 제어에서는 자기 표준편차를 학습할 수 있고, PPO가 그렇게 탐색한다. → [[02-foundations/rl-basics|RL Basics §8]]
- **Stop-gradient** · stop-gradient — Deliberately blocking backpropagation. Usually drawn as a dashed arrow in figures, but dashed arrows can also mean auxiliary or inference-only paths — check the legend. · 역전파를 의도적으로 차단하는 연산. 그림에서는 보통 점선 화살표로 그리지만, 점선은 보조 경로나 추론 전용 경로를 뜻하기도 하므로 범례를 확인하라. → [[02-foundations/calculus-backprop|미적분·역전파 §5]]
- **Straight-through estimator** · straight-through 추정기 — A gradient trick for a discrete step: use the hard sample in the forward pass but send back the gradient of a smooth relaxation, as if the step were differentiable. · 이산 단계를 위한 그래디언트 기법: 순방향에서는 딱딱한 샘플을 쓰고 역방향으로는 매끄러운 완화의 그래디언트를 흘려, 그 단계가 미분 가능한 것처럼 다룬다. → [[02-foundations/calculus-backprop|Calculus & Backprop §6]]
- **Stress testing** · 스트레스 테스트 — Running a fast solution and a slow, obviously correct reference on many small random inputs until they disagree, then shrinking the failing input. It finds the bugs that hand tests did not anticipate. · 빠른 해법과 느리지만 뻔히 옳은 기준 해법을 작은 무작위 입력에 대해 둘이 어긋날 때까지 돌리고, 실패한 입력을 줄이는 테스트. 손 테스트가 예상하지 못한 버그를 찾는다. → [[02-foundations/algorithms/interview-code|11.7 §2]]
- **Strict weak ordering** · 엄격한 약순서 — The rules a sort comparator must obey: never less than itself, never both ways, transitive, and "neither is less" transitive too. In C++, violating them (a <= b, NaN keys) is undefined behaviour. · 정렬 비교자가 지켜야 하는 규칙: 자기 자신보다 작지 않고, 양방향으로 동시에 참이 아니며, 추이적이고, "둘 다 작지 않음"도 추이적이다. C++에서 a <= b나 NaN 키처럼 이를 어기면 정의되지 않은 동작이다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §5]]
- **Strongly connected component (SCC)** · 강연결 요소 — A maximal set of vertices in a directed graph in which every vertex can reach every other. Contracting each one leaves a DAG; Kosaraju's and Tarjan's algorithms find them in $O(V + E)$. · 방향 그래프에서 모든 정점이 서로에게 도달할 수 있는 극대 정점 집합. 각각을 한 노드로 축약하면 DAG가 남으며, Kosaraju와 Tarjan 알고리즘이 $O(V + E)$에 찾는다. → [[02-foundations/algorithms/graph-algorithms|11.6 §3]]
- **Study depth** · study-depth(권장 깊이) — A page's recommended target for how well to command a topic: Literacy explains it, Working uses and diagnoses it, Mastery critiques and defends it. Separate from how much of one paper to read. · 주제를 얼마나 잘 다룰지에 대한 페이지의 권장 목표: Literacy는 설명하고, Working은 고르고 쓰고 진단하며, Mastery는 비판하고 방어한다. 논문 한 편을 얼마나 읽을지와는 별개다. → [[00-study-depth-guide|Study Depth Guide]]
- **Subgoal** · 부분 목표 — An intermediate target on the way to the final goal. ViNT uses a diffusion model to propose candidate subgoals when exploring a new environment. · 최종 목표로 가는 도중의 중간 목표. ViNT는 새 환경을 탐색할 때 확산 모델로 후보 부분 목표를 제안한다. → [[01-canonical-papers/notes/9-navigation/vint-nomad|ViNT and NoMaD]]
- **Subjective and objective measures** · 주관 지표와 객관 지표 — Objective measures are recorded behaviour, such as task time or idle time; subjective measures are participants' questionnaire ratings, such as perceived safety or trust. HRC studies need both. · 객관 지표는 과제 시간이나 유휴 시간 같은 기록된 행동, 주관 지표는 체감 안전이나 신뢰 같은 참가자의 설문 평가다. HRC 연구에는 둘 다 필요하다. → [[01-canonical-papers/notes/8-construction/lasota-shah|Lasota & Shah 2015]]
- **Submodular function** · 부분모듈 함수 — A set function with diminishing returns: an element helps a small set at least as much as any superset. Greedily picking $k$ elements of a monotone one gets at least $1 - 1/e$ of optimal. · 한계 효용이 체감하는 집합 함수: 원소 하나를 더하는 이득이 작은 집합에서가 그것을 포함한 큰 집합에서보다 작지 않다. 단조 함수라면 원소 $k$개를 그리디로 고를 때 최적의 $1 - 1/e$ 이상을 얻는다. → [[02-foundations/algorithms/greedy-mst|11.4 §7]]
- **Subsumption architecture** · Subsumption — Brooks' 1986 architecture of parallel reactive behaviors, each wiring sensors to actuators, where a higher layer suppresses a lower one's input or inhibits its output. Fast reaction, but no world model and no long-horizon planning. · 각자 센서를 액추에이터에 잇는 반응형 행동들을 병렬로 돌리고 상위 층이 하위 층의 입력을 억제하거나 출력을 차단하는 Brooks(1986)의 아키텍처. 반응은 빠르지만 세계 모델도 긴 지평의 계획도 없다. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Success rate** · 성공률 — In robotics results, the fraction of trials that achieve the goal. It depends on time limits, reset conditions, the success definition and the trial distribution, so report counts and uncertainty, not only a percentage. · 로보틱스 결과에서 목표를 달성한 시행의 비율. 시간 제한·리셋 조건·성공 정의·시행 분포에 달려 있으므로, 백분율만이 아니라 횟수와 불확실성을 함께 보고한다. → [[02-foundations/ml-practice|ML Practice §3]], [[02-foundations/ml-practice|ML Practice §5]]
- **Superposition principle** · 중첩 원리 — Additivity and homogeneity combined: $f\big(\sum_k a_k x_k\big)=\sum_k a_k\,f(x_k)$. The response to a sum of inputs is the sum of the responses, so knowing the response to basis inputs gives the response to every input. · 가법성과 동차성을 합친 것: $f\big(\sum_k a_k x_k\big)=\sum_k a_k\,f(x_k)$. 입력 합에 대한 응답은 응답의 합이므로, 기저 입력의 응답만 알면 모든 입력의 응답을 안다. → [[02-foundations/engineering-math|0.5 §4.5]], [[02-foundations/signal-processing|Signal Processing §1]]
- **Supervised autonomy** · 감독 자율 — The machine performs the task, but a human watches, often remotely, and handles exceptions, reset, and recovery. Most commercial heavy-machine systems work this way. · 기계가 과제를 수행하지만 사람이 흔히 원격으로 지켜보며 예외·리셋·복구를 맡는 방식. 상용 중장비 시스템 대부분이 이렇다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §5]]
- **Supervised learning** · 지도학습 — Learning from input–target pairs by minimizing a loss between prediction and target. Behavior cloning is supervised learning on demonstrations; RL differs in receiving a reward instead of a target. · 입력–정답 쌍에서 예측과 정답 사이 손실을 최소화해 배우는 것. 행동 복제는 시연에 대한 지도학습이고, RL은 정답 대신 보상을 받는다는 점이 다르다. → [[02-foundations/neural-network-basics|0.7 §3]], [[02-foundations/rl-basics|RL Basics §7]]
- **Support (of a distribution or dataset)** · support(지지집합) — The region it actually covers. "Outside the support of the demonstrations" means states the policy has literally never seen — the reason behavior cloning cannot recover from its own mistakes. · 그 분포·데이터가 실제로 덮고 있는 영역. "시연의 support 밖"은 정책이 한 번도 본 적 없는 상태를 뜻한다. → [[02-foundations/rl-basics|RL Basics §6]]
- **Support polygon** · 지지 다각형 — The convex hull of a legged robot's ground contact points. The classical static balance test asks whether the center of mass projection stays inside it. · 보행 로봇의 지면 접촉점들의 볼록 껍질. 고전적 정적 균형 검사는 무게중심의 투영이 그 안에 머무는가를 묻는다. → [[04-robotics/legged-locomotion|Legged Locomotion §1.5]]
- **Surprise (self-information, Shannon information content)** · 놀라움 — $-\log p(x)$: the information an outcome carries. Rare outcomes carry more, a perfectly predicted reading carries none, and entropy is its average. · 결과가 담은 정보량. 드문 결과일수록 크고 완벽히 예측한 측정값은 0이며, 엔트로피는 그 평균이다. → [[02-foundations/information-theory|Information Theory §1]]
- **Surveyed workspace** · 측량된 작업 구역 — A work area whose geometry was measured in advance with surveying equipment, from which tasks are specified. Research autonomy usually runs inside such a bounded, prepared area. · 측량 장비로 기하를 미리 측정해 과제를 지정하는 작업 구역. 연구 수준 자율은 대개 이렇게 경계가 정해지고 준비된 구역 안에서 돈다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §5]]
- **SVD / singular value** · SVD/특이값 — The factorization $A = U\Sigma V^\top$ available for *every* matrix; the singular values give rank, the 2-norm, the best low-rank approximation (Eckart–Young), and the axes of a manipulability ellipsoid. · 모든 행렬에 존재하는 분해; 랭크·2-노름·최적 저랭크 근사·가조작성 타원체의 축을 준다. → [[02-foundations/linear-algebra|Linear Algebra §4]]
- **SVM (support vector machine)** · SVM(서포트 벡터 머신) — A classifier that places the boundary between classes so the margin to the nearest examples is as large as possible. It is common on small biosignal datasets such as EEG. · 가장 가까운 예제까지의 여백이 최대가 되도록 클래스 사이 경계를 놓는 분류기. EEG 같은 작은 생체신호 데이터셋에서 흔히 쓴다. → [[01-canonical-papers/notes/8-construction/liu-jebelli-bci|BCI teleoperation 2021]]
- **SwiGLU** · SwiGLU — A gated feed-forward block that adds a third weight matrix; LLaMA-style FFNs shrink the hidden width to about $\tfrac{8}{3}d$ so the parameter count stays roughly unchanged. · 가중치 행렬을 하나 더 쓰는 게이팅 feed-forward 블록. 파라미터 수를 거의 유지하려고 LLaMA 계열 FFN은 은닉 폭을 약 $\tfrac{8}{3}d$로 줄인다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Symlink install (--symlink-install)** · symlink install — A colcon flag that installs symlinks instead of copies where possible, so edited Python files and installed data files take effect without rebuilding. Compiled C++ still needs a rebuild. · 가능한 곳에서 복사 대신 심볼릭 링크로 설치하는 colcon 플래그. 편집한 Python 파일과 설치된 데이터 파일이 재빌드 없이 반영된다. 컴파일된 C++은 여전히 재빌드해야 한다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §7]]
- **Symlog** · symlog — A squashing transform, sign(x) times log(1 plus the absolute value of x), applied to prediction targets. DreamerV3 uses it so losses stay well scaled across domains whose rewards differ by orders of magnitude. · 예측 타깃에 적용하는 압축 변환으로, x의 부호에 log(1 + x의 절댓값)을 곱한다. DreamerV3는 보상 크기가 자릿수만큼 다른 도메인에서도 손실 스케일이 맞도록 이것을 쓴다. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **Synchronized replay (log replay)** · 동기화 재생 — Replaying logged sensors, estimates and commands on a shared clock, preserving delays and dropped updates, to reconstruct what each subsystem knew. Log order is not event order, so aligning row indices misleads. · 기록된 센서, 추정값, 명령을 공통 시계에 맞춰 지연과 누락까지 보존한 채 다시 돌려 각 하위 시스템이 알던 정보를 재구성하는 것. 로그 순서는 사건 순서가 아니므로 행 번호로 맞추면 틀린다. → [[06-research-practice/failure-analysis-system-evaluation|Failure Analysis §3]]
- **Synthetic data** · 합성 데이터 — Training data generated by simulation or rendering rather than recorded by real sensors. Labels come free, but results rest on how faithful the simulated physics and images are. · 실제 센서 기록이 아니라 시뮬레이션이나 렌더링으로 만든 학습 데이터. 라벨은 공짜지만 결과는 시뮬레이션한 물리와 영상의 충실도에 달린다. → [[01-canonical-papers/notes/7-robotics/dex-net-2|Dex-Net 2.0]]
- **Systematic review** · 체계적 리뷰 — A literature survey that searches databases with stated keywords, screens results by explicit criteria, and classifies the corpus. Its conclusions are bounded by the search date and scope. · 명시한 키워드로 데이터베이스를 검색하고 명시적 기준으로 선별해 코퍼스를 분류하는 문헌 조사. 결론은 검색 시점과 범위에 묶인다. → [[01-canonical-papers/notes/8-construction/liang-hrc-survey|Liang HRC survey 2021]]
- **System identification** · 시스템 식별 — Fitting model or simulator parameters such as masses, inertias, and friction to measured robot trajectories instead of trusting CAD values. It can overfit one machine and one operating condition. · CAD 값을 믿는 대신 측정한 로봇 궤적에 질량·관성·마찰 같은 모델·시뮬레이터 파라미터를 맞추는 것. 기계 하나와 운전 조건 하나에 과적합될 수 있다. → [[02-foundations/manipulator-kinematics-dynamics|Manipulator Dynamics §7]]

## T–Z

- **Tap (filter)** · 탭(tap) — One coefficient $b_k$ of an FIR filter, i.e. one past sample it must keep and multiply. More taps = sharper filter, but more memory, arithmetic, and delay. · FIR 필터의 계수 하나 = 들고 있어야 하는 과거 샘플 하나. 탭이 많을수록 날카롭지만 메모리·연산·지연이 커진다. → [[02-foundations/signal-processing|Signal Processing §4]]
- **Target network** · 타깃 네트워크 — A periodically frozen copy of the value network used only to produce bootstrap targets. Makes the target a temporary constant instead of a moving self-reference, which weakens the bootstrapping leg of the deadly triad — the reason DQN has one at all. · 부트스트랩 목표를 만드는 데만 쓰는, 주기적으로 얼린 가치 네트워크 사본. 목표를 움직이는 자기 참조가 아니라 한동안 상수로 만들어 deadly triad의 부트스트랩 다리를 약화시킨다. DQN이 애초에 이것을 갖는 이유다. → [[02-foundations/rl-basics|RL Basics §3.5]]
- **Task and motion planning (TAMP)** · TAMP — Planning that couples discrete symbolic actions, such as pick(block), with continuous geometric feasibility, because a logically valid step may have no collision-free grasp or motion. · pick(block) 같은 이산 기호 행동을 연속적인 기하학적 실행 가능성과 결합하는 계획. 논리적으로 타당한 단계에도 충돌 없는 파지나 운동이 없을 수 있기 때문이다. → [[04-robotics/planning-decision-making|Planning §7]]
- **Task frame** · 과제 프레임 — The coordinate frame chosen at the contact so each axis is clearly free or constrained, letting hybrid control assign position or force control direction by direction. · 각 축이 자유 방향인지 구속 방향인지 분명해지도록 접촉에 잡은 좌표 프레임. 하이브리드 제어가 방향마다 위치 또는 힘 제어를 배정할 수 있게 한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §3]]
- **Task generation (BIM-to-robot)** · 과제 생성 — Turning a construction activity into ordered robot skills with coordinate frames, preconditions, tolerances, verification criteria, and recovery behavior. · 시공 활동을 좌표계·선행조건·공차·검증 기준·복구 거동을 갖춘 순서 있는 로봇 skill로 바꾸는 것. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §3]]
- **Taxel** · 택셀 — A tactile pixel: one element of a pressure-sensing array. Taxel arrays are cheap and robust but coarse and prone to drift, which places them between a single wrist force sensor and an optical tactile sensor in spatial detail. · 촉각 픽셀 — 압력 감지 배열의 한 소자. 택셀 배열은 싸고 튼튼하지만 거칠고 드리프트하기 쉬워, 공간 해상도에서 손목 힘 센서 하나와 광학 촉각 센서 사이에 놓인다. → [[04-robotics/tactile-visuotactile|14. §2]]
- **Taylor expansion** · 테일러 전개 — Approximating a smooth function near a point from its derivatives, $f(x+\delta) \approx f(x) + f'(x)\delta + \tfrac12 f''(x)\delta^2$: locally a line or a parabola. Gradient descent trusts the line; Newton's method trusts the parabola. · 한 점 근처의 매끄러운 함수를 도함수로 근사하는 것으로, 국소적으로 직선이나 포물선이다. 경사 하강은 직선을, 뉴턴법은 포물선을 믿는다. → [[02-foundations/engineering-math|Engineering Math §2]]
- **TD error** · TD 오차 — $r + \gamma V(s') - V(s)$: the most widely used learning signal in RL. · RL에서 가장 널리 쓰이는 학습 신호. → [[02-foundations/rl-basics|RL 기초 §3]]
- **TD(λ) (λ-return)** · TD-λ — A value target that blends returns bootstrapped after one, two, three and more steps with exponentially decaying weights. Dreamer uses it so values reach beyond the short imagination horizon. · 1스텝·2스텝·3스텝 이후에 부트스트랩한 리턴들을 지수적으로 줄어드는 가중치로 섞은 가치 타깃. Dreamer는 가치가 짧은 상상 지평 너머까지 닿도록 이것을 쓴다. → [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **Team fluency** · 유창성 — How smoothly a human and a robot work together, measured by concurrent motion, human and robot idle time, task completion time, and separation distance. · 사람과 로봇이 얼마나 매끄럽게 함께 일하는가. 동시 동작, 사람과 로봇의 유휴 시간, 과제 완료 시간, 이격 거리로 잰다. → [[01-canonical-papers/notes/8-construction/lasota-shah|Lasota & Shah 2015]]
- **Teleoperation** · 원격조작 — A human driving a robot through an input device, typically to produce demonstration data. · 사람이 장치로 로봇을 조종해 시연 데이터를 만드는 방법. → [[01-canonical-papers/notes/4-vla/act|ALOHA]]
- **Temperature** · 온도 — The number that logits or similarities are divided by before a softmax: low temperature sharpens the distribution toward argmax, high temperature flattens it. · softmax 전에 로짓이나 유사도를 나누는 값. 낮으면 분포가 argmax 쪽으로 날카로워지고, 높으면 평평해진다. → [[02-foundations/calculus-backprop|Calculus & Backprop §6]]
- **Temperature scaling** · temperature scaling — Recalibrating a classifier by dividing its logits by one scalar $T$ fitted on held-out data. A larger $T$ flattens overconfident probabilities without changing which class ranks first. · held-out 데이터로 맞춘 스칼라 하나 $T$로 logits를 나눠 분류기를 재보정하는 것. $T$가 크면 과신한 확률이 평평해지지만 1위 클래스는 바뀌지 않는다. → [[04-robotics/human-intent-prediction|Human Intent §4]]
- **Temporal action localization** · 시간적 행동 위치추정 — Finding (start, end, label) segments in untrimmed video, scored by mAP at a temporal IoU threshold: the overlap of predicted and true segments divided by their union. · 자르지 않은 영상에서 (시작, 끝, 레이블) 구간을 찾는 과제로, 예측·실제 구간의 겹침을 합집합 길이로 나눈 시간 IoU 문턱에서의 mAP로 채점한다. → [[04-robotics/video-action-understanding|Video §1]]
- **Temporal calibration** · 시간 보정 — Estimating clock offset and latency between sensors, typically by correlating their motion signals. Timing errors can dominate algorithmic gains and leave a pose wrong despite sub-pixel reprojection error. · 센서 사이의 클럭 오프셋과 지연을 추정하는 일로, 보통 운동 신호의 상관을 쓴다. 시간 오차는 알고리즘 개선보다 성능을 지배할 수 있고, 서브픽셀 reprojection error에도 pose를 틀리게 남긴다. → [[04-robotics/geometric-perception-calibration|Geometric perception §5]]
- **Temporal-difference learning (TD learning)** · TD 학습 — Learning values from single transitions without a model by nudging the estimate toward the bootstrapped target $r + \gamma V(s')$, as in TD(0). Each sweep pushes reward information one step further back. · 모델 없이 전이 하나로, 추정값을 부트스트랩 타깃 $r + \gamma V(s')$ 쪽으로 밀어 가치를 배우는 것(TD(0)). 스윕마다 보상 정보가 한 스텝씩 뒤로 전파된다. → [[02-foundations/rl-basics|RL Basics §3]]
- **Temporal ensembling** · temporal ensembling(시간 앙상블) — Querying a chunking policy at every timestep and averaging all previously predicted actions for the current step. It smooths motion but can blur multimodal choices and delay rapid changes. · 청킹 정책을 매 시점 질의하고, 현재 시점에 대해 이전에 예측된 모든 행동을 평균하는 것. 움직임을 매끄럽게 하지만 다봉 선택지를 흐리고 빠른 변화를 늦출 수 있다. → [[01-canonical-papers/notes/4-vla/act|ACT]]
- **Terminal cost** · 종단 비용 — A cost on the last predicted state of an MPC horizon, added to the per-step stage costs to stand in for everything after the horizon. It is often the LQR Riccati solution $x^\top P x$. · MPC 지평의 마지막 예측 상태에 매기는 비용으로, 스텝별 단계 비용에 더해져 지평 이후 전체를 대신한다. 흔히 LQR 리카티 해 $x^\top P x$다. → [[04-robotics/mpc|MPC]]
- **Terminal set** · 종단 집합 — A region the MPC horizon must end in, invariant under a known local controller that respects the constraints. It underpins recursive feasibility and the stability proof. · MPC 지평이 끝나야 하는 영역으로, 제약을 지키는 알려진 국소 제어기 아래 불변이다. recursive feasibility와 안정성 증명의 토대다. → [[04-robotics/mpc|MPC]]
- **Terrain generator** · 지형 생성기 — The simulator component that creates varied training terrains for a locomotion policy. The policy can only learn to handle the kinds of variation the generator produces. · 로코모션 정책 학습용으로 다양한 지형을 만드는 시뮬레이터 구성요소. 정책은 생성기가 만들어 낸 종류의 변이만 다루는 법을 배울 수 있다. → [[01-canonical-papers/notes/9-navigation/rma|RMA]]
- **Terrestrial laser scanning (TLS)** · 레이저 스캐닝 — Measuring a site as dense point clouds from a laser scanner set up at tripod stations. The conventional workflow needs manual station setup, placed targets, and manual registration afterwards. · 삼각대 스테이션에 세운 레이저 스캐너로 현장을 고밀도 점군으로 측정하는 것. 재래식 워크플로에는 수동 스테이션 설치, 타깃 배치, 후처리 수동 정합이 필요하다. → [[01-canonical-papers/notes/8-construction/cho-slam|Cho SLAM 2018]]
- **Test-time compute** · test-time compute — Computation spent when a problem arrives rather than during training: sampling several candidate answers, checking them, backtracking, thinking longer on harder inputs. The robotics form is re-observing, retrieving past observations, comparing candidate plans or verifying a goal condition mid-run, and it costs latency, energy and money, so systems that use it trigger it selectively — AdaNav ([arXiv:2509.24387](https://arxiv.org/abs/2509.24387)) gates it on the policy's own uncertainty. · 학습 중이 아니라 문제를 만난 순간에 쓰는 계산 — 후보 답을 여러 개 뽑고, 검사하고, 되돌리고, 어려운 입력에 더 오래 생각하는 것. 로보틱스에서의 형태는 실행 중 다시 관측하고, 과거 관측을 검색하고, 후보 계획을 비교하고, 목표 조건을 검증하는 것이다. 지연·전력·비용이 들기 때문에 이것을 쓰는 시스템은 선별해서 켠다. AdaNav([arXiv:2509.24387](https://arxiv.org/abs/2509.24387))는 정책 자신의 불확실성으로 그 문을 연다. → [[03-deep-learning/lineage|Lineage — Robot learning]]
- **TF2 (tf, transform tree)** · TF2(프레임 트리) — The library that answers where frame A is relative to frame B at time t. Frames form a tree with one parent each, and exactly one publisher should own each parent–child edge. · 시각 t에 프레임 A가 프레임 B에 대해 어디 있는지 답하는 라이브러리. 프레임은 각자 부모가 하나인 트리를 이루고, 부모–자식 간선 하나는 퍼블리셔 하나만 소유해야 한다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §7]]
- **TF buffer and lookup time** · TF 버퍼와 조회 시간 — A listener caches transforms in a time-indexed buffer (10 s by default) and queries it: time zero means latest available, a message stamp means when that data was captured, and now usually fails. · 리스너는 변환을 시간 색인 버퍼(기본 10초)에 담아 조회한다. 시간 0은 가장 최근 것, 메시지 스탬프는 그 데이터가 찍힌 시점을 뜻하고, now는 대개 실패한다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §8]]
- **Three-layer architecture** · 3층(three-layer) 아키텍처 — A fast controller, a sequencer (executive) that picks the active behavior and handles failure, and a slow deliberator; only the deliberator may be slow, and an LLM planner replaces only it. · 빠른 controller, 활성 행동을 고르고 실패를 처리하는 sequencer(executive), 느린 deliberator가 각자 제 주기로 돈다. 느려도 되는 것은 deliberator뿐이며, LLM 플래너는 deliberator만 대체한다. → [[04-robotics/robot-systems-deployment|Robot Systems §6.5]]
- **Throughput vs latency** · 처리량 대 지연 — Throughput is how many items finish per second; latency is how long one item takes end to end. A robot can have high throughput and still act on stale data — which is why [[04-robotics/robot-systems-deployment|Robot Systems §3]] insists that "we run at 30 Hz" answers a different question from "how old was the frame you acted on?". · 처리량은 초당 몇 개를 끝내는가, 지연은 하나가 처음부터 끝까지 얼마나 걸리는가다. 처리량이 높아도 낡은 데이터로 행동할 수 있다. → [[04-robotics/robot-systems-deployment|Robot Systems §3]]
- **Time and space complexity** · 시간·공간 복잡도 — How the count of dominant operations (time) or extra memory (space) grows with input size $n$, with constants dropped and the case named: worst, average, expected or amortized. · 지배적 연산 횟수(시간)나 추가 메모리(공간)가 입력 크기 $n$에 따라 어떻게 늘어나는지를, 상수를 버리고 경우(최악, 평균, 기대, 분할상환)를 밝혀 나타낸 것. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]], [[02-foundations/algorithms/complexity-recursion|11.1 §7]]
- **Time-limit truncation** · 시간 제한 종료 — Ending an episode because the clock ran out rather than because the task ended. The value should still be bootstrapped; treating it as terminal teaches the policy that the world ends at the limit. · 과제가 끝나서가 아니라 시계가 다 되어 에피소드를 끝내는 것. 여전히 가치를 부트스트랩해야 하며, 이를 종료로 다루면 시간 제한에서 세계가 끝난다고 정책에게 가르친다. → [[02-foundations/rl-basics|RL Basics §10]]
- **Time parameterization** · 시간 파라미터화 — Assigning times to a geometric path so it becomes a trajectory obeying joint velocity and acceleration limits. Planners do not produce timing; a response adapter such as AddTimeOptimalParameterization does. · 기하학적 경로에 시간을 붙여 관절 속도·가속도 한계를 지키는 궤적으로 만드는 것. 플래너는 시간을 만들지 않고, AddTimeOptimalParameterization 같은 response adapter가 만든다. → [[04-robotics/ros2/manipulation-moveit2|25.8 MoveIt 2 §4]]
- **Time scaling** · 시간 스케일링 — The function $s(t)$ turning a geometric path into an executable trajectory; cubic, quintic, and trapezoidal profiles are the standard choices. · 기하 경로를 실행 가능한 궤적으로 바꾸는 시간 함수 $s(t)$. 3차·5차·사다리꼴 프로파일이 표준 선택지다. → [[04-robotics/modern-robotics/ch09-trajectory-generation|MR 9장]]
- **Timestep embedding** · 타임스텝 임베딩 — A vector encoding of the current noise step $t$ fed to a diffusion network, so one network knows how much noise to remove at every level. DiT injects it through adaLN-Zero. · 현재 노이즈 스텝 $t$를 벡터로 인코딩해 디퓨전 네트워크에 넣는 것으로, 네트워크 하나가 수준마다 노이즈를 얼마나 제거할지 알게 한다. DiT는 이를 adaLN-Zero로 주입한다. → [[01-canonical-papers/notes/6-diffusion/ddpm|DDPM]], [[01-canonical-papers/notes/6-diffusion/dit|DiT]]
- **Time-to-event** · time-to-event — $\Delta=T-t$, the time remaining before the predicted event occurs. Intent performance should be read as a curve over $\Delta$, because one number or an average over $\Delta$ hides the deployment trade-off. · 예측 대상 사건까지 남은 시간 $\Delta=T-t$. 한 숫자나 $\Delta$에 대한 평균은 배포 절충을 가리므로, 의도 예측 성능은 $\Delta$에 대한 곡선으로 읽어야 한다. → [[04-robotics/human-intent-prediction|Human Intent §3]]
- **Token** · 토큰(token) — One discrete piece of input a model processes, such as a word fragment or an image patch. Models that generate autoregressively produce one token per forward pass. · 모델이 처리하는 입력의 이산적인 한 조각(단어 조각, 이미지 패치). 자기회귀로 생성하는 모델은 순전파 한 번에 토큰 하나를 만든다. → [[02-foundations/neural-network-basics|0.7 §6]]
- **Tokenizer** · 토크나이저 — The step that cuts raw input into tokens — sub-word pieces for text, patches or a learned codebook for images. It is a design choice, not a given, so two models with different tokenizers cannot be compared per token. · 날 입력을 토큰으로 자르는 단계 — 텍스트는 부분 단어로, 이미지는 패치나 학습된 코드북으로. 주어진 것이 아니라 설계 선택이므로, 토크나이저가 다른 두 모델은 토큰 단위로 비교할 수 없다. → [[02-foundations/neural-network-basics|Neural Network Basics §6]]
- **Tolerance** · 공차(허용오차) — The allowed deviation of a dimension or position from its nominal value, such as ±10 mm for an anchor. Uncertainty must be budgeted against it before geometry is used for action. · 치수나 위치가 명목값에서 벗어나도 되는 허용 범위(예: 앵커 ±10 mm). 형상을 행동에 쓰기 전에 불확실성을 이 값에 대해 예산해야 한다. → [[05-construction-robotics/digital-twin-workflows|Digital Twins & BIM §3]]
- **Tolerance stack (tolerance accumulation)** · 공차 누적 — Small errors in base pose, part placement, and as-built deviation adding up on the same contact or across many placements. No fixture absorbs them on site. · 베이스 자세·부재 배치·시공 편차의 작은 오차가 같은 접촉이나 여러 배치에 걸쳐 쌓이는 것. 현장에는 이를 흡수할 지그가 없다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §6]], [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §1]]
- **Top-down vs bottom-up pose estimation** · top-down / bottom-up — Top-down detects and crops each person, then estimates pose, with cost linear in the number of people; bottom-up finds all keypoints and groups them into people, roughly constant cost but grouping fails in crowds. · top-down은 사람을 검출·크롭한 뒤 자세를 추정해 비용이 사람 수에 비례하고, bottom-up은 모든 키포인트를 찾아 사람으로 묶어 비용은 거의 일정하지만 군중에서 묶기가 실패한다. → [[04-robotics/human-pose-gaze|Pose & gaze §2]]
- **Topic** · 토픽(topic) — A named, one-way, asynchronous data stream. Any number of publishers and subscribers attach to the name without knowing about each other, which is what makes substitution and introspection easy. · 이름 붙은 단방향 비동기 데이터 스트림. 퍼블리셔와 서브스크라이버가 몇 개든 서로를 모른 채 그 이름에 붙으며, 이 익명성 덕분에 교체와 내성이 쉽다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §3]]
- **Top-k selection** · 상위 k개 — Finding the k largest (or smallest) items without a full sort. A size-k heap takes $O(n \log k)$ time and $O(k)$ memory and works on a stream; argpartition takes $O(n)$ on an array. · 전체 정렬 없이 가장 큰(작은) k개를 찾는 것. 크기 k 힙은 $O(n \log k)$ 시간과 $O(k)$ 메모리로 스트림에도 쓸 수 있고, 배열에서는 argpartition이 $O(n)$이다. → [[02-foundations/algorithms/sorting-divide-conquer|11.3 §7]]
- **Topological sort** · 위상 정렬 — Ordering a DAG's vertices so every edge points forward, as in build order or course prerequisites. Kahn's algorithm (repeatedly output a vertex with no incoming edges) or reverse DFS finish order does it in $O(V + E)$. · 모든 간선이 앞으로 향하도록 DAG의 정점을 나열하는 것(빌드 순서, 선수 과목). 진입 간선이 없는 정점을 반복 출력하는 Kahn 알고리즘이나 DFS 종료 역순으로 $O(V + E)$에 구한다. → [[02-foundations/algorithms/graph-algorithms|11.6 §3]]
- **Torque control** · 토크 제어 — Commanding joint torques directly, rather than sending position targets to a vendor's inner position loop. With a position interface, any claimed compliance depends on that inner loop's stiffness, bandwidth, and delay. · 벤더의 내부 위치 루프에 위치 목표를 보내는 대신 관절 토크를 직접 명령하는 것. 위치 인터페이스라면 주장하는 컴플라이언스가 내부 루프의 강성·대역폭·지연에 달린다. → [[02-foundations/manipulator-kinematics-dynamics|Manipulator Dynamics §8]]
- **Tracked base (crawler)** · 궤도형 베이스 — A mobile base driven by continuous tracks instead of wheels, used for heavy site robots such as a 700 kg drilling robot. · 바퀴 대신 무한궤도로 움직이는 이동 베이스로, 700 kg 드릴링 로봇 같은 무거운 현장 로봇에 쓰인다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §4]]
- **Tracking-by-detection** · tracking-by-detection — Tracking built on a per-frame detector. SORT runs a constant-velocity Kalman filter per box with Hungarian matching on IoU; DeepSORT adds a re-identification appearance embedding so people keep identity after occlusion. · 프레임별 검출기 위에 세운 추적. SORT는 박스마다 등속 칼만 필터와 IoU 위의 헝가리안 매칭을 쓰고, DeepSORT는 재식별 외양 임베딩을 더해 가려졌던 사람이 정체를 유지하게 한다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Track management** · 트랙 관리 — The life cycle of tracks: a detection outside every gate starts a tentative track, M associations in the last N frames confirm it, and too many consecutive misses delete it. · 트랙의 생애 주기: 모든 게이트 밖의 검출이 잠정 트랙을 시작하고, 최근 N 프레임 중 M번 연관되면 확정되며, 연속으로 너무 많이 놓치면 삭제된다. → [[04-robotics/state-estimation-slam|State estimation §8.5]]
- **Trades** · 공종·직종 — The specialized worker groups on a site, such as electricians, drywallers, and ironworkers. Other trades working alongside make safety and shared space central. · 전기공·석고보드공·철골공처럼 현장의 전문 작업자 집단. 다른 공종이 옆에서 일하므로 안전과 공간 공유가 핵심이 된다. → [[05-construction-robotics/construction-manipulation|Construction Manipulation §1]]
- **Train / validation / test split** · train / validation / test 분할 — Fit on train, tune hyperparameters and pick checkpoints on validation, and touch test once at the end. Any decision influenced by test scores turns it into a validation set and inflates results. · train으로 적합하고, validation으로 하이퍼파라미터와 체크포인트를 고르고, test는 맨 끝에 한 번만 만진다. test 점수에 영향받은 결정은 test를 validation으로 만들어 결과를 부풀린다. → [[02-foundations/ml-practice|ML Practice §1]]
- **Trajectory forecasting** · 궤적 예측 — Predicting a person's continuous future positions over a horizon. The future is multimodal, so regressing one path averages incompatible options; scored with ADE and FDE, it feeds continuous plans such as costmaps or MPC constraints. · 사람의 연속적인 미래 위치를 일정 구간에 걸쳐 예측하는 것. 미래는 다봉적이라 경로 하나를 회귀하면 양립 불가능한 선택지를 평균하게 된다. ADE·FDE로 평가하며 코스트맵·MPC 제약 같은 연속 계획에 쓰인다. → [[04-robotics/human-intent-prediction|Human Intent §1]]
- **Trajectory optimization** · 궤적 최적화 — Choosing a state and input sequence that minimizes running cost plus terminal cost subject to dynamics, bounds, and collision constraints. Usually nonconvex and initialization-sensitive; solved offline, or repeatedly online as MPC. · 동역학·한계·충돌 제약 아래 실행 비용과 종단 비용의 합을 최소화하는 상태·입력 시퀀스를 고르는 것. 대개 비볼록이고 초기화에 민감하며, 오프라인으로 풀거나 MPC로 온라인에서 반복해 푼다. → [[04-robotics/planning-decision-making|Planning §6]]
- **Transduction** · 변환(transduction) — The physical step that turns a mechanical event into an electrical signal — the resistance, capacitance, charge, pressure or magnetic field that actually changes. Papers group sensors by what they output; the transduction is what decides whether a reading survives being held still. · 역학적 사건을 전기 신호로 바꾸는 물리적 단계 — 실제로 변하는 저항·정전용량·전하·압력·자기장. 논문은 센서를 출력으로 묶지만, 값이 가만히 유지될 때도 살아남는지를 정하는 것은 변환 방식이다. → [[04-robotics/tactile-visuotactile|14. Tactile & Visuotactile §2.5]]
- **Transfer function** · 전달함수 — $G(s)$, the Laplace-domain ratio of output to input for a system starting at rest. It turns a linear ODE into a division; roots of the denominator are poles and roots of the numerator are zeros. · 정지 상태에서 시작한 시스템의 출력 대 입력 비를 라플라스 영역에서 쓴 $G(s)$. 선형 미분방정식을 나눗셈으로 바꾸며, 분모의 근이 극점, 분자의 근이 영점이다. → [[02-foundations/engineering-math|Engineering Math §9]]
- **Transformer** · Transformer(트랜스포머) — A sequence architecture built only from attention, feed-forward layers, residual connections, and normalization, with no recurrence. It processes all tokens in parallel and underlies today's LLMs, VLMs, and VLAs. · 순환 없이 어텐션, feed-forward 층, residual 연결, 정규화만으로 만든 시퀀스 구조. 모든 토큰을 병렬로 처리하며 오늘날 LLM·VLM·VLA의 토대다. → [[01-canonical-papers/notes/1-foundations/attention-is-all-you-need|Transformer]]
- **Translation equivariance** · 평행이동 등변성 — Shifting the input shifts the output feature map by the same amount. Convolution has it built in, which helps with images but must be learned by a plain Transformer. · 입력을 옮기면 출력 특징 맵도 같은 만큼 옮겨지는 성질. 합성곱에는 내장되어 이미지에 유리하지만, 평범한 Transformer는 이를 학습해야 한다. → [[01-canonical-papers/notes/1-foundations/vit|ViT]]
- **Transparency (teleoperation)** · 투명성 — The ideal that the impedance the operator feels equals the impedance of the environment the follower touches: push into concrete, feel concrete. It trades directly against robust stability, and a bilateral controller is a chosen point on that compromise. · 조작자가 느끼는 임피던스가 팔로워가 닿는 환경의 임피던스와 같아지는 이상. 견고한 안정성과 정면으로 상충하며, 양방향 제어기는 그 타협선 위의 한 점이다. → [[04-robotics/teleoperation-demonstration|12. §2]]
- **Trapezoidal rule (numerical integration)** · 사다리꼴 규칙 — A numerical integrator that averages old and new rates over a step, $x_{k+1}=x_k+\tfrac{T}{2}(v_k+v_{k+1})$, common for simulating dynamic haptic objects. Unrelated to the trapezoidal velocity profile of trajectory generation. · 한 스텝의 이전 변화율과 새 변화율을 평균하는 수치 적분기 $x_{k+1}=x_k+\tfrac{T}{2}(v_k+v_{k+1})$로, 동적 햅틱 물체 시뮬레이션에 흔히 쓴다. 궤적 생성의 사다리꼴 속도 프로파일과는 무관하다. → [[04-robotics/haptics-teleoperation/haptic-rendering-algorithms|24.7 Rendering Algorithms §6]]
- **Travelling salesman problem (TSP)** · 외판원 문제 — Visiting every city exactly once and returning at minimum cost. It is NP-hard; the Held–Karp bitmask DP over (visited set, last city) solves it exactly in $O(n^2 2^n)$, feasible near 20 cities. · 모든 도시를 정확히 한 번씩 방문하고 돌아오는 최소 비용 경로 문제. NP-난해이며, (방문한 집합, 마지막 도시)에 대한 Held–Karp 비트마스크 DP가 $O(n^2 2^n)$에 정확히 풀어 도시 20개 안팎까지 가능하다. → [[02-foundations/algorithms/dynamic-programming|11.5 §6]]
- **Traversability** · traversability(통과 가능성) — Whether, and at what cost, a particular robot can drive over terrain at a given speed; modern work treats it as an affordance learned from the robot's own driving, not a geometric terrain property. · 특정 로봇이 주어진 속도로 지형을 지날 수 있는지와 그 비용. 현대 연구는 이를 지형의 기하 성질이 아니라 로봇 자신의 주행 경험에서 학습하는 어포던스로 본다. → [[04-robotics/traversability-off-road|Traversability §1]]
- **Tree traversal (in-order, pre-order, post-order)** · 순회(중위, 전위, 후위) — Visiting every node of a binary tree once, in $O(n)$. In-order lists BST keys in sorted order, pre-order serves copying and serialization, post-order computes subtree quantities from the children. · 이진 트리의 모든 노드를 한 번씩 $O(n)$에 방문하는 것. 중위는 BST 키를 정렬 순서로 나열하고, 전위는 복사·직렬화에, 후위는 자식 결과로 서브트리 값을 계산하는 데 쓴다. → [[02-foundations/algorithms/data-structures|11.2 §5]]
- **Trench** · 트렌치 — A long, narrow excavation, for example for pipes or foundations. Digging trenches is a standard real-site test for autonomous excavators. · 배관이나 기초를 위한 길고 좁은 굴착 도랑. 트렌치 굴착은 자율 굴착기의 대표 실제 현장 시험이다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §4]]
- **Triangulation** · 삼각측량 — Recovering a 3D point by intersecting rays from two known camera poses. It needs baseline: for distant points or small baselines the rays are nearly parallel and depth degenerates. · 알려진 두 카메라 pose에서 나온 광선을 교차시켜 3D 점을 복원하는 일. 기선이 필요하며, 먼 점이나 짧은 기선에서는 광선이 거의 평행해 깊이가 퇴화한다. → [[04-robotics/geometric-perception-calibration|Geometric perception §2]]
- **Trie (prefix tree)** · 트라이 — A tree storing strings as paths from the root, one character per edge, so strings with a common prefix share a path. Insert, lookup and prefix queries cost $O(L)$ for key length $L$. · 문자열을 루트에서 시작하는 경로로 저장하고 간선마다 문자 하나를 두어, 공통 접두사를 가진 문자열이 경로를 공유하는 트리. 삽입·조회·접두사 질의가 키 길이 $L$에 대해 $O(L)$이다. → [[02-foundations/algorithms/data-structures|11.2 §6]]
- **T-RO (IEEE Transactions on Robotics)** · T-RO — IEEE's flagship robotics journal, double-anonymous since January 2025, with rolling submission and revise-and-resubmit review. An extended conference paper must add new results of substantive significance. · IEEE의 대표 로보틱스 저널로, 2025년 1월부터 양측 익명이며 상시 제출과 revise & resubmit 심사를 한다. 학회 논문을 확장하려면 실질적 의의가 있는 새 결과를 더해야 한다. → [[06-research-practice/venue-strategy|Venue Strategy §2]], [[06-research-practice/venue-strategy|Venue Strategy §5]]
- **Trust parameter** · 신뢰 파라미터 — The $\lambda$ in Levenberg–Marquardt, weighting a penalty on how far the step moves from the current iterate. Small $\lambda$ trusts the linear model and takes the Gauss–Newton step; large $\lambda$ distrusts it and takes a short step along the gradient. Adapted by trial: accept and relax, reject and tighten. · Levenberg–Marquardt의 $\lambda$로, 스텝이 현재 반복점에서 얼마나 멀어지는지에 매기는 벌점의 가중치다. $\lambda$가 작으면 선형 모델을 믿고 Gauss–Newton 스텝을 밟고, 크면 불신하고 그래디언트 방향으로 짧게 간다. 시행으로 조절한다 — 받아들이면 느슨하게, 물리면 조인다. → [[02-foundations/optimization|Optimization §3.5]]
- **Trust region** · trust region — A limit on how far one update may move the policy from the one that collected the data. TRPO enforces it with a KL constraint; PPO approximates it by clipping the probability ratio. · 한 번의 갱신이 데이터를 모은 정책에서 얼마나 멀어질 수 있는지에 대한 제한. TRPO는 KL 제약으로 강제하고, PPO는 확률비를 clip해 근사한다. → [[01-canonical-papers/notes/1-foundations/ppo|PPO]]
- **TSDF (truncated signed distance field)** · TSDF — A voxel map that fuses many noisy depth images into one smooth surface: each voxel stores a truncated projective signed distance along a sensor ray, whose zero crossing estimates the surface. · 잡음 많은 깊이 이미지 여럿을 매끄러운 표면 하나로 융합하는 복셀 지도: 각 복셀이 센서 광선을 따른 잘린 투영 부호 거리를 담고, 그 0 교차가 표면을 추정한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Twist** · 트위스트 — A rigid body's six-number velocity $(\omega, v)$; note $v$ is the velocity of the body point currently at the frame origin, not of "the end-effector point". · 강체의 6차원 속도 $(\omega, v)$. 여기서 $v$는 **지금 프레임 원점에 있는 물체 점**의 속도이지 "말단 점"의 속도가 아니다 — 부호 실수의 단골 원인이다. → [[04-robotics/modern-robotics/ch03-rigid-body-motions|MR 3장]]
- **Two-stream network** · two-stream — A video backbone with an appearance stream on RGB frames and a motion stream on precomputed optical flow; computing the flow dominates its cost. · RGB 프레임의 외형 스트림과 미리 계산한 optical flow의 움직임 스트림을 둔 비디오 백본. 비용은 flow 계산이 지배한다. → [[04-robotics/video-action-understanding|Video §3]]
- **Type support** · type support — Per-type code generated from a .msg file that tells the middleware how to serialize and deserialize that message, produced alongside the C++ header and Python module by rosidl_generate_interfaces. · .msg 파일에서 생성되어 그 메시지를 어떻게 직렬화·역직렬화할지 미들웨어에 알려 주는 타입별 코드. rosidl_generate_interfaces가 C++ 헤더, Python 모듈과 함께 만든다. → [[04-robotics/ros2/nodes-topics-messages|25.2 Nodes & Topics §7]]
- **UAV (unmanned aerial vehicle)** · UAV(무인 항공기) — A drone. On sites UAVs scan, inspect, and even deposit material, often teamed with ground robots. · 드론. 현장에서 스캔·점검을 하고 재료 적층까지 하며, 지상 로봇과 팀을 이루기도 한다. → [[05-construction-robotics/site-perception|Site Perception §2]]
- **UGV (unmanned ground vehicle)** · UGV(무인 지상 차량) — A driverless ground robot base. On sites it carries scanners or an arm, as in mobile welding systems. · 사람이 타지 않는 지상 로봇 베이스. 현장에서 스캐너나 팔을 싣고 다니며, 이동 용접 시스템이 예다. → [[05-construction-robotics/assembly-fabrication|Assembly & Fabrication §4]]
- **Undefined behaviour** · 정의되지 않은 동작 — In C++, a contract violation such as signed integer overflow, a dangling iterator or an invalid sort comparator, for which the language promises nothing: wrong output, infinite loops or crashes. · C++에서 부호 있는 정수 오버플로, 허공을 가리키는 반복자, 잘못된 정렬 비교자 같은 계약 위반으로, 언어가 아무것도 약속하지 않는다: 틀린 출력, 무한 루프, 충돌이 모두 가능하다. → [[02-foundations/algorithms/interview-code|11.7 §4]]
- **Underactuation (passive joint)** · 부족구동 — Having fewer actuators than degrees of freedom, as with an unpowered swinging joint. The controller must exploit dynamics instead of commanding that joint directly. · 동력 없는 흔들 관절처럼 자유도보다 액추에이터가 적은 상태. 제어기가 그 관절을 직접 명령하지 못하고 동역학을 활용해야 한다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §1]]
- **Underfitting** · 과소적합 — A model too weak or undertrained to fit even the training data: training and validation loss both stay high. · 학습 데이터조차 맞추지 못할 만큼 약하거나 덜 학습된 상태: 학습 손실과 validation 손실이 둘 다 높게 머문다. → [[02-foundations/ml-practice|ML Practice §2]]
- **Underlay and overlay** · 언더레이와 오버레이 — A workspace sourced on top of another is the overlay; the one beneath (e.g. /opt/ros/jazzy) is the underlay. Overlay packages override underlay ones, so source underlay first, overlay second. · 다른 워크스페이스 위에 source한 쪽이 오버레이, 아래쪽(예: /opt/ros/jazzy)이 언더레이다. 오버레이의 패키지가 언더레이 것을 덮으므로 언더레이를 먼저, 오버레이를 나중에 source한다. → [[04-robotics/ros2/workspaces-packages-launch|25.4 Workspaces & Launch §6]]
- **U-Net** · U-Net — An encoder–decoder CNN whose skip connections concatenate each encoder resolution into the matching decoder stage, restoring spatial detail for per-pixel output. It is the denoiser backbone of DDPM and Stable Diffusion. · 인코더의 각 해상도 특징을 같은 해상도의 디코더 단계에 이어붙이는 skip 연결을 가진 인코더-디코더 CNN으로, 픽셀별 출력에 필요한 공간 세부를 되살린다. DDPM과 Stable Diffusion의 노이즈 제거 백본이다. → [[01-canonical-papers/notes/2-computer-vision/u-net|U-Net]]
- **Unicycle model** · 외바퀴(unicycle) 모델 — The essential kinematics of most mobile bases: $\dot x = v\cos\theta$, $\dot y = v\sin\theta$, $\dot\theta = \omega$. Two inputs for three configuration variables; the deficit is the no-sideways-slip constraint. · 대부분의 모바일 베이스의 본질적 기구학: $\dot x = v\cos\theta$, $\dot y = v\sin\theta$, $\dot\theta = \omega$. 컨피규레이션 변수 셋에 입력 둘이고, 그 부족분이 옆 미끄럼 금지 제약이다. → [[04-robotics/modern-robotics/ch13-wheeled-mobile-robots|MR ch.13]]
- **Unilateral contact** · 단방향 접촉 — Contact that can push but not pull through an ordinary surface. Motion switches among separation, impact, sticking, and sliding, which makes contact dynamics hybrid and often nonsmooth. · 평범한 표면을 통해 밀 수는 있지만 당길 수는 없는 접촉. 운동이 분리·충격·고착·미끄럼 사이를 오가므로 접촉 동역학이 하이브리드가 되고 대개 비매끄럽다. → [[04-robotics/contact-force-tactile|Contact §1]]
- **Unilateral teleoperation** · 단방향 원격조작 — Motion flows from leader to follower and nothing comes back except what the operator can see, so contact force is not felt. It removes channel-induced instability but does not make every local controller stable. · 운동이 리더에서 팔로워로만 흐르고 조작자가 볼 수 있는 것 외에는 아무것도 돌아오지 않아 접촉력을 느끼지 못한다. 채널 유발 불안정은 없애지만 모든 로컬 제어기의 안정성을 보장하지는 않는다. → [[04-robotics/teleoperation-demonstration|Teleoperation §2]]
- **Unimodal baseline (single-modality baseline)** · 단일 모달리티 베이스라인 — A baseline given only one input, such as vision without the instruction. It measures how much of a multimodal task can be solved while ignoring the other input. · 지시 없이 시각만 받는 식으로 입력 하나만 쓰는 베이스라인. 다른 입력을 무시하고도 멀티모달 과제를 얼마나 풀 수 있는지 잰다. → [[01-canonical-papers/notes/9-navigation/vln-ce|VLN-CE]]
- **Union-find (disjoint-set union, DSU)** · Union-find(서로소 집합) — A forest of parent pointers whose roots name groups, supporting find (which group?) and union (merge two groups). With union by size and path compression each operation is effectively constant; groups cannot be split. · 루트가 그룹 이름인 부모 포인터 숲으로, find(어느 그룹인가)와 union(두 그룹 합치기)을 지원한다. 크기 기준 합치기와 경로 압축을 쓰면 연산당 사실상 상수이며, 그룹을 쪼갤 수는 없다. → [[02-foundations/algorithms/data-structures|11.2 §7]]
- **Universal hashing** · 범용 해싱 — Choosing the hash function at random from a family in which any two distinct keys collide with probability at most $1/m$. Expected $O(1 + \alpha)$ operations then hold for every input, not just random data. · 서로 다른 두 키가 충돌할 확률이 $1/m$ 이하인 함수족에서 해시 함수를 무작위로 고르는 것. 그러면 기대 $O(1 + \alpha)$ 연산이 무작위 데이터만이 아니라 모든 입력에 대해 성립한다. → [[02-foundations/algorithms/data-structures|11.2 §3]]
- **Unmodeled dynamics** · 미모델링 동역학 — Physical effects a controller's model leaves out, such as hydraulic valve dead zones, backlash, or flexible links. They break linear guarantees outright, which is why actuator models get so much effort. · 제어기의 모델이 빠뜨린 물리 효과. 유압 밸브 데드존, 백래시, 유연 링크 등이 있다. 선형 보장을 정면으로 깨므로 액추에이터 모델에 큰 공을 들인다. → [[04-robotics/control-theory-ce397|Control Theory §9]]
- **Unscented Kalman filter (UKF)** · UKF — Propagates a few chosen sigma points, samples whose mean and covariance match the belief, through the true nonlinear model instead of linearizing it. Still assumes a compact unimodal belief. · 평균과 공분산이 belief와 일치하도록 고른 소수의 시그마 포인트를 선형화 대신 진짜 비선형 모델에 통과시키는 필터. 여전히 조밀한 단봉 belief를 가정한다. → [[04-robotics/state-estimation-slam|State estimation §5]]
- **URDF (Unified Robot Description Format)** · URDF — The XML file that statically describes a robot as a tree of links (rigid bodies with visual, collision and inertial data) and joints (parent, child, origin, type). It cannot express closed loops. · 로봇을 링크(visual·collision·inertial 정보를 가진 강체)와 조인트(parent, child, origin, 타입)의 트리로 정적으로 기술하는 XML 파일. 닫힌 루프는 표현하지 못한다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §2]]
- **Usable horizon (Δ\*)** · 가용 지평 — The longest lead time at which a predictor's chosen operating metric still meets its requirement. A system whose $\Delta^*$ is shorter than the actuator's stopping time is a detector, not a predictor. · 선택한 동작점 지표가 요구조건을 아직 넘기는 가장 긴 선행 시간. $\Delta^*$가 구동기의 정지 시간보다 짧은 시스템은 예측기가 아니라 검출기다. → [[04-robotics/human-intent-prediction|Human Intent §3]]
- **use_sim_time (simulated time)** · use_sim_time(시뮬레이션 시간) — A node parameter that makes the node's clock follow the /clock topic instead of the wall clock. Set it for the whole graph in the launch file; a mixture breaks transforms and timers. · 노드 시계가 벽시계 대신 /clock 토픽을 따르게 하는 노드 파라미터. launch 파일에서 그래프 전체에 설정해야 하며, 섞이면 변환과 타이머가 깨진다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §10]]
- **VAE (variational autoencoder)** · VAE(변분 오토인코더) — A generative autoencoder whose encoder outputs an approximate posterior over latents and whose decoder reconstructs the input, trained jointly on the ELBO via the reparameterization trick. Samples tend to be blurry. · 인코더가 잠재변수의 근사 사후분포를 출력하고 디코더가 입력을 복원하며, reparameterization 트릭으로 ELBO에 대해 함께 학습하는 생성형 오토인코더. 샘플이 흐릿해지는 경향이 있다. → [[01-canonical-papers/notes/6-diffusion/vae|VAE]]
- **Value function** · 가치 함수 — $V^\pi(s)$: the expected return from state $s$ when following policy $\pi$. It satisfies the Bellman expectation equation and is what a critic learns. · $V^\pi(s)$: 정책 $\pi$를 따를 때 상태 $s$에서의 기대 리턴. 벨만 기대 방정식을 만족하며, 크리틱이 배우는 것이다. → [[02-foundations/rl-basics|RL Basics §2]]
- **Value iteration** · 가치 반복 — Repeatedly applying the Bellman optimality update to every state until values stop changing. It converges because the operator is a $\gamma$-contraction, and it needs the transition model. · 가치가 더 바뀌지 않을 때까지 모든 상태에 벨만 최적성 갱신을 반복 적용하는 것. 연산자가 $\gamma$-수축이라 수렴하며, 전이 모델이 필요하다. → [[02-foundations/rl-basics|RL Basics §3]]
- **Value map** · 가치 지도 — A map scoring each location by how promising it is for reaching the goal. VLFM fills it with vision-language scores for the target text and explores the highest-scoring frontier. · 각 위치가 목표 도달에 얼마나 유망한지 점수를 매긴 지도. VLFM은 목표 텍스트에 대한 시각-언어 점수로 채우고 점수가 가장 높은 frontier를 탐색한다. → [[01-canonical-papers/notes/9-navigation/vlfm|VLFM]]
- **Vanishing gradients** · 그래디언트 소실 — Backward signals shrinking exponentially with depth or time because Jacobians have norms below 1; each sigmoid multiplies by at most 0.25. LSTM's constant error carousel and ResNet's identity path were answers. · 야코비안의 노름이 1보다 작아 역방향 신호가 깊이나 시간에 따라 지수적으로 줄어드는 현상으로, 시그모이드 하나가 최대 0.25를 곱한다. LSTM의 오차 캐러셀과 ResNet의 항등 경로가 그 대응이다. → [[02-foundations/calculus-backprop|Calculus & Backprop §5]]
- **Variable impedance control** · 가변 임피던스 제어 — Changing stiffness and damping during a task. A learned policy often outputs these impedance parameters at a slow rate while a classical controller renders them at a high rate. · 과제 중 강성과 감쇠를 바꾸는 것. 흔히 학습된 정책이 임피던스 파라미터를 느린 주기로 출력하고 고전 제어기가 빠른 주기로 그것을 구현한다. → [[04-robotics/force-compliance-control|Force & Compliance Control §6]]
- **Variational inference** · 변분 추론 — Approximating an intractable distribution, usually a posterior, by the closest member of a simple family, typically under reverse KL. Maximizing the ELBO does exactly this. · 계산 불가능한 분포(보통 사후 분포)를 단순한 가족에서 가장 가까운 원소로 근사하는 것으로, 흔히 reverse KL을 기준으로 한다. ELBO 최대화가 바로 이것이다. → [[02-foundations/information-theory|Information Theory §3]]
- **VDC (Virtual Design and Construction)** · VDC — Using integrated digital models of a project's design, schedule, and process to plan and manage construction, a practice developed alongside BIM at Stanford CIFE. · 프로젝트의 설계·공정·프로세스를 통합한 디지털 모델로 시공을 계획·관리하는 방식으로, Stanford CIFE에서 BIM과 함께 발전했다. → [[05-construction-robotics/lineage|Lineage §1]]
- **Velocity field** · 속도장 — A function $v(x,t)$ giving the direction and speed a point should move at each time. Flow matching learns it by regression, and sampling integrates it from noise to data. · 각 시점에 점이 어느 방향으로 얼마나 빨리 움직일지 주는 함수 $v(x,t)$. flow matching은 이것을 회귀로 배우고, 샘플링은 노이즈에서 데이터까지 이를 적분한다. → [[01-canonical-papers/notes/6-diffusion/flow-matching|Flow Matching]]
- **Venue** · venue(게재지) — The conference, journal or workshop where a paper appears. Day to day, venues differ less by prestige than by anonymity, whether authors can reply, deadline cadence and audience. · 논문이 실리는 학회, 저널, 워크숍. 실무에서 venue의 차이는 명성보다 익명성, 저자가 답할 기회, 마감 주기, 독자층에 있다. → [[06-research-practice/venue-strategy|Venue Strategy §2]]
- **Verb–noun action label** · 동사 + 명사 레이블 — Egocentric datasets factor an action as verb plus noun ("cut / onion"), making the label space compositional with an unavoidable long tail; report verb and noun accuracy separately. · 자기중심 데이터셋은 행동을 동사와 명사로 인수분해한다("자르다 / 양파"). 레이블 공간이 조합적이 되어 롱테일이 불가피하므로 동사·명사 정확도를 따로 보고한다. → [[04-robotics/egocentric-perception|Egocentric §2]]
- **Verification and validation (V&V)** · 검증과 타당성 확인 — Verification asks whether a system was built as specified; validation asks whether the specification is the right target for users and deployment. Passing one does not establish the other. · verification(검증)은 시스템이 명세대로 만들어졌는지, validation(타당성 확인)은 그 명세가 사용자와 배치 환경에 맞는 목표인지 묻는다. 한쪽을 통과해도 다른 쪽은 확립되지 않는다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §1]]
- **Video tokenizer** · 비디오 토크나이저 — An encoder that compresses video into continuous latents or discrete codes for a generator to model. Genie uses a VQ tokenizer; Cosmos releases continuous and discrete tokenizers at several compression rates. · 생성기가 모델링할 수 있도록 비디오를 연속 잠재나 이산 코드로 압축하는 인코더. Genie는 VQ 토크나이저를 쓰고, Cosmos는 여러 압축률의 연속·이산 토크나이저를 공개한다. → [[01-canonical-papers/notes/5-world-models/cosmos|Cosmos]], [[01-canonical-papers/notes/5-world-models/genie|Genie]]
- **Video world model (world simulator)** · 월드 시뮬레이터 — A video generation model treated as a simulator of how the world evolves. Sora argues scale yields simulation properties; critics note it renders appearances without actions or guaranteed physics. · 세계가 어떻게 변하는지의 시뮬레이터로 취급하는 비디오 생성 모델. Sora는 규모가 시뮬레이션 성질을 낳는다고 주장하지만, 행동도 물리 보장도 없이 겉모습만 그린다는 비판을 받는다. → [[01-canonical-papers/notes/5-world-models/sora|Sora]]
- **View planning (viewpoint planning)** · 시점 계획 — Choosing where a scanning robot or drone should observe from to achieve coverage of the site or defects. A detection benchmark does not validate it. · 현장이나 결함을 빠짐없이 관측하도록 스캔 로봇·드론의 관측 위치를 고르는 것. 검출 벤치마크만으로는 검증되지 않는다. → [[05-construction-robotics/site-perception|Site Perception §2]]
- **Virtual coupling** · virtual coupling(가상 결합) — A deliberately compliant spring–damper link between a device and a virtual proxy or remote command. It isolates fast local haptic dynamics from slower or delayed computation, trading transparency for stability. · 장치와 가상 proxy 또는 원격 명령 사이에 의도적으로 둔 스프링–댐퍼 연결. 빠른 로컬 햅틱 동역학을 느리거나 지연된 계산에서 분리하며, 안정성을 위해 투명성을 일부 포기한다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|Rendering & Stability]]
- **Virtual wall** · 가상 벽 — A rendered surface, typically force $-Kx-B\hat v$ while penetration $x>0$ and zero otherwise. The zero-order hold makes the force lag and leak energy, so passive stiffness is bounded, roughly $K\le 2b/T$ with physical damping $b$. · 침투 $x>0$일 때 흔히 $-Kx-B\hat v$의 힘을, 아니면 0을 내는 렌더링된 표면. zero-order hold 때문에 힘이 늦어 에너지가 새므로, 물리 댐핑 $b$에 대해 수동적인 강성은 대략 $K\le 2b/T$로 제한된다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|24.4 Rendering & Stability §2]]
- **Vision-and-language navigation (VLN)** · VLN — Following a natural-language route instruction through a building. R2R made it discrete, teleporting between navigation-graph nodes; VLN-CE switched to continuous low-level actions, and performance dropped sharply. · 자연어 경로 지시를 따라 건물을 이동하는 과제. R2R은 내비게이션 그래프 노드 사이를 순간이동하는 이산 과제였고, 저수준 연속 행동으로 바꾼 VLN-CE에서 성능이 급락했다. → [[04-robotics/semantic-language-navigation|Semantic navigation §5]]
- **Vision–language connector (projector)** · 연결자(connector) — The trainable module that maps a vision encoder's features into a language model's input: Flamingo's gated cross-attention, BLIP-2's Q-Former, or LLaVA's single linear projection. · 비전 인코더의 특징을 언어모델 입력으로 옮기는 학습 가능한 모듈. Flamingo의 게이트 달린 cross-attention, BLIP-2의 Q-Former, LLaVA의 선형 투영 하나가 예다. → [[01-canonical-papers/notes/3-vlm/llava|LLaVA]], [[01-canonical-papers/notes/3-vlm/blip-2|BLIP-2]]
- **Visual grounding** · 그라운딩(grounding) — Linking language to specific image regions, for example answering with a bounding box written as text. It lets a VLM point to what it is talking about. · 언어를 이미지의 특정 영역과 연결하는 것. 예를 들어 박스 좌표를 텍스트로 답한다. VLM이 말하는 대상을 가리킬 수 있게 한다. → [[01-canonical-papers/notes/3-vlm/qwen-vl|Qwen-VL]]
- **Visual odometry (VO, VIO)** · 시각(-관성) 오도메트리 — Motion estimation from camera images, fused with an IMU in visual-inertial odometry so each covers the other's failure timescale. It fails on texture-poor walls, motion blur and sudden lighting change. · 카메라 영상으로 운동을 추정하며, 시각-관성 오도메트리는 IMU를 융합해 서로의 실패 시간대를 덮는다. 질감 없는 벽, 모션 블러, 급격한 조명 변화에서 실패한다. → [[04-robotics/state-estimation-slam|State estimation §7]]
- **Visual question answering (VQA)** · VQA — Answering a natural-language question about an image. A standard benchmark family for VLMs, often reported zero-shot or few-shot. · 이미지에 대한 자연어 질문에 답하는 과제. VLM의 표준 벤치마크 계열로, zero-shot이나 few-shot으로 자주 보고된다. → [[01-canonical-papers/notes/3-vlm/blip-2|BLIP-2]]
- **Visual servoing (PBVS, IBVS)** · visual servoing — Closing a control loop on vision. Position-based servoing commands a twist from an estimated pose error; image-based servoing reduces feature error through a depth-dependent image Jacobian. Neither guarantees visibility or global convergence. · 비전으로 제어 루프를 닫는 것. PBVS는 추정한 pose 오차로 twist를 명령하고, IBVS는 깊이에 의존하는 image Jacobian으로 특징 오차를 줄인다. 어느 쪽도 시야 유지나 전역 수렴을 보장하지 않는다. → [[04-robotics/geometric-perception-calibration|Geometric perception §7.5]]
- **Visual token** · 시각 토큰 — A vector derived from the image encoder that sits in the language model's input sequence alongside text tokens. How many a model uses bounds fine detail such as OCR. · 이미지 인코더에서 나와 텍스트 토큰과 함께 언어모델 입력 시퀀스에 놓이는 벡터. 개수가 OCR 같은 세밀한 정보의 한계를 정한다. → [[01-canonical-papers/notes/3-vlm/blip-2|BLIP-2]]
- **Visuomotor policy** · 시각-운동 정책 — A policy that maps camera images, often with robot state, directly to motor actions. Diffusion Policy is a single-task visuomotor policy with no language input. · 카메라 이미지(흔히 로봇 상태와 함께)를 곧바로 운동 행동으로 사상하는 정책. Diffusion Policy는 언어 입력이 없는 단일 과제 시각-운동 정책이다. → [[01-canonical-papers/notes/4-vla/diffusion-policy|Diffusion Policy]]
- **Visuotactile** · 시촉각 — Using vision and touch together, either by fusing them into one learned representation or by predicting an action's outcome from both. The honest claim for fusion is usually sample efficiency and robustness, not a capability vision could never reach. · 비전과 촉각을 함께 쓰는 것 — 하나의 학습된 표현으로 융합하거나, 둘 다에서 행동의 결과를 예측하거나. 융합에 대한 정직한 주장은 대개 비전이 결코 도달할 수 없는 능력이 아니라 샘플 효율과 견고성이다. → [[04-robotics/tactile-visuotactile|14. §4]]
- **Viterbi algorithm** · 비터비 — Dynamic programming that finds the single most likely hidden-state path of an HMM: take a max over predecessors each step, record the winner, then follow the back-pointers. It can disagree with the step-by-step filter. · HMM에서 가장 가능성이 큰 은닉 상태 경로 하나를 찾는 동적 계획법. 매 스텝 이전 상태에 대해 max를 취하고 승자를 기록한 뒤 역포인터를 따라간다. 스텝별 필터 결과와 다를 수 있다. → [[02-foundations/probability|Probability §7]]
- **ViT (Vision Transformer)** · ViT — A plain Transformer encoder applied to image patches treated as tokens, with learned position embeddings and a class token. With enough pretraining data it beats CNNs and is now the usual vision encoder in VLMs. · 이미지 패치를 토큰으로 삼아 학습된 위치 임베딩과 class 토큰을 붙여 넣는 평범한 Transformer 인코더. 사전학습 데이터가 충분하면 CNN을 이기며, 지금은 VLM의 일반적인 비전 인코더다. → [[01-canonical-papers/notes/1-foundations/vit|ViT]]
- **VJP (vector–Jacobian product)** · VJP(벡터-야코비안 곱) — The map $v \mapsto J^\top v$ that each primitive operation supplies for reverse-mode autodiff; the framework composes these along the recorded graph to carry loss sensitivities backward. · 역방향 자동 미분에서 각 기본 연산이 제공하는 사상 $v \mapsto J^\top v$. 프레임워크가 기록된 그래프를 따라 이것들을 합성해 손실 민감도를 뒤로 나른다. → [[02-foundations/calculus-backprop|Calculus & Backprop §2]]
- **VLA (Vision-Language-Action)** · VLA — A model that outputs robot actions directly from visual and language input. · 시각·언어 입력에서 로봇 행동을 직접 출력하는 모델. → [[01-canonical-papers/notes/4-vla/rt-2|RT-2]], [[01-canonical-papers/notes/4-vla/pi0|π0]]
- **VLN-CE (vision-and-language navigation in continuous environments)** · VLN-CE(연속 환경 VLN) — VLN in which the agent issues low-level actions in a continuous 3D simulator instead of hopping between graph nodes. Scores drop, suggesting nav-graph results were inflated. · 에이전트가 그래프 노드 사이를 옮겨 다니지 않고 연속 3D 시뮬레이터에서 저수준 행동을 내는 VLN. 점수가 떨어져 nav-graph 결과가 부풀려졌음을 시사한다. → [[01-canonical-papers/notes/9-navigation/vln-ce|VLN-CE]]
- **V–M–C architecture** · V/M/C 분해 — World Models' agent split: V (a VAE) compresses each frame, M (an MDN-RNN) predicts the next latent, and C (a tiny controller) picks actions. It is the skeleton of later latent world-model agents. · World Models의 에이전트 분할: V(VAE)가 프레임을 압축하고, M(MDN-RNN)이 다음 잠재 상태를 예측하고, C(아주 작은 컨트롤러)가 행동을 고른다. 이후 잠재 월드모델 에이전트들의 뼈대다. → [[01-canonical-papers/notes/5-world-models/world-models|World Models]]
- **Volume rendering** · 볼륨 렌더링 — Computing a pixel by marching along its camera ray and accumulating color weighted by density, in practice a weighted sum over sampled points. It is differentiable, so photos can supervise the scene. · 카메라 광선을 따라가며 밀도로 가중한 색을 누적해 픽셀을 계산하는 것으로, 실제로는 샘플 점들에 대한 가중합이다. 미분 가능하므로 사진이 장면을 직접 감독할 수 있다. → [[01-canonical-papers/notes/2-computer-vision/nerf|NeRF]]
- **Voxel** · 복셀 — A cell of a regular 3D grid, the volumetric analogue of a pixel. Voxelizing a point cloud makes it convolution-friendly but costs memory and loses detail to quantization. · 규칙적인 3D 격자의 한 칸으로, 픽셀의 3차원 대응물이다. 포인트 클라우드를 복셀화하면 합성곱에 쓰기 쉬워지지만 메모리가 많이 들고 양자화로 세부를 잃는다. → [[01-canonical-papers/notes/2-computer-vision/pointnet|PointNet]]
- **Voxel hash (spatial hash grid)** · 복셀 해시 — A hash map keyed by integer grid cells. A fixed-radius query at a known scale checks only neighbouring cells, expected $O(1)$ at bounded point density, which is where it can beat a KD-tree. · 정수 격자 셀을 키로 쓰는 해시 맵. 알려진 척도의 고정 반경 질의는 이웃 셀만 확인하므로 점 밀도가 제한되면 기대 $O(1)$이고, 이런 경우 KD-tree를 이길 수 있다. → [[02-foundations/algorithms/data-structures|11.2 §8]]
- **VP-SDE and VE-SDE** · VP-SDE(분산 보존)와 VE-SDE(분산 폭발) — Two forward SDE families: variance preserving keeps total variance bounded while shrinking the signal (DDPM's continuous form); variance exploding adds ever-larger noise without shrinking (the score-matching form). · 두 순방향 SDE 계열: 분산 보존은 신호를 줄이며 전체 분산을 유계로 유지하고(DDPM의 연속형), 분산 폭발은 신호를 줄이지 않고 점점 큰 노이즈를 더한다(score matching형). → [[01-canonical-papers/notes/6-diffusion/score-sde|Score SDE]]
- **VQGAN** · VQGAN — A vector-quantized autoencoder whose decoder is sharpened with perceptual and adversarial (GAN) losses. It compresses images about 8× per side and is one of the autoencoder options behind latent diffusion. · 디코더를 지각 손실과 적대적(GAN) 손실로 선명하게 만든 벡터 양자화 오토인코더. 이미지를 변당 약 8배 압축하며, 잠재 디퓨전 뒤에 있는 오토인코더 선택지 중 하나다. → [[01-canonical-papers/notes/6-diffusion/latent-diffusion|Latent Diffusion]], [[01-canonical-papers/notes/6-diffusion/gan|GAN]]
- **VQ-VAE** · VQ-VAE — A VAE whose latents are snapped to entries of a learned discrete codebook, turning images or video into token grids. Such tokenizers feed autoregressive and diffusion generators and Genie's video model. · 잠재변수를 학습된 이산 코드북의 항목으로 맞춰 이미지나 비디오를 토큰 격자로 바꾸는 VAE. 이런 토크나이저가 자기회귀·디퓨전 생성기와 Genie의 비디오 모델에 입력을 공급한다. → [[01-canonical-papers/notes/6-diffusion/vae|VAE]], [[01-canonical-papers/notes/5-world-models/genie|Genie]]
- **VQ (Vector Quantization)** · VQ — Quantizing a continuous representation into discrete codebook entries; the core of visual and neural tokenizers (different from text tokenizers such as BPE). · 연속 표현을 코드북의 이산 코드로 양자화하는 것. 시각·신경 토크나이저의 핵심이며, BPE 같은 텍스트 토크나이저와는 다르다. → [[01-canonical-papers/notes/5-world-models/genie|Genie]], [[01-canonical-papers/notes/6-diffusion/latent-diffusion|VQGAN]]
- **VR demonstration (virtual demonstration)** · 가상 시연(VR 시연) — A demonstration performed in virtual reality instead of with physical materials. It is cheaper and reusable, but the gap between VR and real contact, a cousin of sim-to-real, goes unmeasured. · 실물 재료 대신 가상현실에서 수행한 시연. 싸고 재사용할 수 있지만 sim-to-real의 사촌인 VR과 실제 접촉 사이 격차는 측정되지 않는다. → [[01-canonical-papers/notes/8-construction/yu-imitation|Yu 2024]]
- **Walking excavator** · 보행 굴착기 — An excavator on legs with wheels, such as the Menzi Muck M545 behind HEAP, which balances its legged chassis on rough terrain. · 바퀴 달린 다리 위의 굴착기로, HEAP의 바탕인 Menzi Muck M545처럼 험지에서 다리 섀시로 균형을 잡는다. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §4]]
- **Wall clock (SystemTime, wall time)** · 벽시계(SystemTime) — The machine's real clock. It ignores /clock, so under simulation or bag replay wall-clock stamps and wall timers disagree with the rest of the graph. · 머신의 실제 시계. /clock을 무시하므로 시뮬레이션이나 bag 재생 중에는 벽시계 스탬프와 wall timer가 그래프의 나머지와 어긋난다. → [[04-robotics/ros2/qos-executors-time|25.5 QoS & Executors §10]]
- **Warm start** · warm start — Initializing an iterative solver at the previous problem's solution rather than from scratch. Standard in SLAM back ends, MPC and calibration, where consecutive problems differ slightly; it is also why reported solve times can be far shorter than a cold solve would take. · 반복 솔버를 처음부터가 아니라 직전 문제의 해에서 초기화하는 것. 연속된 문제가 조금씩만 다른 SLAM 백엔드·MPC·보정에서 표준이며, 보고된 풀이 시간이 냉시작보다 훨씬 짧을 수 있는 이유이기도 하다. → [[02-foundations/optimization|Optimization §3.5]]
- **Watchdog** · watchdog — A monitor detecting an abnormal or timed-out update, process, or subsystem and triggering a fallback. · 비정상이나 timeout을 감지해 fallback을 실행하는 감시 장치. → [[04-robotics/robot-systems-deployment|Robot Systems]]
- **Waterbed effect** · 워터베드 효과 — Bode's integral $\int_0^\infty \log\lvert S\rvert\,d\omega = \pi\sum p_k$ says that for an open-loop-stable plant the sensitivity you remove at one frequency reappears at another; an unstable plant starts the account in debt. It is why a control result is bounded by the machine, not by the designer's skill. · 보드 적분 $\int_0^\infty \log\lvert S\rvert\,d\omega = \pi\sum p_k$는 개루프 안정 플랜트에서 한 주파수에서 덜어낸 감도가 다른 주파수에 다시 나타난다고 말한다. 불안정한 플랜트는 시작부터 빚을 진다. 제어 결과를 한계 짓는 것이 설계자의 솜씨가 아니라 기계인 이유다. → [[04-robotics/control-theory-ce397|Control Theory §5.5]]
- **Wave variables (scattering transformation)** · wave variable(파동 변수) — Sending $u = (b\dot x + F)/\sqrt{2b}$ and $v = (b\dot x - F)/\sqrt{2b}$ across a delayed channel instead of velocity and force, so that power is $\tfrac12(u^2-v^2)$ and the channel can only store energy, never create it. Buys passivity under any constant delay at the cost of transparency. · 속도와 힘 대신 이 조합을 지연 채널로 보내면 일률이 $\tfrac12(u^2-v^2)$가 되어 채널이 에너지를 저장만 할 뿐 만들어내지 못한다. 투명성을 대가로 임의의 상수 지연에서 수동성을 산다. → [[04-robotics/teleoperation-demonstration|12. §3]]
- **Waypoint** · 웨이포인트 — An intermediate position a path passes through. Many navigation models rely on prior maps with discretized waypoints, which ties them to one task setup. · 경로가 지나는 중간 위치. 많은 내비게이션 모델이 이산화된 웨이포인트가 있는 사전 지도에 기대어 특정 과제 설정에 묶인다. → [[01-canonical-papers/notes/9-navigation/uni-navid|Uni-NaVid]]
- **Weber's law** · Weber 법칙 — The local empirical approximation $\Delta I/I\approx k$: the smallest noticeable increment grows in proportion to the reference intensity. Fechner's logarithmic scale follows by integrating it; neither law is exact across all intensities or modalities. · 국소적 경험 근사 $\Delta I/I\approx k$로, 알아챌 수 있는 최소 증가량이 기준 세기에 비례해 커진다. 이를 적분하면 Fechner의 로그 척도가 나오지만, 두 법칙 모두 모든 세기와 감각에서 정확하지는 않다. → [[04-robotics/haptics-teleoperation/human-haptics-psychophysics|24.1 Human Haptics §2]]
- **Wedging and jamming** · wedging·jamming — The two ways a peg-in-hole insertion locks up: wedging when opposing contact forces trap the part geometrically, jamming when the applied wrench falls outside the cone that would produce insertion. Whitney's 1982 analysis turns both into design inequalities on the support compliance. · peg-in-hole 삽입이 멈춰 버리는 두 방식 — wedging은 맞서는 접촉력이 부재를 기하적으로 가두는 것, jamming은 가해진 렌치가 삽입을 만드는 원뿔 밖으로 벗어나는 것. Whitney의 1982년 분석이 둘을 지지부 컴플라이언스에 대한 설계 부등식으로 바꾼다. → [[04-robotics/force-compliance-control|13. §5]]
- **Weight decay** · weight decay — Shrinking weights toward zero at each step, equivalent to an L2 penalty under plain SGD and to a zero-mean Gaussian prior in MAP terms. Under Adam the two versions differ, which is why AdamW exists. · 매 스텝 가중치를 0 쪽으로 줄이는 것. 평범한 SGD에서는 L2 벌점과 같고 MAP 관점에서는 평균 0 가우시안 사전이다. Adam에서는 두 방식이 달라지며, AdamW가 있는 이유다. → [[02-foundations/probability|Probability §4]], [[02-foundations/optimization|Optimization §3]]
- **Weighted A\*** · 가중 A\* — A\* ordered by $g + \varepsilon h$ with $\varepsilon > 1$: greedier, with far fewer expansions, and with a consistent $h$ its returned cost is at most $\varepsilon$ times optimal. Anytime planners start large and lower $\varepsilon$. · $\varepsilon > 1$로 $g + \varepsilon h$ 순서를 쓰는 A\*: 더 탐욕적이라 확장이 훨씬 적고, 일관적 $h$라면 돌려준 비용이 최적의 $\varepsilon$배 이하다. anytime 계획기는 큰 $\varepsilon$에서 시작해 낮춘다. → [[02-foundations/algorithms/graph-algorithms|11.6 §6]]
- **Weights** · 가중치 — The entries of a layer's matrix $W$, which scale how much each input contributes to each output. With the biases they are the parameters that training fits. · 층의 행렬 $W$의 성분으로, 각 입력이 각 출력에 얼마나 기여하는지를 정한다. 편향과 함께 학습이 맞추는 파라미터다. → [[02-foundations/neural-network-basics|0.7 §1]]
- **Wheatstone bridge** · 휘트스톤 브리지 — A four-resistor circuit that turns a tiny resistance change into a measurable voltage; the readout for piezoresistive tactile elements. · 저항 네 개로 된 회로로, 아주 작은 저항 변화를 잴 수 있는 전압으로 바꾼다. 압전저항 촉각 소자의 판독 방식이다. → [[04-robotics/tactile-visuotactile|Tactile §2.5]]
- **Wheel loader** · 휠로더 — A wheeled machine with a front bucket that scoops material from piles and dumps it into trucks or hoppers in short, repeated cycles. · 앞쪽 버킷으로 더미에서 재료를 퍼서 트럭이나 호퍼에 붓는 짧은 반복 사이클을 도는 바퀴식 기계. → [[05-construction-robotics/earthmoving-heavy-machinery|Earthmoving §4]]
- **Whitening (coloring)** · 백색화 — Turning a correlated Gaussian vector into uncorrelated unit-variance coordinates, $z = L^{-1}(x-\mu)$ with the Cholesky factor $\Sigma = LL^\top$. Run backwards, $x = \mu + Lz$ samples a correlated Gaussian (coloring). · 촐레스키 인수 $\Sigma = LL^\top$로 $z = L^{-1}(x-\mu)$를 계산해 상관된 가우시안 벡터를 무상관이고 분산이 1인 좌표로 바꾸는 것. 거꾸로 $x = \mu + Lz$면 상관된 가우시안을 샘플링한다(채색). → [[02-foundations/probability|Probability §6]]
- **White noise** · 백색 잡음 — A random signal whose samples are uncorrelated, which gives a flat spectrum. The default disturbance model in filtering and the $\epsilon$ added in diffusion models. · 샘플끼리 무상관이라 스펙트럼이 평평한 랜덤 신호. 필터링의 기본 외란 모델이자 디퓨전 모델이 더하는 $\epsilon$이다. → [[02-foundations/probability|Probability §5]]
- **Whole-body control (WBC)** · whole-body control — Solving, at every control step, a QP that minimizes weighted task errors subject to joint and torque limits, friction cones, and balance constraints. It replaced strict null-space priority because those limits are inequalities. · 매 제어 스텝마다 관절·토크 한계, 마찰 원뿔, 균형 제약 아래 가중 과제 오차를 최소화하는 QP를 푸는 것. 그 한계들이 부등식이라 엄격한 영공간 우선순위를 대체했다. → [[04-robotics/force-compliance-control|Force & Compliance Control §4]]
- **Whole-body teleoperation** · 전신 원격조작 — Teleoperating a mobile manipulator's base and arms together in one coordinated motion, so demonstrations keep base–arm coordination. In Mobile ALOHA the operator is physically tethered to the base. · 모바일 매니퓰레이터의 베이스와 팔을 한 번의 협응 동작으로 함께 원격조작해 시연이 베이스-팔 협응을 보존하게 하는 것. Mobile ALOHA에서는 조작자가 베이스에 물리적으로 묶인다. → [[01-canonical-papers/notes/7-robotics/mobile-aloha|Mobile ALOHA]]
- **Wilson interval (exact interval, Clopper–Pearson)** · Wilson·정확 구간 — Confidence intervals for a success rate that stay within 0–100% and remain valid near 0 or 1, where the normal-approximation interval is too wide or gives impossible bounds. · 성공률의 신뢰구간으로, 0–100% 안에 머물고 0이나 1 근처에서도 유효하다. 그 근처에서 정규근사 구간은 지나치게 넓거나 불가능한 경계를 준다. → [[06-research-practice/experimental-design-reproducibility|Experimental Design §4]]
- **Within-subject and between-subject designs** · within-subject·between-subject 설계 — Within-subject studies test every condition on the same participants; between-subject studies assign different participants to each condition. Within-subject order must be counterbalanced, because people learn and tire while being measured. · within-subject 연구는 같은 참가자에게 모든 조건을 시험하고, between-subject 연구는 조건마다 다른 참가자를 배정한다. 사람은 측정되는 동안 배우고 지치므로 within-subject에서는 순서를 counterbalancing해야 한다. → [[04-robotics/hri-safety|HRI & Safety §7]]
- **Workload** · 작업 부하(workload) — The mental and physical demand a task places on an operator. Better completion time can hide a growing monitoring burden, so workload is measured alongside behavior, often with NASA-TLX. · 과제가 조작자에게 주는 정신적·신체적 요구. 완료 시간이 좋아져도 감시 부담이 커질 수 있으므로 행동과 함께, 흔히 NASA-TLX로 측정한다. → [[04-robotics/hri-safety|HRI & Safety §4]]
- **Workpiece localization (robot-to-workpiece transform)** · 작업물 위치 정합 — Recovering the pose of a building element relative to the robot on site, because no factory fixture supplies it. Methods range from fiducial markers to sensing the as-built geometry. · 공장 고정구가 주지 않으므로 현장에서 로봇에 대한 건물 부재의 자세를 복원하는 것. 피두셜 마커부터 시공 형상 감지까지 방법이 있다. → [[01-canonical-papers/notes/8-construction/vision-guided-assembly|Feng 2015]]
- **Workshop paper** · 워크숍 논문 — A paper presented at a workshop attached to a conference. Whether it blocks a later submission depends on proceedings, a DOI, or, at CVPR, length over four pages, not on what the workshop calls itself. · 학회에 딸린 워크숍에서 발표하는 논문. 이후 제출을 막는지는 워크숍이 스스로를 뭐라 부르는지가 아니라 프로시딩, DOI, 그리고 CVPR의 경우 4쪽 초과 여부에 달려 있다. → [[06-research-practice/venue-strategy|Venue Strategy §6]]
- **Workspace** · 작업 영역 — The set of end-effector positions a particular arm can actually reach, fixed by its structure. Task space is chosen by the task, so a target can lie in task space yet outside the workspace. · 특정 팔이 실제로 도달할 수 있는 말단 위치의 집합으로, 로봇 구조가 정한다. 작업 공간(task space)은 과제가 정하므로 목표가 작업 공간에는 있어도 작업 영역 밖일 수 있다. → [[04-robotics/modern-robotics/ch02-configuration-space|MR ch.2]]
- **World foundation model (WFM)** · 월드 파운데이션 모델(WFM) — A video world model pretrained on internet-scale physical video, meant to be post-trained into simulators, synthetic-data generators or policy evaluators. Genie founded the category; Cosmos is the open platform. · 인터넷 규모의 물리 비디오로 사전학습해, 시뮬레이터·합성 데이터 생성기·정책 평가기로 사후학습하도록 만든 비디오 월드모델. Genie가 범주를 열었고 Cosmos가 오픈 플랫폼이다. → [[01-canonical-papers/notes/5-world-models/cosmos|Cosmos]]
- **World model** · 월드모델 — A model trained to predict the environment's next state, enabling planning and learning inside imagination. · 환경의 다음 상태를 예측하도록 학습된 모델. → [[01-canonical-papers/notes/5-world-models/world-models|World Models]], [[01-canonical-papers/notes/5-world-models/dreamer|Dreamer]]
- **Worst-case bound** · 최악의 경우 — A running-time guarantee over the slowest input of each size: no input of that size is slower. It is the default meaning of a complexity claim unless average, expected or amortized is named. · 크기마다 가장 느린 입력에 대한 실행 시간 보장: 그 크기의 어떤 입력도 이보다 느리지 않다. 평균, 기대, 분할상환을 따로 밝히지 않으면 복잡도 주장의 기본 의미다. → [[02-foundations/algorithms/complexity-recursion|11.1 §1]]
- **Wrench** · 렌치 — Force and moment packaged as one six-vector; the dual of a twist, and what $\tau = J^\top\mathcal{F}$ maps back into joint torques. · 힘과 모멘트를 하나로 묶은 6차원 벡터. twist의 쌍대이고, $\tau = J^\top\mathcal{F}$가 관절 토크로 되돌리는 대상이 이것이다. → [[04-robotics/modern-robotics/ch05-velocity-kinematics|MR 5장]]
- **Wrist force/torque sensor (F/T sensor)** · 손목 힘/토크 센서 — Gives one calibrated six-axis wrench at high rate: net force and moment, not a pressure map. Everything distal to it, including gripper weight and inertia, is in the reading and must be compensated. · 보정된 6축 렌치 하나를 높은 주기로 준다: 합력과 합모멘트이지 압력 분포가 아니다. 그리퍼 무게·관성 등 센서 말단 쪽 모든 것이 측정값에 들어 있어 보상해야 한다. → [[04-robotics/tactile-visuotactile|Tactile §2]]
- **Xacro** · Xacro — An XML macro language that preprocesses into URDF, adding properties (constants), arithmetic and macros to remove repetition. Nothing downstream understands it, so it is expanded to plain URDF first, often inside the launch file. · 속성(상수), 산술, 매크로를 더해 반복을 없애고 URDF로 전개되는 XML 매크로 언어. 하위 도구는 이것을 모르므로 먼저 순수 URDF로 전개하며, 흔히 launch 파일 안에서 한다. → [[04-robotics/ros2/describing-a-robot|25.6 Describing a Robot §5]]
- **Zero convolution** · zero convolution — A connecting layer initialized to zero, guaranteeing a no-op at the start of training. · 0으로 초기화된 연결층 — 학습 시작 시 no-op을 보장하는 안전장치. → [[01-canonical-papers/notes/6-diffusion/controlnet|ControlNet]]
- **Zero-moment point (ZMP)** · ZMP — The ground point where the ground reaction force produces no horizontal moment. Keeping it strictly inside the support polygon stops the foot tipping; classical walking-pattern generators produce ZMP trajectories the robot can track. · 지면 반력이 수평 모멘트를 만들지 않는 지면 위의 점. 지지 다각형 안에 확실히 두면 발이 들리지 않으며, 고전 보행 패턴 생성기는 로봇이 추종할 수 있는 ZMP 궤적을 만든다. → [[04-robotics/legged-locomotion|Legged Locomotion §1.5]]
- **Zero (of a transfer function)** · 영점 — A root of the transfer function's numerator. When a zero coincides with a pole the factor cancels, and that eigenvalue vanishes from $G(s)$ even if it is unstable. · 전달함수 분자의 근. 영점이 극점과 겹치면 그 인수가 상쇄되어, 해당 고유값이 불안정하더라도 $G(s)$에서 사라진다. → [[02-foundations/engineering-math|Engineering Math §9]]
- **Zero-order hold (ZOH)** · 영차 유지 — Holding a digital controller's input constant between samples. Exact ZOH discretization maps a continuous eigenvalue $\lambda$ to $e^{\lambda T}$, preserving stability; approximate schemes like forward Euler do not. · 디지털 제어기의 입력을 샘플 사이에 일정하게 붙잡아 두는 것. 정확한 영차 유지 이산화는 연속 고유값 $\lambda$를 $e^{\lambda T}$로 보내 안정성을 보존하지만, 전진 오일러 같은 근사는 그렇지 않다. → [[04-robotics/control-theory-ce397|Control Theory §4]]
- **Zero-shot** · zero-shot — Performing a task with no training examples of that task. · 해당 작업의 학습 예시 없이 바로 수행하는 능력. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]], [[01-canonical-papers/notes/2-computer-vision/sam|SAM]]
- **Zero-shot classification** · zero-shot 분류 — Classifying an image by embedding text prompts such as "a photo of a {class}" and choosing the class whose embedding is most similar to the image's; no training on those labels. · "a photo of a {class}" 같은 텍스트 프롬프트를 임베딩하고 이미지 임베딩과 가장 유사한 클래스를 고르는 분류. 해당 라벨로 학습하지 않는다. → [[01-canonical-papers/notes/3-vlm/clip|CLIP]]
- **Zero-shot object navigation (ZSON)** · zero-shot 물체 목표 내비게이션(ZSON) — Object-goal navigation with no ObjectNav-specific training: a pretrained vision–language model or LLM commonsense decides where to explore, as in VLFM, ESC and CoWs. · ObjectNav 전용 학습 없이 하는 물체 목표 내비게이션: 사전학습 시각-언어 모델이나 LLM 상식이 어디를 탐색할지 정한다(VLFM, ESC, CoWs). → [[04-robotics/semantic-language-navigation|Semantic navigation §3]]
- **Z-width** · Z-width — The range of mechanical impedances a haptic device can render stably and perceptibly, from nearly free motion to the stiffest stable contact. It is a system property shaped by hardware, sampling, filtering, control, and the user—not a stiffness number alone. · 햅틱 장치가 안정적이고 지각 가능하게 표현할 수 있는 기계 임피던스 범위로, 거의 자유로운 운동부터 가장 단단한 안정 접촉까지를 뜻한다. 강성 하나가 아니라 하드웨어·샘플링·필터·제어·사용자가 함께 정하는 시스템 성질이다. → [[04-robotics/haptics-teleoperation/rendering-sampling-stability|Rendering & Stability]]
